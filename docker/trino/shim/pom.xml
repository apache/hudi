<?xml version="1.0" encoding="UTF-8"?>
<!--
  Licensed to the Apache Software Foundation (ASF) under one
  or more contributor license agreements.  See the NOTICE file
  distributed with this work for additional information
  regarding copyright ownership.  The ASF licenses this file
  to you under the Apache License, Version 2.0 (the
  "License"); you may not use this file except in compliance
  with the License.  You may obtain a copy of the License at

       http://www.apache.org/licenses/LICENSE-2.0

  Unless required by applicable law or agreed to in writing, software
  distributed under the License is distributed on an "AS IS" BASIS,
  WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
  See the License for the specific language governing permissions and
  limitations under the License.
-->
<!--
  In-repo mirror of the RFC-105 Trino-side shim (trinodb/trino plugin/trino-hudi),
  which is not yet released upstream. It exists so this repo can assemble the
  deployable Trino plugin directory (hudi-trino jar + runtime deps + service
  descriptor) for the E2E docker image without a trinodb/trino checkout, and it
  stays useful afterwards: CI must test hudi-trino at HEAD while the upstream shim
  pins released versions.

  Standalone project - NOT part of the Hudi reactor and never deployed.

  Build (JDK 25, after installing hudi-trino into the local m2):
      mvn -f docker/trino/shim/pom.xml clean package -DskipTests
  Output plugin dir: target/trino-hudi-<trino.version>/
  Use `package`; `install` is hard-disabled via maven.install.skip below because
  installing would shadow the real io.trino:trino-hudi release coordinates in
  the local repository.

  The parent version below must stay in sync with trino.version in the root pom,
  the TRINO_VERSION ARG in docker/trino/Dockerfile, and the compose image tag.
-->
<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>

    <parent>
        <groupId>io.trino</groupId>
        <artifactId>trino-root</artifactId>
        <version>481</version>
        <!-- Resolve the parent from Maven Central, never the file system. -->
        <relativePath />
    </parent>

    <artifactId>trino-hudi</artifactId>
    <packaging>trino-plugin</packaging>
    <description>Trino - Hudi connector plugin assembly (in-repo E2E shim mirroring the upstream plugin/trino-hudi shim planned by RFC-105; never deployed)</description>

    <properties>
        <!-- Hudi connector under test; override with -Ddep.hudi.version=... -->
        <dep.hudi.version>1.3.0-SNAPSHOT</dep.hudi.version>
        <!-- Assembly-only project: skip airbase/basepom checks (license header
             style, checkstyle, enforcer, modernizer, duplicate-finder, dependency
             analysis). ASF headers are enforced separately by
             scripts/release/validate_source_copyright.sh. -->
        <air.check.skip-all>true</air.check.skip-all>
        <!-- Test infra only: never deploy/release this artifact, and never
             install it - a stray install would publish a stub at the real
             io.trino:trino-hudi coordinates and poison the local m2. -->
        <maven.deploy.skip>true</maven.deploy.skip>
        <maven.install.skip>true</maven.install.skip>
        <skipNexusStagingDeployMojo>true</skipNexusStagingDeployMojo>
    </properties>

    <dependencies>
        <!-- HudiPlugin.java uses ImmutableList. -->
        <dependency>
            <groupId>com.google.guava</groupId>
            <artifactId>guava</artifactId>
        </dependency>

        <dependency>
            <groupId>org.apache.hudi</groupId>
            <artifactId>hudi-trino</artifactId>
            <version>${dep.hudi.version}</version>
            <exclusions>
                <!-- Defense in depth, mirroring the upstream shim: the hudi-trino
                     pom should already keep these out, but explicit exclusions
                     survive upstream churn. orc-core ships a stale relocated copy
                     of io.airlift.slice.XxHash64 that would shadow slice at
                     runtime; Trino uses its own ORC reader. -->
                <exclusion>
                    <groupId>org.apache.arrow</groupId>
                    <artifactId>*</artifactId>
                </exclusion>
                <exclusion>
                    <groupId>org.apache.hudi</groupId>
                    <artifactId>hudi-timeline-service</artifactId>
                </exclusion>
                <exclusion>
                    <groupId>org.apache.orc</groupId>
                    <artifactId>orc-core</artifactId>
                </exclusion>
                <exclusion>
                    <groupId>org.lance</groupId>
                    <artifactId>*</artifactId>
                </exclusion>
                <exclusion>
                    <groupId>org.rocksdb</groupId>
                    <artifactId>*</artifactId>
                </exclusion>
            </exclusions>
        </dependency>

        <!-- Trino SPI surface: provided by the server at plugin load; must stay
             out of the assembled plugin directory. Direct provided-scope
             declarations override any compile-scope transitive arrivals so the
             trino-maven-plugin SPI dependency check passes. -->
        <dependency>
            <groupId>com.fasterxml.jackson.core</groupId>
            <artifactId>jackson-annotations</artifactId>
            <scope>provided</scope>
        </dependency>

        <dependency>
            <groupId>io.airlift</groupId>
            <artifactId>slice</artifactId>
            <scope>provided</scope>
        </dependency>

        <dependency>
            <groupId>io.opentelemetry</groupId>
            <artifactId>opentelemetry-api</artifactId>
            <scope>provided</scope>
        </dependency>

        <dependency>
            <groupId>io.opentelemetry</groupId>
            <artifactId>opentelemetry-api-incubator</artifactId>
            <scope>provided</scope>
        </dependency>

        <dependency>
            <groupId>io.opentelemetry</groupId>
            <artifactId>opentelemetry-common</artifactId>
            <scope>provided</scope>
        </dependency>

        <dependency>
            <groupId>io.opentelemetry</groupId>
            <artifactId>opentelemetry-context</artifactId>
            <scope>provided</scope>
        </dependency>

        <dependency>
            <groupId>io.trino</groupId>
            <artifactId>trino-spi</artifactId>
            <scope>provided</scope>
        </dependency>

        <!-- NOTE: unlike the upstream 482-SNAPSHOT shim, jts-core must NOT be
             declared provided here: it is not part of the Trino 481 SPI surface
             and SpiDependencyChecker rejects provided scope for it. It ships
             inside the plugin dir at its transitive scope instead. -->
    </dependencies>
</project>
