<?xml version="1.0" encoding="utf-8"?>
<rss version="2.0" xmlns:dc="http://purl.org/dc/elements/1.1/" xmlns:content="http://purl.org/rss/1.0/modules/content/">
    <channel>
        <title>Apache Hudi: User-Facing Analytics</title>
        <link>https://hudi.apache.org/blog</link>
        <description>Apache Hudi Blog</description>
        <lastBuildDate>Thu, 30 Jul 2026 00:00:00 GMT</lastBuildDate>
        <docs>https://validator.w3.org/feed/docs/rss2.html</docs>
        <generator>https://github.com/jpmonette/feed</generator>
        <language>en</language>
        <item>
            <title><![CDATA[Migrating from Apache Hive Tables to Apache Hudi]]></title>
            <link>https://hudi.apache.org/blog/2026/07/30/migrating-from-hive-to-hudi</link>
            <guid>https://hudi.apache.org/blog/2026/07/30/migrating-from-hive-to-hudi</guid>
            <pubDate>Thu, 30 Jul 2026 00:00:00 GMT</pubDate>
            <description><![CDATA[Step-by-step guide to migrating Apache Hive tables to Apache Hudi: in-place bootstrap vs full rewrite, Hive metastore sync, validation and safe decommissioning.]]></description>
            <content:encoded><![CDATA[<p>Migrating an Apache Hive table to Apache Hudi means converting its underlying data files into a Hudi table — either in place with Hudi's <a class="" href="https://hudi.apache.org/docs/migration_guide">bootstrap</a> mechanism or by rewriting the data with Spark — and re-registering it in your metastore through Hudi's <a class="" href="https://hudi.apache.org/docs/syncing_metastore">catalog sync</a>. Your existing Hive, Spark, Presto and Trino queries keep working against the same metastore entry, and the table gains upserts, deletes, ACID transactions and incremental reads that a plain Hive table never had.</p>
<p>This guide walks through the whole journey: why teams outgrow plain Hive tables, what actually changes on disk and in the metastore, how to plan key and partition mapping, the two migration paths (in-place bootstrap and full rewrite), re-pointing the metastore, validating with the queries you already run, and finally decommissioning the old table safely.</p>
<h2 class="anchor anchorTargetStickyNavbar_Vzrq" id="why-teams-move-off-plain-hive-tables">Why Teams Move Off Plain Hive Tables<a href="https://hudi.apache.org/blog/2026/07/30/migrating-from-hive-to-hudi#why-teams-move-off-plain-hive-tables" class="hash-link" aria-label="Direct link to Why Teams Move Off Plain Hive Tables" title="Direct link to Why Teams Move Off Plain Hive Tables" translate="no">​</a></h2>
<p>A classic Hive external table is a directory convention plus a metastore entry. That simplicity served the Hadoop era well, but it carries structural costs that get worse as tables grow and freshness expectations tighten:</p>
<ul>
<li class=""><strong>No transactions on the data lake.</strong> A plain Hive table over Parquet or ORC has no atomic commit protocol. A failed <code>INSERT OVERWRITE</code> can leave partial files behind, and readers racing a writer can see half-written partitions. Every team ends up building <code>_SUCCESS</code>-file conventions or staging-directory dances around this gap.</li>
<li class=""><strong>Updates mean rewriting partitions.</strong> There is no record-level update or delete. Fixing one late-arriving record, applying a CDC stream, or servicing a GDPR deletion request means rewriting every affected partition wholesale — slow, expensive, and disruptive to readers.</li>
<li class=""><strong>Slow, listing-bound query planning.</strong> Query engines discover data in Hive tables by listing directories. On cloud object stores, listing millions of files across thousands of partitions dominates planning time, and historically the eventual consistency of object-store listings caused correctness surprises too.</li>
<li class=""><strong>No incremental consumption.</strong> Downstream pipelines cannot ask a Hive table "what changed since yesterday?" — they re-scan whole partitions on a schedule, multiplying compute costs.</li>
</ul>
<p>Hive ACID (transactional ORC tables) brings transactions and row-level updates to Hive, but it is tied to ORC, to Hive's own compactor running inside the metastore ecosystem, and its delta files are not first-class citizens in Spark, Trino and the broader lakehouse engine ecosystem the way an open table format is. Lakehouse formats like Hudi were built to provide transactions, mutability and incremental processing directly on open columnar files, queryable from every major engine. If you want the deeper background on table types, see <a class="" href="https://hudi.apache.org/docs/table_types">Table &amp; Query Types</a>.</p>
<h2 class="anchor anchorTargetStickyNavbar_Vzrq" id="what-changes--and-what-doesnt">What Changes — and What Doesn't<a href="https://hudi.apache.org/blog/2026/07/30/migrating-from-hive-to-hudi#what-changes--and-what-doesnt" class="hash-link" aria-label="Direct link to What Changes — and What Doesn't" title="Direct link to What Changes — and What Doesn't" translate="no">​</a></h2>
<p>It helps to be precise about what a migration touches:</p>
<p><strong>What stays the same.</strong> Your data remains in open columnar files — Hudi writes Parquet base files (and can bootstrap existing Parquet in place). The data keeps living on the same HDFS or object storage. Analysts keep querying through the same metastore-backed catalog, from the same engines.</p>
<p><strong>What changes.</strong> The table gains a Hudi timeline (a commit log recording every action), metadata and indexes mapping record keys to files, and Hudi-managed file layout with automatic file sizing. The metastore entry is re-registered through Hudi's sync so that engines route reads through Hudi's integrations — Hive via the Hudi input format, Spark and Trino/Presto via their native Hudi connectors. From then on, writes go through Hudi (Spark, Flink, or <a class="" href="https://hudi.apache.org/docs/hoodie_streaming_ingestion">Hudi Streamer</a>) and become atomic, and the table supports upserts, deletes and incremental queries.</p>
<p>If your estate also includes raw Parquet directories that were never registered in Hive, the companion post on <a class="" href="https://hudi.apache.org/blog/2026/07/29/migrating-from-parquet-to-hudi">migrating from Parquet to Hudi</a> covers that path; this post focuses on tables living in the Hive metastore.</p>
<h2 class="anchor anchorTargetStickyNavbar_Vzrq" id="planning-the-migration">Planning the Migration<a href="https://hudi.apache.org/blog/2026/07/30/migrating-from-hive-to-hudi#planning-the-migration" class="hash-link" aria-label="Direct link to Planning the Migration" title="Direct link to Planning the Migration" translate="no">​</a></h2>
<p>Before running anything, map three concepts from your Hive schema onto Hudi's table configs:</p>
<p><strong>Record key.</strong> Hudi identifies each record by a key so it can find and update it later. Pick the column (or comma-separated columns) that uniquely identifies a row — an id column, or a composite of natural keys. This becomes <code>hoodie.datasource.write.recordkey.field</code> (or <code>primaryKey</code> in Spark SQL table properties). If the Hive table genuinely has no key and is append-only, you can migrate without one, but you give up upserts on that table.</p>
<p><strong>Ordering field.</strong> When two versions of the same key arrive, Hudi resolves the winner using ordering fields (<code>hoodie.table.ordering.fields</code>). An <code>updated_at</code>-style timestamp is the usual choice; it prevents older records from overwriting newer ones.</p>
<p><strong>Partition mapping.</strong> Hudi partitioning maps directly from Hive partitioning:</p>
<ul>
<li class=""><em>Single-level partitions</em> (<code>ds=2026-08-01</code>) map to a single <code>hoodie.datasource.write.partitionpath.field</code> with <code>hoodie.datasource.write.hive_style_partitioning=true</code> to preserve the <code>key=value</code> directory style your Hive table already uses.</li>
<li class=""><em>Multi-level partitions</em> (<code>year=2026/month=08/day=01</code>) map to a comma-separated partition path field list (e.g. <code>year,month,day</code>); on the sync side, the default <code>MultiPartKeysValueExtractor</code> splits partition values on <code>/</code> correctly.</li>
<li class=""><em>Non-partitioned tables</em> simply omit the partition path field; during metastore sync Hudi infers <code>NonPartitionedExtractor</code> automatically.</li>
</ul>
<p>Also decide the table type now: Copy-on-Write (CoW) is the simplest starting point for formerly-Hive analytics tables; Merge-on-Read (MoR) suits update-heavy or streaming write patterns. The <a class="" href="https://hudi.apache.org/docs/table_types">table types guide</a> covers the trade-off; note that MoR changes what appears in your metastore after sync (more below).</p>
<h2 class="anchor anchorTargetStickyNavbar_Vzrq" id="path-a-in-place-bootstrap-for-large-tables">Path A: In-Place Bootstrap for Large Tables<a href="https://hudi.apache.org/blog/2026/07/30/migrating-from-hive-to-hudi#path-a-in-place-bootstrap-for-large-tables" class="hash-link" aria-label="Direct link to Path A: In-Place Bootstrap for Large Tables" title="Direct link to Path A: In-Place Bootstrap for Large Tables" translate="no">​</a></h2>
<p>For big tables, rewriting terabytes of perfectly good Parquet just to adopt a new format is wasteful. Hudi's <a class="" href="https://hudi.apache.org/docs/migration_guide">bootstrap</a> converts a table in place, with two modes you can mix per partition:</p>
<ul>
<li class=""><strong>METADATA_ONLY</strong> generates skeleton files containing only Hudi's metadata columns and record keys, alongside pointers to your original data files — avoiding the full cost of rewriting the dataset. Queries and upserts work normally. The caveat: Hudi still relies on the original files, so they must not be deleted or modified.</li>
<li class=""><strong>FULL_RECORD</strong> copies the full record data into Hudi-managed files, functionally equivalent to a bulk insert. After it completes, the Hudi table is fully self-contained.</li>
</ul>
<p>A common large-table strategy is METADATA_ONLY for cold historical partitions and FULL_RECORD for recent, actively-updated partitions — selected with a regex over partition paths.</p>
<h3 class="anchor anchorTargetStickyNavbar_Vzrq" id="with-hudi-streamer">With Hudi Streamer<a href="https://hudi.apache.org/blog/2026/07/30/migrating-from-hive-to-hudi#with-hudi-streamer" class="hash-link" aria-label="Direct link to With Hudi Streamer" title="Direct link to With Hudi Streamer" translate="no">​</a></h3>
<p>Hudi Streamer supports bootstrap via the <code>--run-bootstrap</code> flag. This example applies FULL_RECORD to all partitions matching the regex <code>.*</code>, keeping hive-style partitioning:</p>
<div class="language-bash codeBlockContainer_Ckt0 theme-code-block" style="--prism-color:#F8F8F2;--prism-background-color:#282A36"><div class="codeBlockContent_QJqH"><pre tabindex="0" class="prism-code language-bash codeBlock_bY9V thin-scrollbar" style="color:#F8F8F2;background-color:#282A36"><code class="codeBlockLines_e6Vv"><span class="token-line" style="color:#F8F8F2"><span class="token plain">spark-submit \</span><br></span><span class="token-line" style="color:#F8F8F2"><span class="token plain">--conf 'spark.serializer=org.apache.spark.serializer.KryoSerializer' \</span><br></span><span class="token-line" style="color:#F8F8F2"><span class="token plain">--class org.apache.hudi.utilities.streamer.HoodieStreamer /path/to/hudi-utilities-bundle.jar \</span><br></span><span class="token-line" style="color:#F8F8F2"><span class="token plain">--run-bootstrap \</span><br></span><span class="token-line" style="color:#F8F8F2"><span class="token plain">--target-base-path hdfs://ns1/warehouse/hudi/trips \</span><br></span><span class="token-line" style="color:#F8F8F2"><span class="token plain">--target-table trips \</span><br></span><span class="token-line" style="color:#F8F8F2"><span class="token plain">--table-type COPY_ON_WRITE \</span><br></span><span class="token-line" style="color:#F8F8F2"><span class="token plain">--hoodie-conf hoodie.bootstrap.base.path=hdfs://ns1/hive/warehouse/mydb.db/trips \</span><br></span><span class="token-line" style="color:#F8F8F2"><span class="token plain">--hoodie-conf hoodie.datasource.write.recordkey.field=trip_id \</span><br></span><span class="token-line" style="color:#F8F8F2"><span class="token plain">--hoodie-conf hoodie.datasource.write.partitionpath.field=ds \</span><br></span><span class="token-line" style="color:#F8F8F2"><span class="token plain">--hoodie-conf hoodie.table.ordering.fields=updated_at \</span><br></span><span class="token-line" style="color:#F8F8F2"><span class="token plain">--hoodie-conf hoodie.bootstrap.mode.selector=org.apache.hudi.client.bootstrap.selector.BootstrapRegexModeSelector \</span><br></span><span class="token-line" style="color:#F8F8F2"><span class="token plain">--hoodie-conf hoodie.bootstrap.mode.selector.regex='.*' \</span><br></span><span class="token-line" style="color:#F8F8F2"><span class="token plain">--hoodie-conf hoodie.bootstrap.mode.selector.regex.mode=FULL_RECORD \</span><br></span><span class="token-line" style="color:#F8F8F2"><span class="token plain">--hoodie-conf hoodie.datasource.write.hive_style_partitioning=true</span><br></span></code></pre></div></div>
<p><code>hoodie.bootstrap.base.path</code> points at the existing Hive table's location — the warehouse directory the metastore already knows about. To split modes by partition, keep the <code>BootstrapRegexModeSelector</code> and set the regex to match the partitions that should get the mode named in <code>hoodie.bootstrap.mode.selector.regex.mode</code>; unmatched partitions get the other mode. With only <code>hoodie.bootstrap.base.path</code> provided and no selector configs, METADATA_ONLY is the default for everything.</p>
<h3 class="anchor anchorTargetStickyNavbar_Vzrq" id="with-the-spark-sql-call-procedure">With the Spark SQL CALL procedure<a href="https://hudi.apache.org/blog/2026/07/30/migrating-from-hive-to-hudi#with-the-spark-sql-call-procedure" class="hash-link" aria-label="Direct link to With the Spark SQL CALL procedure" title="Direct link to With the Spark SQL CALL procedure" translate="no">​</a></h3>
<p>If you prefer staying in Spark SQL, the <a class="" href="https://hudi.apache.org/docs/procedures#run_bootstrap"><code>run_bootstrap</code> procedure</a> does the same job:</p>
<div class="language-sql codeBlockContainer_Ckt0 theme-code-block" style="--prism-color:#F8F8F2;--prism-background-color:#282A36"><div class="codeBlockContent_QJqH"><pre tabindex="0" class="prism-code language-sql codeBlock_bY9V thin-scrollbar" style="color:#F8F8F2;background-color:#282A36"><code class="codeBlockLines_e6Vv"><span class="token-line" style="color:#F8F8F2"><span class="token keyword" style="color:rgb(189, 147, 249);font-style:italic">CALL</span><span class="token plain"> run_bootstrap</span><span class="token punctuation" style="color:rgb(248, 248, 242)">(</span><span class="token plain"></span><br></span><span class="token-line" style="color:#F8F8F2"><span class="token plain">  </span><span class="token keyword" style="color:rgb(189, 147, 249);font-style:italic">table</span><span class="token plain"> </span><span class="token operator">=</span><span class="token operator">&gt;</span><span class="token plain"> </span><span class="token string" style="color:rgb(255, 121, 198)">'trips'</span><span class="token punctuation" style="color:rgb(248, 248, 242)">,</span><span class="token plain"></span><br></span><span class="token-line" style="color:#F8F8F2"><span class="token plain">  table_type </span><span class="token operator">=</span><span class="token operator">&gt;</span><span class="token plain"> </span><span class="token string" style="color:rgb(255, 121, 198)">'COPY_ON_WRITE'</span><span class="token punctuation" style="color:rgb(248, 248, 242)">,</span><span class="token plain"></span><br></span><span class="token-line" style="color:#F8F8F2"><span class="token plain">  bootstrap_path </span><span class="token operator">=</span><span class="token operator">&gt;</span><span class="token plain"> </span><span class="token string" style="color:rgb(255, 121, 198)">'hdfs://ns1/hive/warehouse/mydb.db/trips'</span><span class="token punctuation" style="color:rgb(248, 248, 242)">,</span><span class="token plain"></span><br></span><span class="token-line" style="color:#F8F8F2"><span class="token plain">  base_path </span><span class="token operator">=</span><span class="token operator">&gt;</span><span class="token plain"> </span><span class="token string" style="color:rgb(255, 121, 198)">'hdfs://ns1/warehouse/hudi/trips'</span><span class="token punctuation" style="color:rgb(248, 248, 242)">,</span><span class="token plain"></span><br></span><span class="token-line" style="color:#F8F8F2"><span class="token plain">  rowKey_field </span><span class="token operator">=</span><span class="token operator">&gt;</span><span class="token plain"> </span><span class="token string" style="color:rgb(255, 121, 198)">'trip_id'</span><span class="token punctuation" style="color:rgb(248, 248, 242)">,</span><span class="token plain"></span><br></span><span class="token-line" style="color:#F8F8F2"><span class="token plain">  partition_path_field </span><span class="token operator">=</span><span class="token operator">&gt;</span><span class="token plain"> </span><span class="token string" style="color:rgb(255, 121, 198)">'ds'</span><span class="token plain"></span><br></span><span class="token-line" style="color:#F8F8F2"><span class="token plain"></span><span class="token punctuation" style="color:rgb(248, 248, 242)">)</span><span class="token punctuation" style="color:rgb(248, 248, 242)">;</span><br></span></code></pre></div></div>
<p>By default this uses the METADATA_ONLY selector; pass <code>selector_class =&gt; 'org.apache.hudi.client.bootstrap.selector.FullRecordBootstrapModeSelector'</code> for a full-record bootstrap.</p>
<p>A third option worth knowing: Hudi does not require converting the entire table at once. Because the lowest granularity Hudi manages is a Hive partition, an append-only table can simply start writing <em>new</em> partitions through Hudi while old partitions stay as-is — with the caveat that upserts and incremental pulls do not work on the unconverted partitions.</p>
<h2 class="anchor anchorTargetStickyNavbar_Vzrq" id="path-b-rewrite-via-spark">Path B: Rewrite via Spark<a href="https://hudi.apache.org/blog/2026/07/30/migrating-from-hive-to-hudi#path-b-rewrite-via-spark" class="hash-link" aria-label="Direct link to Path B: Rewrite via Spark" title="Direct link to Path B: Rewrite via Spark" translate="no">​</a></h2>
<p>For small and medium tables — or whenever you want a clean, fully Hudi-managed copy with per-record ordering applied — a straight rewrite is the simplest path. Since the source is already a metastore table, Spark SQL can read it directly:</p>
<div class="language-sql codeBlockContainer_Ckt0 theme-code-block" style="--prism-color:#F8F8F2;--prism-background-color:#282A36"><div class="codeBlockContent_QJqH"><pre tabindex="0" class="prism-code language-sql codeBlock_bY9V thin-scrollbar" style="color:#F8F8F2;background-color:#282A36"><code class="codeBlockLines_e6Vv"><span class="token-line" style="color:#F8F8F2"><span class="token comment" style="color:rgb(98, 114, 164)">-- define the Hudi table with key, ordering and partitioning mapped from the Hive schema</span><span class="token plain"></span><br></span><span class="token-line" style="color:#F8F8F2"><span class="token plain"></span><span class="token keyword" style="color:rgb(189, 147, 249);font-style:italic">CREATE</span><span class="token plain"> </span><span class="token keyword" style="color:rgb(189, 147, 249);font-style:italic">TABLE</span><span class="token plain"> hudi_trips </span><span class="token punctuation" style="color:rgb(248, 248, 242)">(</span><span class="token plain"></span><br></span><span class="token-line" style="color:#F8F8F2"><span class="token plain">  trip_id STRING</span><span class="token punctuation" style="color:rgb(248, 248, 242)">,</span><span class="token plain"></span><br></span><span class="token-line" style="color:#F8F8F2"><span class="token plain">  rider STRING</span><span class="token punctuation" style="color:rgb(248, 248, 242)">,</span><span class="token plain"></span><br></span><span class="token-line" style="color:#F8F8F2"><span class="token plain">  fare </span><span class="token keyword" style="color:rgb(189, 147, 249);font-style:italic">DOUBLE</span><span class="token punctuation" style="color:rgb(248, 248, 242)">,</span><span class="token plain"></span><br></span><span class="token-line" style="color:#F8F8F2"><span class="token plain">  updated_at </span><span class="token keyword" style="color:rgb(189, 147, 249);font-style:italic">BIGINT</span><span class="token punctuation" style="color:rgb(248, 248, 242)">,</span><span class="token plain"></span><br></span><span class="token-line" style="color:#F8F8F2"><span class="token plain">  ds STRING</span><br></span><span class="token-line" style="color:#F8F8F2"><span class="token plain"></span><span class="token punctuation" style="color:rgb(248, 248, 242)">)</span><span class="token plain"> </span><span class="token keyword" style="color:rgb(189, 147, 249);font-style:italic">USING</span><span class="token plain"> HUDI</span><br></span><span class="token-line" style="color:#F8F8F2"><span class="token plain">TBLPROPERTIES </span><span class="token punctuation" style="color:rgb(248, 248, 242)">(</span><span class="token plain"></span><br></span><span class="token-line" style="color:#F8F8F2"><span class="token plain">  primaryKey </span><span class="token operator">=</span><span class="token plain"> </span><span class="token string" style="color:rgb(255, 121, 198)">'trip_id'</span><span class="token punctuation" style="color:rgb(248, 248, 242)">,</span><span class="token plain"></span><br></span><span class="token-line" style="color:#F8F8F2"><span class="token plain">  orderingFields </span><span class="token operator">=</span><span class="token plain"> </span><span class="token string" style="color:rgb(255, 121, 198)">'updated_at'</span><span class="token plain"></span><br></span><span class="token-line" style="color:#F8F8F2"><span class="token plain"></span><span class="token punctuation" style="color:rgb(248, 248, 242)">)</span><span class="token plain"></span><br></span><span class="token-line" style="color:#F8F8F2"><span class="token plain">PARTITIONED </span><span class="token keyword" style="color:rgb(189, 147, 249);font-style:italic">BY</span><span class="token plain"> </span><span class="token punctuation" style="color:rgb(248, 248, 242)">(</span><span class="token plain">ds</span><span class="token punctuation" style="color:rgb(248, 248, 242)">)</span><span class="token plain"></span><br></span><span class="token-line" style="color:#F8F8F2"><span class="token plain">LOCATION </span><span class="token string" style="color:rgb(255, 121, 198)">'hdfs://ns1/warehouse/hudi/trips'</span><span class="token punctuation" style="color:rgb(248, 248, 242)">;</span><span class="token plain"></span><br></span><span class="token-line" style="color:#F8F8F2"><span class="token plain" style="display:inline-block"></span><br></span><span class="token-line" style="color:#F8F8F2"><span class="token plain"></span><span class="token comment" style="color:rgb(98, 114, 164)">-- use bulk_insert for the one-time load: fastest, skips upsert-path indexing work</span><span class="token plain"></span><br></span><span class="token-line" style="color:#F8F8F2"><span class="token plain"></span><span class="token keyword" style="color:rgb(189, 147, 249);font-style:italic">SET</span><span class="token plain"> hoodie</span><span class="token punctuation" style="color:rgb(248, 248, 242)">.</span><span class="token plain">spark</span><span class="token punctuation" style="color:rgb(248, 248, 242)">.</span><span class="token keyword" style="color:rgb(189, 147, 249);font-style:italic">sql</span><span class="token punctuation" style="color:rgb(248, 248, 242)">.</span><span class="token keyword" style="color:rgb(189, 147, 249);font-style:italic">insert</span><span class="token punctuation" style="color:rgb(248, 248, 242)">.</span><span class="token keyword" style="color:rgb(189, 147, 249);font-style:italic">into</span><span class="token punctuation" style="color:rgb(248, 248, 242)">.</span><span class="token plain">operation </span><span class="token operator">=</span><span class="token plain"> bulk_insert</span><span class="token punctuation" style="color:rgb(248, 248, 242)">;</span><span class="token plain"></span><br></span><span class="token-line" style="color:#F8F8F2"><span class="token plain" style="display:inline-block"></span><br></span><span class="token-line" style="color:#F8F8F2"><span class="token plain"></span><span class="token keyword" style="color:rgb(189, 147, 249);font-style:italic">INSERT</span><span class="token plain"> </span><span class="token keyword" style="color:rgb(189, 147, 249);font-style:italic">INTO</span><span class="token plain"> hudi_trips</span><br></span><span class="token-line" style="color:#F8F8F2"><span class="token plain"></span><span class="token keyword" style="color:rgb(189, 147, 249);font-style:italic">SELECT</span><span class="token plain"> trip_id</span><span class="token punctuation" style="color:rgb(248, 248, 242)">,</span><span class="token plain"> rider</span><span class="token punctuation" style="color:rgb(248, 248, 242)">,</span><span class="token plain"> fare</span><span class="token punctuation" style="color:rgb(248, 248, 242)">,</span><span class="token plain"> updated_at</span><span class="token punctuation" style="color:rgb(248, 248, 242)">,</span><span class="token plain"> ds </span><span class="token keyword" style="color:rgb(189, 147, 249);font-style:italic">FROM</span><span class="token plain"> mydb</span><span class="token punctuation" style="color:rgb(248, 248, 242)">.</span><span class="token plain">trips</span><span class="token punctuation" style="color:rgb(248, 248, 242)">;</span><br></span></code></pre></div></div>
<p>The <code>bulk_insert</code> operation is the right choice for the initial load — it is optimized for large one-time writes and lets Hudi apply its file-sizing configs so the new table starts life with well-sized files rather than inheriting whatever small-file sprawl the Hive table had. The equivalent DataFrame API flow (read the Hive table with <code>spark.table(...)</code>, write with <code>format("hudi")</code>) is shown in the <a class="" href="https://hudi.apache.org/docs/quick-start-guide">quick start guide</a>. For very large tables where a single job is impractical, you can loop partition by partition, reading each partition path and appending it to the Hudi base path, as described in the <a class="" href="https://hudi.apache.org/docs/migration_guide">migration guide</a>.</p>
<h2 class="anchor anchorTargetStickyNavbar_Vzrq" id="re-pointing-the-metastore">Re-Pointing the Metastore<a href="https://hudi.apache.org/blog/2026/07/30/migrating-from-hive-to-hudi#re-pointing-the-metastore" class="hash-link" aria-label="Direct link to Re-Pointing the Metastore" title="Direct link to Re-Pointing the Metastore" translate="no">​</a></h2>
<p>The step that makes the migration invisible to downstream users is <a class="" href="https://hudi.apache.org/docs/syncing_metastore">syncing the Hudi table to the Hive metastore</a>. Hudi's Hive sync registers the table (schema, partitions, location, input format) in the metastore and keeps it current as new commits add columns or partitions.</p>
<p>The easiest way is to enable sync on the writer itself, so every write keeps the catalog fresh:</p>
<div class="language-text codeBlockContainer_Ckt0 theme-code-block" style="--prism-color:#F8F8F2;--prism-background-color:#282A36"><div class="codeBlockContent_QJqH"><pre tabindex="0" class="prism-code language-text codeBlock_bY9V thin-scrollbar" style="color:#F8F8F2;background-color:#282A36"><code class="codeBlockLines_e6Vv"><span class="token-line" style="color:#F8F8F2"><span class="token plain">hoodie.datasource.meta.sync.enable=true</span><br></span><span class="token-line" style="color:#F8F8F2"><span class="token plain">hoodie.datasource.hive_sync.mode=hms</span><br></span><span class="token-line" style="color:#F8F8F2"><span class="token plain">hoodie.datasource.hive_sync.metastore.uris=thrift://hive-metastore:9083</span><br></span><span class="token-line" style="color:#F8F8F2"><span class="token plain">hoodie.datasource.hive_sync.database=mydb</span><br></span><span class="token-line" style="color:#F8F8F2"><span class="token plain">hoodie.datasource.hive_sync.table=trips</span><br></span></code></pre></div></div>
<p>Alternatively, run the standalone <code>HiveSyncTool</code> from the command line for a one-shot or externally scheduled sync:</p>
<div class="language-bash codeBlockContainer_Ckt0 theme-code-block" style="--prism-color:#F8F8F2;--prism-background-color:#282A36"><div class="codeBlockContent_QJqH"><pre tabindex="0" class="prism-code language-bash codeBlock_bY9V thin-scrollbar" style="color:#F8F8F2;background-color:#282A36"><code class="codeBlockLines_e6Vv"><span class="token-line" style="color:#F8F8F2"><span class="token plain">cd hudi-hive</span><br></span><span class="token-line" style="color:#F8F8F2"><span class="token plain">./run_sync_tool.sh --jdbc-url jdbc:hive2://hiveserver:10000 \</span><br></span><span class="token-line" style="color:#F8F8F2"><span class="token plain">  --user hive --pass hive \</span><br></span><span class="token-line" style="color:#F8F8F2"><span class="token plain">  --partitioned-by ds --base-path hdfs://ns1/warehouse/hudi/trips \</span><br></span><span class="token-line" style="color:#F8F8F2"><span class="token plain">  --database mydb --table trips</span><br></span></code></pre></div></div>
<p>Sync supports three modes — <code>hms</code> (metastore thrift APIs, recommended), <code>jdbc</code> (via HiveServer2) and <code>hiveql</code> — detailed in the <a class="" href="https://hudi.apache.org/docs/syncing_metastore">sync doc</a>. A few migration-specific notes:</p>
<ul>
<li class=""><strong>Partition values</strong> are extracted by <code>hoodie.datasource.hive_sync.partition_value_extractor</code>. The default <code>MultiPartKeysValueExtractor</code> handles multi-level partitions split on <code>/</code>; hive-style partitioned and non-partitioned tables get <code>HiveStylePartitionValueExtractor</code> and <code>NonPartitionedExtractor</code> inferred automatically.</li>
<li class=""><strong>CoW tables</strong> appear as a single table under the same name your users expect — one metastore entry, drop-in replacement.</li>
<li class=""><strong>MoR tables</strong> sync as two views: <code>&lt;table&gt;_ro</code> (read-optimized — queries only the compacted columnar base files) and <code>&lt;table&gt;_rt</code> (real-time/snapshot — merges in the latest log files for the freshest data). Point latency-sensitive dashboards at <code>_ro</code> and freshness-sensitive consumers at <code>_rt</code>. If you migrate a Hive table to MoR, plan for downstream queries to pick one of the two names.</li>
<li class="">If you sync to AWS Glue instead of a self-managed metastore, the flow is the same with a Glue-specific sync client — see <a class="" href="https://hudi.apache.org/docs/syncing_aws_glue_data_catalog">Syncing to AWS Glue Data Catalog</a>.</li>
</ul>
<h2 class="anchor anchorTargetStickyNavbar_Vzrq" id="validating-with-your-existing-queries">Validating with Your Existing Queries<a href="https://hudi.apache.org/blog/2026/07/30/migrating-from-hive-to-hudi#validating-with-your-existing-queries" class="hash-link" aria-label="Direct link to Validating with Your Existing Queries" title="Direct link to Validating with Your Existing Queries" translate="no">​</a></h2>
<p>Before flipping any traffic, verify the migrated table from every engine you actually use:</p>
<p><strong>Row counts and spot checks.</strong> Compare <code>SELECT COUNT(*)</code> between old and new, overall and per partition. Spot-check a sample of records by key. Note that Hudi adds meta columns (<code>_hoodie_commit_time</code>, <code>_hoodie_record_key</code>, etc.) — <code>SELECT *</code> outputs will include them, so audit any consumers doing positional column access.</p>
<p><strong>Hive.</strong> Query through beeline; ensure the <code>hudi-hadoop-mr-bundle</code> jar is available to Hive (e.g. in its <code>auxlib/</code> directory) and set the input format so splits are computed correctly:</p>
<div class="language-text codeBlockContainer_Ckt0 theme-code-block" style="--prism-color:#F8F8F2;--prism-background-color:#282A36"><div class="codeBlockContent_QJqH"><pre tabindex="0" class="prism-code language-text codeBlock_bY9V thin-scrollbar" style="color:#F8F8F2;background-color:#282A36"><code class="codeBlockLines_e6Vv"><span class="token-line" style="color:#F8F8F2"><span class="token plain">beeline -u jdbc:hive2://hiveserver:10000/mydb \</span><br></span><span class="token-line" style="color:#F8F8F2"><span class="token plain">  --hiveconf hive.input.format=org.apache.hadoop.hive.ql.io.HiveInputFormat</span><br></span></code></pre></div></div>
<p><strong>Spark and Trino/Presto.</strong> These engines have native Hudi support and resolve the synced table straight from the metastore — run your representative dashboards and pipeline queries against the new table name. For MoR, run them against both <code>_ro</code> and <code>_rt</code> so you understand the freshness difference before choosing defaults.</p>
<p><strong>Exercise the new capabilities.</strong> Do a small <code>UPDATE</code>/<code>DELETE</code> or an upsert write, confirm it lands atomically, and try an incremental query — this is, after all, why you migrated.</p>
<h2 class="anchor anchorTargetStickyNavbar_Vzrq" id="decommissioning-the-old-table-safely">Decommissioning the Old Table Safely<a href="https://hudi.apache.org/blog/2026/07/30/migrating-from-hive-to-hudi#decommissioning-the-old-table-safely" class="hash-link" aria-label="Direct link to Decommissioning the Old Table Safely" title="Direct link to Decommissioning the Old Table Safely" translate="no">​</a></h2>
<p>Once validation passes and writers have been cut over to Hudi, retire the old table deliberately:</p>
<ol>
<li class=""><strong>Freeze writes to the old Hive table first.</strong> Run the final bootstrap or rewrite <em>after</em> the freeze so no records land in the old location and get missed. (For rewrites you can also do an initial bulk load, then a short catch-up pass for late data before cutover.)</li>
<li class=""><strong>Repoint consumers, then watch.</strong> Keep the old table readable during a burn-in period while dashboards and pipelines run against the Hudi table.</li>
<li class=""><strong>Mind the METADATA_ONLY dependency.</strong> If you bootstrapped with METADATA_ONLY, the Hudi table still references the original data files — deleting or modifying them causes data loss or corruption. Either keep the original files permanently (drop only the old metastore <em>entry</em>, not the data), or convert to fully Hudi-managed files before deleting anything.</li>
<li class=""><strong>Drop the metastore entry last.</strong> For an external Hive table, <code>DROP TABLE</code> removes only metadata; delete the underlying directory separately, and only when rule 3 allows.</li>
</ol>
<h2 class="anchor anchorTargetStickyNavbar_Vzrq" id="conclusion">Conclusion<a href="https://hudi.apache.org/blog/2026/07/30/migrating-from-hive-to-hudi#conclusion" class="hash-link" aria-label="Direct link to Conclusion" title="Direct link to Conclusion" translate="no">​</a></h2>
<p>Migrating from Hive to Hudi is less a leap than a re-registration: the files stay open columnar, the catalog stays the metastore, the engines stay Spark, Hive, Trino and Presto. What changes is the table layer in between — and with it, everything a plain Hive table couldn't do. Choose in-place <a class="" href="https://hudi.apache.org/docs/migration_guide">bootstrap</a> (METADATA_ONLY, FULL_RECORD, or a regex-selected mix) for large tables, a <code>bulk_insert</code> rewrite for smaller ones, wire up <a class="" href="https://hudi.apache.org/docs/syncing_metastore">metastore sync</a> so the table appears exactly where your users already look, validate from every engine, and decommission the old table with the METADATA_ONLY caveat in mind. From that point on, your formerly static Hive table takes upserts, deletes, ACID commits and incremental reads like any other Hudi table.</p>
<h2 class="anchor anchorTargetStickyNavbar_Vzrq" id="faq">FAQ<a href="https://hudi.apache.org/blog/2026/07/30/migrating-from-hive-to-hudi#faq" class="hash-link" aria-label="Direct link to FAQ" title="Direct link to FAQ" translate="no">​</a></h2>
<section><details style="margin-bottom:0.75rem"><summary><strong>Can Hive still query a table after it's migrated to Hudi?</strong></summary><p style="margin-top:0.5rem">Yes. Hudi's Hive sync registers the table in the Hive metastore with Hudi's input format, and Hive queries it through the hudi-hadoop-mr bundle. Spark, Presto and Trino also query the synced table through their native Hudi integrations, so existing SQL keeps working.</p></details><details style="margin-bottom:0.75rem"><summary><strong>Do I need to stop writes during the migration?</strong></summary><p style="margin-top:0.5rem">Only briefly. Freeze writes to the old Hive table, run the final bootstrap or catch-up rewrite pass, then cut writers over to the Hudi table. For rewrite migrations you can bulk load most of the data ahead of time and keep the write freeze down to a short catch-up window.</p></details><details style="margin-bottom:0.75rem"><summary><strong>What happens to my Hive partitions?</strong></summary><p style="margin-top:0.5rem">They map directly. Hudi preserves hive style key=value partition directories when hive_style_partitioning is enabled, multi level partitions map to a comma separated partition path field list, and Hive sync re-registers all partitions in the metastore using a partition value extractor that is inferred automatically in common cases.</p></details><details style="margin-bottom:0.75rem"><summary><strong>Should I use METADATA_ONLY or FULL_RECORD bootstrap?</strong></summary><p style="margin-top:0.5rem">METADATA_ONLY writes only skeleton files with record keys and avoids rewriting data, making it ideal for large cold partitions, but the original files must never be deleted or modified. FULL_RECORD copies data into fully Hudi managed files, equivalent to a bulk insert. A common pattern applies FULL_RECORD to recent hot partitions and METADATA_ONLY to the rest using a regex mode selector.</p></details><details style="margin-bottom:0.75rem"><summary><strong>What are the _ro and _rt tables that appear after migrating to Merge-on-Read?</strong></summary><p style="margin-top:0.5rem">For a Merge-on-Read table, Hive sync registers two views in the metastore: a read optimized view suffixed _ro that queries only compacted columnar base files, and a real time view suffixed _rt that merges in the latest log files for the freshest data. Copy-on-Write tables sync as a single table under the original name.</p></details><details style="margin-bottom:0.75rem"><summary><strong>Can I migrate only part of a Hive table to Hudi?</strong></summary><p style="margin-top:0.5rem">Yes. Because Hudi manages data at Hive partition granularity, an append only table can start writing new partitions through Hudi while historical partitions stay untouched. The caveat is that upserts and incremental queries only work on the Hudi managed partitions, so update heavy tables should be converted fully.</p></details></section>]]></content:encoded>
            <category>migration</category>
            <category>bootstrap</category>
            <category>apache hive</category>
            <category>guide</category>
        </item>
        <item>
            <title><![CDATA[Migrating from Parquet to Apache Hudi: A Practical Guide]]></title>
            <link>https://hudi.apache.org/blog/2026/07/29/migrating-from-parquet-to-hudi</link>
            <guid>https://hudi.apache.org/blog/2026/07/29/migrating-from-parquet-to-hudi</guid>
            <pubDate>Wed, 29 Jul 2026 00:00:00 GMT</pubDate>
            <description><![CDATA[How to migrate Parquet tables to Apache Hudi: in-place bootstrap vs full rewrite with bulk_insert, key and partitioning choices, code examples, validation and rollback.]]></description>
            <content:encoded><![CDATA[<p>You can migrate existing Parquet datasets to Apache Hudi either in place — using Hudi's bootstrap operation, which adds Hudi metadata without rewriting data files — or by rewriting the data through a one-time bulk insert. Which path you take comes down to how large the table is, how much rewrite cost and downtime you can absorb, and how the table will be written to afterwards. This guide walks through both paths end to end: choosing a strategy, making the up-front design decisions that are hard to reverse, running the migration with current APIs, validating the result, and what to do if you need to back out.</p>
<p>Why migrate at all? A directory of Parquet files is a perfectly good archive, but it is not a table. It cannot accept updates or deletes without rewriting whole partitions, it offers no transactional guarantees to concurrent readers and writers, and downstream consumers must rescan everything to find what changed. Moving that data under Hudi management gets you <a class="" href="https://hudi.apache.org/blog/2026/07/15/what-is-upsert-on-a-data-lake">upserts and deletes</a> keyed on individual records, ACID commits on the <a class="" href="https://hudi.apache.org/docs/timeline">timeline</a>, incremental queries that expose only what changed since a point in time, and automatic file-size management — on the data you already have, queryable by the engines you already use.</p>
<h2 class="anchor anchorTargetStickyNavbar_Vzrq" id="choose-your-migration-strategy">Choose Your Migration Strategy<a href="https://hudi.apache.org/blog/2026/07/29/migrating-from-parquet-to-hudi#choose-your-migration-strategy" class="hash-link" aria-label="Direct link to Choose Your Migration Strategy" title="Direct link to Choose Your Migration Strategy" translate="no">​</a></h2>
<p>Hudi gives you three ways to bring an existing Parquet table under management, described in detail in the <a class="" href="https://hudi.apache.org/docs/migration_guide">migration guide</a>. Two of them are variants of the <em>bootstrap</em> operation, which builds Hudi's timeline and metadata over data that stays where it is; the third is a plain rewrite.</p>
<ul>
<li class=""><strong>METADATA_ONLY bootstrap (in place).</strong> Hudi generates <em>skeleton</em> files containing only the record-level metadata columns and key information, one skeleton per original Parquet file, and links them to your original data files. The original data is never copied, so a multi-terabyte table can be migrated in a fraction of the time a rewrite would take. The trade-off: Hudi continues to rely on the original files, so if they are deleted or modified outside Hudi, the table is corrupted. The mechanics of skeleton files are covered in the original deep-dive, <a class="" href="https://hudi.apache.org/blog/2020/08/20/efficient-migration-of-large-parquet-tables">Efficient Migration of Large Parquet Tables to Apache Hudi</a>.</li>
<li class=""><strong>FULL_RECORD bootstrap.</strong> Hudi copies the full records into new Hudi files, adding the metadata columns as it goes. This is functionally equivalent to a bulk insert — after it completes, the table is self-contained and the original files can eventually be retired. You pay the full rewrite cost.</li>
<li class=""><strong>Full rewrite with <code>bulk_insert</code>.</strong> Read the Parquet data with Spark and write it out as a new Hudi table using the <a class="" href="https://hudi.apache.org/docs/write_operations#bulk_insert"><code>bulk_insert</code> write operation</a>. Operationally this is the simplest path — it is just a Spark job using the same datasource API you will use for all subsequent writes — and it gives you a chance to re-layout the data (sorting, partitioning) as you write.</li>
</ul>
<p>The bootstrap modes can be mixed <em>within one table, per partition</em>: a common pattern is FULL_RECORD for a small set of hot, frequently-updated partitions and METADATA_ONLY for the long tail of cold history.</p>
<table><thead><tr><th></th><th>METADATA_ONLY bootstrap</th><th>FULL_RECORD bootstrap</th><th>Full rewrite (<code>bulk_insert</code>)</th></tr></thead><tbody><tr><td>Data files rewritten</td><td>No — skeleton files only</td><td>Yes</td><td>Yes</td></tr><tr><td>Migration time / cost</td><td>Low, proportional to metadata</td><td>High, proportional to data size</td><td>High, proportional to data size</td></tr><tr><td>Original Parquet files after migration</td><td>Still required — must not be modified or deleted</td><td>Independent; originals can be retired</td><td>Independent; originals can be retired</td></tr><tr><td>Resulting table</td><td>Hudi table referencing external base files</td><td>Self-contained Hudi table</td><td>Self-contained Hudi table</td></tr><tr><td>Chance to re-sort / re-layout data</td><td>No</td><td>No</td><td>Yes (<code>hoodie.bulkinsert.sort.mode</code>)</td></tr><tr><td>Best for</td><td>Very large tables where rewrite is impractical</td><td>Hot partitions of a large table (via regex selector)</td><td>Small-to-medium tables, or when you want a clean re-layout</td></tr></tbody></table>
<p>Decision rule of thumb: if the table is small enough that rewriting it is an overnight Spark job you can afford to run once, take the full rewrite — it leaves no strings attached to the old files. If the table is tens of terabytes or more and a rewrite is measured in days and real money, bootstrap in place with METADATA_ONLY, optionally upgrading hot partitions to FULL_RECORD.</p>
<h2 class="anchor anchorTargetStickyNavbar_Vzrq" id="before-you-start-three-decisions-that-are-hard-to-change">Before You Start: Three Decisions That Are Hard to Change<a href="https://hudi.apache.org/blog/2026/07/29/migrating-from-parquet-to-hudi#before-you-start-three-decisions-that-are-hard-to-change" class="hash-link" aria-label="Direct link to Before You Start: Three Decisions That Are Hard to Change" title="Direct link to Before You Start: Three Decisions That Are Hard to Change" translate="no">​</a></h2>
<p>However you migrate, you are about to give this dataset a table identity. Three choices deserve deliberate thought <em>before</em> you run anything, because changing them later effectively means migrating again.</p>
<ol>
<li class=""><strong>Record key.</strong> The field (or comma-separated fields) that uniquely identifies each record — set via <code>hoodie.datasource.write.recordkey.field</code>. Every upsert and delete is keyed on it, and <a class="" href="https://hudi.apache.org/docs/indexes">indexes</a> map keys to file groups. Pick something genuinely unique and stable: a natural business key or an existing surrogate key. See <a class="" href="https://hudi.apache.org/docs/key_generation">key generation</a> for composite and timestamp-based options.</li>
<li class=""><strong>Ordering field(s).</strong> Set via <code>hoodie.table.ordering.fields</code>. When two records with the same key collide — within one batch or across writes — the record with the larger ordering value wins. An event timestamp or a monotonically increasing version column is the usual choice.</li>
<li class=""><strong>Partitioning.</strong> Set via <code>hoodie.datasource.write.partitionpath.field</code>. If you bootstrap in place, your existing directory layout <em>is</em> your partitioning, so make peace with it first. If you do a full rewrite, this is your one cheap opportunity to fix a bad partition scheme — too many tiny partitions, or partitioning on a column no query filters on. Also decide on <code>hoodie.datasource.write.hive_style_partitioning</code> (<code>city=chennai/</code> style paths) now, for catalog compatibility.</li>
</ol>
<p>You will also choose a <a class="" href="https://hudi.apache.org/docs/table_types">table type</a> — Copy-on-Write is the right default for a freshly migrated table; you can adopt Merge-on-Read where write latency demands it.</p>
<h2 class="anchor anchorTargetStickyNavbar_Vzrq" id="path-a-full-rewrite-with-bulk_insert">Path A: Full Rewrite with bulk_insert<a href="https://hudi.apache.org/blog/2026/07/29/migrating-from-parquet-to-hudi#path-a-full-rewrite-with-bulk_insert" class="hash-link" aria-label="Direct link to Path A: Full Rewrite with bulk_insert" title="Direct link to Path A: Full Rewrite with bulk_insert" translate="no">​</a></h2>
<p>For small and medium tables, migration is a single Spark job: read Parquet, write Hudi. The <code>bulk_insert</code> operation exists precisely for this — unlike <code>upsert</code>/<code>insert</code>, it uses a disk-based, sort-oriented write path that scales to very large initial loads without caching input in memory (see <a class="" href="https://hudi.apache.org/docs/write_operations">write operations</a> for the full comparison).</p>
<div class="language-scala codeBlockContainer_Ckt0 theme-code-block" style="--prism-color:#F8F8F2;--prism-background-color:#282A36"><div class="codeBlockContent_QJqH"><pre tabindex="0" class="prism-code language-scala codeBlock_bY9V thin-scrollbar" style="color:#F8F8F2;background-color:#282A36"><code class="codeBlockLines_e6Vv"><span class="token-line" style="color:#F8F8F2"><span class="token plain">// spark-shell with the Hudi bundle, per the quick start guide</span><br></span><span class="token-line" style="color:#F8F8F2"><span class="token plain">val df = spark.read.format("parquet").load("s3://my-bucket/warehouse/trips_parquet")</span><br></span><span class="token-line" style="color:#F8F8F2"><span class="token plain" style="display:inline-block"></span><br></span><span class="token-line" style="color:#F8F8F2"><span class="token plain">df.write.format("hudi").</span><br></span><span class="token-line" style="color:#F8F8F2"><span class="token plain">  option("hoodie.table.name", "trips").</span><br></span><span class="token-line" style="color:#F8F8F2"><span class="token plain">  option("hoodie.datasource.write.operation", "bulk_insert").</span><br></span><span class="token-line" style="color:#F8F8F2"><span class="token plain">  option("hoodie.datasource.write.recordkey.field", "trip_id").</span><br></span><span class="token-line" style="color:#F8F8F2"><span class="token plain">  option("hoodie.datasource.write.partitionpath.field", "city").</span><br></span><span class="token-line" style="color:#F8F8F2"><span class="token plain">  option("hoodie.table.ordering.fields", "ts").</span><br></span><span class="token-line" style="color:#F8F8F2"><span class="token plain">  option("hoodie.datasource.write.hive_style_partitioning", "true").</span><br></span><span class="token-line" style="color:#F8F8F2"><span class="token plain">  mode("overwrite").</span><br></span><span class="token-line" style="color:#F8F8F2"><span class="token plain">  save("s3://my-bucket/warehouse/trips")</span><br></span></code></pre></div></div>
<p>Two knobs worth knowing:</p>
<ul>
<li class=""><code>hoodie.bulkinsert.sort.mode</code> controls layout of the written files. The default <code>NONE</code> matches plain <code>spark.write.parquet()</code> in speed and file count; <code>GLOBAL_SORT</code> costs a sort but produces the best file sizes and, if you sort by a commonly filtered column, better data skipping forever after.</li>
<li class="">Note that <code>bulk_insert</code> does a best-effort job at file sizing rather than guaranteeing it; ongoing <code>upsert</code>/<code>insert</code> writes will progressively correct small files.</li>
</ul>
<p>From here on, the table behaves exactly like any Hudi table in the <a class="" href="https://hudi.apache.org/docs/quick-start-guide">quick start guide</a>: switch subsequent writes to <code>upsert</code> with <code>mode("append")</code>, and query it from Spark, Trino, Presto, or Hive.</p>
<h2 class="anchor anchorTargetStickyNavbar_Vzrq" id="path-b-in-place-bootstrap-for-large-tables">Path B: In-Place Bootstrap for Large Tables<a href="https://hudi.apache.org/blog/2026/07/29/migrating-from-parquet-to-hudi#path-b-in-place-bootstrap-for-large-tables" class="hash-link" aria-label="Direct link to Path B: In-Place Bootstrap for Large Tables" title="Direct link to Path B: In-Place Bootstrap for Large Tables" translate="no">​</a></h2>
<p>When rewriting is off the table, use bootstrap. The two most convenient front-ends are the Hudi Streamer utility and the Spark SQL <code>CALL</code> procedure; both drive the same underlying mechanism, and both are documented in the <a class="" href="https://hudi.apache.org/docs/migration_guide">migration guide</a>.</p>
<h3 class="anchor anchorTargetStickyNavbar_Vzrq" id="using-hudi-streamer">Using Hudi Streamer<a href="https://hudi.apache.org/blog/2026/07/29/migrating-from-parquet-to-hudi#using-hudi-streamer" class="hash-link" aria-label="Direct link to Using Hudi Streamer" title="Direct link to Using Hudi Streamer" translate="no">​</a></h3>
<p><a class="" href="https://hudi.apache.org/docs/hoodie_streaming_ingestion#hudi-streamer">Hudi Streamer</a> supports bootstrap via the <code>--run-bootstrap</code> flag. This example applies FULL_RECORD mode to all partitions matching the regex <code>.*</code> using the regex mode selector — change the regex to bootstrap only hot partitions as FULL_RECORD, and unmatched partitions get the other mode:</p>
<div class="language-bash codeBlockContainer_Ckt0 theme-code-block" style="--prism-color:#F8F8F2;--prism-background-color:#282A36"><div class="codeBlockContent_QJqH"><pre tabindex="0" class="prism-code language-bash codeBlock_bY9V thin-scrollbar" style="color:#F8F8F2;background-color:#282A36"><code class="codeBlockLines_e6Vv"><span class="token-line" style="color:#F8F8F2"><span class="token plain">spark-submit \</span><br></span><span class="token-line" style="color:#F8F8F2"><span class="token plain">--conf 'spark.serializer=org.apache.spark.serializer.KryoSerializer' \</span><br></span><span class="token-line" style="color:#F8F8F2"><span class="token plain">--class org.apache.hudi.utilities.streamer.HoodieStreamer /path/to/hudi-utilities-bundle.jar \</span><br></span><span class="token-line" style="color:#F8F8F2"><span class="token plain">--run-bootstrap \</span><br></span><span class="token-line" style="color:#F8F8F2"><span class="token plain">--target-base-path s3://my-bucket/warehouse/trips \</span><br></span><span class="token-line" style="color:#F8F8F2"><span class="token plain">--target-table trips \</span><br></span><span class="token-line" style="color:#F8F8F2"><span class="token plain">--table-type COPY_ON_WRITE \</span><br></span><span class="token-line" style="color:#F8F8F2"><span class="token plain">--hoodie-conf hoodie.bootstrap.base.path=s3://my-bucket/warehouse/trips_parquet \</span><br></span><span class="token-line" style="color:#F8F8F2"><span class="token plain">--hoodie-conf hoodie.datasource.write.recordkey.field=trip_id \</span><br></span><span class="token-line" style="color:#F8F8F2"><span class="token plain">--hoodie-conf hoodie.datasource.write.partitionpath.field=city \</span><br></span><span class="token-line" style="color:#F8F8F2"><span class="token plain">--hoodie-conf hoodie.table.ordering.fields=ts \</span><br></span><span class="token-line" style="color:#F8F8F2"><span class="token plain">--hoodie-conf hoodie.bootstrap.mode.selector=org.apache.hudi.client.bootstrap.selector.BootstrapRegexModeSelector \</span><br></span><span class="token-line" style="color:#F8F8F2"><span class="token plain">--hoodie-conf hoodie.bootstrap.mode.selector.regex='.*' \</span><br></span><span class="token-line" style="color:#F8F8F2"><span class="token plain">--hoodie-conf hoodie.bootstrap.mode.selector.regex.mode=FULL_RECORD \</span><br></span><span class="token-line" style="color:#F8F8F2"><span class="token plain">--hoodie-conf hoodie.datasource.write.hive_style_partitioning=true</span><br></span></code></pre></div></div>
<p>The key config is <code>hoodie.bootstrap.base.path</code> — the location of the existing Parquet dataset. If you supply <em>only</em> that config, Hudi defaults to METADATA_ONLY mode for every partition (<code>hoodie.bootstrap.mode.selector</code> defaults to <code>MetadataOnlyBootstrapModeSelector</code>), which is exactly the cheap in-place migration most large tables want.</p>
<h3 class="anchor anchorTargetStickyNavbar_Vzrq" id="using-the-spark-sql-call-procedure">Using the Spark SQL CALL procedure<a href="https://hudi.apache.org/blog/2026/07/29/migrating-from-parquet-to-hudi#using-the-spark-sql-call-procedure" class="hash-link" aria-label="Direct link to Using the Spark SQL CALL procedure" title="Direct link to Using the Spark SQL CALL procedure" translate="no">​</a></h3>
<p>If you live in Spark SQL, the <a class="" href="https://hudi.apache.org/docs/procedures#run_bootstrap"><code>run_bootstrap</code> procedure</a> does the same job:</p>
<div class="language-sql codeBlockContainer_Ckt0 theme-code-block" style="--prism-color:#F8F8F2;--prism-background-color:#282A36"><div class="codeBlockContent_QJqH"><pre tabindex="0" class="prism-code language-sql codeBlock_bY9V thin-scrollbar" style="color:#F8F8F2;background-color:#282A36"><code class="codeBlockLines_e6Vv"><span class="token-line" style="color:#F8F8F2"><span class="token keyword" style="color:rgb(189, 147, 249);font-style:italic">CALL</span><span class="token plain"> run_bootstrap</span><span class="token punctuation" style="color:rgb(248, 248, 242)">(</span><span class="token plain"></span><br></span><span class="token-line" style="color:#F8F8F2"><span class="token plain">  </span><span class="token keyword" style="color:rgb(189, 147, 249);font-style:italic">table</span><span class="token plain"> </span><span class="token operator">=</span><span class="token operator">&gt;</span><span class="token plain"> </span><span class="token string" style="color:rgb(255, 121, 198)">'trips'</span><span class="token punctuation" style="color:rgb(248, 248, 242)">,</span><span class="token plain"></span><br></span><span class="token-line" style="color:#F8F8F2"><span class="token plain">  table_type </span><span class="token operator">=</span><span class="token operator">&gt;</span><span class="token plain"> </span><span class="token string" style="color:rgb(255, 121, 198)">'COPY_ON_WRITE'</span><span class="token punctuation" style="color:rgb(248, 248, 242)">,</span><span class="token plain"></span><br></span><span class="token-line" style="color:#F8F8F2"><span class="token plain">  bootstrap_path </span><span class="token operator">=</span><span class="token operator">&gt;</span><span class="token plain"> </span><span class="token string" style="color:rgb(255, 121, 198)">'s3://my-bucket/warehouse/trips_parquet'</span><span class="token punctuation" style="color:rgb(248, 248, 242)">,</span><span class="token plain"></span><br></span><span class="token-line" style="color:#F8F8F2"><span class="token plain">  base_path </span><span class="token operator">=</span><span class="token operator">&gt;</span><span class="token plain"> </span><span class="token string" style="color:rgb(255, 121, 198)">'s3://my-bucket/warehouse/trips'</span><span class="token punctuation" style="color:rgb(248, 248, 242)">,</span><span class="token plain"></span><br></span><span class="token-line" style="color:#F8F8F2"><span class="token plain">  rowKey_field </span><span class="token operator">=</span><span class="token operator">&gt;</span><span class="token plain"> </span><span class="token string" style="color:rgb(255, 121, 198)">'trip_id'</span><span class="token punctuation" style="color:rgb(248, 248, 242)">,</span><span class="token plain"></span><br></span><span class="token-line" style="color:#F8F8F2"><span class="token plain">  partition_path_field </span><span class="token operator">=</span><span class="token operator">&gt;</span><span class="token plain"> </span><span class="token string" style="color:rgb(255, 121, 198)">'city'</span><span class="token plain"></span><br></span><span class="token-line" style="color:#F8F8F2"><span class="token plain"></span><span class="token punctuation" style="color:rgb(248, 248, 242)">)</span><span class="token punctuation" style="color:rgb(248, 248, 242)">;</span><br></span></code></pre></div></div>
<p><code>bootstrap_path</code> points at the existing Parquet data and <code>base_path</code> at the new Hudi table location. The default <code>selector_class</code> is again <code>MetadataOnlyBootstrapModeSelector</code>; pass <code>selector_class =&gt; 'org.apache.hudi.client.bootstrap.selector.FullRecordBootstrapModeSelector'</code> for a full-record bootstrap. There is also a <a class="" href="https://hudi.apache.org/docs/migration_guide#using-hudi-cli">Hudi CLI path</a> (<code>bootstrap run</code>) if you prefer driving it from the CLI, and a plain Spark datasource loop for partition-by-partition conversion.</p>
<p>One operational note for METADATA_ONLY tables: from this point on, the original Parquet location is part of your table. Lock it down — revoke write access, exclude it from retention/lifecycle policies — because any modification or deletion there corrupts the Hudi table.</p>
<h2 class="anchor anchorTargetStickyNavbar_Vzrq" id="migrating-new-partitions-only">Migrating New Partitions Only<a href="https://hudi.apache.org/blog/2026/07/29/migrating-from-parquet-to-hudi#migrating-new-partitions-only" class="hash-link" aria-label="Direct link to Migrating New Partitions Only" title="Direct link to Migrating New Partitions Only" translate="no">​</a></h2>
<p>There is a zero-migration option worth knowing: keep historical partitions as plain Parquet and let Hudi manage only <em>new</em> partitions, as described in the <a class="" href="https://hudi.apache.org/docs/migration_guide#use-hudi-for-new-partitions-alone">migration guide</a>. Hudi tolerates such mixed tables as long as each partition is entirely Hudi-managed or entirely not. Start writing new partitions (or convert just the last N) through Hudi, and leave the rest untouched.</p>
<p>The caveat is fundamental: none of Hudi's primitives work on the unmanaged partitions — no upserts, no deletes, no incremental pull there. This pattern fits genuinely append-only tables (immutable event logs partitioned by date) where old partitions will never be touched again. If there is any chance of updates landing in history — CDC replication, GDPR deletes, late corrections — migrate the whole table.</p>
<h2 class="anchor anchorTargetStickyNavbar_Vzrq" id="validation-checklist">Validation Checklist<a href="https://hudi.apache.org/blog/2026/07/29/migrating-from-parquet-to-hudi#validation-checklist" class="hash-link" aria-label="Direct link to Validation Checklist" title="Direct link to Validation Checklist" translate="no">​</a></h2>
<p>Before pointing production at the new table, verify it. Bootstrap and bulk insert are one-shot operations; catching a wrong key choice now costs minutes, catching it in three months costs a re-migration.</p>
<ol>
<li class=""><strong>Row counts match.</strong> <code>spark.read.format("hudi").load(basePath).count()</code> against the source Parquet count, overall and per partition.</li>
<li class=""><strong>Keys are actually unique.</strong> Group by your record key and confirm no count exceeds 1 — duplicate keys silently collapse into one record on the first upsert.</li>
<li class=""><strong>Sample lookups.</strong> Pick a handful of known records and confirm field-level equality between source and target, including partition placement.</li>
<li class=""><strong>Exercise a write.</strong> Upsert a few records and delete one on a staging copy or a scratch partition; confirm the change is visible and row counts move as expected.</li>
<li class=""><strong>Query engines see the table.</strong> Sync the table to your catalog (Hive Metastore, AWS Glue, etc. — see <a class="" href="https://hudi.apache.org/docs/syncing_metastore">syncing to catalogs</a>) and run representative queries from every engine that matters — Spark, Trino, Presto. This matters doubly for METADATA_ONLY tables, where engines read merged skeleton + original data.</li>
<li class=""><strong>Incremental query sanity.</strong> Run an incremental query from the bootstrap commit forward and confirm it returns your test writes.</li>
</ol>
<h2 class="anchor anchorTargetStickyNavbar_Vzrq" id="rollback-plan">Rollback Plan<a href="https://hudi.apache.org/blog/2026/07/29/migrating-from-parquet-to-hudi#rollback-plan" class="hash-link" aria-label="Direct link to Rollback Plan" title="Direct link to Rollback Plan" translate="no">​</a></h2>
<p>A comforting property of both paths: the original Parquet data is never mutated. Bootstrap writes skeleton files and a timeline in a <em>new</em> base path; a full rewrite writes an entirely new copy. Your rollback plan is therefore simple:</p>
<ul>
<li class="">Keep the source Parquet dataset intact and readable until validation completes and the new table has served real workloads for an agreed soak period.</li>
<li class="">If something is wrong — bad record key, wrong partitioning — delete the Hudi base path, fix the configuration, and re-run the migration. Nothing about the source has changed.</li>
<li class="">For METADATA_ONLY bootstrap, remember the inverse: you can always abandon the Hudi table, but you can never abandon the original files while keeping it.</li>
</ul>
<p>The one thing to avoid during the migration window is dual-writing to both the old Parquet location and the new Hudi table without a plan for reconciling them. Freeze writes to the source, migrate, validate, then cut writers over to Hudi.</p>
<h2 class="anchor anchorTargetStickyNavbar_Vzrq" id="operational-follow-ups">Operational Follow-Ups<a href="https://hudi.apache.org/blog/2026/07/29/migrating-from-parquet-to-hudi#operational-follow-ups" class="hash-link" aria-label="Direct link to Operational Follow-Ups" title="Direct link to Operational Follow-Ups" translate="no">​</a></h2>
<p>Migration makes the data Hudi-managed; a few follow-ups make it fast.</p>
<ul>
<li class=""><strong>Metadata table and indexes.</strong> Hudi's <a class="" href="https://hudi.apache.org/docs/metadata">metadata table</a> eliminates file listings, and <a class="" href="https://hudi.apache.org/docs/indexes">indexes</a> — column stats for data skipping, record level index for point lookups — determine upsert performance. Review which indexes fit your workload once real update traffic starts.</li>
<li class=""><strong>Compaction, if you adopt Merge-on-Read.</strong> MoR tables need <a class="" href="https://hudi.apache.org/docs/compaction">compaction</a> to fold log files into base files; make sure it is scheduled and running.</li>
<li class=""><strong>Clustering.</strong> If you bootstrapped in place (no chance to sort at write time) or bulk-inserted with <code>NONE</code> sort mode, <a class="" href="https://hudi.apache.org/docs/clustering">clustering</a> can reorganize and sort data in the background later — regaining the layout benefits without another migration.</li>
<li class=""><strong>Cleaning and file sizing.</strong> The defaults are sane, but confirm cleaner retention matches your longest-running queries, and let ongoing writes correct any small files the initial load produced.</li>
</ul>
<h2 class="anchor anchorTargetStickyNavbar_Vzrq" id="conclusion">Conclusion<a href="https://hudi.apache.org/blog/2026/07/29/migrating-from-parquet-to-hudi#conclusion" class="hash-link" aria-label="Direct link to Conclusion" title="Direct link to Conclusion" translate="no">​</a></h2>
<p>Migrating Parquet to Hudi is not a leap; it is a choice between two well-worn paths. Small and medium tables: one <code>bulk_insert</code> Spark job and you have a self-contained Hudi table, with a free chance to fix layout on the way through. Large tables: bootstrap in place with METADATA_ONLY (upgrading hot partitions to FULL_RECORD) and skip the rewrite entirely. In both cases the original data survives untouched, so the risk profile is a validation exercise, not a bet. Decide your record key, ordering field, and partitioning deliberately, validate before cutover, and keep the source until you trust the result. Start with the <a class="" href="https://hudi.apache.org/docs/migration_guide">migration guide</a> and the <a class="" href="https://hudi.apache.org/docs/quick-start-guide">quick start</a>, and if you want the design rationale behind skeleton files, the <a class="" href="https://hudi.apache.org/blog/2020/08/20/efficient-migration-of-large-parquet-tables">deep-dive on efficient migration</a> covers it.</p>
<h2 class="anchor anchorTargetStickyNavbar_Vzrq" id="faq">FAQ<a href="https://hudi.apache.org/blog/2026/07/29/migrating-from-parquet-to-hudi#faq" class="hash-link" aria-label="Direct link to FAQ" title="Direct link to FAQ" translate="no">​</a></h2>
<section><details style="margin-bottom:0.75rem"><summary><strong>Do I have to rewrite my Parquet files to use Hudi?</strong></summary><p style="margin-top:0.5rem">No. Hudi's bootstrap operation in METADATA_ONLY mode generates small skeleton files containing only record-level metadata and links them to your existing Parquet files, so the original data is never copied or modified. A FULL_RECORD bootstrap or a bulk_insert rewrite is only needed if you want the table to be fully self-contained or want to change its layout.</p></details><details style="margin-bottom:0.75rem"><summary><strong>How long does a Parquet to Hudi migration take?</strong></summary><p style="margin-top:0.5rem">A METADATA_ONLY bootstrap only writes metadata, so it runs in a small fraction of the time of a rewrite, even on multi-terabyte tables. A full rewrite with bulk_insert or a FULL_RECORD bootstrap is proportional to data size, roughly comparable to a Spark job that reads and rewrites the whole dataset. Many teams mix the two, fully rewriting a few hot partitions and metadata-bootstrapping the cold history.</p></details><details style="margin-bottom:0.75rem"><summary><strong>Can I roll back a migration to Hudi?</strong></summary><p style="margin-top:0.5rem">Yes. Neither bootstrap nor a bulk_insert rewrite modifies the original Parquet files, so rolling back means deleting the new Hudi base path and continuing to use the source data. Keep the source dataset intact until you have validated row counts, key uniqueness, and queries from all your engines against the new table.</p></details><details style="margin-bottom:0.75rem"><summary><strong>What is the difference between METADATA_ONLY and FULL_RECORD bootstrap?</strong></summary><p style="margin-top:0.5rem">METADATA_ONLY writes skeleton files with just the Hudi metadata columns and keeps relying on your original Parquet files, so it is fast but the originals must never be modified or deleted. FULL_RECORD copies the complete records into new Hudi files, which costs a full rewrite but produces a self-contained table. You can apply different modes to different partitions of the same table using a regex-based selector.</p></details><details style="margin-bottom:0.75rem"><summary><strong>Can I keep old partitions as plain Parquet and only use Hudi for new data?</strong></summary><p style="margin-top:0.5rem">Yes, as long as each partition is entirely Hudi-managed or entirely not. This works well for append-only tables where historical partitions never change. The catch is that upserts, deletes, and incremental queries only work on the Hudi-managed partitions, so if updates might ever touch history, migrate the whole table instead.</p></details><details style="margin-bottom:0.75rem"><summary><strong>What should I decide before migrating a table to Hudi?</strong></summary><p style="margin-top:0.5rem">Three things: the record key that uniquely identifies each record, the ordering field used to pick a winner when the same key appears twice, and the partitioning scheme. These shape every subsequent write and are hard to change without re-migrating. If you bootstrap in place, the existing directory layout becomes your partitioning, so review it before you start.</p></details></section>]]></content:encoded>
            <category>migration</category>
            <category>bootstrap</category>
            <category>apache parquet</category>
            <category>apache spark</category>
            <category>guide</category>
        </item>
        <item>
            <title><![CDATA[Using Apache Hudi with Apache Iceberg: Interoperability via Apache XTable]]></title>
            <link>https://hudi.apache.org/blog/2026/07/28/using-hudi-with-apache-iceberg-via-xtable</link>
            <guid>https://hudi.apache.org/blog/2026/07/28/using-hudi-with-apache-iceberg-via-xtable</guid>
            <pubDate>Tue, 28 Jul 2026 00:00:00 GMT</pubDate>
            <description><![CDATA[How Apache XTable lets Iceberg engines and catalogs read Hudi tables as Iceberg by translating table metadata in place — no data copies, no rewrites.]]></description>
            <content:encoded><![CDATA[<p>Yes — Apache Iceberg engines and catalogs can read Apache Hudi tables. <a href="https://xtable.apache.org/" target="_blank" rel="noopener noreferrer" class="">Apache XTable</a> (incubating) translates Hudi table metadata into Apache Iceberg (and Delta Lake) metadata in place, so a single copy of data on object storage is readable as any of the three formats. No data is copied or rewritten; XTable reads the Hudi table's metadata and writes out the equivalent Iceberg metadata alongside the same Parquet files.</p>
<p>This matters because many teams believe they face a binary choice. Their streaming ingestion, CDC pipelines, and update-heavy workloads point toward Hudi's write-side strengths, but a query engine, BI tool, or managed service in their stack only speaks Iceberg — so they conclude they must give up one to keep the other. They don't. This post explains how XTable dissolves that constraint, walks through exposing a Hudi table as Iceberg step by step, and covers the limitations you should understand before putting it in production.</p>
<h2 class="anchor anchorTargetStickyNavbar_Vzrq" id="the-format-lock-in-problem">The Format Lock-In Problem<a href="https://hudi.apache.org/blog/2026/07/28/using-hudi-with-apache-iceberg-via-xtable#the-format-lock-in-problem" class="hash-link" aria-label="Direct link to The Format Lock-In Problem" title="Direct link to The Format Lock-In Problem" translate="no">​</a></h2>
<p>An <a class="" href="https://hudi.apache.org/blog/2026/07/14/what-is-an-open-table-format">open table format</a> is the metadata layer that turns Parquet files on object storage into a transactional table — the load-bearing component of the <a class="" href="https://hudi.apache.org/blog/2024/07/11/what-is-a-data-lakehouse">data lakehouse</a> architecture. Apache Hudi, Apache Iceberg, and Delta Lake all implement this idea, and all three ultimately describe the same thing: a set of Parquet data files plus metadata recording which files form the table, what schema they follow, and how the table has changed over time.</p>
<p>The friction arises at the edges of the ecosystem. Some engines and managed services support only one format for external tables — most commonly Iceberg, given its broad catalog and vendor ecosystem. If your organization has standardized its serving layer on such an engine, every table you want to query there apparently needs to be an Iceberg table.</p>
<p>At the same time, Hudi's design center is the write path. Teams pick Hudi for fast upserts and deletes backed by <a class="" href="https://hudi.apache.org/docs/indexes">pluggable, multi-modal indexing</a>, streaming and CDC ingestion, incremental processing, and built-in <a class="" href="https://hudi.apache.org/docs/hudi_stack">table services</a> — compaction, clustering, cleaning — that keep tables optimized without separate orchestration. Giving those up to satisfy a read-side format requirement is a real cost, paid on every pipeline, every day.</p>
<p>The choice is false because the formats differ in metadata, not data. If the Parquet files can stay put and only the metadata needs a second representation, no migration is required — just a translation. That is precisely what Apache XTable does.</p>
<h2 class="anchor anchorTargetStickyNavbar_Vzrq" id="what-is-apache-xtable">What Is Apache XTable?<a href="https://hudi.apache.org/blog/2026/07/28/using-hudi-with-apache-iceberg-via-xtable#what-is-apache-xtable" class="hash-link" aria-label="Direct link to What Is Apache XTable?" title="Direct link to What Is Apache XTable?" translate="no">​</a></h2>
<p><a href="https://xtable.apache.org/" target="_blank" rel="noopener noreferrer" class="">Apache XTable</a> is an open source project incubating at the Apache Software Foundation (it was previously known as OneTable). In the project's own words, it provides "cross-table omni-directional interop between lakehouse table formats." Two properties define it:</p>
<ul>
<li class=""><strong>It is not another table format.</strong> XTable introduces no new specification for engines to adopt. It provides abstractions and tools for translating existing table format metadata between Apache Hudi, Apache Iceberg, and Delta Lake.</li>
<li class=""><strong>It translates metadata, not data.</strong> XTable reads the existing metadata of your table and writes out metadata for one or more other table formats into the same table directory. The Parquet data files are never copied, moved, or rewritten.</li>
</ul>
<p>Any of the three formats can act as the source, and you can target one or several formats in a single sync. For this post's scenario, the source is Hudi and the target is Iceberg — but the same mechanics work Iceberg-to-Hudi, Delta-to-Iceberg, and every other direction.</p>
<p>The Hudi community treats this as a first-class interoperability path: the <a class="" href="https://hudi.apache.org/docs/syncing_xtable">XTable sync documentation</a> covers it, and Hudi 1.1's pluggable table format work goes further by letting XTable supply format adapters inside Hudi's own write path (more on that below).</p>
<h2 class="anchor anchorTargetStickyNavbar_Vzrq" id="how-it-works-metadata-translation-not-data-movement">How It Works: Metadata Translation, Not Data Movement<a href="https://hudi.apache.org/blog/2026/07/28/using-hudi-with-apache-iceberg-via-xtable#how-it-works-metadata-translation-not-data-movement" class="hash-link" aria-label="Direct link to How It Works: Metadata Translation, Not Data Movement" title="Direct link to How It Works: Metadata Translation, Not Data Movement" translate="no">​</a></h2>
<p>Conceptually, every sync run does three things:</p>
<ol>
<li class=""><strong>Read the source metadata.</strong> XTable reads the Hudi table's timeline and metadata to determine the current state of the table: which data files are live, the schema, partition information, and file-level column statistics.</li>
<li class=""><strong>Translate to the target model.</strong> That state is mapped into Iceberg's metadata model — snapshots, manifests, and table metadata files describing the very same Parquet files.</li>
<li class=""><strong>Write target metadata alongside.</strong> The Iceberg metadata is written under the table's base path (in the standard <code>metadata/</code> directory Iceberg readers expect), next to Hudi's <code>.hoodie</code> directory. One directory on storage, two formats' views of it.</li>
</ol>
<p>The translation carries more than the file list. Data files are synced along with their column-level statistics and partition metadata, so Iceberg readers keep the file-skipping benefits they expect from Iceberg's own statistics. Schema updates in the source are reflected in the target table metadata. XTable also performs format-appropriate metadata maintenance on the target — for example, snapshot expiration for Iceberg targets — so translated metadata doesn't grow without bound.</p>
<p>XTable supports two sync modes. <strong>Full sync</strong> recomputes the target metadata from the source table's current state. <strong>Incremental sync</strong> translates only the changes since the last sync, which is more lightweight and performs better, especially on large tables; if incremental sync can't proceed for some reason, XTable automatically falls back to a full sync. In practice you run incremental sync on a schedule (or per commit), and correctness is preserved either way.</p>
<h2 class="anchor anchorTargetStickyNavbar_Vzrq" id="hands-on-exposing-a-hudi-table-as-iceberg">Hands-On: Exposing a Hudi Table as Iceberg<a href="https://hudi.apache.org/blog/2026/07/28/using-hudi-with-apache-iceberg-via-xtable#hands-on-exposing-a-hudi-table-as-iceberg" class="hash-link" aria-label="Direct link to Hands-On: Exposing a Hudi Table as Iceberg" title="Direct link to Hands-On: Exposing a Hudi Table as Iceberg" translate="no">​</a></h2>
<p>Suppose you have a Hudi table of orders at <code>s3://warehouse/orders</code>, written by a streaming pipeline (Hudi tables created with 0.14.0 or later are supported). Exposing it as Iceberg takes a config file and one command.</p>
<p>First, get the XTable utilities jar — build it from <a href="https://github.com/apache/incubator-xtable" target="_blank" rel="noopener noreferrer" class="">source</a> or download it from the project's GitHub packages. Then describe the translation in a YAML file:</p>
<div class="language-yaml codeBlockContainer_Ckt0 theme-code-block" style="--prism-color:#F8F8F2;--prism-background-color:#282A36"><div class="codeBlockTitle_OeMC">my_config.yaml</div><div class="codeBlockContent_QJqH"><pre tabindex="0" class="prism-code language-yaml codeBlock_bY9V thin-scrollbar" style="color:#F8F8F2;background-color:#282A36"><code class="codeBlockLines_e6Vv"><span class="token-line" style="color:#F8F8F2"><span class="token key atrule">sourceFormat</span><span class="token punctuation" style="color:rgb(248, 248, 242)">:</span><span class="token plain"> HUDI</span><br></span><span class="token-line" style="color:#F8F8F2"><span class="token plain"></span><span class="token key atrule">targetFormats</span><span class="token punctuation" style="color:rgb(248, 248, 242)">:</span><span class="token plain"></span><br></span><span class="token-line" style="color:#F8F8F2"><span class="token plain">  </span><span class="token punctuation" style="color:rgb(248, 248, 242)">-</span><span class="token plain"> ICEBERG</span><br></span><span class="token-line" style="color:#F8F8F2"><span class="token plain"></span><span class="token key atrule">datasets</span><span class="token punctuation" style="color:rgb(248, 248, 242)">:</span><span class="token plain"></span><br></span><span class="token-line" style="color:#F8F8F2"><span class="token plain">  </span><span class="token punctuation" style="color:rgb(248, 248, 242)">-</span><span class="token plain"> </span><span class="token key atrule">tableBasePath</span><span class="token punctuation" style="color:rgb(248, 248, 242)">:</span><span class="token plain"> s3</span><span class="token punctuation" style="color:rgb(248, 248, 242)">:</span><span class="token plain">//warehouse/orders</span><br></span><span class="token-line" style="color:#F8F8F2"><span class="token plain">    </span><span class="token key atrule">tableName</span><span class="token punctuation" style="color:rgb(248, 248, 242)">:</span><span class="token plain"> orders</span><br></span><span class="token-line" style="color:#F8F8F2"><span class="token plain">    </span><span class="token key atrule">partitionSpec</span><span class="token punctuation" style="color:rgb(248, 248, 242)">:</span><span class="token plain"> order_date</span><span class="token punctuation" style="color:rgb(248, 248, 242)">:</span><span class="token plain">VALUE</span><br></span></code></pre></div></div>
<p><code>sourceFormat</code> names the format to read, <code>targetFormats</code> lists one or more formats to produce (you could add <code>DELTA</code> to the list to get both at once), and each entry under <code>datasets</code> points at a table. <code>partitionSpec</code> tells XTable how the Hudi table is partitioned so it can map partitions into Iceberg's partition spec.</p>
<p>Run the sync:</p>
<div class="language-shell codeBlockContainer_Ckt0 theme-code-block" style="--prism-color:#F8F8F2;--prism-background-color:#282A36"><div class="codeBlockContent_QJqH"><pre tabindex="0" class="prism-code language-shell codeBlock_bY9V thin-scrollbar" style="color:#F8F8F2;background-color:#282A36"><code class="codeBlockLines_e6Vv"><span class="token-line" style="color:#F8F8F2"><span class="token plain">java -jar path/to/xtable-utilities-bundled.jar --datasetConfig my_config.yaml</span><br></span></code></pre></div></div>
<p>When the run completes, the table's base path contains Iceberg metadata files — schema, commit history, partitions, and column statistics — describing the same Parquet files Hudi manages. Re-running the command picks up new Hudi commits incrementally. You can batch many tables into one config, and schedule the job with whatever orchestrator you already use.</p>
<h3 class="anchor anchorTargetStickyNavbar_Vzrq" id="continuous-sync-from-the-ingestion-pipeline">Continuous sync from the ingestion pipeline<a href="https://hudi.apache.org/blog/2026/07/28/using-hudi-with-apache-iceberg-via-xtable#continuous-sync-from-the-ingestion-pipeline" class="hash-link" aria-label="Direct link to Continuous sync from the ingestion pipeline" title="Direct link to Continuous sync from the ingestion pipeline" translate="no">​</a></h3>
<p>If the table is fed by <a class="" href="https://hudi.apache.org/docs/hoodie_streaming_ingestion">Hudi Streamer</a>, you can sync every commit as it lands instead of running a separate job. Add the XTable <a href="https://github.com/apache/incubator-xtable/tree/main/hudi-support/extensions" target="_blank" rel="noopener noreferrer" class="">extensions jar</a> to the classpath, add <code>org.apache.xtable.hudi.sync.OneTableSyncTool</code> to the pipeline's sync classes, and configure the targets:</p>
<div class="language-text codeBlockContainer_Ckt0 theme-code-block" style="--prism-color:#F8F8F2;--prism-background-color:#282A36"><div class="codeBlockContent_QJqH"><pre tabindex="0" class="prism-code language-text codeBlock_bY9V thin-scrollbar" style="color:#F8F8F2;background-color:#282A36"><code class="codeBlockLines_e6Vv"><span class="token-line" style="color:#F8F8F2"><span class="token plain">hoodie.onetable.formats.to.sync=ICEBERG</span><br></span><span class="token-line" style="color:#F8F8F2"><span class="token plain">hoodie.onetable.target.metadata.retention.hr=168</span><br></span></code></pre></div></div>
<p>With this in place, the Iceberg view advances in lockstep with Hudi commits, and the freshness gap between the two views effectively disappears.</p>
<h2 class="anchor anchorTargetStickyNavbar_Vzrq" id="registering-with-catalogs-so-iceberg-engines-find-the-table">Registering with Catalogs So Iceberg Engines Find the Table<a href="https://hudi.apache.org/blog/2026/07/28/using-hudi-with-apache-iceberg-via-xtable#registering-with-catalogs-so-iceberg-engines-find-the-table" class="hash-link" aria-label="Direct link to Registering with Catalogs So Iceberg Engines Find the Table" title="Direct link to Registering with Catalogs So Iceberg Engines Find the Table" translate="no">​</a></h2>
<p>Producing Iceberg metadata is half the job; Iceberg-speaking engines discover tables through a catalog, so the synced table needs to be registered. XTable's <a href="https://xtable.apache.org/docs/catalogs-index" target="_blank" rel="noopener noreferrer" class="">catalog integration docs</a> cover registering synced target tables with Hive Metastore, AWS Glue Data Catalog, Unity Catalog, and BigLake Metastore — registration is an explicit step after the sync. XTable also supports keeping table metadata synchronized across catalogs such as Hive Metastore and Glue.</p>
<p>Once registered, the table is, from the engine's point of view, simply an Iceberg table. Cloud warehouses and services that can query Iceberg tables — Snowflake, Google BigQuery, Amazon Athena and Redshift, among others — can read it through their Iceberg support, subject to each vendor's own capabilities and requirements. Distributed SQL engines like Trino and Presto query it through their Iceberg connectors. None of them need to know the table is written by Hudi.</p>
<p>Hudi separately ships its own <a class="" href="https://hudi.apache.org/docs/syncing_metastore">catalog sync tools</a> for the Hudi-native view of the table, so the same data can be discoverable both as a Hudi table (for engines with Hudi support) and as an Iceberg table (for everything else).</p>
<h2 class="anchor anchorTargetStickyNavbar_Vzrq" id="what-this-architecture-unlocks">What This Architecture Unlocks<a href="https://hudi.apache.org/blog/2026/07/28/using-hudi-with-apache-iceberg-via-xtable#what-this-architecture-unlocks" class="hash-link" aria-label="Direct link to What This Architecture Unlocks" title="Direct link to What This Architecture Unlocks" translate="no">​</a></h2>
<p>Put together, the pattern looks like this: write once with Hudi, serve everywhere.</p>
<ul>
<li class=""><strong>Ingestion</strong> uses Hudi's strengths — record-level indexes for fast upserts and deletes, streaming and CDC ingestion, non-blocking table services, incremental pulls for downstream pipelines. See <a class="" href="https://hudi.apache.org/docs/table_types">table types</a> for how Copy-on-Write and Merge-on-Read trade write latency against read cost.</li>
<li class=""><strong>Storage</strong> holds exactly one copy of the data, as Parquet files on object storage, with both Hudi and Iceberg metadata describing it.</li>
<li class=""><strong>Serving</strong> spans every engine in the organization: Hudi-native engines read the Hudi view; Iceberg-only engines and services read the Iceberg view; nothing is exported, duplicated, or re-ingested.</li>
</ul>
<p>There is no second pipeline to operate, no divergence between "the Hudi copy" and "the Iceberg copy," and no per-engine storage bill. Format choice stops being an organization-wide standardization battle and becomes a per-workload decision about write-path capabilities.</p>
<p>This direction is also converging with Hudi itself. Starting with Hudi 1.1, a <a class="" href="https://hudi.apache.org/docs/hudi_stack">pluggable table format framework</a> decouples Hudi's storage engine — timeline, indexes, concurrency control, table services — from the metadata format written for data files, with XTable supplying format adapters for Iceberg and Delta Lake. The trajectory is that maintaining Iceberg metadata becomes part of the Hudi write path itself, rather than a companion sync.</p>
<p><img decoding="async" loading="lazy" alt="Hudi storage engine with pluggable format adapters via Apache XTable" src="https://hudi.apache.org/assets/images/pluggable_tf-c14c58c53931f285bdfe832c5014361a.png" width="894" height="610" class="img_ev3q">
</p><p align="center">Figure: Hudi's storage engine with format adapters for Iceberg and Delta Lake via Apache XTable</p><p></p>
<h2 class="anchor anchorTargetStickyNavbar_Vzrq" id="honest-limitations">Honest Limitations<a href="https://hudi.apache.org/blog/2026/07/28/using-hudi-with-apache-iceberg-via-xtable#honest-limitations" class="hash-link" aria-label="Direct link to Honest Limitations" title="Direct link to Honest Limitations" translate="no">​</a></h2>
<p>XTable is a pragmatic tool, and it's worth being precise about its boundaries.</p>
<ul>
<li class=""><strong>It's read-side interoperability.</strong> The translated Iceberg metadata gives Iceberg engines a read view of the table. Writes should continue to go through Hudi; XTable does not merge concurrent writes made independently in two formats to the same files.</li>
<li class=""><strong>Freshness trails the sync cadence.</strong> The Iceberg view reflects the table as of the last sync, so it lags the latest Hudi commit by up to the sync interval. Per-commit sync via the Hudi Streamer extension closes this gap; a nightly cron does not. Decide the cadence based on how fresh the Iceberg consumers need to be.</li>
<li class=""><strong>Merge-on-Read log files aren't translated.</strong> XTable syncs the underlying Parquet base files; Hudi log files (and, in the other directions, Delta deletion vectors and Iceberg delete files) are not captured. For a Hudi MoR table, the target format therefore sees the read-optimized view — records still sitting in log files become visible to Iceberg readers after compaction. Copy-on-Write tables don't have this caveat.</li>
<li class=""><strong>Source table requirements.</strong> XTable requires Hudi 0.14.0 or later and depends on Hudi's metadata table; check the <a href="https://xtable.apache.org/docs/features-and-limitations" target="_blank" rel="noopener noreferrer" class="">XTable docs</a> for current prerequisites and configuration.</li>
<li class=""><strong>Feature-mapping edge cases exist.</strong> The formats are not feature-identical, and format-specific constructs don't always have an exact counterpart on the other side (for example, Delta generated columns as a source have limited support). Each format's advanced features work fully only in that format's native engines.</li>
</ul>
<p>None of these undermine the core pattern — most production uses are exactly "Hudi writes, Iceberg engines read" — but they should inform table type, sync cadence, and which features you rely on across the boundary.</p>
<h2 class="anchor anchorTargetStickyNavbar_Vzrq" id="xtable-vs-actually-migrating-formats">XTable vs Actually Migrating Formats<a href="https://hudi.apache.org/blog/2026/07/28/using-hudi-with-apache-iceberg-via-xtable#xtable-vs-actually-migrating-formats" class="hash-link" aria-label="Direct link to XTable vs Actually Migrating Formats" title="Direct link to XTable vs Actually Migrating Formats" translate="no">​</a></h2>
<p>XTable also answers the "convert Hudi to Iceberg" question, but it's worth distinguishing two intents:</p>
<ul>
<li class=""><strong>You need Iceberg read compatibility while keeping Hudi's write path.</strong> Use XTable in continuous incremental sync mode, as described above. This is the common case, and nothing is migrated — both views persist indefinitely.</li>
<li class=""><strong>You genuinely want to switch formats.</strong> XTable can serve as a low-risk migration mechanism: sync the table, repoint readers at the Iceberg view, validate, and only then move writers to an Iceberg-native write path — all without rewriting data files. Because the translation is omni-directional, the door swings both ways: teams migrating an Iceberg or Delta table <em>to</em> Hudi to gain its ingestion and table-service capabilities follow the same steps with source and target swapped.</li>
</ul>
<p>The practical takeaway from the <a class="" href="https://hudi.apache.org/blog/2026/07/14/what-is-an-open-table-format">open table format landscape</a> applies here: pick the format whose write path fits each workload, and let interoperability handle the rest. Migration becomes a repointing exercise, not a data-movement project.</p>
<h2 class="anchor anchorTargetStickyNavbar_Vzrq" id="conclusion">Conclusion<a href="https://hudi.apache.org/blog/2026/07/28/using-hudi-with-apache-iceberg-via-xtable#conclusion" class="hash-link" aria-label="Direct link to Conclusion" title="Direct link to Conclusion" translate="no">​</a></h2>
<p>The premise behind "we can't use Hudi because our engine only reads Iceberg" no longer holds. Hudi, Iceberg, and Delta Lake all describe Parquet files with metadata, and Apache XTable translates that metadata in any direction — incrementally, in place, without copying data. A table written with Hudi's indexing, streaming ingestion, and table services can be registered in a catalog and queried by any Iceberg-capable engine as an ordinary Iceberg table, while Hudi-native engines keep the full-fidelity view. With Hudi's pluggable table format framework pushing this further into the write path itself, choosing Hudi is not a bet against Iceberg — it's a way to get Hudi's write-side strengths and Iceberg's read-side reach from a single copy of your data.</p>
<p>To try it, start with the <a class="" href="https://hudi.apache.org/docs/syncing_xtable">Hudi XTable sync guide</a> and the <a href="https://xtable.apache.org/" target="_blank" rel="noopener noreferrer" class="">Apache XTable documentation</a>.</p>
<h2 class="anchor anchorTargetStickyNavbar_Vzrq" id="faq">FAQ<a href="https://hudi.apache.org/blog/2026/07/28/using-hudi-with-apache-iceberg-via-xtable#faq" class="hash-link" aria-label="Direct link to FAQ" title="Direct link to FAQ" translate="no">​</a></h2>
<section><details style="margin-bottom:0.75rem"><summary><strong>Can Iceberg engines read Hudi tables?</strong></summary><p style="margin-top:0.5rem">Yes. Apache XTable translates a Hudi table's metadata into Apache Iceberg metadata written alongside the same Parquet data files. Once that Iceberg metadata is registered in a catalog, any engine with Iceberg read support can query the table as an ordinary Iceberg table.</p></details><details style="margin-bottom:0.75rem"><summary><strong>Can Snowflake read Hudi tables?</strong></summary><p style="margin-top:0.5rem">Not directly, but Snowflake can query Iceberg tables, and Apache XTable can expose a Hudi table as Iceberg by translating its metadata in place. After syncing with XTable and registering the Iceberg metadata, Snowflake reads the table through its Iceberg support, subject to Snowflake's own Iceberg capabilities and requirements.</p></details><details style="margin-bottom:0.75rem"><summary><strong>Does XTable copy my data?</strong></summary><p style="margin-top:0.5rem">No. XTable translates only table metadata. It reads the source format's metadata and writes out the target format's metadata into the same table directory, while the Parquet data files stay exactly where they are. One copy of data serves all synced formats.</p></details><details style="margin-bottom:0.75rem"><summary><strong>Can I convert a Hudi table to Iceberg?</strong></summary><p style="margin-top:0.5rem">Yes. Running XTable with sourceFormat HUDI and targetFormats ICEBERG produces complete Iceberg metadata for the table without rewriting any data files. You can keep both views in sync continuously, or use the translation as a migration step by repointing readers and writers to the Iceberg view after validating it.</p></details><details style="margin-bottom:0.75rem"><summary><strong>Is Apache XTable production ready?</strong></summary><p style="margin-top:0.5rem">Apache XTable is an incubating project at the Apache Software Foundation, which is a statement about ASF governance maturity rather than code quality. It is used in real deployments and its metadata-only design is low risk since source data and metadata are never modified, but you should validate it against your own tables and review the documented limitations, such as Merge-on-Read log files not being captured, before relying on it in production.</p></details><details style="margin-bottom:0.75rem"><summary><strong>Does XTable work with Hudi Merge-on-Read tables?</strong></summary><p style="margin-top:0.5rem">Partially. XTable syncs the Parquet base files but not Hudi log files, so the Iceberg view of a Merge-on-Read table corresponds to the read-optimized view. Records still in log files become visible to Iceberg readers after compaction. Copy-on-Write tables do not have this caveat.</p></details></section>]]></content:encoded>
            <category>apache xtable</category>
            <category>apache iceberg</category>
            <category>interoperability</category>
            <category>data lakehouse</category>
            <category>table format</category>
        </item>
        <item>
            <title><![CDATA[Open Table Format vs Data Lakehouse: What's the Difference?]]></title>
            <link>https://hudi.apache.org/blog/2026/07/24/open-table-format-vs-data-lakehouse</link>
            <guid>https://hudi.apache.org/blog/2026/07/24/open-table-format-vs-data-lakehouse</guid>
            <pubDate>Fri, 24 Jul 2026 00:00:00 GMT</pubDate>
            <description><![CDATA[An open table format is a component; a data lakehouse is an architecture. This post walks the lakehouse stack layer by layer and corrects common conflations.]]></description>
            <content:encoded><![CDATA[<p>An open table format is a component; a data lakehouse is an architecture. The table format is the metadata layer that turns files on object storage into transactional tables — the lakehouse is the complete system built around it: storage, table format, catalog, table services and query engines. The two terms get used interchangeably in vendor marketing and conference talks, and the conflation causes real confusion: teams "adopt a lakehouse" by picking a table format and then discover that most of the operational work still lies ahead, or dismiss an entire architecture because of an opinion about one project's metadata layout.</p>
<p>This post untangles the two terms. We will walk the lakehouse stack from the bottom up, pin down exactly what a table format contributes at its layer, spell out what a table format alone does <em>not</em> give you, and correct the most common conflations along the way.</p>
<h2 class="anchor anchorTargetStickyNavbar_Vzrq" id="the-lakehouse-stack-layer-by-layer">The Lakehouse Stack, Layer by Layer<a href="https://hudi.apache.org/blog/2026/07/24/open-table-format-vs-data-lakehouse#the-lakehouse-stack-layer-by-layer" class="hash-link" aria-label="Direct link to The Lakehouse Stack, Layer by Layer" title="Direct link to The Lakehouse Stack, Layer by Layer" translate="no">​</a></h2>
<p>A <a class="" href="https://hudi.apache.org/blog/2024/07/11/what-is-a-data-lakehouse">data lakehouse</a> is best understood as a stack of open, modular layers. Each layer has one job, and the table format is exactly one of them. From the bottom up:</p>
<p><strong>Cloud object storage.</strong> Amazon S3, Azure Blob Storage, Google Cloud Storage. This layer stores bytes durably and cheaply at effectively unlimited scale, and decouples storage cost from compute cost. It knows nothing about tables — it serves GETs, PUTs and LISTs.</p>
<p><strong>Open file formats.</strong> <a href="https://parquet.apache.org/" target="_blank" rel="noopener noreferrer" class="">Apache Parquet</a> and <a href="https://orc.apache.org/" target="_blank" rel="noopener noreferrer" class="">Apache ORC</a> define how records are encoded <em>inside a single file</em>: columnar layout, compression, encodings, and per-file statistics. A file format makes one file efficient to scan; it has no concept of the other files around it.</p>
<p><strong>The open table format.</strong> <a href="https://hudi.apache.org/" target="_blank" rel="noopener noreferrer" class="">Apache Hudi</a>, <a href="https://iceberg.apache.org/" target="_blank" rel="noopener noreferrer" class="">Apache Iceberg</a>, and <a href="https://delta.io/" target="_blank" rel="noopener noreferrer" class="">Delta Lake</a> live here. A table format is versioned metadata over many files: an ordered log of commits (in Hudi, the <a class="" href="https://hudi.apache.org/docs/timeline">timeline</a>), a record of which files constitute the table at each version, the schema and its evolution history, and file-level statistics for pruning. This is the layer that makes a pile of Parquet behave like a database table — atomic commits, snapshot isolation, record-level updates and deletes, time travel. For a full treatment of this one layer, see the companion post on <a class="" href="https://hudi.apache.org/blog/2026/07/14/what-is-an-open-table-format">what an open table format is</a>.</p>
<p><strong>Storage engine and table services.</strong> Metadata alone does not keep a table healthy. Something has to compact log files into base files, cluster data for query locality, size small files, clean up old versions, and build and maintain indexes. In some stacks this is the query engine's job or a vendor service; in Hudi it is a built-in layer of table services.</p>
<p><strong>Catalogs and metadata services.</strong> A catalog (Hive Metastore, AWS Glue, Apache Polaris, Unity Catalog) tracks which tables exist, their schemas, and where each table's metadata lives — plus, increasingly, access control and lineage. It is what lets an engine resolve <code>orders</code> to something it can plan a scan against.</p>
<p><strong>Query and compute engines.</strong> Apache Spark, Apache Flink, Trino, Presto, plus warehouses and Python-native engines. Engines do the actual reading, writing and computing, speaking to tables through the table format's specification and finding them through the catalog.</p>
<p><img decoding="async" loading="lazy" alt="Data lakehouse architecture" src="https://hudi.apache.org/assets/images/dlh_new-ae34f6d692de93292db9eb4e19690670.png" width="2323" height="1259" class="img_ev3q">
</p><p align="center">Figure: The layered data lakehouse architecture</p><p></p>
<p>Assemble all of these — storage, file format, table format, table services, catalog, engines — and <em>that</em> is a data lakehouse. The table format is the load-bearing middle of the stack, arguably the layer that made the architecture possible at all. But it is one layer, the way a storage engine is one layer of a database, not the database.</p>
<h2 class="anchor anchorTargetStickyNavbar_Vzrq" id="what-a-table-format-alone-does-not-give-you">What a Table Format Alone Does Not Give You<a href="https://hudi.apache.org/blog/2026/07/24/open-table-format-vs-data-lakehouse#what-a-table-format-alone-does-not-give-you" class="hash-link" aria-label="Direct link to What a Table Format Alone Does Not Give You" title="Direct link to What a Table Format Alone Does Not Give You" translate="no">​</a></h2>
<p>Here is where the conflation bites in practice. A table format specification tells you what a valid table looks like on storage. It does not, by itself, operate anything. Adopt a bare table format and you still own:</p>
<ul>
<li class=""><strong>Table maintenance.</strong> Someone has to run compaction, clustering, cleaning of old file versions, and file sizing — continuously, for every table, coordinated with in-flight writers. Skip it and small files and stale metadata quietly erode query performance and inflate storage bills.</li>
<li class=""><strong>Ingestion.</strong> The format defines how commits land; it does not build the pipelines that pull from Kafka, CDC streams, or upstream databases, handle schema drift, deduplicate, and checkpoint.</li>
<li class=""><strong>Catalog sync and discovery.</strong> Tables must be registered and kept current in one or more catalogs so every engine can find them.</li>
<li class=""><strong>Access control and governance.</strong> The format stores data; deciding who can read which columns of it is a platform concern layered above.</li>
<li class=""><strong>Monitoring and operations.</strong> Commit failures, clustering backlogs, concurrency conflicts — someone has to watch them.</li>
</ul>
<p>This is the "platform gap" between a format and a lakehouse, and it is exactly where projects differ in scope. Apache Iceberg deliberately scopes itself to the specification and libraries, leaving maintenance to engines and vendors. Hudi deliberately scopes itself wider: built-in table services (compaction, clustering, cleaning, indexing) that run inline or asynchronously without external orchestration, ingestion utilities like <a class="" href="https://hudi.apache.org/docs/hoodie_streaming_ingestion">Hudi Streamer</a> for pulling from Kafka and CDC sources, catalog sync tools, and a <a class="" href="https://hudi.apache.org/docs/indexes">multi-modal indexing subsystem</a> that accelerates both writes and reads. Neither scoping is wrong — but they are different amounts of the lakehouse, and comparing them as if both were "just formats" misprices the operational work each leaves on your plate.</p>
<p><img decoding="async" loading="lazy" alt="Apache Hudi stack" src="https://hudi.apache.org/assets/images/hstck_new-a0f2451aad8bf4e2003f1efb98c5e179.png" width="2076" height="1400" class="img_ev3q">
</p><p align="center">Figure: The Apache Hudi stack — a table format plus table services and platform components</p><p></p>
<h2 class="anchor anchorTargetStickyNavbar_Vzrq" id="common-conflations-corrected">Common Conflations, Corrected<a href="https://hudi.apache.org/blog/2026/07/24/open-table-format-vs-data-lakehouse#common-conflations-corrected" class="hash-link" aria-label="Direct link to Common Conflations, Corrected" title="Direct link to Common Conflations, Corrected" translate="no">​</a></h2>
<p>A few statements you will hear regularly, each slightly wrong in an instructive way:</p>
<p><strong>"Iceberg is a lakehouse."</strong> No — Apache Iceberg is a table format a lakehouse can use. An Iceberg table sitting in S3 with no catalog, no maintenance jobs and no engines wired up is not a lakehouse; it is well-organized metadata. The same is true of Hudi and Delta Lake at the format layer: the lakehouse is what you get after you assemble the rest of the stack around the tables.</p>
<p><strong>"Databricks' lakehouse is the lakehouse."</strong> Databricks popularized the term and built an excellent platform around Delta Lake, but the lakehouse is an open architectural pattern, not one vendor's product. Uber was running Hudi in what we would now call a lakehouse architecture in 2016–2017, and today you can assemble a lakehouse from entirely open components — or buy one from any of several vendors — on any of the three major formats.</p>
<p><strong>"Choosing a table format means choosing a vendor."</strong> The formats are openly governed (Hudi and Iceberg at the ASF, Delta Lake at the Linux Foundation), and every major engine reads more than one of them. Vendors have preferred formats, but the specification belongs to the community — that openness is the entire point of the "open" in open table format.</p>
<p><strong>"Parquet is our table format."</strong> A file format is not a table format. Parquet defines the bytes inside one file; a table format defines how thousands of Parquet files plus a transaction log behave as a single versioned table. They are adjacent layers of the stack, not alternatives.</p>
<p>For where the lakehouse itself sits relative to the architectures it evolved from, see the companion comparison of <a class="" href="https://hudi.apache.org/blog/2026/07/23/lakehouse-vs-data-warehouse-vs-data-lake">lakehouse vs data warehouse vs data lake</a>.</p>
<h2 class="anchor anchorTargetStickyNavbar_Vzrq" id="the-layers-interoperate--you-are-not-locked-in">The Layers Interoperate — You Are Not Locked In<a href="https://hudi.apache.org/blog/2026/07/24/open-table-format-vs-data-lakehouse#the-layers-interoperate--you-are-not-locked-in" class="hash-link" aria-label="Direct link to The Layers Interoperate — You Are Not Locked In" title="Direct link to The Layers Interoperate — You Are Not Locked In" translate="no">​</a></h2>
<p>Because the layers are modular and the specifications open, the mapping between them is many-to-many. Spark, Flink and Trino each read and write all three major formats. A single lakehouse can, and often does, contain tables in more than one format, discovered through the same catalog and queried by the same engines.</p>
<p>Interoperability now extends to the format layer itself. <a href="https://xtable.apache.org/" target="_blank" rel="noopener noreferrer" class="">Apache XTable</a> (incubating) translates table metadata between Hudi, Iceberg and Delta Lake — in any direction — without copying or rewriting data files, either as a one-time conversion or as continuous incremental sync. A table written with Hudi's ingestion and indexing capabilities can be exposed as an Iceberg table to a service that only reads Iceberg. The practical consequence is that the table format is a per-workload choice inside your lakehouse, not a one-way architectural door: the architecture outlives any single format decision.</p>
<h2 class="anchor anchorTargetStickyNavbar_Vzrq" id="practical-guidance-architecture-first-then-format">Practical Guidance: Architecture First, Then Format<a href="https://hudi.apache.org/blog/2026/07/24/open-table-format-vs-data-lakehouse#practical-guidance-architecture-first-then-format" class="hash-link" aria-label="Direct link to Practical Guidance: Architecture First, Then Format" title="Direct link to Practical Guidance: Architecture First, Then Format" translate="no">​</a></h2>
<p>The right order of decisions falls straight out of the layering. First design the architecture: what workloads (BI, ML, streaming, ad-hoc SQL) must the system serve? Which engines? What ingestion latency? Who operates table maintenance — your team, built-in services, or a vendor? What are the governance requirements? That exercise defines the lakehouse you need.</p>
<p>Only then choose the table format — as a component decision driven by workload fit. Mutable, incremental, streaming-heavy ingestion favors Hudi's write path, indexing and <a class="" href="https://hudi.apache.org/docs/table_types">table types</a> (Copy-on-Write vs Merge-on-Read let you tune the write/read trade-off per table). Engine-neutral batch analytics with broad catalog support is Iceberg's design center; deep Spark and Databricks integration is Delta's. If the operational gap matters — no team to babysit compaction jobs — weight built-in table services heavily. And with XTable, a wrong-for-one-workload choice is a translation, not a migration.</p>
<p>Teams that pick a format first and call it a lakehouse strategy usually end up rediscovering the rest of the stack the hard way, one missing layer at a time.</p>
<h2 class="anchor anchorTargetStickyNavbar_Vzrq" id="conclusion">Conclusion<a href="https://hudi.apache.org/blog/2026/07/24/open-table-format-vs-data-lakehouse#conclusion" class="hash-link" aria-label="Direct link to Conclusion" title="Direct link to Conclusion" translate="no">​</a></h2>
<p>Keep the two terms at their own altitudes and the confusion disappears. The open table format is a layer: versioned, transactional metadata that turns files on object storage into tables. The data lakehouse is the architecture: that layer plus storage, file formats, table services, catalogs and engines, assembled into a system that does warehouse-grade work on open data. A table format cannot be a lakehouse any more than a storage engine can be a database — but no lakehouse stands without one. Evaluate architectures as architectures, formats as components, and remember that the projects in this space scope themselves differently: some stop at the specification, while Apache Hudi ships much of the surrounding platform — table services, ingestion, indexing — out of the box. Get the architecture right, pick formats per workload, and let open specifications keep every layer replaceable.</p>
<h2 class="anchor anchorTargetStickyNavbar_Vzrq" id="faq">FAQ<a href="https://hudi.apache.org/blog/2026/07/24/open-table-format-vs-data-lakehouse#faq" class="hash-link" aria-label="Direct link to FAQ" title="Direct link to FAQ" translate="no">​</a></h2>
<section><details style="margin-bottom:0.75rem"><summary><strong>What is the difference between an open table format and a data lakehouse?</strong></summary><p style="margin-top:0.5rem">An open table format is a single component: the metadata layer that turns files on object storage into transactional tables with ACID commits, schema evolution and time travel. A data lakehouse is the complete architecture built around it, combining object storage, open file formats, the table format, table services, catalogs and query engines into one system.</p></details><details style="margin-bottom:0.75rem"><summary><strong>Is Apache Iceberg a data lakehouse?</strong></summary><p style="margin-top:0.5rem">No. Apache Iceberg is an open table format — one layer of a lakehouse architecture. A lakehouse using Iceberg still needs object storage, a catalog, query engines, and services to maintain the tables. Iceberg is a table format a lakehouse can use, not a lakehouse itself.</p></details><details style="margin-bottom:0.75rem"><summary><strong>Is Delta Lake a table format or a lakehouse?</strong></summary><p style="margin-top:0.5rem">Delta Lake is an open table format governed by the Linux Foundation. Databricks builds its lakehouse platform on top of Delta Lake, which is why the two are often conflated, but Delta Lake itself is the table layer, and the lakehouse is the surrounding platform of storage, catalogs, engines and services.</p></details><details style="margin-bottom:0.75rem"><summary><strong>Do I need a table format to build a lakehouse?</strong></summary><p style="margin-top:0.5rem">Yes, in practice. The table format is what provides atomic commits, snapshot isolation, updates and deletes, and versioning over files in object storage. Without one you have a data lake of raw files, not a lakehouse. Apache Hudi, Apache Iceberg and Delta Lake are the major open options.</p></details><details style="margin-bottom:0.75rem"><summary><strong>Is Apache Hudi a table format or a lakehouse platform?</strong></summary><p style="margin-top:0.5rem">Both, by design. Hudi includes an open table format built around its timeline and file layout, but the project also ships built-in table services like compaction, clustering and cleaning, ingestion utilities such as Hudi Streamer, and a multi-modal indexing subsystem. That puts Hudi closer to a lakehouse platform than a bare format specification.</p></details><details style="margin-bottom:0.75rem"><summary><strong>Does choosing a table format lock me into one vendor?</strong></summary><p style="margin-top:0.5rem">No. The major table formats are openly governed, and engines like Apache Spark, Apache Flink and Trino support all of them. Apache XTable can also translate table metadata between Hudi, Iceberg and Delta Lake without rewriting data files, so the format choice is a per-workload decision rather than a permanent commitment.</p></details></section>]]></content:encoded>
            <category>table format</category>
            <category>data lakehouse</category>
            <category>open architecture</category>
            <category>architecture</category>
        </item>
        <item>
            <title><![CDATA[Data Lakehouse vs Data Warehouse vs Data Lake: What's the Difference?]]></title>
            <link>https://hudi.apache.org/blog/2026/07/23/lakehouse-vs-data-warehouse-vs-data-lake</link>
            <guid>https://hudi.apache.org/blog/2026/07/23/lakehouse-vs-data-warehouse-vs-data-lake</guid>
            <pubDate>Thu, 23 Jul 2026 00:00:00 GMT</pubDate>
            <description><![CDATA[Data warehouse vs data lake vs data lakehouse compared: costs, transactions, performance, openness, and when to choose each architecture.]]></description>
            <content:encoded><![CDATA[<p>A <strong>data warehouse</strong> is a proprietary, structured, SQL-first analytical database: you load cleaned, modeled data into it and get fast, governed BI queries out. A <strong>data lake</strong> is cheap, open storage — typically cloud object stores holding raw files of any shape, from CSVs to images — that scales practically without limit but offers few database guarantees. A <strong>data lakehouse</strong> is the data lake upgraded with database capabilities — ACID transactions, upserts and deletes, indexing, schema enforcement — so that one copy of data on open storage can serve BI, machine learning, and streaming workloads alike.</p>
<p>Which one you should build on depends on what your data looks like, who queries it, and how much you value keeping your storage open. If all you need is dashboards over modest volumes of structured data, a warehouse remains the path of least resistance. If you are juggling large volumes, unstructured data, machine learning, streaming ingestion, or multiple query engines, the calculus shifts quickly toward a lakehouse. The rest of this post walks through each architecture honestly — strengths and costs — and ends with a side-by-side table and concrete guidance on choosing.</p>
<h2 class="anchor anchorTargetStickyNavbar_Vzrq" id="the-data-warehouse-fast-governed-and-proprietary">The Data Warehouse: Fast, Governed, and Proprietary<a href="https://hudi.apache.org/blog/2026/07/23/lakehouse-vs-data-warehouse-vs-data-lake#the-data-warehouse-fast-governed-and-proprietary" class="hash-link" aria-label="Direct link to The Data Warehouse: Fast, Governed, and Proprietary" title="Direct link to The Data Warehouse: Fast, Governed, and Proprietary" translate="no">​</a></h2>
<p>Data warehouses have been the workhorse of analytics for decades, from on-premise appliances like Teradata to cloud-native services like <a href="https://www.snowflake.com/" target="_blank" rel="noopener noreferrer" class="">Snowflake</a>, <a href="https://cloud.google.com/bigquery" target="_blank" rel="noopener noreferrer" class="">Google BigQuery</a>, and <a href="https://aws.amazon.com/redshift/" target="_blank" rel="noopener noreferrer" class="">Amazon Redshift</a>. The model is simple: extract data from operational systems, transform it into well-defined relational schemas, and load it into a system purpose-built for analytical SQL.</p>
<p>The strengths are real and should not be understated:</p>
<ul>
<li class=""><strong>Query performance.</strong> Warehouses control the full stack — storage layout, statistics, caching, query optimization — and are ruthlessly tuned for low-latency SQL. For interactive BI dashboards over structured data, they set the bar.</li>
<li class=""><strong>Governance and management.</strong> Mature access controls, auditing, data masking, and workload management come built in. Schemas are enforced on write, so the data inside a warehouse tends to be clean by construction.</li>
<li class=""><strong>Simplicity.</strong> A cloud warehouse is a single managed product. There is one system to secure, one SQL dialect to learn, one vendor to call.</li>
</ul>
<p>The costs show up as the footprint grows:</p>
<ul>
<li class=""><strong>Lock-in.</strong> Warehouse storage formats are proprietary. Your data lives inside the vendor's system, readable only through the vendor's compute. Migrating out — or even letting a second engine read the same tables — means exporting and copying.</li>
<li class=""><strong>ELT copies.</strong> Because the warehouse only speaks structured SQL, data must be copied into it, and often copied again into marts and extracts for different teams. Each copy adds pipeline complexity, staleness, and cost.</li>
<li class=""><strong>Price at scale.</strong> Coupled compute and proprietary storage are priced at a premium. At tens or hundreds of terabytes with heavy transformation workloads, warehouse bills routinely become one of the largest line items in a data platform budget.</li>
<li class=""><strong>Poor fit for ML and unstructured data.</strong> Training a model on images, logs, or raw event streams inside a warehouse ranges from awkward to impossible. Data science teams typically end up exporting data back out — another copy.</li>
</ul>
<h2 class="anchor anchorTargetStickyNavbar_Vzrq" id="the-data-lake-cheap-open-and-unreliable">The Data Lake: Cheap, Open, and Unreliable<a href="https://hudi.apache.org/blog/2026/07/23/lakehouse-vs-data-warehouse-vs-data-lake#the-data-lake-cheap-open-and-unreliable" class="hash-link" aria-label="Direct link to The Data Lake: Cheap, Open, and Unreliable" title="Direct link to The Data Lake: Cheap, Open, and Unreliable" translate="no">​</a></h2>
<p>Data lakes emerged from the Hadoop era and matured on cloud object storage — <a href="https://aws.amazon.com/s3/" target="_blank" rel="noopener noreferrer" class="">Amazon S3</a>, <a href="https://cloud.google.com/storage" target="_blank" rel="noopener noreferrer" class="">Google Cloud Storage</a>, <a href="https://azure.microsoft.com/en-us/products/storage/data-lake-storage" target="_blank" rel="noopener noreferrer" class="">Azure Data Lake Storage</a>. The idea inverts the warehouse: instead of modeling data before loading it, land everything raw and cheap, and decide the schema when you read it.</p>
<p>The strengths:</p>
<ul>
<li class=""><strong>Cost.</strong> Object storage costs a small fraction of warehouse storage, with durability and availability handled by the cloud provider. Storage and compute scale independently — you pay for queries only when you run them.</li>
<li class=""><strong>Openness.</strong> Data sits in open file formats like <a href="https://parquet.apache.org/" target="_blank" rel="noopener noreferrer" class="">Apache Parquet</a> and <a href="https://orc.apache.org/" target="_blank" rel="noopener noreferrer" class="">Apache ORC</a>. Any engine — Apache Spark, Apache Flink, Trino, Presto, Ray, or a Python script — can read the same files. There is no vendor gatekeeping access to your own data.</li>
<li class=""><strong>Any data shape.</strong> Structured tables, semi-structured JSON, free text, images, audio, model checkpoints — a lake stores all of it, which is exactly why ML and data science teams gravitate to lakes.</li>
</ul>
<p>The weaknesses are equally well documented:</p>
<ul>
<li class=""><strong>No transactions.</strong> A plain data lake is just files in directories. A failed job can leave partial writes visible to readers; two concurrent writers can corrupt each other. There is no atomic commit, no isolation, no rollback.</li>
<li class=""><strong>No upserts or deletes.</strong> Object stores are append-oriented. Updating a single record — say, for a GDPR deletion request or a late-arriving correction — traditionally meant rewriting entire partitions.</li>
<li class=""><strong>Data swamps.</strong> Without schema enforcement, quality controls, or table-level metadata, lakes decay into "data swamps": directories of files nobody trusts and nobody dares delete.</li>
<li class=""><strong>Slow BI.</strong> Query engines scanning raw files lack the indexes, statistics, and optimized layouts a warehouse maintains, so interactive dashboard performance over a plain lake generally disappoints.</li>
</ul>
<h2 class="anchor anchorTargetStickyNavbar_Vzrq" id="the-data-lakehouse-the-lake-upgraded-to-a-database">The Data Lakehouse: The Lake, Upgraded to a Database<a href="https://hudi.apache.org/blog/2026/07/23/lakehouse-vs-data-warehouse-vs-data-lake#the-data-lakehouse-the-lake-upgraded-to-a-database" class="hash-link" aria-label="Direct link to The Data Lakehouse: The Lake, Upgraded to a Database" title="Direct link to The Data Lakehouse: The Lake, Upgraded to a Database" translate="no">​</a></h2>
<p>The lakehouse closes the gap between the two by adding a transactional metadata layer — an <a class="" href="https://hudi.apache.org/blog/2026/07/14/what-is-an-open-table-format">open table format</a> — on top of the open files already sitting in the lake. Projects like <a href="https://hudi.apache.org/" target="_blank" rel="noopener noreferrer" class="">Apache Hudi</a>, <a href="https://iceberg.apache.org/" target="_blank" rel="noopener noreferrer" class="">Apache Iceberg</a>, and <a href="https://delta.io/" target="_blank" rel="noopener noreferrer" class="">Delta Lake</a> define tables (not just files) over Parquet and ORC, and bring the database capabilities the lake was missing:</p>
<ul>
<li class=""><strong>ACID transactions</strong> with concurrency control, so writers commit atomically and readers never see partial results.</li>
<li class=""><strong>Upserts and deletes</strong>, made efficient by indexing mechanisms that locate the records to change without rewriting whole partitions — the capability that first motivated Hudi's creation at Uber.</li>
<li class=""><strong>Schema enforcement and evolution</strong>, so tables reject malformed writes and can add or change columns without full rewrites.</li>
<li class=""><strong>Performance features</strong> — indexing, statistics, clustering, compaction, time travel — that let query engines approach warehouse-class performance on lake storage.</li>
</ul>
<p>Two properties follow that neither predecessor offers. First, <strong>unified batch and streaming</strong>: because the table layer supports fast upserts and incremental pulls, the same table can be written by a streaming pipeline and read by both a dashboard and a nightly batch job, eliminating the classic lambda-architecture split. Second, <strong>one copy of data, many engines</strong>: the table is defined in an open format on open storage, so Spark, Flink, Trino, Presto, StarRocks, Daft, and warehouse engines with external-table support can all query the same physical bytes. No ELT copy into a proprietary system, no extract back out for ML.</p>
<p><img decoding="async" loading="lazy" alt="Data lakehouse architecture" src="https://hudi.apache.org/assets/images/dlh_new-ae34f6d692de93292db9eb4e19690670.png" width="2323" height="1259" class="img_ev3q">
</p><p align="center">Figure: Data Lakehouse Architecture — open table formats and a storage engine layered over cloud object storage, serving multiple compute engines</p><p></p>
<p>For a deeper treatment of the architecture itself — the storage, file-format, table-format, catalog, and compute layers — see <a class="" href="https://hudi.apache.org/blog/2024/07/11/what-is-a-data-lakehouse">What is a Data Lakehouse &amp; How does it Work?</a></p>
<p>The honest caveat: a lakehouse is assembled from components rather than bought as one product, so it demands more engineering judgment than swiping a credit card at a warehouse vendor. Managed lakehouse platforms narrow that gap, but the trade-off between operational simplicity and openness has not disappeared entirely.</p>
<h2 class="anchor anchorTargetStickyNavbar_Vzrq" id="side-by-side-comparison">Side-by-Side Comparison<a href="https://hudi.apache.org/blog/2026/07/23/lakehouse-vs-data-warehouse-vs-data-lake#side-by-side-comparison" class="hash-link" aria-label="Direct link to Side-by-Side Comparison" title="Direct link to Side-by-Side Comparison" translate="no">​</a></h2>
<table><thead><tr><th>Dimension</th><th>Data Warehouse</th><th>Data Lake</th><th>Data Lakehouse</th></tr></thead><tbody><tr><td><strong>Storage cost</strong></td><td>High — proprietary storage at premium pricing</td><td>Low — commodity cloud object storage</td><td>Low — same object storage as the lake</td></tr><tr><td><strong>Data types</strong></td><td>Structured (some semi-structured support)</td><td>Structured, semi-structured, unstructured</td><td>Structured, semi-structured, unstructured</td></tr><tr><td><strong>Transactions</strong></td><td>Full ACID</td><td>None</td><td>ACID via table formats (Hudi, Iceberg, Delta Lake)</td></tr><tr><td><strong>Updates / deletes</strong></td><td>Native SQL DML</td><td>Rewrite files manually</td><td>Native upserts and deletes, index-accelerated</td></tr><tr><td><strong>Query performance</strong></td><td>Excellent for SQL/BI</td><td>Poor to moderate (raw file scans)</td><td>Good to excellent with indexing, clustering, compaction</td></tr><tr><td><strong>Openness / lock-in</strong></td><td>Proprietary format, single vendor engine</td><td>Fully open files, any engine</td><td>Open table formats, any engine</td></tr><tr><td><strong>Streaming support</strong></td><td>Micro-batch loading, often at extra cost</td><td>Append-only file drops</td><td>Native streaming ingestion with incremental processing</td></tr><tr><td><strong>ML support</strong></td><td>Weak — data must be exported</td><td>Strong — raw data access for any framework</td><td>Strong — same open data, plus reliable managed tables</td></tr><tr><td><strong>Governance / quality</strong></td><td>Mature, built in</td><td>Do-it-yourself, frequently neglected</td><td>Schema enforcement, time travel, auditing via table layer</td></tr><tr><td><strong>Typical tools</strong></td><td>Snowflake, BigQuery, Redshift</td><td>S3 / GCS / ADLS + Spark, Presto</td><td>Hudi / Iceberg / Delta Lake + Spark, Flink, Trino, StarRocks</td></tr></tbody></table>
<h2 class="anchor anchorTargetStickyNavbar_Vzrq" id="when-is-each-the-right-choice">When Is Each the Right Choice?<a href="https://hudi.apache.org/blog/2026/07/23/lakehouse-vs-data-warehouse-vs-data-lake#when-is-each-the-right-choice" class="hash-link" aria-label="Direct link to When Is Each the Right Choice?" title="Direct link to When Is Each the Right Choice?" translate="no">​</a></h2>
<p><strong>Choose a data warehouse</strong> when your data is modest in volume, almost entirely structured, and your consumers are BI analysts writing SQL. A small team running dashboards over a few terabytes will likely ship faster and operate more cheaply on a managed warehouse than by assembling a lakehouse stack. Lock-in is a real cost, but at small scale it may be a cost worth paying for simplicity.</p>
<p><strong>Choose a plain data lake</strong> when you primarily need a cheap landing zone and archive — raw event dumps, ML training corpora, cold retention — and you do not need transactional tables or fast SQL over that data. Many lakehouses begin life as exactly this.</p>
<p><strong>Choose a lakehouse</strong> when one or more of these is true: data volumes are growing into the tens of terabytes and beyond and warehouse bills sting; you need machine learning and BI on the same data; you ingest streaming or frequently-updated data (CDC from operational databases is the canonical case); you need record-level updates and deletes for compliance; or you want multiple engines — and the freedom to change vendors — over a single copy of data. These are the workloads the architecture was built for; see the <a class="" href="https://hudi.apache.org/docs/use_cases">Hudi use cases</a> for concrete patterns.</p>
<p>The choice is also not exclusive. A common and pragmatic pattern is a lakehouse as the system of record with a warehouse serving a subset of curated, high-concurrency dashboards — increasingly by querying the lakehouse tables in place rather than holding a copy.</p>
<h2 class="anchor anchorTargetStickyNavbar_Vzrq" id="the-convergence-trend">The Convergence Trend<a href="https://hudi.apache.org/blog/2026/07/23/lakehouse-vs-data-warehouse-vs-data-lake#the-convergence-trend" class="hash-link" aria-label="Direct link to The Convergence Trend" title="Direct link to The Convergence Trend" translate="no">​</a></h2>
<p>The sharp lines between these categories are blurring, and they are blurring <em>toward</em> the lakehouse. Warehouses are opening up: Snowflake, BigQuery, and Redshift all now query and, in varying degrees, write open table formats on customer-owned object storage, an acknowledgment that customers refuse to keep their primary copy of data in a proprietary format. Meanwhile, lakes are getting more database-like: table formats keep absorbing capabilities that were once warehouse differentiators — multi-modal indexing, materialized-view-style pre-computation, richer catalogs and governance — while interoperability projects like <a href="https://xtable.apache.org/" target="_blank" rel="noopener noreferrer" class="">Apache XTable</a> let one physical table be exposed in multiple table formats at once.</p>
<p>Follow both trends to their end point and you arrive at the same place: open data on cheap storage, database-grade table management in an open layer above it, and interchangeable compute engines on top. That meeting point is the lakehouse. The remaining competition is less about <em>whether</em> to structure data this way and more about which table formats, catalogs, and engines earn a place in the stack.</p>
<h2 class="anchor anchorTargetStickyNavbar_Vzrq" id="conclusion">Conclusion<a href="https://hudi.apache.org/blog/2026/07/23/lakehouse-vs-data-warehouse-vs-data-lake#conclusion" class="hash-link" aria-label="Direct link to Conclusion" title="Direct link to Conclusion" translate="no">​</a></h2>
<p>Warehouses deliver performance and governance at the price of lock-in and copies; lakes deliver cost and openness at the price of reliability; the lakehouse keeps the lake's economics and openness while restoring the database guarantees — transactions, upserts, indexing, schema management — that make data trustworthy and fast to query. For small, purely structured BI workloads, a warehouse is still a fine and simple answer. For everything pushing past that — scale, streaming, ML, multiple engines, or simply the desire to own your data in an open format — the lakehouse is where both the technology and the industry are heading. Apache Hudi's approach to this is a full <a class="" href="https://hudi.apache.org/blog/2024/07/11/what-is-a-data-lakehouse">transactional database layer for the lake</a>, and the <a class="" href="https://hudi.apache.org/docs/use_cases">use cases</a> it serves in production are the clearest evidence that the gap between lake and warehouse has effectively closed.</p>
<h2 class="anchor anchorTargetStickyNavbar_Vzrq" id="faq">FAQ<a href="https://hudi.apache.org/blog/2026/07/23/lakehouse-vs-data-warehouse-vs-data-lake#faq" class="hash-link" aria-label="Direct link to FAQ" title="Direct link to FAQ" translate="no">​</a></h2>
<section><details style="margin-bottom:0.75rem"><summary><strong>Is a lakehouse cheaper than a warehouse?</strong></summary><p style="margin-top:0.5rem">Usually, at scale. A lakehouse stores data on commodity cloud object storage, which costs a small fraction of proprietary warehouse storage, and it avoids maintaining multiple ELT copies of the same data. At small volumes the difference narrows, and a managed warehouse can be cheaper in engineering time, so total cost depends on scale and team.</p></details><details style="margin-bottom:0.75rem"><summary><strong>Can a lakehouse replace a data warehouse?</strong></summary><p style="margin-top:0.5rem">For many organizations, yes. Open table formats bring transactions, upserts, indexing, and schema management to lake storage, and engines like Trino, StarRocks, and Spark deliver strong SQL performance on those tables. Some teams still keep a warehouse for very high-concurrency, low-latency dashboards, often querying the lakehouse tables directly instead of holding a separate copy.</p></details><details style="margin-bottom:0.75rem"><summary><strong>Is Snowflake a data lakehouse?</strong></summary><p style="margin-top:0.5rem">Snowflake is a cloud data warehouse at its core, storing data in a proprietary format managed by its own engine. It has added lakehouse-style capabilities, notably support for Apache Iceberg tables on customer-owned object storage. Whether a Snowflake deployment qualifies as a lakehouse depends on whether the primary data copy lives in an open format that other engines can access.</p></details><details style="margin-bottom:0.75rem"><summary><strong>What is the main difference between a data lake and a data lakehouse?</strong></summary><p style="margin-top:0.5rem">A data lake is raw files on cheap object storage with no database guarantees, while a lakehouse adds an open table format layer, such as Apache Hudi, Apache Iceberg, or Delta Lake, on top of those files. That layer provides ACID transactions, record-level updates and deletes, indexing, and schema enforcement, turning unreliable file collections into managed tables.</p></details><details style="margin-bottom:0.75rem"><summary><strong>Do I need Hadoop to build a data lake or lakehouse?</strong></summary><p style="margin-top:0.5rem">No. Modern lakes and lakehouses are built on cloud object stores like Amazon S3, Google Cloud Storage, or Azure Data Lake Storage, with engines such as Apache Spark, Apache Flink, and Trino running against them. Hadoop HDFS is still supported by these tools but is no longer required or typical for new deployments.</p></details><details style="margin-bottom:0.75rem"><summary><strong>Which lakehouse table format should I choose?</strong></summary><p style="margin-top:0.5rem">Apache Hudi, Apache Iceberg, and Delta Lake all provide ACID transactions and schema evolution on open storage, and all are production-proven. Hudi stands out for update-heavy and streaming workloads thanks to its indexing and incremental processing, while the best fit overall depends on your engines, workloads, and ecosystem. Interoperability tools like Apache XTable also let one table be read in multiple formats.</p></details></section>]]></content:encoded>
            <category>data lakehouse</category>
            <category>data warehouse</category>
            <category>comparison</category>
            <category>open architecture</category>
        </item>
        <item>
            <title><![CDATA[What is CDC on a Data Lake?]]></title>
            <link>https://hudi.apache.org/blog/2026/07/22/what-is-cdc-on-a-data-lake</link>
            <guid>https://hudi.apache.org/blog/2026/07/22/what-is-cdc-on-a-data-lake</guid>
            <pubDate>Wed, 22 Jul 2026 00:00:00 GMT</pubDate>
            <description><![CDATA[Learn what change data capture (CDC) on a data lake is, why it requires upserts and deletes, and how tools like Debezium and Apache Hudi make it work.]]></description>
            <content:encoded><![CDATA[<p>Change data capture (CDC) on a data lake is the practice of continuously replicating inserts, updates and deletes from operational databases into tables on cloud storage, so that analytics, machine learning and downstream pipelines always work against a fresh, queryable copy of the source data. Teams adopt it for three main reasons: it offloads heavy analytical queries from transaction-processing (OLTP) databases that were never built for them, it consolidates data from many operational systems into a single source of truth, and it shrinks data freshness from "last night's batch" to minutes. The catch is that plain data lakes — collections of immutable files on object storage — historically could not absorb a change stream at all, because applying an update or a delete requires rewriting data in place, and raw file storage offers no way to do that safely. This post explains, from first principles, what CDC is, why the data lake became its natural destination, and what has to sit in between to make the combination work.</p>
<p>If you already know the basics and want a deeper treatment of capture mechanisms and architecture trade-offs, read the companion deep dive, <a class="" href="https://hudi.apache.org/blog/2024/07/30/data-lake-cdc">Understanding Data Lake Change Data Capture</a>, after this one.</p>
<h2 class="anchor anchorTargetStickyNavbar_Vzrq" id="what-is-change-data-capture">What is change data capture?<a href="https://hudi.apache.org/blog/2026/07/22/what-is-cdc-on-a-data-lake#what-is-change-data-capture" class="hash-link" aria-label="Direct link to What is change data capture?" title="Direct link to What is change data capture?" translate="no">​</a></h2>
<p>Every operational database is constantly changing: orders are placed, profiles are edited, rows are deleted. Change data capture is any technique that identifies those row-level changes as they happen and delivers them, in order, to another system. Instead of periodically copying a whole table, CDC ships only the delta — each event typically carrying the operation type (insert, update or delete), the affected row, and often its before and after state.</p>
<p>There are two broad families of capture:</p>
<ul>
<li class=""><strong>Query-based capture</strong> polls the source tables for changes, usually by filtering on a <code>last_updated</code> timestamp column. It is simple to set up but has real gaps: it misses hard deletes entirely (a deleted row simply stops appearing in query results), it puts recurring query load on the production database, and it requires the schema to cooperate.</li>
<li class=""><strong>Log-based capture</strong> reads the database's own transaction log — the binlog in MySQL, the write-ahead log (WAL) in PostgreSQL — which records every committed change in commit order. Because it taps a log the database writes anyway, it captures every insert, update and delete with minimal impact on the source, and it is the approach virtually all serious CDC deployments use today.</li>
</ul>
<p><img decoding="async" loading="lazy" alt="Log-based CDC" src="https://hudi.apache.org/assets/images/log-based-cdc-92eff429e89653b892b63f1af3485ac6.png" width="1301" height="320" class="img_ev3q"></p>
<p><a href="https://debezium.io/" target="_blank" rel="noopener noreferrer" class="">Debezium</a> is the most widely used open source log-based CDC tool. It runs as a Kafka Connect connector, tails the binlog or WAL, and publishes one change event per row modification to a Kafka topic per table, typically alongside a schema registry that tracks the shape of those events. Cloud services such as AWS DMS and various managed connectors play the same role. Whatever the tool, the output is the same in spirit: an ordered stream of row-level change events that some downstream system must now apply.</p>
<h2 class="anchor anchorTargetStickyNavbar_Vzrq" id="why-the-data-lake-is-the-natural-cdc-destination">Why the data lake is the natural CDC destination<a href="https://hudi.apache.org/blog/2026/07/22/what-is-cdc-on-a-data-lake#why-the-data-lake-is-the-natural-cdc-destination" class="hash-link" aria-label="Direct link to Why the data lake is the natural CDC destination" title="Direct link to Why the data lake is the natural CDC destination" translate="no">​</a></h2>
<p>The destination question — where should that change stream land? — has an economic answer and an architectural one.</p>
<p>Economically, cloud object storage (Amazon S3, Google Cloud Storage, Azure Blob Storage) is the cheapest place to keep large volumes of data, and it scales without capacity planning. Replicating dozens or hundreds of operational tables, with full history, is affordable there in a way it is not in a warehouse whose storage and compute pricing were designed for curated, aggregated data.</p>
<p>Architecturally, data stored in open file formats like Apache Parquet on object storage is not locked to any one engine. The same replicated tables can serve Spark for ETL, Trino or Presto for interactive SQL, and Python-based ML frameworks — without making another copy per engine. That "one copy, many engines" property is the core promise of the <a class="" href="https://hudi.apache.org/blog/2024/07/11/what-is-a-data-lakehouse">data lakehouse</a>, and a continuously replicated mirror of your operational databases is one of the most valuable datasets you can put at its base.</p>
<p>But there is a fundamental mismatch. A CDC stream is, by definition, mostly <em>mutations</em> — updates and deletes to existing rows. A plain data lake is a pile of immutable files: you can add new files, but you cannot change a row inside one. Without something extra, teams historically resorted to painful workarounds — appending every change event and forcing every query to deduplicate, or periodically rewriting entire partitions to fold changes in. Both are slow, expensive and error-prone. Closing this gap is exactly why transactional table layers for the lake emerged; Apache Hudi, notably, grew out of the need to apply database change streams to lake storage at scale at Uber.</p>
<h2 class="anchor anchorTargetStickyNavbar_Vzrq" id="what-applying-cdc-requires-from-the-table-layer">What applying CDC requires from the table layer<a href="https://hudi.apache.org/blog/2026/07/22/what-is-cdc-on-a-data-lake#what-applying-cdc-requires-from-the-table-layer" class="hash-link" aria-label="Direct link to What applying CDC requires from the table layer" title="Direct link to What applying CDC requires from the table layer" translate="no">​</a></h2>
<p>To turn a change stream into a correct, queryable table on object storage, the layer above the raw files needs a specific set of capabilities:</p>
<ul>
<li class=""><strong>Upserts and deletes.</strong> Each change event must be applied against the existing table: an update replaces the current version of a row identified by its primary key, and a delete removes it. Doing this efficiently means quickly finding which file holds a given key — Hudi does so with pluggable <a class="" href="https://hudi.apache.org/docs/indexes">indexes</a>, including a record-level index, so it does not have to scan the table on every write.</li>
<li class=""><strong>Ordering and late events.</strong> Change events can arrive out of order, especially after retries or repartitioning in the transport layer. If an older update is applied after a newer one, the table silently travels back in time. The table layer must merge records by an ordering field — the source database's log sequence number or position — so the correct final state wins regardless of arrival order. Hudi supports this natively through event-time ordered merging via its <a class="" href="https://hudi.apache.org/docs/record_merger">record merger</a> APIs.</li>
<li class=""><strong>Schema evolution.</strong> Source tables change: columns get added, types get widened. A CDC pipeline that fails on every upstream <code>ALTER TABLE</code> is not operable. The table layer needs to evolve the target <a class="" href="https://hudi.apache.org/docs/schema_evolution">schema safely</a>, usually in coordination with a schema registry.</li>
<li class=""><strong>Transactional guarantees.</strong> Writers apply changes continuously while queries run concurrently. ACID transactions ensure readers never see a half-applied batch, and failed writes can be rolled back cleanly rather than leaving corrupt partial files behind.</li>
</ul>
<p>One more design choice matters for CDC specifically: how the table absorbs writes. Copy-on-write tables rewrite whole data files for every batch of updates, which is simple but costly when updates are frequent. Merge-on-read tables, by contrast, log changes as compact delta files and compact them into columnar base files asynchronously — which is why <a class="" href="https://hudi.apache.org/docs/table_types">merge-on-read</a> is generally the recommended table type for update-heavy CDC ingestion.</p>
<h2 class="anchor anchorTargetStickyNavbar_Vzrq" id="the-two-sides-of-cdc-on-a-lakehouse">The two sides of CDC on a lakehouse<a href="https://hudi.apache.org/blog/2026/07/22/what-is-cdc-on-a-data-lake#the-two-sides-of-cdc-on-a-lakehouse" class="hash-link" aria-label="Direct link to The two sides of CDC on a lakehouse" title="Direct link to The two sides of CDC on a lakehouse" translate="no">​</a></h2>
<p>It is worth separating two roles that often get blurred together, because a lakehouse can — and usually should — play both.</p>
<p><strong>Consuming CDC</strong> is the direction discussed so far: the lakehouse table is the <em>destination</em>, continuously applying a change stream from an operational database so it stays an up-to-date mirror.</p>
<p><strong>Producing change streams</strong> is the reverse: the lakehouse table acts as a <em>source</em> of changes for whatever sits downstream. Once your bronze tables absorb CDC, the next question is how the silver and gold tables built on top of them stay fresh. If each downstream job re-reads the whole upstream table, you have merely moved the batch-processing problem one hop over. Hudi addresses this with incremental queries — a consumer can ask a table for "everything that changed since my last checkpoint" and receive just those records, with commit-time watermarks serving as reliable checkpoints. Hudi can also emit full before/after images of changed records, database-changelog style, for consumers that need them. This is what turns a single CDC feed into a chain of <a class="" href="https://hudi.apache.org/docs/use_cases">incremental pipelines</a>, where every layer processes only deltas.</p>
<h2 class="anchor anchorTargetStickyNavbar_Vzrq" id="a-reference-architecture-mysqlpostgres-to-a-hudi-table">A reference architecture: MySQL/Postgres to a Hudi table<a href="https://hudi.apache.org/blog/2026/07/22/what-is-cdc-on-a-data-lake#a-reference-architecture-mysqlpostgres-to-a-hudi-table" class="hash-link" aria-label="Direct link to A reference architecture: MySQL/Postgres to a Hudi table" title="Direct link to A reference architecture: MySQL/Postgres to a Hudi table" translate="no">​</a></h2>
<p>Here is what a typical end-to-end CDC pipeline onto a data lake looks like, using widely deployed open source components:</p>
<p><img decoding="async" loading="lazy" alt="CDC architecture with Apache Hudi" src="https://hudi.apache.org/assets/images/hudi-cdc-263ca6e0f40b6bff91517bd02c798e2d.jpg" width="1337" height="601" class="img_ev3q"></p>
<ol>
<li class=""><strong>Source database.</strong> MySQL or PostgreSQL, configured to expose its change log (binlog or logical-replication WAL).</li>
<li class=""><strong>Capture: Debezium on Kafka Connect.</strong> A Debezium connector tails the log and writes one change event per row modification into a Kafka topic per table, registering event schemas with a schema registry (Confluent or Apicurio).</li>
<li class=""><strong>Apply: Hudi Streamer or Flink.</strong> <a class="" href="https://hudi.apache.org/docs/hoodie_streaming_ingestion">Hudi Streamer</a> is a self-contained ingestion utility that ships with Hudi and includes a Debezium source out of the box: running in continuous mode, it reads the change events from Kafka and upserts them into a Hudi table on cloud storage, using the table's record key (the source primary key) to route updates and the log position to order them. Teams standardized on Flink can use Hudi's Flink integration for the same job. For a hands-on walkthrough with configurations, see <a class="" href="https://hudi.apache.org/blog/2022/01/14/change-data-capture-with-debezium-and-apache-hudi">Change Data Capture with Debezium and Apache Hudi</a>.</li>
<li class=""><strong>Bronze table.</strong> The result is a "raw" replicated table — typically merge-on-read — that mirrors the source, kept fresh within minutes, registered in a catalog (Glue, Hive Metastore, etc.) and queryable from Spark, Trino, Presto and other engines.</li>
<li class=""><strong>Incremental ETL downstream.</strong> Silver and gold tables subscribe to the bronze table via incremental queries, pulling and transforming only the records that changed since their last run.</li>
</ol>
<p>Getting an existing table's history in before streaming begins — the bootstrap — is handled either by letting Debezium take an initial consistent snapshot or by bulk-loading the table directly (for example over JDBC) and then starting the change stream from the matching checkpoint.</p>
<h2 class="anchor anchorTargetStickyNavbar_Vzrq" id="common-pitfalls-and-how-the-table-layer-absorbs-them">Common pitfalls, and how the table layer absorbs them<a href="https://hudi.apache.org/blog/2026/07/22/what-is-cdc-on-a-data-lake#common-pitfalls-and-how-the-table-layer-absorbs-them" class="hash-link" aria-label="Direct link to Common pitfalls, and how the table layer absorbs them" title="Direct link to Common pitfalls, and how the table layer absorbs them" translate="no">​</a></h2>
<p>A few failure modes recur in almost every CDC-on-the-lake deployment. Knowing them up front is most of the battle:</p>
<ul>
<li class=""><strong>Silently dropped deletes.</strong> Query-based capture cannot see hard deletes, and some pipelines ignore delete events on the sink side. The mirror then diverges from the source, one deleted row at a time — a correctness and compliance problem (GDPR erasure requests must propagate). The fix is log-based capture plus a sink that applies deletes as first-class operations, as Hudi's Debezium source does by honoring the delete marker in each event.</li>
<li class=""><strong>Out-of-order events.</strong> Retries and parallelism can deliver an old version of a row after a newer one. A sink that blindly takes the last arrival will regress data. Merging by the source log position, rather than arrival time, makes the pipeline immune to this.</li>
<li class=""><strong>Small files.</strong> Frequent micro-batches naturally produce many tiny files, and query performance on object storage degrades sharply as file counts explode. Hudi manages <a class="" href="https://hudi.apache.org/docs/file_sizing">file sizing</a> during writes and runs compaction and clustering as background table services, so ingestion frequency does not have to be traded against query speed.</li>
<li class=""><strong>Backfills colliding with the stream.</strong> Sooner or later you must reload history — a missed window, a schema fix — while live ingestion continues. Without transactions, a backfill job and a streaming writer stepping on each other corrupt the table. A transactional table layer with concurrency control lets the backfill run alongside the stream safely.</li>
</ul>
<h2 class="anchor anchorTargetStickyNavbar_Vzrq" id="conclusion">Conclusion<a href="https://hudi.apache.org/blog/2026/07/22/what-is-cdc-on-a-data-lake#conclusion" class="hash-link" aria-label="Direct link to Conclusion" title="Direct link to Conclusion" translate="no">​</a></h2>
<p>CDC on a data lake pairs the most valuable data most organizations have — the live state of their operational databases — with the cheapest, most open place to analyze it. The capture side is a solved problem thanks to log-based tools like Debezium; the hard part has always been the destination, because applying a stream of updates and deletes to immutable cloud storage requires upserts, ordering, schema evolution and transactions that raw file storage lacks. That is precisely the gap transactional lakehouse layers like Apache Hudi close, while adding the second half of the story: tables that can <em>emit</em> change streams so everything downstream becomes incremental too. To go deeper on capture mechanisms and architecture patterns, continue with <a class="" href="https://hudi.apache.org/blog/2024/07/30/data-lake-cdc">Understanding Data Lake Change Data Capture</a>; to build one yourself, start with the <a class="" href="https://hudi.apache.org/blog/2022/01/14/change-data-capture-with-debezium-and-apache-hudi">Debezium how-to</a> and the <a class="" href="https://hudi.apache.org/docs/hoodie_streaming_ingestion">Hudi Streamer docs</a>.</p>
<h2 class="anchor anchorTargetStickyNavbar_Vzrq" id="faq">FAQ<a href="https://hudi.apache.org/blog/2026/07/22/what-is-cdc-on-a-data-lake#faq" class="hash-link" aria-label="Direct link to FAQ" title="Direct link to FAQ" translate="no">​</a></h2>
<section><details style="margin-bottom:0.75rem"><summary><strong>What is the difference between CDC and ETL?</strong></summary><p style="margin-top:0.5rem">ETL is the general process of extracting data from sources, transforming it and loading it into a target system, traditionally as periodic bulk jobs that reprocess full tables. CDC is a specific extraction technique that captures only the row-level inserts, updates and deletes as they happen in the source database. In practice CDC often serves as the extract step of a continuous, incremental ETL pipeline, replacing full-table copies with a stream of deltas.</p></details><details style="margin-bottom:0.75rem"><summary><strong>Can you do CDC without Kafka?</strong></summary><p style="margin-top:0.5rem">Yes. Kafka is a popular transport for change events, but it is not required. Debezium can run as a standalone server that writes to other messaging systems, cloud services like AWS DMS can deliver changes directly to object storage, and ingestion tools can pull changes from a database over JDBC. Kafka earns its place in larger deployments because it buffers events durably and lets multiple consumers replay the same stream independently.</p></details><details style="margin-bottom:0.75rem"><summary><strong>How are deletes handled in CDC on a data lake?</strong></summary><p style="margin-top:0.5rem">Log-based CDC tools emit an explicit delete event when a row is removed from the source database. A transactional table layer such as Apache Hudi applies that event by locating the record via its key and removing it from the table, so the lake copy stays consistent with the source. This matters for correctness and for compliance, since regulations like GDPR require deletions to propagate to downstream copies.</p></details><details style="margin-bottom:0.75rem"><summary><strong>What is the difference between log-based and query-based CDC?</strong></summary><p style="margin-top:0.5rem">Query-based CDC repeatedly queries source tables for rows with a recent last-updated timestamp, which is simple but misses hard deletes and adds load to the production database. Log-based CDC reads the database's own transaction log, such as the MySQL binlog or PostgreSQL WAL, capturing every insert, update and delete in commit order with minimal source impact. Log-based capture is the standard choice for production pipelines.</p></details><details style="margin-bottom:0.75rem"><summary><strong>Why can't a plain data lake handle CDC?</strong></summary><p style="margin-top:0.5rem">A plain data lake stores immutable files on object storage, so there is no built-in way to update or delete an individual record, which is exactly what most CDC events require. Teams either appended all events and deduplicated at query time or rewrote whole partitions, both slow and error-prone. Transactional table layers like Apache Hudi add upserts, deletes, indexing and ACID guarantees on top of the same storage, making CDC ingestion practical.</p></details><details style="margin-bottom:0.75rem"><summary><strong>How fresh can data be with CDC on a data lake?</strong></summary><p style="margin-top:0.5rem">End-to-end latency from a database commit to a queryable row on the lake is typically measured in minutes with continuously running ingestion. Merge-on-read tables help by logging changes cheaply on write and compacting them in the background, instead of rewriting large files on every batch. That is near real-time for analytics, though not a replacement for the millisecond latencies of an operational database.</p></details></section>]]></content:encoded>
            <category>cdc</category>
            <category>streaming</category>
            <category>debezium</category>
            <category>data lakehouse</category>
            <category>beginner</category>
        </item>
        <item>
            <title><![CDATA[What is a Streaming Data Lake?]]></title>
            <link>https://hudi.apache.org/blog/2026/07/21/what-is-a-streaming-data-lake</link>
            <guid>https://hudi.apache.org/blog/2026/07/21/what-is-a-streaming-data-lake</guid>
            <pubDate>Tue, 21 Jul 2026 00:00:00 GMT</pubDate>
            <description><![CDATA[What a streaming data lake is, how it differs from batch data lakes and Kafka + warehouse stacks, and the storage building blocks that make it work.]]></description>
            <content:encoded><![CDATA[<p>A streaming data lake is a data lake that ingests and serves data continuously — with minute-level freshness — instead of through periodic batch loads. Batch-era data lakes refresh on a schedule, typically hourly or daily, which was acceptable when the lake mostly fed reports and offline model training. Today, operational analytics, change data capture (CDC) from transactional databases, and AI applications all want much fresher data — and the traditional answers, standing up a separate warehouse for the fresh slice or keeping everything in Kafka, either duplicate data into proprietary systems or become prohibitively expensive to retain at scale. A streaming data lake closes that gap: one copy of the data, on open formats in cloud storage, written to continuously and readable within minutes.</p>
<p>This post is the definitional companion to the original 2021 essay, <a class="" href="https://hudi.apache.org/blog/2021/07/21/streaming-data-lake-platform">Apache Hudi - The Data Lake Platform</a>, which first articulated this vision in depth. Here we focus on what the term means in 2026, what it takes to build one, and how it relates to the data lakehouse.</p>
<h2 class="anchor anchorTargetStickyNavbar_Vzrq" id="from-batch-data-lakes-to-streaming-data-lakes">From Batch Data Lakes to Streaming Data Lakes<a href="https://hudi.apache.org/blog/2026/07/21/what-is-a-streaming-data-lake#from-batch-data-lakes-to-streaming-data-lakes" class="hash-link" aria-label="Direct link to From Batch Data Lakes to Streaming Data Lakes" title="Direct link to From Batch Data Lakes to Streaming Data Lakes" translate="no">​</a></h2>
<p>A batch data lake and a streaming data lake can look identical on the surface — Parquet files on object storage, queried by Spark, Trino, or Presto. The difference lies in three operating assumptions.</p>
<p><strong>Ingestion cadence.</strong> A batch lake receives data in large, scheduled loads: a nightly dump, an hourly partition. A streaming data lake receives data continuously, committing every few minutes. This is not just a smaller batch interval. Committing hundreds of times a day instead of once means the storage layer must absorb many small writes without accumulating thousands of tiny files, must keep table metadata cheap to update on every commit, and must reconcile new data against old data constantly rather than once a night.</p>
<p><strong>Mutation support.</strong> Batch lakes were built around append-only, immutable files, and rewrote whole partitions to "update" anything. Streaming sources are dominated by mutation: CDC streams from databases are sequences of inserts, updates, and deletes keyed by primary key; late-arriving events revise records that were already written. A streaming data lake must treat record-level upserts and deletes as first-class write operations, applied continuously.</p>
<p><strong>Read patterns.</strong> Batch consumers scan the latest partition after the nightly load lands. Streaming consumers need more: dashboards want the freshest consistent snapshot, downstream pipelines want to ask "what changed since I last ran?" and process only that delta. A streaming data lake serves both the classic full-table scan and this incremental, change-oriented consumption.</p>
<p>None of these can be bolted onto a batch lake with a faster scheduler. They require rethinking storage itself.</p>
<p><img decoding="async" loading="lazy" alt="The incremental stack blends the latency profile of stream processing with the efficiency and scale of batch storage and compute." src="https://hudi.apache.org/assets/images/hudi-data-lake-platform_-_Page_2_4-a088663c0cb4f7e97b5b74da634975ef.png" width="1229" height="961" class="img_ev3q">
</p><p align="center">Figure: The incremental stack, alongside batch and stream processing</p><p></p>
<h2 class="anchor anchorTargetStickyNavbar_Vzrq" id="the-required-building-blocks">The Required Building Blocks<a href="https://hudi.apache.org/blog/2026/07/21/what-is-a-streaming-data-lake#the-required-building-blocks" class="hash-link" aria-label="Direct link to The Required Building Blocks" title="Direct link to The Required Building Blocks" translate="no">​</a></h2>
<p>Five capabilities separate a lake that can operate in streaming mode from one that cannot.</p>
<p><strong>Continuous upserts, backed by indexes.</strong> Applying a CDC stream means locating, for every incoming record, the file that holds the previous version of that key — millions of times per commit. Doing this by scanning data is hopeless; a streaming data lake needs indexes that map record keys to file locations so upserts and deletes touch only the files that actually change. This is why indexing sits in the write path of Apache Hudi rather than being only a query-side optimization.</p>
<p><strong>Merge-on-Read storage.</strong> With frequent commits, rewriting columnar base files on every write (Copy-on-Write) multiplies write amplification: a handful of changed rows can force rewriting an entire file, hundreds of times a day. The <a class="" href="https://hudi.apache.org/docs/table_types">Merge-on-Read (MOR) table type</a> instead absorbs changes into compact delta log files and merges them with base files at query time or during compaction. Write cost then scales with the amount of change, not with the size of accumulated state — the property that makes minute-level commits economical. The MOR series on this blog makes the deeper case that this is <a class="" href="https://hudi.apache.org/blog/2026/05/14/mor-isnt-a-storage-optimization-its-an-architectural-shift">an architectural shift, not a storage tuning knob</a>, one that extends all the way into <a class="" href="https://hudi.apache.org/blog/2026/06/05/why-metadata-has-to-be-mutation-friendly">the metadata layer itself</a>.</p>
<p><img decoding="async" loading="lazy" alt="Merge-on-Read table with base files, delta log files, periodic compaction, and both snapshot and read-optimized queries." src="https://hudi.apache.org/assets/images/MOR_new-aa806492cc5034a48039f9d8a392b9d8.png" width="6652" height="3176" class="img_ev3q">
</p><p align="center">Figure: A Merge-on-Read table absorbing frequent commits into delta logs</p><p></p>
<p><strong>Asynchronous table services.</strong> Deferring merge work is only viable if something reliably performs it — without stopping ingestion. A streaming data lake needs built-in table services: compaction to fold delta logs into new base files, clustering to reorganize data layout for query performance, and cleaning to reclaim old file versions. Crucially, these must run asynchronously, concurrently with the ingestion stream. If maintenance requires pausing the writer, the lake is not really streaming; it is alternating between ingesting and repairing itself. Hudi's services are aware of each other and of active writers through a shared timeline, which is what allows <a class="" href="https://hudi.apache.org/blog/2026/06/25/building-indexes-on-a-moving-target">even indexes to be built on tables that never stop changing</a>.</p>
<p><strong>Change streams.</strong> Once tables update continuously, downstream pipelines should not recompute from full scans. A streaming data lake exposes record-level change streams — the ability to query exactly the records that changed between two points in time — so that derived tables can be maintained incrementally. This is what turns a single fast table into an end-to-end incremental architecture, where each layer consumes the changes of the previous one, much like chained stream processing jobs but over columnar lake storage.</p>
<p><strong>Concurrency control.</strong> Continuous writers, background services, and readers all operate on the same table at once. Readers need snapshot isolation so queries always see a consistent, committed view. Writers and table services need to proceed without blocking one another — an optimistic model that simply fails one side wastes enormous resources when a long-running compaction overlaps a stream that commits every minute. Streaming-first designs favor non-blocking, MVCC-style coordination between the writer and table services, reserving conflict resolution for the cases that genuinely need it.</p>
<h2 class="anchor anchorTargetStickyNavbar_Vzrq" id="the-write-path-streaming-ingestion">The Write Path: Streaming Ingestion<a href="https://hudi.apache.org/blog/2026/07/21/what-is-a-streaming-data-lake#the-write-path-streaming-ingestion" class="hash-link" aria-label="Direct link to The Write Path: Streaming Ingestion" title="Direct link to The Write Path: Streaming Ingestion" translate="no">​</a></h2>
<p>In practice, the write path of a streaming data lake is a stream processing job that terminates in lake storage. <a class="" href="https://hudi.apache.org/docs/flink-quick-start-guide">Apache Flink can write continuously into Hudi tables</a>, applying CDC streams from Kafka or Debezium as upserts; Spark Structured Streaming and Hudi's own streaming ingestion utilities do the same. The writer commits at a chosen interval — commonly every one to five minutes — and each commit is atomic: queries see all of it or none of it.</p>
<p>Two streaming realities shape this path. First, <strong>ordering</strong>: events for the same key can arrive out of order, so the writer resolves collisions deterministically using an ordering field (typically event time), ensuring an older update never overwrites a newer one regardless of arrival order. Second, <strong>late data</strong>: an event may arrive hours or days after the period it belongs to. In a batch lake this forces re-running jobs over old partitions; in a streaming data lake the index locates the affected records wherever they live, and the late event is applied as a normal upsert — late data is just another update.</p>
<h2 class="anchor anchorTargetStickyNavbar_Vzrq" id="the-read-path-snapshot-read-optimized-and-incremental-queries">The Read Path: Snapshot, Read-Optimized, and Incremental Queries<a href="https://hudi.apache.org/blog/2026/07/21/what-is-a-streaming-data-lake#the-read-path-snapshot-read-optimized-and-incremental-queries" class="hash-link" aria-label="Direct link to The Read Path: Snapshot, Read-Optimized, and Incremental Queries" title="Direct link to The Read Path: Snapshot, Read-Optimized, and Incremental Queries" translate="no">​</a></h2>
<p>A streaming data lake gives readers an explicit choice of freshness versus cost, expressed as <a class="" href="https://hudi.apache.org/docs/table_types">three query types</a>:</p>
<ul>
<li class=""><strong>Snapshot queries</strong> merge base files with delta logs at read time, returning the freshest committed state — data written minutes ago. This is the near-real-time analytics path.</li>
<li class=""><strong>Read-optimized queries</strong> read only compacted base files, giving pure columnar scan performance at the price of bounded staleness (data freshness tracks the compaction schedule). Latency-tolerant reporting can run here at lower cost.</li>
<li class=""><strong>Incremental queries</strong> return the records that changed between two commit times, powering the incremental pipelines described above.</li>
</ul>
<p>The same table serves all three, from the same single copy of data, through standard engines. There is no separate "speed layer" holding fresh data and "batch layer" holding history to reconcile — the failure mode that made Lambda architectures notoriously hard to operate.</p>
<h2 class="anchor anchorTargetStickyNavbar_Vzrq" id="streaming-data-lake-vs-kafka--warehouse">Streaming Data Lake vs. Kafka + Warehouse<a href="https://hudi.apache.org/blog/2026/07/21/what-is-a-streaming-data-lake#streaming-data-lake-vs-kafka--warehouse" class="hash-link" aria-label="Direct link to Streaming Data Lake vs. Kafka + Warehouse" title="Direct link to Streaming Data Lake vs. Kafka + Warehouse" translate="no">​</a></h2>
<p>The common alternative for fresh analytics is to keep data in Kafka and continuously load it into a cloud warehouse. It is worth being honest about the trade-offs in both directions.</p>
<p><strong>Where Kafka + warehouse wins.</strong> Kafka delivers events in milliseconds to seconds, and a stream processor reading Kafka can react at that speed — a streaming data lake, committing every few minutes to object storage, cannot. If the use case is sub-second serving, alerting, or event-driven applications, Kafka (plus a stream processor or real-time OLAP store) is the right tool. Warehouses also offer a highly polished, fully managed SQL experience.</p>
<p><strong>Where the streaming data lake wins.</strong> Retention and scale economics. Keeping weeks or months of history in Kafka is expensive, and Kafka is not queryable with analytical SQL — so data ends up copied into a warehouse anyway, where storage and compute are metered at warehouse prices and the data lands in a proprietary format usable only by that warehouse's engine. A streaming data lake stores one copy on cheap object storage in open formats, retains history indefinitely, serves large scans, joins, and ML training workloads directly, and remains accessible to every engine. For the broad middle ground — data that must be fresh within minutes and queryable for years — it removes both the Kafka retention bill and the warehouse copy.</p>
<p>The two are complementary rather than exclusive: a common pattern keeps Kafka as the transport at the edge, with the streaming data lake as the durable, queryable system of record a few minutes behind it.</p>
<h2 class="anchor anchorTargetStickyNavbar_Vzrq" id="how-this-connects-to-the-lakehouse">How This Connects to the Lakehouse<a href="https://hudi.apache.org/blog/2026/07/21/what-is-a-streaming-data-lake#how-this-connects-to-the-lakehouse" class="hash-link" aria-label="Direct link to How This Connects to the Lakehouse" title="Direct link to How This Connects to the Lakehouse" translate="no">​</a></h2>
<p>A <a class="" href="https://hudi.apache.org/blog/2024/07/11/what-is-a-data-lakehouse">data lakehouse</a> brings warehouse capabilities — transactions, schema management, mutability — to data lake storage. A streaming data lake is best understood as <strong>a lakehouse operated in streaming mode</strong>: the same open storage and transactional foundation, but with continuous writes instead of scheduled loads, change streams instead of full refreshes, and table services that run concurrently with ingestion instead of in maintenance windows. Every streaming data lake is a lakehouse; not every lakehouse is operated — or architecturally able to operate — as a streaming data lake, because the building blocks above (write-path indexing, MOR, async services, record-level change streams, non-blocking concurrency) must be designed in rather than added on.</p>
<p>This is where Apache Hudi's history matters. Hudi was created at Uber in 2016 precisely for this problem — applying massive upsert streams to petabyte-scale tables on lake storage with minute-level freshness — and its architecture follows from that origin: pluggable indexing in the write path, Merge-on-Read storage, a suite of built-in asynchronous table services, and incremental queries as a core primitive. The <a class="" href="https://hudi.apache.org/blog/2021/07/21/streaming-data-lake-platform">2021 essay</a> laid out this "streaming data lake platform" framing when much of the industry was still discussing table formats for batch workloads; the intervening years of CDC, near-real-time analytics, and AI-driven freshness demands have made it the mainstream requirement it is today.</p>
<h2 class="anchor anchorTargetStickyNavbar_Vzrq" id="conclusion">Conclusion<a href="https://hudi.apache.org/blog/2026/07/21/what-is-a-streaming-data-lake#conclusion" class="hash-link" aria-label="Direct link to Conclusion" title="Direct link to Conclusion" translate="no">​</a></h2>
<p>A streaming data lake is not a faster batch lake or a bigger Kafka cluster. It is a data lake whose storage layer is designed for continuous change: upserts resolved through indexes, writes absorbed by Merge-on-Read, maintenance performed asynchronously by table services, changes exposed as first-class streams, and all of it coordinated safely under concurrent access. In return, it delivers minute-level data freshness on open formats and cheap storage, serving snapshot, read-optimized, and incremental consumers from a single copy of data. If the lakehouse defined <em>what</em> modern lake storage can do, the streaming data lake defines <em>how continuously</em> it can do it — and it is increasingly the default way fresh data platforms are built.</p>
<h2 class="anchor anchorTargetStickyNavbar_Vzrq" id="faq">FAQ<a href="https://hudi.apache.org/blog/2026/07/21/what-is-a-streaming-data-lake#faq" class="hash-link" aria-label="Direct link to FAQ" title="Direct link to FAQ" translate="no">​</a></h2>
<section><details style="margin-bottom:0.75rem"><summary><strong>What is a streaming data lake?</strong></summary><p style="margin-top:0.5rem">A streaming data lake is a data lake that ingests and serves data continuously, with minute-level freshness, instead of through periodic batch loads. It keeps data in open formats on cloud object storage while supporting continuous upserts, incremental change streams, and background table maintenance that runs without stopping ingestion.</p></details><details style="margin-bottom:0.75rem"><summary><strong>What is the difference between a streaming data lake and a data lakehouse?</strong></summary><p style="margin-top:0.5rem">A data lakehouse brings warehouse capabilities like transactions and mutability to data lake storage, while a streaming data lake is a lakehouse operated in streaming mode. It adds continuous minute-level ingestion, record-level change streams for incremental consumers, and asynchronous table services that run alongside live writers. Every streaming data lake is a lakehouse, but many lakehouses are only operated as batch systems.</p></details><details style="margin-bottom:0.75rem"><summary><strong>Can a data lake handle real-time data?</strong></summary><p style="margin-top:0.5rem">Yes, within limits. A streaming data lake built on storage like Apache Hudi can commit continuously and make data queryable within minutes, which covers most operational analytics and CDC replication needs. For sub-second latency, such as event-driven applications or instant alerting, a stream processor or real-time OLAP system reading from Kafka is still the better fit.</p></details><details style="margin-bottom:0.75rem"><summary><strong>Do I still need Kafka with a streaming data lake?</strong></summary><p style="margin-top:0.5rem">Usually yes, but in a narrower role. Kafka remains an excellent transport for moving events from producers to consumers in seconds, and it commonly feeds the streaming data lake. What changes is retention and analytics: instead of keeping long histories in Kafka or copying everything into a warehouse, the lake becomes the durable, cheaply stored, SQL-queryable system of record a few minutes behind the stream.</p></details><details style="margin-bottom:0.75rem"><summary><strong>Why is Merge-on-Read important for streaming ingestion?</strong></summary><p style="margin-top:0.5rem">Merge-on-Read absorbs updates and deletes into small delta log files instead of rewriting large columnar files on every commit, so write cost scales with the amount of change rather than the size of the table. That is what makes committing every few minutes economical. A background compaction service later folds the logs into optimized base files without pausing ingestion.</p></details><details style="margin-bottom:0.75rem"><summary><strong>How fresh can data in a streaming data lake be?</strong></summary><p style="margin-top:0.5rem">Typically within a few minutes of the source event, determined by the commit interval of the streaming writer. Snapshot queries see every committed change immediately, while read-optimized queries trade some freshness for faster scans by reading only compacted files. The freshness floor comes from object storage and columnar file economics, which favor batching writes into small intervals rather than per-event commits.</p></details></section>]]></content:encoded>
            <category>streaming</category>
            <category>data lakehouse</category>
            <category>mor</category>
            <category>cdc</category>
            <category>incremental processing</category>
        </item>
        <item>
            <title><![CDATA[What is ACID on a Data Lake?]]></title>
            <link>https://hudi.apache.org/blog/2026/07/17/what-is-acid-on-a-data-lake</link>
            <guid>https://hudi.apache.org/blog/2026/07/17/what-is-acid-on-a-data-lake</guid>
            <pubDate>Fri, 17 Jul 2026 00:00:00 GMT</pubDate>
            <description><![CDATA[What ACID means for tables on S3, GCS or ADLS: how table formats like Apache Hudi deliver atomic commits, snapshot isolation and safe concurrent writes.]]></description>
            <content:encoded><![CDATA[<p>ACID on a data lake means that writes to tables stored on object storage are Atomic, Consistent, Isolated and Durable — the same transactional guarantees a database provides, applied to files in S3, GCS or ADLS. That sentence is easy to say and surprisingly hard to deliver. A database owns its storage engine end to end; a data lake is a pile of Parquet files on an object store that offers durable single-object PUTs and nothing more. There is no multi-file atomic operation, no rename that atomically swaps a directory of files, and no built-in notion of "these 500 files belong to one write." Without extra machinery, a reader can list a directory while a writer is halfway through it and see a partial write, and two jobs writing to the same partition can silently clobber each other's output. ACID on a lake is the machinery — a commit protocol layered over the object store — that closes those gaps.</p>
<p>This post walks through what each of the four letters means concretely at the file level, how lakes worked before table formats existed, how formats like <a href="https://hudi.apache.org/" target="_blank" rel="noopener noreferrer" class="">Apache Hudi</a> deliver these guarantees, and — just as important — what ACID on a lake does <em>not</em> promise.</p>
<h2 class="anchor anchorTargetStickyNavbar_Vzrq" id="what-a-c-i-and-d-mean-on-object-storage">What A, C, I and D Mean on Object Storage<a href="https://hudi.apache.org/blog/2026/07/17/what-is-acid-on-a-data-lake#what-a-c-i-and-d-mean-on-object-storage" class="hash-link" aria-label="Direct link to What A, C, I and D Mean on Object Storage" title="Direct link to What A, C, I and D Mean on Object Storage" translate="no">​</a></h2>
<p>The textbook definitions of ACID were written for databases processing many small transactions. On a data lake, the transactions are large batch or streaming writes that produce files, so it is worth restating each property in file terms.</p>
<p><strong>Atomicity</strong> means a write either publishes completely or not at all. Consider a Spark job writing 500 Parquet files into a table and dying at file 320 — an executor is lost, a spot instance is reclaimed, someone kills the job. Atomicity requires that those 320 orphaned files are never visible to any query, and that the failed write can be cleanly rolled back. Without it, every downstream consumer inherits a partially written dataset and no reliable way to detect it.</p>
<p><strong>Consistency</strong> means every write moves the table from one valid state to another valid state. On a lake this covers schema enforcement (a write with an incompatible schema is rejected rather than quietly producing unreadable files), key constraints like upsert semantics (a record with the same key updates in place rather than duplicating), and the invariant that the table's metadata always describes exactly the set of files that constitute the table.</p>
<p><strong>Isolation</strong> means concurrent readers and writers do not observe each other's intermediate states. Two failure modes matter here. First, two writers targeting the same partition at the same time — say, a backfill job and a streaming ingest job — must not interleave their files such that the table ends up with a mix of both writes' partial output. Second, a reader that starts listing files mid-write must not see half of a commit; it should see the table exactly as it was at some committed point, even if the query runs for an hour while new commits land.</p>
<p><strong>Durability</strong> means a committed write survives failures. Object stores already provide excellent durability for individual objects — S3, GCS and ADLS replicate objects across zones. The lake-specific part of durability is that the <em>commit record itself</em> is persisted to the same durable storage, so that "was this write committed?" has a crash-proof answer, and a committed transaction is never silently rolled back by a subsequent cleanup process.</p>
<h2 class="anchor anchorTargetStickyNavbar_Vzrq" id="life-before-acid-on-data-lakes">Life Before ACID on Data Lakes<a href="https://hudi.apache.org/blog/2026/07/17/what-is-acid-on-a-data-lake#life-before-acid-on-data-lakes" class="hash-link" aria-label="Direct link to Life Before ACID on Data Lakes" title="Direct link to Life Before ACID on Data Lakes" translate="no">​</a></h2>
<p>The Hadoop and early cloud-lake era answered these problems with conventions, not guarantees. It is worth remembering how fragile those conventions were, because they explain why table formats exist.</p>
<p>The classic Hive pattern was the <strong>partition swap</strong>: write data into a staging directory, then "atomically" move it into place and update the Hive metastore partition location. On HDFS a directory rename was genuinely atomic, so this mostly worked. On object stores there are no directories and no atomic rename — a "rename" is a copy-then-delete of every object — so the same pattern became a slow, non-atomic window during which readers could see anything.</p>
<p>The <strong><code>_SUCCESS</code> file</strong> convention had MapReduce and Spark drop an empty marker file when a job finished, and downstream jobs would poll for it. This signaled completion but protected nothing: readers that listed the directory directly saw partial data, a rerun of a failed job could mix old and new files, and there was no rollback story at all — cleanup after a failed job meant a human with a delete command deciding which files were orphans.</p>
<p>Layered on top of this, early S3 offered only <strong>eventual consistency</strong> for listings, so even a fully written directory might not be fully <em>visible</em> yet. Whole projects (S3Guard, committers with DynamoDB bookkeeping) existed just to paper over that. S3 became strongly consistent in 2020, which removed one class of bugs — but strong single-object consistency still does not give you multi-file atomicity, isolation between writers, or rollback. Those require a transaction log.</p>
<h2 class="anchor anchorTargetStickyNavbar_Vzrq" id="how-table-formats-deliver-acid">How Table Formats Deliver ACID<a href="https://hudi.apache.org/blog/2026/07/17/what-is-acid-on-a-data-lake#how-table-formats-deliver-acid" class="hash-link" aria-label="Direct link to How Table Formats Deliver ACID" title="Direct link to How Table Formats Deliver ACID" translate="no">​</a></h2>
<p>The shared insight behind Apache Hudi, Apache Iceberg and Delta Lake is that the table's state should be defined by a <strong>metadata log with an atomic publish step</strong>, not by whatever files happen to be in a directory. Data files are written first, invisibly; then a single, small metadata operation makes them visible all at once. Because that final operation is one atomic object-store action (a single PUT, or a conditional/compare-and-swap write), the entire multi-file write inherits its atomicity.</p>
<p>In Hudi, that log is the <a class="" href="https://hudi.apache.org/docs/timeline">timeline</a> — an ordered event log of every action performed on the table, stored under the table's <code>.hoodie/timeline</code> directory. Each write is an action that moves through explicit states: <code>REQUESTED</code> (the write is planned), <code>INFLIGHT</code> (data files are being produced), and <code>COMPLETED</code> (the write is published). The transition to <code>COMPLETED</code> is a single atomic object-store operation, and Hudi timestamps every action with monotonically increasing, <a class="" href="https://hudi.apache.org/docs/timeline#truetime-generation">TrueTime</a>-style instants so actions are globally ordered even across independent processes.</p>
<p><img decoding="async" loading="lazy" alt="Actions in the Hudi timeline" src="https://hudi.apache.org/assets/images/hudi-timeline-actions-e56d0d9fad5645d9910f2591ad7775de.png" width="817" height="424" class="img_ev3q">
</p><p align="center">Figure: Writes and table services recorded as actions on the Hudi timeline</p><p></p>
<p>This structure delivers each letter directly:</p>
<ul>
<li class=""><strong>Atomicity</strong>: our 500-file Spark job that dies at file 320 leaves an <code>INFLIGHT</code> action on the timeline and 320 uncommitted files that no query will ever read, because queries resolve visible files through completed timeline actions — never by listing directories. A rollback action later removes the orphaned files. Nothing partial is ever served.</li>
<li class=""><strong>Consistency</strong>: the timeline is the single source of truth for table state, and schema and key constraints are enforced as part of the write before the commit is published.</li>
<li class=""><strong>Isolation</strong>: readers get <strong>snapshot isolation</strong>. A query binds to the set of completed commits as of its start and sees exactly that state for its entire duration, no matter how many writes complete concurrently. Multi-version concurrency control (MVCC) keeps prior file versions around so long-running queries are never yanked out from under.</li>
<li class=""><strong>Durability</strong>: the commit metadata lives on the same replicated object storage as the data, so a completed instant is as durable as the data it describes.</li>
</ul>
<p>For a deeper walkthrough of how the timeline underpins each transaction state transition, see the earlier post <a class="" href="https://hudi.apache.org/blog/2023/07/09/Hoodie-Timeline-Foundational-pillar-for-ACID-transactions">Hoodie Timeline: Foundational pillar for ACID transactions</a>.</p>
<p>To be fair to the broader ecosystem: Apache Iceberg and Delta Lake achieve ACID through the same fundamental idea with different mechanics. Iceberg builds immutable snapshot trees of manifest files and commits by atomically swapping a pointer to the new table metadata (typically via a catalog's compare-and-swap). Delta Lake maintains a JSON transaction log (<code>_delta_log</code>) where writing log entry <em>N+1</em> is the atomic commit point, using conditional writes on the object store. All three give you atomic multi-file commits and snapshot isolation; they differ in how metadata is organized, how conflicts are detected, and what concurrency patterns they optimize for. This transactional layer is a defining ingredient of the <a class="" href="https://hudi.apache.org/blog/2024/07/11/what-is-a-data-lakehouse">lakehouse architecture</a> generally, not a Hudi-only concept.</p>
<h2 class="anchor anchorTargetStickyNavbar_Vzrq" id="concurrency-control-models">Concurrency Control Models<a href="https://hudi.apache.org/blog/2026/07/17/what-is-acid-on-a-data-lake#concurrency-control-models" class="hash-link" aria-label="Direct link to Concurrency Control Models" title="Direct link to Concurrency Control Models" translate="no">​</a></h2>
<p>Atomic commits solve the single-writer case. The harder question — and where table formats differ most — is what happens when multiple processes write to the same table. Hudi's <a class="" href="https://hudi.apache.org/docs/concurrency_control">concurrency control</a> documentation lays out a spectrum of models:</p>
<p><strong>Single writer with table services.</strong> Most pipelines have one writer, plus background table services (compaction, clustering, cleaning) that rewrite data for performance. Hudi distinguishes these by design: table services run lock-free alongside the writer, coordinated through the timeline via MVCC, either inline or fully async in the same process. For this very common case, no external lock infrastructure is needed at all.</p>
<p><strong>Optimistic concurrency control (OCC).</strong> When genuinely separate jobs write to one table — a streaming ingest plus a batch backfill, say — Hudi supports OCC at file-group granularity. Each writer proceeds without blocking, and at commit time takes a short-lived distributed lock to check whether a concurrently completed commit touched the same files. Non-overlapping writes both succeed; overlapping writes cause one to abort and retry. The lock is held only around the commit-time critical section, not for the duration of the job.</p>
<p><img decoding="async" loading="lazy" alt="Optimistic concurrency control" src="https://hudi.apache.org/assets/images/OCC-91d80dc77e9585e8811d80913d368a72.png" width="2014" height="700" class="img_ev3q">
</p><p align="center">Figure: Optimistic concurrency control — conflicts detected at commit time</p><p></p>
<p>OCC works well when conflicts are rare, but it has an honest weakness for streaming: two high-frequency writers that routinely touch the same file groups will repeatedly abort each other, wasting compute and starving progress.</p>
<p><strong>Non-blocking concurrency control (NBCC).</strong> For multi-writer streaming, Hudi supports a mode where multiple writers can write into the <em>same</em> file groups simultaneously without aborting each other. Instead of preventing the conflict, resolution is deferred: commits are ordered by completion time on the timeline, and the query reader and the compactor merge overlapping writes deterministically. A lock is needed only for the instant-metadata write to the timeline itself. NBCC currently targets merge-on-read tables with bucket indexes, and reflects a streaming-native view of concurrency: serialize the log, not the writers.</p>
<h2 class="anchor anchorTargetStickyNavbar_Vzrq" id="what-acid-on-a-data-lake-does-not-mean">What ACID on a Data Lake Does Not Mean<a href="https://hudi.apache.org/blog/2026/07/17/what-is-acid-on-a-data-lake#what-acid-on-a-data-lake-does-not-mean" class="hash-link" aria-label="Direct link to What ACID on a Data Lake Does Not Mean" title="Direct link to What ACID on a Data Lake Does Not Mean" translate="no">​</a></h2>
<p>Honesty matters here, because "ACID" invites comparisons to OLTP databases that lakes are not designed to win.</p>
<ul>
<li class=""><strong>It is not OLTP.</strong> Commit latency on a lakehouse is measured in seconds — an object-store round trip plus metadata work — not the milliseconds of a transactional database. Lakehouse transactions are designed for batches and micro-batches of records, not single-row point writes at high QPS.</li>
<li class=""><strong>Transactions span one table.</strong> The unit of atomicity in Hudi (and its peers, by and large) is a single table's commit. Multi-table transactions, cross-table foreign keys, and interactive multi-statement transactions with <code>BEGIN</code>/<code>COMMIT</code> semantics are not part of the model.</li>
<li class=""><strong>Isolation is snapshot isolation, not serializability of arbitrary read-write transactions.</strong> Writers are serialized against each other per table, and readers see consistent snapshots — which is exactly what analytical workloads need — but you should not port an OLTP application's locking assumptions onto a lake.</li>
<li class=""><strong>ACID does not fix bad pipelines.</strong> Atomic commits guarantee that what was written is either fully visible or invisible; they do not validate that the data itself was correct.</li>
</ul>
<p>If your workload is high-QPS point reads and writes, use an operational database and land the changes onto the lake via CDC — which, conveniently, is a workload where lake ACID shines.</p>
<h2 class="anchor anchorTargetStickyNavbar_Vzrq" id="why-it-matters">Why It Matters<a href="https://hudi.apache.org/blog/2026/07/17/what-is-acid-on-a-data-lake#why-it-matters" class="hash-link" aria-label="Direct link to Why It Matters" title="Direct link to Why It Matters" translate="no">​</a></h2>
<p>These guarantees are not academic; they are what make several load-bearing patterns possible at all.</p>
<p><strong>CDC correctness.</strong> Replicating a database table into the lake via change data capture means a continuous stream of upserts and deletes. Without atomic commits and key-based upserts, a mid-batch failure leaves the lake copy in a state the source database never had — and there is no way to reconcile. With ACID, every commit is an all-or-nothing application of a batch of changes, and incremental consumers downstream see change batches in a well-defined order, never partially.</p>
<p><strong>Concurrent ingestion and table services.</strong> Real tables need continuous compaction, clustering and cleaning to stay queryable. Running those alongside ingestion without transactions means either stopping the world or risking corruption. MVCC on the timeline lets Hudi rewrite data in the background while writers write and readers read, with each process pinned to a consistent snapshot.</p>
<p><strong>Regulatory deletes.</strong> GDPR and CCPA erasure requests require deleting specific records from immutable files — physically, a read-modify-rewrite of affected files. ACID makes each deletion pass atomic and auditable: the timeline records exactly when the delete committed, queries never see a half-deleted state, and the commit history serves as evidence of compliance.</p>
<h2 class="anchor anchorTargetStickyNavbar_Vzrq" id="conclusion">Conclusion<a href="https://hudi.apache.org/blog/2026/07/17/what-is-acid-on-a-data-lake#conclusion" class="hash-link" aria-label="Direct link to Conclusion" title="Direct link to Conclusion" translate="no">​</a></h2>
<p>ACID on a data lake is a commit protocol over object storage: data files are written invisibly, then published through one atomic metadata operation, with snapshot isolation for readers and explicit concurrency control between writers. Hudi implements this with its <a class="" href="https://hudi.apache.org/docs/timeline">timeline</a> — an ordered, durable event log of every action on the table — plus a spectrum of <a class="" href="https://hudi.apache.org/docs/concurrency_control">concurrency control</a> modes ranging from lock-free single-writer deployments to OCC and non-blocking multi-writer streaming. Iceberg and Delta Lake deliver the same core guarantees through their own log designs. None of this turns a lake into an OLTP database, and it should not be sold that way; what it does is make tables on cheap object storage trustworthy enough to run CDC pipelines, concurrent services and compliance workloads that raw file lakes could never support. That trust is the foundation the rest of the lakehouse stack is built on.</p>
<h2 class="anchor anchorTargetStickyNavbar_Vzrq" id="faq">FAQ<a href="https://hudi.apache.org/blog/2026/07/17/what-is-acid-on-a-data-lake#faq" class="hash-link" aria-label="Direct link to FAQ" title="Direct link to FAQ" translate="no">​</a></h2>
<section><details style="margin-bottom:0.75rem"><summary><strong>Does S3 support ACID transactions?</strong></summary><p style="margin-top:0.5rem">Not by itself. S3 provides durable, strongly consistent operations on individual objects, but no atomic operation spanning multiple objects and no isolation between concurrent writers. ACID transactions on S3 come from a table format layered on top, such as Apache Hudi, Apache Iceberg or Delta Lake, which publishes multi-file writes through a single atomic metadata commit.</p></details><details style="margin-bottom:0.75rem"><summary><strong>Is a data lake ACID compliant?</strong></summary><p style="margin-top:0.5rem">A raw data lake of Parquet or ORC files is not ACID compliant: readers can observe partial writes, concurrent jobs can corrupt each other, and failed jobs leave orphaned files. A data lake becomes ACID compliant when tables are managed by a transactional table format, at which point it is usually called a lakehouse.</p></details><details style="margin-bottom:0.75rem"><summary><strong>How does Hudi guarantee atomicity?</strong></summary><p style="margin-top:0.5rem">Hudi records every write as an action on its timeline, moving through requested, inflight and completed states. Data files are written first but remain invisible; the transition to completed is a single atomic object-store operation. Queries only read files referenced by completed actions, so a failed write is never visible and is later rolled back cleanly.</p></details><details style="margin-bottom:0.75rem"><summary><strong>What is snapshot isolation on a data lake?</strong></summary><p style="margin-top:0.5rem">Snapshot isolation means every query reads the table as of a specific committed state and sees exactly that state for its entire run, even while new writes commit concurrently. Table formats implement it with multi-version concurrency control, keeping prior file versions available until no reader needs them.</p></details><details style="margin-bottom:0.75rem"><summary><strong>Can multiple jobs write to the same lakehouse table at once?</strong></summary><p style="margin-top:0.5rem">Yes, with concurrency control. Hudi supports optimistic concurrency control, where writers proceed in parallel and conflicts on overlapping files are detected at commit time using a short-lived lock, and non-blocking concurrency control, where multiple writers can even target the same file group and conflicts are resolved at read and compaction time.</p></details><details style="margin-bottom:0.75rem"><summary><strong>Is ACID on a data lake the same as ACID in a database like PostgreSQL?</strong></summary><p style="margin-top:0.5rem">The guarantees are the same in kind but different in scope and latency. Lakehouse transactions cover batches of records within one table and commit in seconds, whereas an OLTP database offers millisecond, multi-statement, often multi-table transactions. Lakes are built for analytical and streaming workloads, not high-QPS point reads and writes.</p></details></section>]]></content:encoded>
            <category>acid</category>
            <category>concurrency control</category>
            <category>hudi timeline</category>
            <category>data lakehouse</category>
        </item>
        <item>
            <title><![CDATA[Can a Lakehouse Really Run Maintenance Without Blocking Writes?]]></title>
            <link>https://hudi.apache.org/blog/2026/07/17/async-compaction-background-maintenance</link>
            <guid>https://hudi.apache.org/blog/2026/07/17/async-compaction-background-maintenance</guid>
            <pubDate>Fri, 17 Jul 2026 00:00:00 GMT</pubDate>
            <description><![CDATA[Async compaction in Hudi is not a performance tweak layered onto ingestion — it is the natural consequence of a storage engine that separates correctness from optimization, a principle mature databases have followed for decades.]]></description>
            <content:encoded><![CDATA[<hr>
<p><em>This is the fourth post in a series on Merge-On-Read as an architectural shift in Apache Hudi. The <a class="" href="https://hudi.apache.org/blog/2026/05/14/mor-isnt-a-storage-optimization-its-an-architectural-shift">first post</a> made the broad case; the <a class="" href="https://hudi.apache.org/blog/2026/06/05/why-metadata-has-to-be-mutation-friendly">second</a> showed how the metadata table inherits the same append-first model; the <a class="" href="https://hudi.apache.org/blog/2026/06/25/building-indexes-on-a-moving-target">third</a> traced how async indexing extends the pattern to live index construction. This one follows the pattern one step further — into how storage optimization itself becomes an independent concern.</em></p>
<hr>
<div style="font-size:1.15rem;line-height:1.75"><p>Every storage system eventually accumulates technical debt. As data is continuously written, small files begin to appear, updates scatter across existing layouts, indexes drift away from the latest state, and storage gradually becomes less efficient for analytical queries. Left unattended, write performance deteriorates, read latency increases, and operational costs steadily rise. Storage optimization is therefore not an optional feature — it is an unavoidable consequence of maintaining a long-lived data system.</p><p>The question is <strong>not whether maintenance should happen, but when it should happen.</strong></p><p>Traditional database systems answered this question decades ago. They established a simple but profound principle: user transactions are responsible for correctness, while background maintenance is responsible for efficiency. Operations such as <a href="https://www.postgresql.org/docs/current/sql-vacuum.html" target="_blank" rel="noopener noreferrer" class="">VACUUM</a> in PostgreSQL, <a href="https://en.wikipedia.org/wiki/Log-structured_merge-tree" target="_blank" rel="noopener noreferrer" class="">compaction</a> in LSM-based databases, online index builds, and checkpointing continuously improve the storage engine without becoming part of the critical transaction path. Applications continue writing data while the storage engine quietly reorganizes itself in the background.</p><p>The first generation of lakehouse table formats emerged under a different set of assumptions. They were designed during a period when most analytical datasets were largely append-only, refreshed through scheduled batch pipelines, and stored as immutable Parquet files on object storage. Snapshot isolation and file replacement naturally became the dominant architectural primitives, providing a simple and robust foundation for managing analytical data at scale.</p><p>Today's workloads have moved on, and we'll return to what that shift looks like shortly. But the architectural question it raises is one modern databases answered decades ago:</p><p><strong>Should storage optimization compete with writes, or should it evolve independently?</strong></p><p>Apache Hudi's Merge-On-Read architecture approached this question differently from the beginning. Introduced in 2017, with asynchronous compaction following in 2019, it treated background maintenance not as an optimization layered onto the write path, but as a foundational architectural primitive. At the time, these capabilities were often viewed as implementation details that improved write performance. Looking back, they represented something much deeper: a different philosophy for organizing work inside a mutable storage engine.</p><hr><h2 class="anchor anchorTargetStickyNavbar_Vzrq" id="the-evolution-of-background-maintenance">The Evolution of Background Maintenance<a href="https://hudi.apache.org/blog/2026/07/17/async-compaction-background-maintenance#the-evolution-of-background-maintenance" class="hash-link" aria-label="Direct link to The Evolution of Background Maintenance" title="Direct link to The Evolution of Background Maintenance" translate="no">​</a></h2><p>Every long-running storage system eventually converges on the same realization: maintaining correctness is only half the problem. The other half is maintaining efficiency.</p><p>Every storage engine continuously makes local decisions. A newly inserted row is written wherever space is available. An updated record is appended to a write-optimized structure. An index is modified just enough to preserve correctness. Each individual operation is intentionally optimized for low latency.</p><p>Over time, however, thousands or millions of these local decisions accumulate. Data becomes fragmented. Small files proliferate. Obsolete versions remain on disk. Physical layouts drift away from what analytical queries would ideally prefer.</p><p>Left unattended, the storage engine gradually becomes less efficient — not because anything is incorrect, but because correctness alone does not guarantee an optimal physical layout.</p><p>This distinction gave rise to one of the most important architectural ideas in modern databases: <strong>background maintenance</strong>.</p><div style="text-align:center"><img src="https://hudi.apache.org/assets/images/blog/2026-07-17-async-compaction-background-maintenance/background-maintenance-across-storage-engines.png" alt="Background maintenance across storage engines" width="800"></div><p>This philosophy appears across nearly every mature storage engine, although the implementation differs.</p><p>PostgreSQL periodically runs <strong>VACUUM</strong> to reclaim obsolete row versions created by MVCC. LSM-tree based systems such as <a href="https://github.com/facebook/rocksdb/wiki/Universal-Compaction" target="_blank" rel="noopener noreferrer" class="">RocksDB</a>, <a href="https://github.com/google/leveldb/blob/main/doc/impl.md#compactions" target="_blank" rel="noopener noreferrer" class="">LevelDB</a>, Cassandra, and ScyllaDB continuously compact SSTables to reduce read amplification and reclaim space. SQL Server and Oracle rebuild indexes online, while checkpointing mechanisms asynchronously persist in-memory state without delaying foreground transactions.</p><p>Although these systems differ substantially in implementation, they all share a common architectural principle:</p><p><strong>Foreground operations establish correctness. Background operations restore efficiency.</strong></p><p>This separation is remarkably powerful.</p><p>A user inserting a row into PostgreSQL does not wait for VACUUM to complete. A write into RocksDB is not blocked until multiple SSTables have been compacted into a larger one. An online index rebuild does not require applications to stop modifying the underlying table.</p><p>Instead, user transactions continue making forward progress while the storage engine quietly reorganizes itself in the background.</p><p>This distinction is easy to overlook because modern databases have made it feel ordinary. Yet it represents one of the most important architectural lessons learned over decades of storage engine evolution: <strong>maintenance should improve the system without becoming part of the critical write path.</strong></p><p>An important consequence of this separation is that maintenance becomes schedulable. Storage optimization can run opportunistically when resources are available, be throttled during peak ingestion, prioritized for frequently accessed data, or even paused temporarily without affecting transactional correctness. Once maintenance is decoupled from writes, it becomes another independently managed service within the storage engine rather than a prerequisite for every mutation.</p><hr><h2 class="anchor anchorTargetStickyNavbar_Vzrq" id="two-architectural-philosophies">Two Architectural Philosophies<a href="https://hudi.apache.org/blog/2026/07/17/async-compaction-background-maintenance#two-architectural-philosophies" class="hash-link" aria-label="Direct link to Two Architectural Philosophies" title="Direct link to Two Architectural Philosophies" translate="no">​</a></h2><p>When transactional lakehouse formats began to emerge, they shared a common objective: bringing ACID guarantees to open data lakes.</p><p>The architectural paths they took, however, were not identical.</p><p>One school of thought naturally extended the strengths of immutable data lakes. Immutable files, snapshot isolation, and file replacement became the primary mechanisms for coordinating updates while preserving compatibility with cloud object storage. This approach provided a simple and elegant foundation for the analytical workloads of the time.</p><p>Apache Hudi approached the problem from a different perspective.</p><p>Rather than asking how immutable files could support transactions, it asked a different question:</p><p><strong>How can a data lake behave more like a database while retaining the openness of object storage?</strong></p><p>That seemingly small shift in perspective led to a different set of architectural primitives. Merge-On-Read established append-first mutation as the primary write path. Asynchronous compaction separated storage optimization from transactional correctness, allowing physical layout optimization to proceed independently of ingestion.</p><p>They embodied the same principle that had long existed in database storage engines:</p><p><strong>Correctness should be established immediately. Optimization should happen independently.</strong></p><hr><h2 class="anchor anchorTargetStickyNavbar_Vzrq" id="the-workloads-changed">The Workloads Changed<a href="https://hudi.apache.org/blog/2026/07/17/async-compaction-background-maintenance#the-workloads-changed" class="hash-link" aria-label="Direct link to The Workloads Changed" title="Direct link to The Workloads Changed" translate="no">​</a></h2><p>For many years, the dominant workloads in data lakes were largely append-oriented. Data was periodically ingested through scheduled ETL pipelines, analytical reports were refreshed in batches, and updates to existing records were relatively infrequent. Under these conditions, the primary challenge was bringing transactional guarantees to immutable data stored on object stores. The architectural primitives adopted by early lakehouse systems reflected these priorities.</p><p>Over the past decade, however, the role of the lakehouse has steadily expanded.</p><p>Instead of serving primarily as the final destination for analytical data, it has increasingly become the operational data platform itself. Change Data Capture (CDC) pipelines continuously synchronize operational databases into the lakehouse. Streaming applications produce an uninterrupted flow of events. Feature stores constantly update machine learning features. AI systems continuously generate embeddings, observations, and feedback signals that evolve throughout the lifecycle of a model. Rather than being refreshed periodically, datasets have become living systems that are expected to evolve continuously.</p><p>This shift fundamentally changes what the storage engine must optimize for.</p><p>The challenge is no longer simply storing immutable files efficiently. It is sustaining a continuous stream of mutations while simultaneously preserving the physical organization required for efficient analytics.</p><p>Those two objectives often pull the storage engine in opposite directions.</p><p>Applications want every update to become durable immediately. Analytical queries prefer well-organized, compacted, and clustered data layouts. Indexes need to reflect the latest state of the table. Metadata must remain synchronized. Statistics need to be refreshed. None of these activities can be ignored indefinitely, yet forcing them to occur as part of every write steadily increases ingestion latency and makes maintenance compete directly with foreground work.</p><p>This is precisely the challenge that background maintenance was designed to solve.</p><p>Rather than requiring every mutation to produce an optimal physical layout, a mutable storage engine can establish correctness first and improve efficiency later. The storage engine continues accepting writes while independent background services gradually reconcile the physical representation of the table.</p><p>This architectural principle is not unique to databases, nor is it unique to Apache Hudi. What is noteworthy is that Merge-On-Read adopted this philosophy several years before continuously mutating lakehouse workloads became commonplace — it treated storage optimization as work that could evolve independently from ingestion rather than as a prerequisite for every successful write.</p><p>At the time, this distinction was easy to overlook. Today, it is becoming one of the defining characteristics of modern mutable lakehouse architectures.</p><hr><h2 class="anchor anchorTargetStickyNavbar_Vzrq" id="separating-correctness-from-optimization">Separating Correctness from Optimization<a href="https://hudi.apache.org/blog/2026/07/17/async-compaction-background-maintenance#separating-correctness-from-optimization" class="hash-link" aria-label="Direct link to Separating Correctness from Optimization" title="Direct link to Separating Correctness from Optimization" translate="no">​</a></h2><p>One of the most important consequences of continuously mutating data is that correctness and optimization no longer need to happen at the same time.</p><p>For many years, storage engines largely treated these two objectives as inseparable. A successful write not only had to make new data visible, but also leave the physical representation of the table in a state suitable for future reads. As long as mutations were relatively infrequent, this coupling was a perfectly reasonable tradeoff. Every successful write incrementally moved the table toward its next optimized state.</p><p>As mutation frequency increases, however, this coupling becomes increasingly expensive.</p><p>Every write now carries two responsibilities. First, it must establish the correctness of the new state. Second, it must participate in maintaining an efficient physical layout for future reads. These responsibilities serve different goals, compete for the same resources, and often operate on the same physical data.</p><p>Modern database systems solved this tension decades ago by recognizing that these responsibilities need not be coupled. User transactions establish correctness immediately. Storage optimization becomes an independent concern that can be performed later, when resources permit, without affecting the visibility or correctness of committed data.</p><p>Apache Hudi's Merge-On-Read architecture adopts the same principle.</p><div style="text-align:center"><img src="https://hudi.apache.org/assets/images/blog/2026-07-17-async-compaction-background-maintenance/mor-separates-correctness-from-optimization.png" alt="Merge-On-Read separates correctness from optimization" width="800"></div><p>Instead of treating every write as both a mutation and a storage optimization event, Merge-On-Read separates them into two independent stages. Incoming updates are durably recorded first, making them immediately visible through the transactional timeline. Physical layout optimization is deferred to independent background services that reorganize storage without becoming part of the critical write path.</p><p>This distinction is subtle, but profound.</p><p>Merge-On-Read is often described as a storage format consisting of base files and log files. While technically accurate, that description misses the architectural motivation behind the design. Log files are not merely a different physical representation of data; they are the mechanism that allows correctness and optimization to evolve independently. They provide a place where new mutations can accumulate while background services gradually reconcile them into optimized storage layouts.</p><p>Once this separation exists, an entirely different storage architecture becomes possible.</p><p>Compaction is no longer required before new writes can proceed. Clustering can reorganize layouts independently of ingestion. Secondary indexes can evolve without delaying mutations. Each service becomes responsible for improving one aspect of the storage engine while leaving the critical write path focused on a single objective: making new data correct and visible as quickly as possible.</p><p>Background maintenance is therefore not a feature layered on top of Merge-On-Read. It is a direct consequence of the architectural separation between correctness and optimization.</p><p>This architectural distinction is more than an implementation detail. It fundamentally changes how work is organized inside the storage engine. Foreground writes focus exclusively on establishing new transactional state, while background services continuously improve the physical representation of the table without becoming part of the critical write path.</p><p>Architectural principles are ultimately judged not by their elegance, but by the behavior they produce under real workloads. Does separating correctness from optimization simply produce a cleaner design, or does it fundamentally change the behavior of a storage engine under continuous mutation?</p><p>That question naturally leads us from architectural principles to experimental validation.</p><hr><h2 class="anchor anchorTargetStickyNavbar_Vzrq" id="from-architectural-principle-to-experimental-validation">From Architectural Principle to Experimental Validation<a href="https://hudi.apache.org/blog/2026/07/17/async-compaction-background-maintenance#from-architectural-principle-to-experimental-validation" class="hash-link" aria-label="Direct link to From Architectural Principle to Experimental Validation" title="Direct link to From Architectural Principle to Experimental Validation" translate="no">​</a></h2><p>Answering that question requires a different kind of benchmark.</p><p>Traditional storage benchmarks typically compare write latency, read performance, or throughput. While those metrics are useful, they don't fully capture the architectural property we've been discussing. The more fundamental question is <strong>whether a storage engine can continue making steady forward progress while background maintenance executes concurrently.</strong></p><p>The benchmark is intentionally simple in concept. Foreground ingestion continuously mutates the table while background services optimize its physical layout. Rather than measuring the latency of an individual write, we observe whether ingestion continues progressing as maintenance runs in parallel.</p><div style="text-align:center"><img src="https://hudi.apache.org/assets/images/blog/2026-07-17-async-compaction-background-maintenance/benchmark-conceptual-setup.png" alt="Conceptual illustration of the experimental setup" width="800"></div><div style="text-align:center;font-style:italic;font-size:0.95rem;margin-top:-1rem;margin-bottom:1.5rem"><p>The outcomes the benchmark is designed to distinguish — from an engine that sustains continuous forward progress under concurrent maintenance, to one that regresses to intermittent stalls. Which real engines land where, and why, is the subject of the companion article.</p></div><p>The remainder of this series explores this question through a detailed benchmark of Apache Hudi, Apache Iceberg, and Delta Lake under identical continuous-ingestion workloads. Rather than focusing solely on throughput or latency, the study examines how different architectural choices influence a storage engine's ability to sustain forward progress while background maintenance is active.</p><p>We'll cover the benchmark design, workload, methodology, and analysis in the next article.</p><hr><h2 class="anchor anchorTargetStickyNavbar_Vzrq" id="background-maintenance-is-bigger-than-compaction">Background Maintenance Is Bigger Than Compaction<a href="https://hudi.apache.org/blog/2026/07/17/async-compaction-background-maintenance#background-maintenance-is-bigger-than-compaction" class="hash-link" aria-label="Direct link to Background Maintenance Is Bigger Than Compaction" title="Direct link to Background Maintenance Is Bigger Than Compaction" translate="no">​</a></h2><p>The benchmark will answer an important question about asynchronous compaction. But compaction itself is only one manifestation of a much broader architectural idea. Once a storage engine separates correctness from optimization, storage optimization itself becomes an independent concern rather than an extension of every write. The architecture no longer asks every mutation to leave the table in its ideal physical state. Instead, it allows specialized background services to continuously improve different aspects of the system over time.</p><p>Compaction is one such service.</p><p>Clustering is another, reorganizing data layouts to improve query locality without interrupting foreground ingestion. Metadata maintenance can evolve independently as new commits arrive rather than requiring expensive reconstruction during reads.</p><p>Seen through this lens, asynchronous compaction is not an isolated feature. It is one manifestation of a storage engine that has embraced background maintenance as a first-class primitive.</p><p>This observation also explains a common theme across the previous articles in this series.</p><p>The Merge-On-Read architecture is often introduced through individual capabilities: <a class="" href="https://hudi.apache.org/blog/2026/06/05/why-metadata-has-to-be-mutation-friendly">Metadata Table</a>, <a class="" href="https://hudi.apache.org/blog/2026/06/25/building-indexes-on-a-moving-target">asynchronous indexing</a>, compaction, or <a href="https://hudi.apache.org/docs/concurrency_control#non-blocking-concurrency-control" target="_blank" rel="noopener noreferrer" class="">Non-Blocking Concurrency Control</a>. While each solves a different problem, they all emerge from the same architectural philosophy.</p><p>The critical write path should establish correctness and little else.</p><p>Everything that improves the storage engine without affecting correctness should evolve independently whenever possible.</p><p>This is precisely the philosophy that mature database systems have followed for decades. As lakehouses increasingly become continuously mutating data platforms rather than repositories of mostly immutable analytical datasets, the same architectural principles are becoming increasingly relevant.</p><p>As the industry continues to move toward continuously evolving datasets, the question is no longer whether background maintenance belongs in a lakehouse. The question is how completely the storage engine embraces it.</p><hr><p><em><strong>Coming next in the series:</strong> a benchmark study comparing Apache Hudi, Apache Iceberg, and Delta Lake under identical continuous-ingestion workloads, focused on how each format sustains forward progress while background maintenance runs concurrently.</em></p><p><em>Series so far:</em></p><ol>
<li class=""><a class="" href="https://hudi.apache.org/blog/2026/05/14/mor-isnt-a-storage-optimization-its-an-architectural-shift">MOR Isn't a Storage Optimization. It's an Architectural Shift</a></li>
<li class=""><a class="" href="https://hudi.apache.org/blog/2026/06/05/why-metadata-has-to-be-mutation-friendly">Why Metadata Has to Be Mutation-Friendly</a></li>
<li class=""><a class="" href="https://hudi.apache.org/blog/2026/06/25/building-indexes-on-a-moving-target">Building Indexes on a Moving Target</a></li>
<li class=""><strong>Can a Lakehouse Really Run Maintenance Without Blocking Writes?</strong> (this post)</li>
</ol></div>]]></content:encoded>
            <category>mor</category>
            <category>merge on read</category>
            <category>compaction</category>
            <category>architecture</category>
            <category>data lakehouse</category>
            <category>metadata</category>
            <category>clustering</category>
        </item>
        <item>
            <title><![CDATA[What is Incremental ETL on a Data Lake?]]></title>
            <link>https://hudi.apache.org/blog/2026/07/16/what-is-incremental-etl-on-a-data-lake</link>
            <guid>https://hudi.apache.org/blog/2026/07/16/what-is-incremental-etl-on-a-data-lake</guid>
            <pubDate>Thu, 16 Jul 2026 00:00:00 GMT</pubDate>
            <description><![CDATA[Incremental ETL processes only changed data instead of recomputing entire tables. Learn how it works on a data lake and how Apache Hudi enables it.]]></description>
            <content:encoded><![CDATA[<p>Incremental ETL is a pipeline pattern that processes only the data that changed since the last run, instead of recomputing entire tables or partitions on every execution. Each run of an incremental pipeline consumes a delta — the records inserted, updated, or deleted after a known checkpoint — applies its transformation logic to just that delta, and merges the results into the target table. The pipeline's work is proportional to how much the data changed, not how large the tables have grown.</p>
<p>That distinction matters because most data pipelines today still run as full-recompute batch jobs: every night (or every hour), they re-scan source tables end to end and rewrite the derived tables downstream, even when only a tiny fraction of the rows actually changed. As data volumes grow, that recomputation gets slower and more expensive at exactly the moment businesses are asking for fresher data.</p>
<p>Incremental ETL occupies the middle ground between classic batch processing and full stream processing. It keeps the familiar economics and tooling of batch — scheduled jobs, SQL or DataFrame transformations, cheap <a class="" href="https://hudi.apache.org/blog/2024/07/11/what-is-a-data-lakehouse">data lakehouse</a> storage — while borrowing the core idea of stream processing: treat data as a continuous flow of changes, and process each change roughly once.</p>
<h2 class="anchor anchorTargetStickyNavbar_Vzrq" id="the-problem-with-full-recomputation">The Problem with Full Recomputation<a href="https://hudi.apache.org/blog/2026/07/16/what-is-incremental-etl-on-a-data-lake#the-problem-with-full-recomputation" class="hash-link" aria-label="Direct link to The Problem with Full Recomputation" title="Direct link to The Problem with Full Recomputation" translate="no">​</a></h2>
<p>Consider a typical medallion-style warehouse or lakehouse: raw data lands in bronze tables, gets cleaned into silver tables, and is aggregated into gold tables for reporting. In a full-recompute design, each layer is rebuilt by scanning its inputs wholesale. Three problems compound as the tables grow:</p>
<ul>
<li class="">
<p><strong>Cost.</strong> A daily job that scans a multi-terabyte fact table to pick up one day's worth of changes does orders of magnitude more I/O and compute than the size of the change justifies. Joins are the worst offenders: rewriting a large joined table means re-shuffling both sides in full, every run. Organizations routinely spend a large share of their data platform budget on pipelines that mostly reprocess data they already processed yesterday.</p>
</li>
<li class="">
<p><strong>Latency.</strong> If rebuilding a table takes four hours, the data in it can never be fresher than four hours plus the scheduling interval. Teams respond by running the job less often (making data staler) or throwing more compute at it (making it more expensive). Neither addresses the root cause: the work grows with table size rather than change size.</p>
</li>
<li class="">
<p><strong>Missed SLAs and cascading delays.</strong> Derived pipelines form DAGs. When an upstream full-recompute job runs long, every downstream job either starts late or reads stale inputs. Late-arriving data makes this worse: when records for an old partition show up, the whole partition — and every downstream partition derived from it — must be recomputed, silently blowing through freshness SLAs.</p>
</li>
</ul>
<p>Stream processing solves the latency problem but replaces batch's cost profile with a different one: always-on clusters, state stores to manage, and a second storage system (typically a log store like Kafka) holding another copy of the data. For the large class of pipelines where "fresh within a few minutes" is sufficient, that operational overhead buys latency headroom no one uses.</p>
<h2 class="anchor anchorTargetStickyNavbar_Vzrq" id="what-makes-etl-incremental">What Makes ETL "Incremental"<a href="https://hudi.apache.org/blog/2026/07/16/what-is-incremental-etl-on-a-data-lake#what-makes-etl-incremental" class="hash-link" aria-label="Direct link to What Makes ETL &quot;Incremental&quot;" title="Direct link to What Makes ETL &quot;Incremental&quot;" translate="no">​</a></h2>
<p>An ETL pipeline is incremental when both its input and its output are expressed as changes:</p>
<ol>
<li class="">
<p><strong>Change streams in.</strong> Instead of scanning the full source table, the pipeline asks the storage layer a question a plain file listing cannot answer: <em>"give me all records that changed since commit X."</em> The result is a delta — new inserts, the latest values of updated records, and deletes — along with a new checkpoint to resume from next run.</p>
</li>
<li class="">
<p><strong>Incremental writes out.</strong> Instead of rewriting the target table, the pipeline applies its transformed delta using record-level operations — upserts and deletes — so only the affected file groups are rewritten. Crucially, this makes the <em>output</em> table itself an incremental source: the next pipeline in the DAG can consume its changes the same way.</p>
</li>
</ol>
<p>Because deltas flow in and deltas flow out, the pattern composes. A chain of incremental pipelines behaves like a topology of stream processors — each stage consuming a change stream and emitting one — except the "streams" are ordinary tables on cheap cloud storage, queryable by any engine at any time. This is the <a class="" href="https://hudi.apache.org/blog/2020/08/18/hudi-incremental-processing-on-data-lakes">duality between change logs and tables</a> that stream processing literature has long described, realized directly on the data lake.</p>
<p>The efficiency win is easy to see with numbers. If a 10 TB fact table receives 50 GB of changes per hour, an hourly incremental pipeline processes 50 GB per run instead of 10 TB — a 200x reduction in data scanned, while <em>improving</em> freshness from daily to hourly.</p>
<h2 class="anchor anchorTargetStickyNavbar_Vzrq" id="what-the-storage-layer-must-provide">What the Storage Layer Must Provide<a href="https://hudi.apache.org/blog/2026/07/16/what-is-incremental-etl-on-a-data-lake#what-the-storage-layer-must-provide" class="hash-link" aria-label="Direct link to What the Storage Layer Must Provide" title="Direct link to What the Storage Layer Must Provide" translate="no">​</a></h2>
<p>Incremental ETL is not primarily a compute-engine feature; it is a storage contract. Plain files in cloud storage — even columnar formats like Parquet — cannot tell a consumer what changed, and cannot accept record-level updates without rewriting whole files or partitions. Open table formats close this gap to varying degrees. Concretely, the storage layer must provide three primitives:</p>
<ul>
<li class="">
<p><strong>Record-level change retrieval.</strong> The table must be able to answer "what changed between time T1 and T2" efficiently, without scanning everything. This comes in two flavors: <em>incremental queries</em>, which return the latest value of each changed record (ideal for driving downstream upserts), and <em>CDC queries</em>, which return before/after images of every change (needed when the transformation must see each individual mutation). Apache Hudi supports <a class="" href="https://hudi.apache.org/docs/sql_queries#incremental-query">both query types</a> on Copy-on-Write and Merge-on-Read tables.</p>
</li>
<li class="">
<p><strong>Upserts and deletes to apply changes.</strong> Producing a delta is only useful if the next table can absorb it cheaply. That requires primary keys and indexes to locate which files contain the affected records, so a small change touches a small amount of storage. Hudi's <a class="" href="https://hudi.apache.org/docs/write_operations">write operations</a> are built around indexed upserts and deletes, with merge modes to handle out-of-order and late-arriving data correctly.</p>
</li>
<li class="">
<p><strong>Ordering and commit points.</strong> Consumers need a reliable notion of "where I left off." Hudi's <a class="" href="https://hudi.apache.org/docs/timeline">timeline</a> records every action on the table as an atomically committed instant; incremental queries use commit completion times to define exact, repeatable change windows, and checkpoints are managed automatically by tools like Hudi Streamer's incremental sources. Hudi additionally tracks record-level metadata (commit time and sequence number on every record), so change history survives background operations like compaction and clustering rather than depending on which snapshots happen to be retained.</p>
</li>
</ul>
<p>These primitives — upsert and incremental consumption — are exactly the pair that defines the pattern: with them, you can maintain one table from a change stream, then derive another table from <em>its</em> change stream, end to end. The <a class="" href="https://hudi.apache.org/docs/use_cases">use cases documentation</a> covers how this plays out across ingestion, warehouse offload, and derived pipelines.</p>
<h3 class="anchor anchorTargetStickyNavbar_Vzrq" id="frequent-table-maintenance-becomes-the-norm">Frequent Table Maintenance Becomes the Norm<a href="https://hudi.apache.org/blog/2026/07/16/what-is-incremental-etl-on-a-data-lake#frequent-table-maintenance-becomes-the-norm" class="hash-link" aria-label="Direct link to Frequent Table Maintenance Becomes the Norm" title="Direct link to Frequent Table Maintenance Becomes the Norm" translate="no">​</a></h3>
<p>There is a fourth requirement that follows directly from the first three: the format must be designed for frequent table maintenance, because incremental processing fundamentally increases how often the table is written. A table that once absorbed one nightly batch now commits every few minutes — producing small files, accumulating deltas in log files, and continuously churning the storage layout. At that write frequency, maintenance work — <a class="" href="https://hudi.apache.org/docs/compaction">compaction</a>, clustering, cleaning, file sizing — stops being an occasional chore and becomes the norm, running constantly alongside ingestion.</p>
<p>And it must run <em>without blocking the writers it serves</em>. If compacting or clustering a table means pausing ingestion, or if maintenance jobs contend with writers as just another optimistic transaction — conflicting and forcing retries on exactly the hot file groups that change most — then every maintenance pass reintroduces the latency and cost the pipeline went incremental to eliminate. Hudi builds table services into the storage engine for this reason: compaction, clustering, cleaning, and indexing all ship built-in and run <a class="" href="https://hudi.apache.org/docs/compaction">asynchronously</a> — in the same process as the writer or as separate jobs — coordinated through MVCC and <a class="" href="https://hudi.apache.org/docs/concurrency_control">non-blocking concurrency control</a> so writers keep committing while maintenance proceeds in the background.</p>
<h2 class="anchor anchorTargetStickyNavbar_Vzrq" id="incremental-etl-vs-stream-processing">Incremental ETL vs. Stream Processing<a href="https://hudi.apache.org/blog/2026/07/16/what-is-incremental-etl-on-a-data-lake#incremental-etl-vs-stream-processing" class="hash-link" aria-label="Direct link to Incremental ETL vs. Stream Processing" title="Direct link to Incremental ETL vs. Stream Processing" translate="no">​</a></h2>
<p>Incremental ETL and stream processing share semantics but differ sharply in infrastructure and economics:</p>
<table><thead><tr><th></th><th>Stream processing (Flink, Kafka Streams)</th><th>Incremental ETL (Hudi on a data lake)</th></tr></thead><tbody><tr><td>Latency</td><td>Sub-second to seconds</td><td>Minutes</td></tr><tr><td>Compute model</td><td>Always-on jobs with managed state</td><td>Scheduled or continuous mini-batch jobs; state lives in tables</td></tr><tr><td>Storage</td><td>Event log (Kafka) + state stores + eventual sink</td><td>One copy: lakehouse tables on cloud object storage</td></tr><tr><td>Reprocessing/backfill</td><td>Replay the log, rebuild state</td><td>Re-run the job over a commit range or full table</td></tr><tr><td>Query access to intermediate results</td><td>Requires sinking to another system</td><td>Every intermediate table is directly queryable</td></tr></tbody></table>
<p>A useful way to summarize the trade: <strong>incremental ETL delivers streaming semantics at minute-level batch economics.</strong> If a use case genuinely needs sub-second reactions — fraud blocking, operational alerting — a stream processor is the right tool. But a large majority of analytics pipelines have freshness requirements in the minutes-to-hour range, where paying for always-on stream infrastructure and a second copy of the data buys nothing. The two also compose rather than compete: Flink writing into Hudi tables gives low-latency ingestion upstream, with cheaper incremental pipelines fanning out downstream.</p>
<h2 class="anchor anchorTargetStickyNavbar_Vzrq" id="a-concrete-pipeline-walkthrough">A Concrete Pipeline Walkthrough<a href="https://hudi.apache.org/blog/2026/07/16/what-is-incremental-etl-on-a-data-lake#a-concrete-pipeline-walkthrough" class="hash-link" aria-label="Direct link to A Concrete Pipeline Walkthrough" title="Direct link to A Concrete Pipeline Walkthrough" translate="no">​</a></h2>
<p>Here is the pattern end to end, using a familiar CDC scenario: an operational database (say, PostgreSQL) whose changes must feed an analytics table.</p>
<p><strong>Step 1 — CDC into a bronze table.</strong> A capture tool such as Debezium streams the database's change log into Kafka, and an ingestion job — for example <a class="" href="https://hudi.apache.org/docs/hoodie_streaming_ingestion">Hudi Streamer</a> — continuously upserts those changes into a bronze Hudi table. The bronze table is now a queryable, transactional mirror of the source, and every commit on its timeline is a consumable change point.</p>
<p><strong>Step 2 — incrementally read the bronze table.</strong> The silver pipeline wakes up on its schedule (or runs continuously) and pulls only what changed since its last checkpoint:</p>
<div class="language-scala codeBlockContainer_Ckt0 theme-code-block" style="--prism-color:#F8F8F2;--prism-background-color:#282A36"><div class="codeBlockContent_QJqH"><pre tabindex="0" class="prism-code language-scala codeBlock_bY9V thin-scrollbar" style="color:#F8F8F2;background-color:#282A36"><code class="codeBlockLines_e6Vv"><span class="token-line" style="color:#F8F8F2"><span class="token plain">// Read only the records that changed since the last run</span><br></span><span class="token-line" style="color:#F8F8F2"><span class="token plain">val ordersDelta = spark.read.format("hudi").</span><br></span><span class="token-line" style="color:#F8F8F2"><span class="token plain">  option("hoodie.datasource.query.type", "incremental").</span><br></span><span class="token-line" style="color:#F8F8F2"><span class="token plain">  option("hoodie.datasource.read.begin.instanttime", lastCheckpoint).</span><br></span><span class="token-line" style="color:#F8F8F2"><span class="token plain">  load(bronzeOrdersPath)</span><br></span></code></pre></div></div>
<p>The same read is available in SQL through the <code>hudi_table_changes</code> table-valued function:</p>
<div class="language-sql codeBlockContainer_Ckt0 theme-code-block" style="--prism-color:#F8F8F2;--prism-background-color:#282A36"><div class="codeBlockContent_QJqH"><pre tabindex="0" class="prism-code language-sql codeBlock_bY9V thin-scrollbar" style="color:#F8F8F2;background-color:#282A36"><code class="codeBlockLines_e6Vv"><span class="token-line" style="color:#F8F8F2"><span class="token keyword" style="color:rgb(189, 147, 249);font-style:italic">SELECT</span><span class="token plain"> </span><span class="token operator">*</span><span class="token plain"> </span><span class="token keyword" style="color:rgb(189, 147, 249);font-style:italic">FROM</span><span class="token plain"> hudi_table_changes</span><span class="token punctuation" style="color:rgb(248, 248, 242)">(</span><span class="token string" style="color:rgb(255, 121, 198)">'bronze_orders'</span><span class="token punctuation" style="color:rgb(248, 248, 242)">,</span><span class="token plain"> </span><span class="token string" style="color:rgb(255, 121, 198)">'latest_state'</span><span class="token punctuation" style="color:rgb(248, 248, 242)">,</span><span class="token plain"> </span><span class="token string" style="color:rgb(255, 121, 198)">'20260716083000000'</span><span class="token punctuation" style="color:rgb(248, 248, 242)">)</span><span class="token punctuation" style="color:rgb(248, 248, 242)">;</span><br></span></code></pre></div></div>
<p><strong>Step 3 — transform and upsert into a silver table.</strong> The transformation runs over just the delta, and the result is applied as an upsert keyed on the record key:</p>
<div class="language-scala codeBlockContainer_Ckt0 theme-code-block" style="--prism-color:#F8F8F2;--prism-background-color:#282A36"><div class="codeBlockContent_QJqH"><pre tabindex="0" class="prism-code language-scala codeBlock_bY9V thin-scrollbar" style="color:#F8F8F2;background-color:#282A36"><code class="codeBlockLines_e6Vv"><span class="token-line" style="color:#F8F8F2"><span class="token plain">val silverDelta = ordersDelta.</span><br></span><span class="token-line" style="color:#F8F8F2"><span class="token plain">  filter(col("order_status") =!= "CANCELLED").</span><br></span><span class="token-line" style="color:#F8F8F2"><span class="token plain">  withColumn("order_total", col("quantity") * col("unit_price"))</span><br></span><span class="token-line" style="color:#F8F8F2"><span class="token plain" style="display:inline-block"></span><br></span><span class="token-line" style="color:#F8F8F2"><span class="token plain">silverDelta.write.format("hudi").</span><br></span><span class="token-line" style="color:#F8F8F2"><span class="token plain">  option("hoodie.datasource.write.operation", "upsert").</span><br></span><span class="token-line" style="color:#F8F8F2"><span class="token plain">  option("hoodie.table.name", "silver_orders").</span><br></span><span class="token-line" style="color:#F8F8F2"><span class="token plain">  mode("append").</span><br></span><span class="token-line" style="color:#F8F8F2"><span class="token plain">  save(silverOrdersPath)</span><br></span></code></pre></div></div>
<p>The job then persists the new checkpoint (the latest commit completion time it consumed) for the next run. Because the silver table was written with upserts, it exposes its own change stream — a gold-layer aggregation job can consume <code>silver_orders</code> incrementally in exactly the same way, re-aggregating only the keys whose inputs changed.</p>
<p><img decoding="async" loading="lazy" alt="Incremental ETL: upserts into an upstream table, incrementally consumed to maintain a derived projected table" src="https://hudi.apache.org/assets/images/image7-0a4cfd0fa02ba7efc07901ae75d1c188.png" width="1200" height="300" class="img_ev3q">
</p><p align="center">Figure: Changes are upserted into an upstream table and incrementally consumed to maintain a derived table</p><p></p>
<p>One honest caveat: not every transformation is trivially incremental. Simple projections, filters, and enrichments map one-to-one onto deltas. Aggregations require merging new partial results into existing ones, and some full-table operations (percentiles, complex window functions) may still warrant periodic full recomputation. A pragmatic design runs the same logic incrementally for freshness and occasionally in full for correction — on the same tables and infrastructure.</p>
<h2 class="anchor anchorTargetStickyNavbar_Vzrq" id="a-brief-history-pioneered-at-uber">A Brief History: Pioneered at Uber<a href="https://hudi.apache.org/blog/2026/07/16/what-is-incremental-etl-on-a-data-lake#a-brief-history-pioneered-at-uber" class="hash-link" aria-label="Direct link to A Brief History: Pioneered at Uber" title="Direct link to A Brief History: Pioneered at Uber" translate="no">​</a></h2>
<p>Incremental processing on the data lake is not a recent idea bolted onto table formats — it is the problem Hudi was created to solve. In 2016, Uber's data team faced exactly the trade-off described above: petabyte-scale Hadoop tables that could only be rewritten wholesale, and business use cases (like driver-partner incentives and trip pricing) that needed data in minutes, not hours. The case for a third processing style — incremental, on top of batch storage — was laid out in the O'Reilly article <a class="" href="https://hudi.apache.org/blog/2016/08/04/The-Case-for-incremental-processing-on-Hadoop">The Case for Incremental Processing on Hadoop</a>, and Hudi (originally "Hoodie" — Hadoop Upserts, Deletes and Incrementals) was built at Uber to provide the two missing primitives: upserts and incremental consumption. The project entered the Apache Incubator in 2019 and became a top-level Apache project in 2020; a deeper treatment of the ideas appears in <a class="" href="https://hudi.apache.org/blog/2020/08/18/hudi-incremental-processing-on-data-lakes">Incremental Processing on the Data Lake</a>. The name itself encodes the pattern — the "I" in Hudi stands for incrementals.</p>
<h2 class="anchor anchorTargetStickyNavbar_Vzrq" id="conclusion">Conclusion<a href="https://hudi.apache.org/blog/2026/07/16/what-is-incremental-etl-on-a-data-lake#conclusion" class="hash-link" aria-label="Direct link to Conclusion" title="Direct link to Conclusion" translate="no">​</a></h2>
<p>Incremental ETL replaces "recompute everything, always" with "process what changed, continuously." It requires a storage layer that can serve record-level change streams, absorb upserts and deletes efficiently, and expose reliable commit points to checkpoint against — the primitives Apache Hudi has provided on data lakes since its inception. The payoff is pipelines whose cost tracks the rate of change in the business rather than the accumulated size of history, with data freshness measured in minutes on the same cheap, open storage batch jobs already use. For most analytics workloads sitting between nightly batch and hard real-time, it is the most economical point on the latency-cost curve.</p>
<h2 class="anchor anchorTargetStickyNavbar_Vzrq" id="faq">FAQ<a href="https://hudi.apache.org/blog/2026/07/16/what-is-incremental-etl-on-a-data-lake#faq" class="hash-link" aria-label="Direct link to FAQ" title="Direct link to FAQ" translate="no">​</a></h2>
<section><details style="margin-bottom:0.75rem"><summary><strong>What is incremental ETL?</strong></summary><p style="margin-top:0.5rem">Incremental ETL is a data pipeline pattern that processes only the records that were inserted, updated, or deleted since the pipeline's last run, instead of re-scanning and rewriting entire tables. Each run consumes a delta from a checkpoint, transforms it, and merges the result into the target table with upserts and deletes. This makes pipeline cost proportional to the volume of change rather than total table size.</p></details><details style="margin-bottom:0.75rem"><summary><strong>What is the difference between incremental ETL and streaming?</strong></summary><p style="margin-top:0.5rem">Both process data as a flow of changes, but stream processors like Apache Flink run always-on jobs with managed state and typically require a separate event log such as Kafka, delivering sub-second latency. Incremental ETL runs as scheduled or continuous mini-batch jobs directly against lakehouse tables on cloud storage, delivering minute-level freshness at much lower infrastructure cost. A common summary is that incremental ETL provides streaming semantics at batch economics.</p></details><details style="margin-bottom:0.75rem"><summary><strong>How does incremental ETL reduce cost?</strong></summary><p style="margin-top:0.5rem">It eliminates repeated scans and rewrites of data that did not change. If a large table receives a small volume of changes between runs, an incremental pipeline reads and processes only that delta instead of the whole table, often reducing data scanned per run by orders of magnitude. Compute clusters also only run for the duration of each small job rather than staying on permanently.</p></details><details style="margin-bottom:0.75rem"><summary><strong>What does the storage layer need to support incremental ETL?</strong></summary><p style="margin-top:0.5rem">Four things: the ability to efficiently retrieve record-level changes between two points in time, support for upserts and deletes so downstream tables can absorb deltas cheaply, an ordered log of commits that consumers can checkpoint against, and table maintenance that runs without blocking writers — because incremental processing multiplies write frequency, compaction, clustering, and cleaning become continuous background work rather than occasional jobs. Plain Parquet files on object storage provide none of these, which is why incremental ETL depends on a transactional table format. Apache Hudi provides all four through its timeline, indexes, incremental and CDC queries, and built-in asynchronous table services.</p></details><details style="margin-bottom:0.75rem"><summary><strong>Can every transformation be made incremental?</strong></summary><p style="margin-top:0.5rem">No. Projections, filters, enrichments, and key-based joins map naturally onto change streams, and aggregations can be maintained by merging partial results for the affected keys. Some computations, like exact percentiles or complex full-table window functions, are hard to update incrementally, so teams often run those periodically in full while keeping the rest of the pipeline incremental.</p></details><details style="margin-bottom:0.75rem"><summary><strong>Which Apache Hudi features enable incremental ETL?</strong></summary><p style="margin-top:0.5rem">Hudi provides incremental queries that return the latest values of changed records after a given commit, CDC queries that return before and after images of each change, and indexed upsert and delete write operations to apply deltas downstream. Its timeline records every commit with completion times that define exact change windows, and record-level metadata preserves change history across compaction and clustering. Ingestion tools like Hudi Streamer manage incremental checkpoints automatically.</p></details></section>]]></content:encoded>
            <category>incremental processing</category>
            <category>etl</category>
            <category>streaming</category>
            <category>data lakehouse</category>
            <category>cdc</category>
        </item>
        <item>
            <title><![CDATA[What is Upsert on a Data Lake?]]></title>
            <link>https://hudi.apache.org/blog/2026/07/15/what-is-upsert-on-a-data-lake</link>
            <guid>https://hudi.apache.org/blog/2026/07/15/what-is-upsert-on-a-data-lake</guid>
            <pubDate>Wed, 15 Jul 2026 00:00:00 GMT</pubDate>
            <description><![CDATA[What an upsert is, why record updates were historically hard on data lakes, and how table formats like Apache Hudi implement fast, indexed upserts.]]></description>
            <content:encoded><![CDATA[<p>An upsert is a write operation that updates a record if it already exists and inserts it if it does not — a single atomic operation that combines <em>update</em> and <em>insert</em>, keyed on a unique record identifier. Databases have offered this for decades (<code>MERGE</code> in SQL, <code>ON CONFLICT DO UPDATE</code> in PostgreSQL, <code>replace into</code> in MySQL). On a data lake, however, upserts were effectively impossible for most of the technology's history, and making them fast is still what separates data lake storage systems from one another today. This post explains why data lakes couldn't update records, how open table formats changed that, what actually happens inside an upsert, and why indexing determines whether an upsert takes minutes or hours.</p>
<h2 class="anchor anchorTargetStickyNavbar_Vzrq" id="why-data-lakes-couldnt-update-records">Why Data Lakes Couldn't Update Records<a href="https://hudi.apache.org/blog/2026/07/15/what-is-upsert-on-a-data-lake#why-data-lakes-couldnt-update-records" class="hash-link" aria-label="Direct link to Why Data Lakes Couldn't Update Records" title="Direct link to Why Data Lakes Couldn't Update Records" translate="no">​</a></h2>
<p>Data lakes are built on files — typically columnar formats like Apache Parquet or ORC — sitting on distributed file systems or cloud object storage such as Amazon S3. Two properties of this foundation make record-level updates hard.</p>
<p>First, the files themselves are immutable. A Parquet file is written once, with column chunks, compression, and statistics computed over the whole file; there is no way to reach into it and change one row in place. Second, the storage layer underneath offers no transactional semantics across files. Early cloud object stores were even only eventually consistent, so a reader listing a directory mid-write could see a partial, corrupt view of the table. There were no primary keys, no notion of "this record already exists," and no atomic way to swap old data for new.</p>
<p>The Hive-era workaround was coarse-grained rewriting: partition the table (say, by day), and when any record in a partition changed, recompute and overwrite the <em>entire partition</em>. Pipelines re-read hours or days of data to apply what might be a handful of changed rows, then swapped directories and hoped no query was reading at that moment. This pattern wasted enormous compute, delayed data freshness from minutes to hours or days, and provided no isolation guarantees. It is precisely the batch-overwrite model that incremental write operations were <a class="" href="https://hudi.apache.org/docs/write_operations">designed to replace</a>.</p>
<h2 class="anchor anchorTargetStickyNavbar_Vzrq" id="how-open-table-formats-make-upserts-possible">How Open Table Formats Make Upserts Possible<a href="https://hudi.apache.org/blog/2026/07/15/what-is-upsert-on-a-data-lake#how-open-table-formats-make-upserts-possible" class="hash-link" aria-label="Direct link to How Open Table Formats Make Upserts Possible" title="Direct link to How Open Table Formats Make Upserts Possible" translate="no">​</a></h2>
<p>Open table formats — <a href="https://hudi.apache.org/" target="_blank" rel="noopener noreferrer" class="">Apache Hudi</a>, Apache Iceberg, and Delta Lake — introduced a transactional metadata layer on top of immutable files, turning a directory of Parquet files into a proper table. That metadata layer is what makes upserts possible, through a few key mechanisms (covered more broadly in <a class="" href="https://hudi.apache.org/blog/2024/07/11/what-is-a-data-lakehouse">What is a Data Lakehouse?</a>):</p>
<ul>
<li class=""><strong>Atomic commits.</strong> Every write produces new file versions, and a commit protocol atomically publishes them. In Hudi, each write is an action on a <a class="" href="https://hudi.apache.org/docs/timeline">timeline</a>; queries only ever see fully committed data, so a failed or in-flight write is invisible to readers.</li>
<li class=""><strong>Snapshot isolation and versioning.</strong> Because old file versions are retained until cleaned, readers see a consistent snapshot while writers produce the next one. Updating a record no longer risks corrupting a running query.</li>
<li class=""><strong>Record keys.</strong> Hudi brings the database notion of a primary key to the lake: every record has a key (record key, plus optionally a partition path) that uniquely identifies it. This is the precondition for "update if exists" to even be well defined.</li>
<li class=""><strong>Fine-grained file management.</strong> Instead of rewriting a whole partition, the system can identify exactly which files contain the affected records and rewrite — or log changes against — only those.</li>
</ul>
<p>With these pieces in place, an upsert on a data lake becomes what it is in a database: hand the system a batch of records, and it transactionally reconciles them against what is already stored. In Hudi, <code>upsert</code> is the <a class="" href="https://hudi.apache.org/docs/write_operations#upsert">default write operation</a>: incoming records are tagged as inserts or updates via an index lookup, sized into files, written, and committed atomically. The target table never shows duplicates.</p>
<h2 class="anchor anchorTargetStickyNavbar_Vzrq" id="the-anatomy-of-an-upsert">The Anatomy of an Upsert<a href="https://hudi.apache.org/blog/2026/07/15/what-is-upsert-on-a-data-lake#the-anatomy-of-an-upsert" class="hash-link" aria-label="Direct link to The Anatomy of an Upsert" title="Direct link to The Anatomy of an Upsert" translate="no">​</a></h2>
<p>Understanding the cost of an upsert requires walking through what actually happens. Hudi's <a class="" href="https://hudi.apache.org/docs/write_operations#write-path">write path</a> breaks it into distinct steps.</p>
<p><strong>1. Key definition and deduplication.</strong> Each incoming record carries a record key. Since an input batch may itself contain multiple versions of the same key (common with change streams), records are first combined by key, keeping the winner according to configured ordering fields.</p>
<p><strong>2. Index lookup.</strong> The system must answer: <em>does this key already exist, and if so, in which file?</em> Hudi maintains a mapping from each record key to a <em>file group</em> — the unit of storage that holds all versions of a set of records. The <a class="" href="https://hudi.apache.org/docs/indexes">index lookup</a> tags each incoming record as an update (routed to its existing file group) or an insert (assigned to a new or under-sized file group). This step is the heart of the upsert, and — as the next section shows — the main determinant of its performance.</p>
<p><strong>3. Merge and write.</strong> Tagged records are then written, and here the <a class="" href="https://hudi.apache.org/docs/table_types">table type</a> determines the strategy:</p>
<ul>
<li class=""><strong>Copy-on-Write (CoW)</strong> rewrites the affected base files: the writer reads the current base file of each touched file group, merges in the updates, and writes a new versioned base file. Queries stay simple and fast — they read pure columnar data with zero merge overhead — but write amplification is high: updating even a few records in a file means rewriting the whole file. CoW suits read-heavy tables with moderate update rates.</li>
<li class=""><strong>Merge-on-Read (MoR)</strong> instead appends the changed records to compact delta log files attached to each file group, deferring the expensive base-file rewrite. Writes become lightweight and fast, enabling commits every few minutes; queries merge the logs with base files at read time, and a background <a class="" href="https://hudi.apache.org/docs/compaction">compaction</a> process periodically folds logs into new base files to restore pure-columnar read performance. MoR suits high-frequency updates and streaming ingestion, trading some query-time merge cost for much lower write latency and amplification.</li>
</ul>
<p>The trade-off is summarized well by the <a class="" href="https://hudi.apache.org/docs/table_types#comparison">comparison table</a> in the Hudi docs: CoW pays <code>O(file groups written)</code> in write amplification for zero read amplification; MoR pays <code>O(records changed)</code> on both sides.</p>
<p><strong>4. Commit.</strong> Finally, the write — new base files, new log blocks, and index updates — is committed atomically to the timeline, at which point queries can see it.</p>
<h2 class="anchor anchorTargetStickyNavbar_Vzrq" id="why-indexing-is-the-difference-between-minutes-and-hours">Why Indexing Is the Difference Between Minutes and Hours<a href="https://hudi.apache.org/blog/2026/07/15/what-is-upsert-on-a-data-lake#why-indexing-is-the-difference-between-minutes-and-hours" class="hash-link" aria-label="Direct link to Why Indexing Is the Difference Between Minutes and Hours" title="Direct link to Why Indexing Is the Difference Between Minutes and Hours" translate="no">​</a></h2>
<p>Step 2 above deserves its own section, because it is where naive upsert implementations fall down. Without an index, the only way to find which files contain the incoming keys is to <em>join the input batch against the entire table</em> — reading key columns from every file, every time you write. For a large table receiving a small trickle of updates, this means the cost of each write is proportional to the size of the table, not the size of the change.</p>
<p><img decoding="async" loading="lazy" alt="Comparison of merge cost for updates against base files, with and without an index" src="https://hudi.apache.org/assets/images/with_without_index-c0808363df23ac1aba63bc81a68b6c8c.png" width="3994" height="1704" class="img_ev3q">
</p><p align="center">Figure: Merge cost for updates (dark blue) against base files (light blue), with and without an index</p><p></p>
<p>Hudi treats <a class="" href="https://hudi.apache.org/docs/indexes">indexing</a> as an integral part of its storage engine and offers several index types tuned to different workloads:</p>
<ul>
<li class=""><strong>Bloom index</strong>: stores bloom filters and key ranges (in file footers, or centrally in the metadata table) so most files can be pruned without reading data. Works very well when keys have some ordering — e.g., event tables with timestamp-prefixed keys — where range pruning eliminates most candidate files.</li>
<li class=""><strong>Simple index</strong>: a lean join against keys extracted from existing files; a better fit for random-update workloads like dimension tables, where bloom filters would return true for nearly every file anyway.</li>
<li class=""><strong>Bucket index</strong>: hashes keys directly to file groups, eliminating the lookup entirely at the cost of a fixed (or consistent-hashed) bucket layout.</li>
<li class=""><strong><a class="" href="https://hudi.apache.org/blog/2023/11/01/record-level-index">Record Level Index</a></strong>: a scalable, exact key-to-file-group mapping stored in Hudi's metadata table, sharded by hash. Instead of pruning candidate files probabilistically, it answers the location question with a point lookup, delivering order-of-magnitude speedups on large deployments where index lookup dominates write latency.</li>
</ul>
<p>The practical impact is dramatic. In a table with tens of thousands of files, an untagged upsert must consider all of them; a well-chosen index narrows the work to only the file groups that actually contain updated records. Merge-on-Read benefits doubly: the index bounds how many change records any base file must be merged against at query time, not just at write time. For concrete tuning guidance, see <a class="" href="https://hudi.apache.org/blog/2023/05/10/top-3-things-you-can-do-to-get-fast-upsert-performance-in-apache-hudi">Top 3 Things You Can Do to Get Fast Upsert Performance in Apache Hudi</a>.</p>
<h2 class="anchor anchorTargetStickyNavbar_Vzrq" id="what-upserts-unlock">What Upserts Unlock<a href="https://hudi.apache.org/blog/2026/07/15/what-is-upsert-on-a-data-lake#what-upserts-unlock" class="hash-link" aria-label="Direct link to What Upserts Unlock" title="Direct link to What Upserts Unlock" translate="no">​</a></h2>
<p>Fast record-level mutation is not a niche feature; it changes what a data lake can be used for.</p>
<ul>
<li class=""><strong>Change data capture (CDC) replication.</strong> Streaming a database's change log (via Debezium, AWS DMS, or similar) into the lake requires applying inserts, updates, and deletes in order, continuously. Upserts make it possible to maintain a query-ready mirror of an operational database with minute-level freshness, instead of nightly full dumps.</li>
<li class=""><strong>Compliance deletes (GDPR/CCPA).</strong> "Delete this user's data" is a targeted mutation across potentially every partition of a table. With keyed <a class="" href="https://hudi.apache.org/docs/write_operations#delete">deletes</a> — implemented on the same indexed path as upserts — this becomes a routine operation rather than a table-rewriting project.</li>
<li class=""><strong>Deduplication.</strong> Event pipelines deliver duplicates; upserting by event key means the table enforces uniqueness on write, so consumers never need to deduplicate at query time.</li>
<li class=""><strong>Mutable fact and dimension tables.</strong> Late-arriving corrections, order status changes, transaction settlements, slowly changing dimensions — all become incremental updates against the affected records, rather than periodic partition rewrites.</li>
<li class=""><strong>Incremental pipelines.</strong> Because upserts record exactly what changed and when, downstream jobs can consume just the changes via <a class="" href="https://hudi.apache.org/docs/table_types#query-types">incremental queries</a>, replacing full-table rescans with streaming-style processing.</li>
</ul>
<h2 class="anchor anchorTargetStickyNavbar_Vzrq" id="how-hudi-iceberg-and-delta-lake-handle-mutation">How Hudi, Iceberg, and Delta Lake Handle Mutation<a href="https://hudi.apache.org/blog/2026/07/15/what-is-upsert-on-a-data-lake#how-hudi-iceberg-and-delta-lake-handle-mutation" class="hash-link" aria-label="Direct link to How Hudi, Iceberg, and Delta Lake Handle Mutation" title="Direct link to How Hudi, Iceberg, and Delta Lake Handle Mutation" translate="no">​</a></h2>
<p>All three major open table formats support upserts today, but with different designs and different cost profiles. Delta Lake and Iceberg expose mutation primarily through <code>MERGE INTO</code>: the engine joins the source batch against the target table to find affected files, then either rewrites them (copy-on-write) or, in newer versions, writes delete files/deletion vectors that mark rows as removed (merge-on-read style reads). Neither maintains a persistent record-level index, so locating affected files generally relies on the join plus file-level statistics pruning. Hudi was built mutation-first with primary keys, <a class="" href="https://hudi.apache.org/docs/indexes">pluggable indexes</a>, and both <a class="" href="https://hudi.apache.org/docs/table_types">CoW and MoR</a> writer paths as core design elements, plus managed table services (compaction, clustering, cleaning) to keep continuously mutated tables performant. Which design performs better depends on the workload: for occasional, large batch merges the approaches converge, while for frequent, targeted updates against large tables, an indexed write path avoids repeatedly scanning table state to find what changed. Evaluate against your own update rate, latency requirements, and table sizes.</p>
<h2 class="anchor anchorTargetStickyNavbar_Vzrq" id="conclusion">Conclusion<a href="https://hudi.apache.org/blog/2026/07/15/what-is-upsert-on-a-data-lake#conclusion" class="hash-link" aria-label="Direct link to Conclusion" title="Direct link to Conclusion" translate="no">​</a></h2>
<p>An upsert — update if the record exists, insert if it does not — is the operation that turns a data lake from an append-only archive into a live, continuously updated foundation for analytics. It was historically out of reach because lakes stored immutable files with no transactions, keys, or record-level metadata, forcing wasteful partition rewrites. Open table formats supplied the transactional layer; what determines real-world performance is everything layered on top: how records are keyed, how quickly the system can locate existing records (indexing), and how the merge is physically executed (Copy-on-Write versus Merge-on-Read). Apache Hudi's answer — indexed, key-based writes with a choice of merge strategies and automated table services — is documented in depth in the <a class="" href="https://hudi.apache.org/docs/write_operations">write operations</a>, <a class="" href="https://hudi.apache.org/docs/indexes">indexes</a>, and <a class="" href="https://hudi.apache.org/docs/table_types">table types</a> pages, and is a good starting point for anyone building update-heavy lakehouse workloads.</p>
<h2 class="anchor anchorTargetStickyNavbar_Vzrq" id="faq">FAQ<a href="https://hudi.apache.org/blog/2026/07/15/what-is-upsert-on-a-data-lake#faq" class="hash-link" aria-label="Direct link to FAQ" title="Direct link to FAQ" translate="no">​</a></h2>
<section><details style="margin-bottom:0.75rem"><summary><strong>What is the difference between upsert and merge?</strong></summary><p style="margin-top:0.5rem">They achieve similar outcomes through different interfaces. MERGE is a SQL statement where you write an explicit join condition and specify what happens on match or no match, giving full control including conditional logic. Upsert is a keyed write operation: the system uses the table's record key to decide automatically whether each incoming record is an update or an insert. In Apache Hudi, upsert is the default write operation and uses an index to locate existing records, while SQL MERGE INTO is also supported on top of the same machinery.</p></details><details style="margin-bottom:0.75rem"><summary><strong>Can you update a record in a Parquet file?</strong></summary><p style="margin-top:0.5rem">Not in place. Parquet files are immutable once written, so there is no way to modify a single row inside one. Table formats work around this by rewriting the affected file with the change applied, or by logging the change separately and merging it with the base file when the data is read. Either way, the update is expressed as new files plus a metadata commit, never as an in-place edit.</p></details><details style="margin-bottom:0.75rem"><summary><strong>Is upsert slow on a data lake?</strong></summary><p style="margin-top:0.5rem">It depends almost entirely on how the system finds existing records and how it applies changes. Without an index, every upsert must scan or join against the whole table to locate matching records, so write cost grows with table size. With an index such as Hudi's bloom index or record level index, the write only touches file groups that actually contain updated records. Choosing Merge-on-Read further reduces write latency by logging changes instead of rewriting columnar files on every commit.</p></details><details style="margin-bottom:0.75rem"><summary><strong>What is the difference between upsert and insert in Apache Hudi?</strong></summary><p style="margin-top:0.5rem">Upsert performs an index lookup to tag each incoming record as an update or an insert, guaranteeing the table never shows duplicate keys. Insert skips the index lookup entirely, which makes it faster but allows duplicates unless the input is pre-deduplicated. Upsert is recommended for change data capture and other update-heavy workloads, while insert suits append-mostly data that can tolerate or separately handle duplicates.</p></details><details style="margin-bottom:0.75rem"><summary><strong>How do GDPR deletes work on a data lake?</strong></summary><p style="margin-top:0.5rem">A GDPR or CCPA delete request means removing specific records, identified by key, from potentially every partition of a table. Table formats implement this on the same path as upserts: an index lookup finds the files containing the keys, and the delete is applied either by rewriting those files or by logging delete markers that compaction later reconciles. Hudi supports both soft deletes, which null out non-key fields, and hard deletes, which remove the records entirely.</p></details><details style="margin-bottom:0.75rem"><summary><strong>Do Apache Iceberg and Delta Lake support upserts?</strong></summary><p style="margin-top:0.5rem">Yes. Both support row-level mutation, primarily through the SQL MERGE INTO statement, with copy-on-write file rewrites or merge-on-read style delete files and deletion vectors. The main design difference from Apache Hudi is that they do not maintain a persistent record-level index, so finding affected files relies on joining the source data against the table with statistics-based pruning. Hudi instead defines record keys as a table-level concept and maintains indexes that map keys to file groups.</p></details></section>]]></content:encoded>
            <category>upsert</category>
            <category>dml</category>
            <category>indexing</category>
            <category>data lakehouse</category>
            <category>cow</category>
            <category>mor</category>
        </item>
        <item>
            <title><![CDATA[What is an Open Table Format?]]></title>
            <link>https://hudi.apache.org/blog/2026/07/14/what-is-an-open-table-format</link>
            <guid>https://hudi.apache.org/blog/2026/07/14/what-is-an-open-table-format</guid>
            <pubDate>Tue, 14 Jul 2026 00:00:00 GMT</pubDate>
            <description><![CDATA[An open table format is table metadata plus a transaction protocol that turns files on cloud storage into database tables with ACID commits, schema evolution and time travel — Apache Iceberg, Hudi, Delta Lake and Paimon compared.]]></description>
            <content:encoded><![CDATA[<p>An open table format is an openly governed specification with two inseparable parts: <strong>table metadata</strong> — which files make up the table, what schema they follow, what statistics they carry, at every version — and a <strong>transaction protocol</strong> — the rules by which writers commit changes atomically and readers see consistent snapshots. Together they turn a collection of files on cloud object storage into a database-like table with ACID transactions, schema evolution, and time travel. Apache Hudi, Apache Iceberg, Delta Lake, and Apache Paimon are the major open table formats; in all of them the data itself stays in open file formats like <a href="https://parquet.apache.org/" target="_blank" rel="noopener noreferrer" class="">Apache Parquet</a> or <a href="https://orc.apache.org/" target="_blank" rel="noopener noreferrer" class="">Apache ORC</a>.
Because the specification is open, any query engine — Apache Spark, Apache Flink, Trino, and many others — can read and write the same table without going through a proprietary system.</p>
<p>Open table formats emerged to fix a specific gap. Cloud object stores such as Amazon S3, Azure Blob Storage, and Google Cloud Storage made it cheap to store virtually unlimited data, and columnar file formats made it efficient to scan. But a pile of Parquet files is not a table: no atomic commits, no record-level updates or deletes, no history to query, and no protection for a reader scanning a directory mid-rewrite.</p>
<p>Table formats close that gap by layering a transaction log and rich table metadata over the files, and in doing so became the foundational component of the <a class="" href="https://hudi.apache.org/blog/2024/07/11/what-is-a-data-lakehouse">data lakehouse</a> architecture — warehouse-grade reliability and management over open data on the lake.</p>
<p>This post covers the problem, the anatomy of a table format, the major open projects, and how the pieces fit together.</p>
<h2 class="anchor anchorTargetStickyNavbar_Vzrq" id="the-problem-before-table-formats">The Problem Before Table Formats<a href="https://hudi.apache.org/blog/2026/07/14/what-is-an-open-table-format#the-problem-before-table-formats" class="hash-link" aria-label="Direct link to The Problem Before Table Formats" title="Direct link to The Problem Before Table Formats" translate="no">​</a></h2>
<p>Until roughly a decade ago, the de-facto "table" abstraction on data lakes was the Apache Hive model: a table was a directory, partitions were subdirectories, and the Hive Metastore recorded the schema and the list of partitions. This worked well enough on HDFS for append-only batch workloads, but it had structural weaknesses that cloud object storage made acute.</p>
<ul>
<li class=""><strong>No atomicity.</strong> A failed job left partial files behind, and readers could not distinguish committed data from garbage; jobs resorted to fragile rename tricks that object stores do not support atomically.</li>
<li class=""><strong>No record-level mutations.</strong> Updating or deleting a record meant rewriting entire partitions, making change data capture (CDC) from operational databases and GDPR deletions painful and expensive.</li>
<li class=""><strong>Coarse, expensive planning.</strong> Query planning relied on listing directories — slow and costly on object stores — and on partition values as the only pruning mechanism; statistics about the actual data in each file lived nowhere.</li>
<li class=""><strong>No isolation or history.</strong> A query could read an inconsistent mix of old and new files while a pipeline rewrote the table, and with no table versions there was no time travel, rollback, or incremental "what changed since yesterday" queries.</li>
</ul>
<p>The fix was to stop treating a table as a directory listing and treat it instead as versioned metadata plus a commit protocol: a transactionally maintained record of the files that make up the table. Apache Hudi pioneered this idea in production, built at Uber in 2016 for near-real-time ingestion with updates, deletes, and incremental processing on one of the world's largest data lakes, and described publicly in <a href="https://www.uber.com/blog/hoodie/" target="_blank" rel="noopener noreferrer" class="">Uber's engineering blog</a> in early 2017. Hudi solved the transaction problem as one piece of a broader goal — what the industry now calls the <strong>transactional data lake</strong>, a term AWS itself uses when <a href="https://aws.amazon.com/blogs/big-data/choosing-an-open-table-format-for-your-transactional-data-lake-on-aws/" target="_blank" rel="noopener noreferrer" class="">comparing these formats</a> — so it shipped table maintenance, indexing, and record-level updates alongside the format from the start. Apache Iceberg arrived independently from Netflix's need for correct, scalable table metadata, and positioned itself squarely around the table format core: the specification and libraries, with maintenance left to engines. Delta Lake came out of Databricks' Spark workloads and, like Hudi, always paired its format with broader capabilities such as updates and table maintenance on its platform. Apache Paimon is a later entrant with a narrower focus: LSM-tree storage on the lake for high-frequency streaming writes from Apache Flink. A useful shorthand: Iceberg <em>is</em> a table format; Hudi and Delta Lake are more, with a table format inside.</p>
<h2 class="anchor anchorTargetStickyNavbar_Vzrq" id="what-a-table-format-actually-consists-of">What a Table Format Actually Consists Of<a href="https://hudi.apache.org/blog/2026/07/14/what-is-an-open-table-format#what-a-table-format-actually-consists-of" class="hash-link" aria-label="Direct link to What a Table Format Actually Consists Of" title="Direct link to What a Table Format Actually Consists Of" translate="no">​</a></h2>
<p>Strip away the branding and every open table format is a specification for a handful of cooperating pieces of metadata stored alongside the data files.</p>
<p><img decoding="async" loading="lazy" alt="Table format layer in the Apache Hudi stack" src="https://hudi.apache.org/assets/images/table_format_1-40380ed7db1d89a0f66dba3ba70c229a.png" width="3172" height="1296" class="img_ev3q">
</p><p align="center">Figure: The table format as a metadata layer above open data file formats</p><p></p>
<p><strong>A log of changes to the table.</strong> The heart of a table format is an ordered record of every commit — data writes, deletes, schema changes, and maintenance operations. In Hudi this is the <a class="" href="https://hudi.apache.org/docs/timeline">timeline</a>; Delta Lake calls it the transaction log; Iceberg maintains a chain of snapshots referenced from table metadata files. Whatever the name, this log is the source of truth: a change is part of the table if and only if it is committed to the log, and commit is an atomic operation on storage. The designs are converging here: proposals in the Iceberg community for format version 4 — single-file commits and an adaptive root manifest — would move its commit metadata toward the log-structured approach Hudi's timeline and Delta's transaction log have used from the beginning, though these are not yet part of the Iceberg specification.</p>
<p><strong>Snapshots and versioning.</strong> Every commit produces a new consistent view of the table, so readers can query it "as of" any retained version — enabling time travel for debugging and audits, rollback of bad writes, and, where the format tracks record-level changes, incremental queries that return only what changed between two points in time.</p>
<p><strong>File-level statistics and indexes.</strong> Table formats track per-file and per-column statistics — min/max values, null counts, record counts — that engines use to skip files that cannot contain matching rows, often eliminating most I/O for selective queries. Hudi generalizes this into a <a class="" href="https://hudi.apache.org/docs/indexes">multi-modal indexing subsystem</a> stored in an internal <a class="" href="https://hudi.apache.org/docs/metadata">metadata table</a>, including column statistics, bloom filters, and record-level indexes that also accelerate the write path for updates and deletes.</p>
<p><strong>Schema tracking and evolution.</strong> The format stores the table's schema and defines rules for <a class="" href="https://hudi.apache.org/docs/schema_evolution">evolving it</a> — adding, renaming, or promoting column types — without rewriting existing data files, plus schema enforcement on write to catch pipeline breakages before bad data lands in the table.</p>
<p><strong>Concurrency control.</strong> Finally, the specification defines how multiple writers and background jobs coexist. All major formats give readers snapshot isolation — a query sees a consistent version of the table regardless of in-flight writes — while for concurrent writers they define mechanisms from optimistic concurrency control to non-blocking approaches for jobs touching disjoint or reconcilable data; Hudi's <a class="" href="https://hudi.apache.org/docs/concurrency_control">concurrency control</a> documentation covers these trade-offs in depth.</p>
<p>How data is physically organized under this metadata varies too. Hudi, for example, defines two <a class="" href="https://hudi.apache.org/docs/table_types">table types</a>: Copy-on-Write, which rewrites base files on update for maximum read performance, and Merge-on-Read, which writes changes to compact log files that are periodically merged — a trade-off between write latency and read cost that other formats have since adopted variants of.</p>
<p>Note, though, that everything above is <em>specification</em> — passive metadata and protocol rules. A table format defines what a valid table looks like; it does not keep the table healthy. In database terms, that job belongs to a <strong>storage engine</strong>: the machinery that compacts small files, clusters data, cleans up old versions, and builds and maintains indexes. Every table on a lake needs this running somewhere — built into the project, supplied by the query engine, or bought from a vendor — and this distinction, more than any metadata layout detail, is where the major projects genuinely differ.</p>
<h2 class="anchor anchorTargetStickyNavbar_Vzrq" id="the-major-open-table-formats">The Major Open Table Formats<a href="https://hudi.apache.org/blog/2026/07/14/what-is-an-open-table-format#the-major-open-table-formats" class="hash-link" aria-label="Direct link to The Major Open Table Formats" title="Direct link to The Major Open Table Formats" translate="no">​</a></h2>
<p>Four open source projects dominate the space today. All are openly governed (three at the Apache Software Foundation, one at the Linux Foundation), and all deliver the core capabilities above; they differ in design center and scope.</p>
<p><strong><a href="https://hudi.apache.org/" target="_blank" rel="noopener noreferrer" class="">Apache Hudi</a></strong> originated at Uber in 2016 for fast, mutable ingestion — upserts, deletes, and incremental processing — on the data lake. Hudi is accurately described as a table format plus a storage engine: beyond the storage specification (timeline, file layout, metadata table), it ships a writer runtime with pluggable indexing for fast updates, and built-in <a class="" href="https://hudi.apache.org/docs/hudi_stack">table services</a> — compaction, clustering, cleaning, and indexing — that run inline or asynchronously to keep tables optimized without separate orchestration. Its design center is incremental: the table as a stream of changes that downstream pipelines can efficiently consume.</p>
<p><strong><a href="https://iceberg.apache.org/" target="_blank" rel="noopener noreferrer" class="">Apache Iceberg</a></strong> originated at Netflix and was donated to the ASF in 2018. Iceberg's design center is an engine-agnostic specification for table metadata, with immutable snapshots, hidden partitioning (partition values derived from column transforms), and well-defined metadata evolution. It deliberately scopes itself to the format and libraries, leaving table maintenance to engines and vendors, and has attracted the broadest catalog and vendor ecosystem via its REST catalog specification.</p>
<p><strong><a href="https://delta.io/" target="_blank" rel="noopener noreferrer" class="">Delta Lake</a></strong> was created at Databricks and open-sourced in 2019; it is governed under the Linux Foundation. Delta centers on a JSON-based transaction log with periodic Parquet checkpoints, offering ACID transactions, schema enforcement, and time travel. Like Hudi, it has always been more than a format on its home platform, pairing the log with maintenance operations such as optimize and vacuum. It is the native format of Databricks and has first-class Spark integration, with a growing standalone ecosystem through the Delta Kernel and Rust implementations.</p>
<p><strong><a href="https://paimon.apache.org/" target="_blank" rel="noopener noreferrer" class="">Apache Paimon</a></strong> is the newest of the four, grown out of the Flink Table Store subproject and a top-level Apache project since 2024. Paimon is narrower in scope than the other three: its design center is LSM-tree (log-structured merge tree) storage on the lake, optimizing write speed for high-frequency updates and changelog-producing tables consumed by Apache Flink, while remaining readable by batch engines like Spark and Trino. The trade-off is a classic one from storage theory (the <a href="https://bytearray.substack.com/p/computer-science-behind-lakehouse" target="_blank" rel="noopener noreferrer" class="">RUM conjecture</a>): LSM-style layouts defer merge work to read time, so scan performance degrades unless compaction keeps pace — a consideration that matters on lakes, where reads are the dominant workload.</p>
<p>An honest summary: the formats have converged substantially on core capabilities — ACID commits, schema evolution, time travel, statistics-based pruning — and continue to borrow ideas from one another. But write performance and read performance remain distinct problems with different trade-offs, even today: a design that makes writes cheap taxes readers unless compaction and read-side indexing keep pace, while one that optimizes purely for scans makes every update a rewrite. Because reads dominate lake workloads, a format has to be engineered for both sides, and this is where the remaining differences live: write-path design (indexing, merge strategies), how much storage-engine machinery is built in versus left to the engine or vendor, and ecosystem depth per engine. Hudi's long-standing bet is performance across both paths — indexed writes to make updates fast, automated compaction and clustering to keep reads fast — which is why it remains the strongest fit for high-volume mutable and incremental workloads.</p>
<h2 class="anchor anchorTargetStickyNavbar_Vzrq" id="how-query-engines-integrate">How Query Engines Integrate<a href="https://hudi.apache.org/blog/2026/07/14/what-is-an-open-table-format#how-query-engines-integrate" class="hash-link" aria-label="Direct link to How Query Engines Integrate" title="Direct link to How Query Engines Integrate" translate="no">​</a></h2>
<p>A table format is only useful insofar as engines speak it. Integration happens at two levels.</p>
<p>At the <strong>data plane</strong>, each format ships libraries or specifications that engines embed. Apache Spark and Apache Flink integrate with all four formats for both reading and writing; distributed SQL engines such as Trino and Presto ship native connectors. Cloud warehouses — Amazon Athena and Redshift, Google BigQuery, Snowflake — can query open table formats directly, with capabilities varying by format and vendor, and Python-native engines (DuckDB, Daft, Ray) extend the same reach to lightweight workloads.</p>
<p>At the <strong>control plane</strong>, a catalog tracks which tables exist and points engines at each table's current metadata. Options range from the venerable Hive Metastore and AWS Glue Data Catalog to newer catalogs like Apache Polaris, Unity Catalog, and Apache Gravitino. The catalog is what turns <code>SELECT * FROM orders</code> into "resolve <code>orders</code> to a storage location and metadata pointer, then plan the scan." Hudi syncs its table metadata to these catalogs through catalog sync tools, keeping tables discoverable across engines.</p>
<h2 class="anchor anchorTargetStickyNavbar_Vzrq" id="interoperability-you-dont-have-to-pick-one-forever">Interoperability: You Don't Have to Pick One Forever<a href="https://hudi.apache.org/blog/2026/07/14/what-is-an-open-table-format#interoperability-you-dont-have-to-pick-one-forever" class="hash-link" aria-label="Direct link to Interoperability: You Don't Have to Pick One Forever" title="Direct link to Interoperability: You Don't Have to Pick One Forever" translate="no">​</a></h2>
<p>A common source of anxiety is that choosing a table format is a one-way door. Increasingly, it is not. <a href="https://xtable.apache.org/" target="_blank" rel="noopener noreferrer" class="">Apache XTable</a> (incubating) is an open source project that translates table metadata between Hudi, Iceberg, and Delta Lake — in any direction — without copying or rewriting the underlying data files. Because all three formats ultimately describe Parquet files plus metadata, XTable can read one format's metadata and write out the equivalent for another, as a one-time conversion or continuous incremental sync — so a table written with Hudi's ingestion and indexing capabilities can be exposed as an Iceberg table to an Iceberg-only service, or a Delta table queried by a Hudi-native pipeline.</p>
<p>This is how major vendors ship interoperability today. XTable was co-launched by Microsoft, Google, and Onehouse before its donation to the ASF; <a href="https://xtable.apache.org/docs/fabric/" target="_blank" rel="noopener noreferrer" class="">Microsoft OneLake</a> in Fabric uses it under the hood to surface tables across Delta and Iceberg formats, and Onehouse uses it to keep customer tables continuously queryable as Hudi, Iceberg, and Delta at once. Databricks takes the same one-copy approach within Delta Lake through UniForm, which generates Iceberg metadata alongside the Delta transaction log so one copy of data registers and reads as either format. Format choice becomes a per-workload decision about write-path and management capabilities, rather than a permanent bet that fragments your data.</p>
<h2 class="anchor anchorTargetStickyNavbar_Vzrq" id="table-format-vs-file-format-vs-lakehouse">Table Format vs File Format vs Lakehouse<a href="https://hudi.apache.org/blog/2026/07/14/what-is-an-open-table-format#table-format-vs-file-format-vs-lakehouse" class="hash-link" aria-label="Direct link to Table Format vs File Format vs Lakehouse" title="Direct link to Table Format vs File Format vs Lakehouse" translate="no">​</a></h2>
<p>These three terms get conflated constantly, so a short disambiguation is worth the space.</p>
<ul>
<li class="">A <strong>file format</strong> (Apache Parquet, ORC, Avro) defines how records are encoded <em>inside a single file</em> — columnar layout, compression, per-file statistics. It knows nothing about other files.</li>
<li class="">A <strong>table format</strong> (Hudi, Iceberg, Delta Lake, Paimon) defines how <em>many files plus metadata</em> form a versioned, transactional table. It sits directly above the file format.</li>
<li class="">A <strong>data lakehouse</strong> is the overall <em>architecture</em> that combines cloud object storage, open file formats, a table format, catalogs, and query engines into a warehouse-like platform on the lake. The table format is its load-bearing component, alongside storage engines/table services, catalogs, and compute.</li>
</ul>
<p><img decoding="async" loading="lazy" alt="Data lakehouse architecture" src="https://hudi.apache.org/assets/images/dlh_new-ae34f6d692de93292db9eb4e19690670.png" width="2323" height="1259" class="img_ev3q">
</p><p align="center">Figure: Where the table format sits in a lakehouse architecture</p><p></p>
<p>So Parquet is not a table format, and a table format alone is not a lakehouse — it is the layer that makes one possible. For the architectural view, see the companion post on <a class="" href="https://hudi.apache.org/blog/2024/07/11/what-is-a-data-lakehouse">what a data lakehouse is</a>.</p>
<h2 class="anchor anchorTargetStickyNavbar_Vzrq" id="conclusion">Conclusion<a href="https://hudi.apache.org/blog/2026/07/14/what-is-an-open-table-format#conclusion" class="hash-link" aria-label="Direct link to Conclusion" title="Direct link to Conclusion" translate="no">​</a></h2>
<p>An open table format — table metadata plus a transaction protocol — turns cheap, scalable object storage into a real analytical database substrate: files become tables, jobs become transactions, and a directory of Parquet becomes something you can update, evolve, audit, and query as of any point in time, from any engine that implements the open specification. Apache Hudi proved the idea in production first while building the transactional data lake; Apache Iceberg, Delta Lake, and Apache Paimon arrived from different starting workloads and remain strongest nearest their design centers. But the format is only the specification — someone still has to run the storage engine that keeps tables fast, and reads and writes pull that engineering in different directions. With Apache XTable translating metadata between formats, the practical takeaway is straightforward: pick the format whose write path and management capabilities fit each workload, keep your data in open formats, and let the specification — not a vendor — define what your tables are.</p>
<h2 class="anchor anchorTargetStickyNavbar_Vzrq" id="faq">FAQ<a href="https://hudi.apache.org/blog/2026/07/14/what-is-an-open-table-format#faq" class="hash-link" aria-label="Direct link to FAQ" title="Direct link to FAQ" translate="no">​</a></h2>
<section><details style="margin-bottom:0.75rem"><summary><strong>What is an open table format?</strong></summary><p style="margin-top:0.5rem">An open table format is an openly governed specification combining table metadata (which files form the table, their schema and statistics, at every version) with a transaction protocol (how writers commit atomically and readers see consistent snapshots). Together they turn files on cloud object storage into a database-like table with ACID transactions, schema evolution, and time travel. Apache Hudi, Apache Iceberg, Delta Lake, and Apache Paimon are the major open table formats.</p></details><details style="margin-bottom:0.75rem"><summary><strong>Is Parquet an open table format?</strong></summary><p style="margin-top:0.5rem">No. Apache Parquet is an open file format: it defines how records are encoded inside a single file. A table format like Apache Hudi or Apache Iceberg sits above Parquet and defines how many files plus transactional metadata form a versioned table.</p></details><details style="margin-bottom:0.75rem"><summary><strong>What is the difference between a table format and a file format?</strong></summary><p style="margin-top:0.5rem">A file format defines the byte layout of one file, including encoding, compression, and per-file statistics. A table format defines how a collection of such files behaves as a single transactional table, tracking committed files, schema, versions, and statistics across the whole table. They are complementary layers, not alternatives.</p></details><details style="margin-bottom:0.75rem"><summary><strong>Which open table format should I use?</strong></summary><p style="margin-top:0.5rem">It depends on your workloads, and note that write and read performance involve different trade-offs. Hudi is strongest for mutable, incremental ingestion at high volume, with built-in indexing for fast writes and automated compaction and clustering to keep reads fast; Iceberg emphasizes an engine-neutral specification with broad catalog support; Delta Lake offers deep Spark and Databricks integration; Paimon targets LSM-based streaming writes with Flink. Apache XTable can translate metadata between Hudi, Iceberg, and Delta, so the choice is not permanent.</p></details><details style="margin-bottom:0.75rem"><summary><strong>Do open table formats support ACID transactions?</strong></summary><p style="margin-top:0.5rem">Yes. All major open table formats provide atomic commits and snapshot isolation for readers, so queries always see a consistent version of the table. They differ in how they handle concurrent writers, using techniques such as optimistic concurrency control and non-blocking concurrency mechanisms.</p></details><details style="margin-bottom:0.75rem"><summary><strong>Is Apache Hudi just a table format?</strong></summary><p style="margin-top:0.5rem">No. Hudi includes an open table format, but pairs it with a built-in storage engine: a writer runtime with pluggable indexing plus table services such as compaction, clustering, and cleaning that keep tables optimized automatically. Hudi was also the first open table format in production, built at Uber in 2016 for its transactional data lake.</p></details></section>]]></content:encoded>
            <category>table format</category>
            <category>data lakehouse</category>
            <category>apache iceberg</category>
            <category>delta lake</category>
            <category>open architecture</category>
            <category>apache xtable</category>
        </item>
        <item>
            <title><![CDATA[Bringing Vector Search to the Lakehouse with Apache Hudi]]></title>
            <link>https://hudi.apache.org/blog/2026/07/06/bringing-vector-search-to-the-lakehouse-with-apache-hudi</link>
            <guid>https://hudi.apache.org/blog/2026/07/06/bringing-vector-search-to-the-lakehouse-with-apache-hudi</guid>
            <pubDate>Mon, 06 Jul 2026 00:00:00 GMT</pubDate>
            <description><![CDATA[AI applications increasingly need to search data by meaning, not just by exact fields or keywords. Product descriptions, support tickets, documents, images, and other unstructured data are becoming first-class inputs to analytics and application workflows. But adding semantic search to lakehouse data has usually meant introducing a separate vector database, copying embeddings into it, and maintaining a sync pipeline to keep records, metadata, deletes, and updates aligned.]]></description>
            <content:encoded><![CDATA[<div style="text-align:center"><img src="https://hudi.apache.org/assets/images/blog/2026-07-06-bringing-vector-search-to-the-lakehouse-with-apache-hudi/1-hero-vector-search-lakehouse.png" alt="Bringing Vector Search to the Lakehouse with Apache Hudi" width="1000"></div>
<div class="theme-admonition theme-admonition-tip admonition_xJq3 alert alert--success"><div class="admonitionHeading_Gvgb"><span class="admonitionIcon_Rf37"><svg viewBox="0 0 12 16"><path fill-rule="evenodd" d="M6.5 0C3.48 0 1 2.19 1 5c0 .92.55 2.25 1 3 1.34 2.25 1.78 2.78 2 4v1h5v-1c.22-1.22.66-1.75 2-4 .45-.75 1-2.08 1-3 0-2.81-2.48-5-5.5-5zm3.64 7.48c-.25.44-.47.8-.67 1.11-.86 1.41-1.25 2.06-1.45 3.23-.02.05-.02.11-.02.17H5c0-.06 0-.13-.02-.17-.2-1.17-.59-1.83-1.45-3.23-.2-.31-.42-.67-.67-1.11C2.44 6.78 2 5.65 2 5c0-2.2 2.02-4 4.5-4 1.22 0 2.36.42 3.22 1.19C10.55 2.94 11 3.94 11 5c0 .66-.44 1.78-.86 2.48zM4 14h5c-.23 1.14-1.3 2-2.5 2s-2.27-.86-2.5-2z"></path></svg></span>TL;DR</div><div class="admonitionContent_BuS1"><p>AI applications increasingly need to search data by meaning, not just by exact fields or keywords. Product descriptions, support tickets, documents, images, and other unstructured data are becoming first-class inputs to analytics and application workflows. But adding semantic search to lakehouse data has usually meant introducing a separate vector database, copying embeddings into it, and maintaining a sync pipeline to keep records, metadata, deletes, and updates aligned.</p><p>Apache Hudi's native vector search brings that workflow closer to the table itself. Embeddings can live in the same Hudi table as the rest of the row, represented as a first-class VECTOR column and queried directly from SQL. This lets semantic search compose with structured filters, joins, updates, time travel, and incremental queries instead of requiring a separate sidecar system.</p><p>Hudi exposes vector search through two SQL functions: hudi_vector_search for a single query vector and hudi_vector_search_batch for running top-k search across a table of query vectors. Embeddings can be stored as FLOAT, DOUBLE, or INT8, and compared using cosine, L2, or dot product distance. The initial implementation uses distributed brute-force KNN, while an optimized ANN vector index is being developed behind the same SQL interface so applications can adopt the API today and benefit from faster algorithms as they land.</p></div></div>
<h2 class="anchor anchorTargetStickyNavbar_Vzrq" id="what-is-semantic-search">What is Semantic search?<a href="https://hudi.apache.org/blog/2026/07/06/bringing-vector-search-to-the-lakehouse-with-apache-hudi#what-is-semantic-search" class="hash-link" aria-label="Direct link to What is Semantic search?" title="Direct link to What is Semantic search?" translate="no">​</a></h2>
<p>Consider an example of a user interacting with an ecommerce site that has millions of product listings. Typically in most data analytics systems, a user would have to craft a query on exact fields, such as <code>category</code>, <code>brand</code>, <code>price</code>, <code>size</code>, or <code>inventory_status</code> in order to find a match.</p>
<p>But many searches start with intent, and not direct fields. A user instead might search for "comfortable shoes for walking around Europe," even though there is no column called <code>comfortable</code> and no guarantee that the best listings use those exact words. Some products may say "travel sneakers," "all-day support," or "lightweight walking shoes" instead.</p>
<p>To handle this, semantic search is a way to search by <strong>meaning</strong> rather than by exact fields or keywords. It works by converting text, images, or other content into what are known as "vector embeddings".</p>
<p>A <strong>vector embedding</strong> is a fixed-size array of numbers that represents the meaning of some piece of data, such as a product description, customer review, image, support message, or document chunk.</p>
<p>Embeddings are typically produced by machine learning models. For text, this might be an embedding model or LLM-based encoder that converts a sentence, paragraph, or document chunk into a vector. For images, it might be a vision model that converts visual content into the same kind of numerical representation.</p>
<p>For example, a marketplace product description like:</p>
<div class="language-text codeBlockContainer_Ckt0 theme-code-block" style="--prism-color:#F8F8F2;--prism-background-color:#282A36"><div class="codeBlockContent_QJqH"><pre tabindex="0" class="prism-code language-text codeBlock_bY9V thin-scrollbar" style="color:#F8F8F2;background-color:#282A36"><code class="codeBlockLines_e6Vv"><span class="token-line" style="color:#F8F8F2"><span class="token plain">Lightweight sneakers with cushioned soles for all-day walking</span><br></span></code></pre></div></div>
<p>might be converted into a vector that looks conceptually like this:</p>
<div class="language-text codeBlockContainer_Ckt0 theme-code-block" style="--prism-color:#F8F8F2;--prism-background-color:#282A36"><div class="codeBlockContent_QJqH"><pre tabindex="0" class="prism-code language-text codeBlock_bY9V thin-scrollbar" style="color:#F8F8F2;background-color:#282A36"><code class="codeBlockLines_e6Vv"><span class="token-line" style="color:#F8F8F2"><span class="token plain">[0.12, -0.03, 0.44, ..., 0.08]</span><br></span></code></pre></div></div>
<p>The individual numbers in an embedding are often called <strong>features</strong>. Each one helps place the original item somewhere in a high-dimensional space. For a product description, the model may learn features related to things like product type, use case, style, material, comfort, or activity. For an image, the features may capture patterns related to shape, color, texture, or visual similarity.</p>
<p>These features are usually not directly interpretable on their own. One number does not simply mean "comfort" or "travel." Instead, the full vector captures many learned signals together. What matters is how one vector relates to another. Products with similar meaning should produce vectors that are close together, even if the words are different.</p>
<p>That is what allows a search like "comfortable shoes for walking around Europe" to match products described as "travel sneakers," "supportive walking shoes," or "lightweight everyday trainers," even when those listings do not use the exact same words as the query.</p>
<div style="text-align:center"><img src="https://hudi.apache.org/assets/images/blog/2026-07-06-bringing-vector-search-to-the-lakehouse-with-apache-hudi/2-semantic-search-example.png" alt="A shopper query is embedded, compared against nearest product embeddings, and matched to semantically similar products such as travel sneakers and supportive walking shoes." width="1000"></div>
<h2 class="anchor anchorTargetStickyNavbar_Vzrq" id="the-need-for-both-structured-and-unstructured-data">The need for both structured and unstructured data<a href="https://hudi.apache.org/blog/2026/07/06/bringing-vector-search-to-the-lakehouse-with-apache-hudi#the-need-for-both-structured-and-unstructured-data" class="hash-link" aria-label="Direct link to The need for both structured and unstructured data" title="Direct link to The need for both structured and unstructured data" translate="no">​</a></h2>
<p>The marketplace example shows why modern AI applications need both unstructured and structured data. The meaning of a product may live in descriptions, reviews, images, or support conversations, which can be converted into embeddings and searched semantically. But deciding whether a result is useful still depends on structured fields such as <code>price</code>, <code>size</code>, <code>inventory_status</code>, <code>shipping_region</code>, and <code>seller_rating</code>.</p>
<p>This pattern has become more important with the rise of LLM-powered applications. Search, recommendations, RAG, document intelligence, and agent workflows increasingly operate over text, images, and other unstructured data. But these applications still need the guarantees of structured data systems: filters, joins, updates, deletes, freshness, and governance.</p>
<div style="text-align:center"><img src="https://hudi.apache.org/assets/images/blog/2026-07-06-bringing-vector-search-to-the-lakehouse-with-apache-hudi/3-structured-unstructured-data.png" alt="Unstructured data such as product descriptions, reviews, and images is embedded and combined with structured fields through SQL filters to produce a combined semantic and structured retrieval result." width="1000"></div>
<p>This is where the lakehouse becomes a natural fit. In many AI applications, the embedding is not an independent object. It is derived from the row itself: a product description, review, image, support conversation, document chunk, or some combination of fields. When that source data changes, the embedding needs to change with it. When a row is deleted, expired, corrected, or reprocessed, the corresponding embedding needs to follow the same lifecycle.</p>
<p>A standalone vector database can be a good serving layer, especially for ultra-low-latency retrieval at very large scale. But if the source records live in the lakehouse, a separate vector database introduces another copy of the data. That copy has to be kept in sync with the table, including inserts, updates, deletes, schema changes, metadata filters, and backfills. Over time, the hard problem becomes less about computing nearest neighbors and more about maintaining consistency between the source data and the vector index.</p>
<p>Hudi vector search is built around a simpler model: keep the original records, structured metadata, and embeddings together in the same table. In an ingestion pipeline, new and changed records can be processed incrementally, embeddings can be generated or refreshed from the latest source fields, and both the source data and vector columns can be committed together into Hudi. That makes the Hudi table the single source of truth for both analytics and semantic search.</p>
<p>For example, Hudi <a href="https://hudi.apache.org/docs/hoodie_streaming_ingestion/" target="_blank" rel="noopener noreferrer" class="">Streamer</a> can ingest changes from sources such as Kafka or cloud storage into a Hudi table. An embedding step can be added to the pipeline so that when product descriptions, reviews, images, or metadata change, the corresponding embeddings are updated as part of the same table workflow.</p>
<h2 class="anchor anchorTargetStickyNavbar_Vzrq" id="how-does-hudi-represent-vector">How does Hudi represent VECTOR?<a href="https://hudi.apache.org/blog/2026/07/06/bringing-vector-search-to-the-lakehouse-with-apache-hudi#how-does-hudi-represent-vector" class="hash-link" aria-label="Direct link to How does Hudi represent VECTOR?" title="Direct link to How does Hudi represent VECTOR?" translate="no">​</a></h2>
<p>Once embeddings are produced, they need to be stored in a table in a way that preserves their meaning. A plain array of numbers can store the values, but it does not tell the table system that the column is a fixed-dimensional vector intended for similarity search.</p>
<p>Hudi represents embeddings using a first-class logical type: <code>VECTOR(dimension)</code> or <code>VECTOR(dimension, element_type)</code>. The dimension value defines the fixed length of the vector. For example, <code>VECTOR(768)</code> represents an embedding with 768 values.</p>
<p>A product catalog table can declare a vector column directly in SQL DDL:</p>
<div class="language-sql codeBlockContainer_Ckt0 theme-code-block" style="--prism-color:#F8F8F2;--prism-background-color:#282A36"><div class="codeBlockContent_QJqH"><pre tabindex="0" class="prism-code language-sql codeBlock_bY9V thin-scrollbar" style="color:#F8F8F2;background-color:#282A36"><code class="codeBlockLines_e6Vv"><span class="token-line" style="color:#F8F8F2"><span class="token keyword" style="color:rgb(189, 147, 249);font-style:italic">CREATE</span><span class="token plain"> </span><span class="token keyword" style="color:rgb(189, 147, 249);font-style:italic">TABLE</span><span class="token plain"> products </span><span class="token punctuation" style="color:rgb(248, 248, 242)">(</span><span class="token plain"></span><br></span><span class="token-line" style="color:#F8F8F2"><span class="token plain">  product_id string</span><span class="token punctuation" style="color:rgb(248, 248, 242)">,</span><span class="token plain"></span><br></span><span class="token-line" style="color:#F8F8F2"><span class="token plain">  title string</span><span class="token punctuation" style="color:rgb(248, 248, 242)">,</span><span class="token plain"></span><br></span><span class="token-line" style="color:#F8F8F2"><span class="token plain">  category string</span><span class="token punctuation" style="color:rgb(248, 248, 242)">,</span><span class="token plain"></span><br></span><span class="token-line" style="color:#F8F8F2"><span class="token plain">  brand string</span><span class="token punctuation" style="color:rgb(248, 248, 242)">,</span><span class="token plain"></span><br></span><span class="token-line" style="color:#F8F8F2"><span class="token plain">  price </span><span class="token keyword" style="color:rgb(189, 147, 249);font-style:italic">double</span><span class="token punctuation" style="color:rgb(248, 248, 242)">,</span><span class="token plain"></span><br></span><span class="token-line" style="color:#F8F8F2"><span class="token plain">  inventory_status string</span><span class="token punctuation" style="color:rgb(248, 248, 242)">,</span><span class="token plain"></span><br></span><span class="token-line" style="color:#F8F8F2"><span class="token plain">  description string</span><span class="token punctuation" style="color:rgb(248, 248, 242)">,</span><span class="token plain"></span><br></span><span class="token-line" style="color:#F8F8F2"><span class="token plain">  description_embedding VECTOR</span><span class="token punctuation" style="color:rgb(248, 248, 242)">(</span><span class="token number">768</span><span class="token punctuation" style="color:rgb(248, 248, 242)">)</span><span class="token plain"></span><br></span><span class="token-line" style="color:#F8F8F2"><span class="token plain"></span><span class="token punctuation" style="color:rgb(248, 248, 242)">)</span><span class="token plain"> </span><span class="token keyword" style="color:rgb(189, 147, 249);font-style:italic">USING</span><span class="token plain"> hudi</span><span class="token punctuation" style="color:rgb(248, 248, 242)">;</span><br></span></code></pre></div></div>
<p>When the element type is omitted, Hudi uses <code>FLOAT</code> by default. This is the most common representation for embeddings produced by text and image models. Hudi also supports <code>DOUBLE</code> and <code>INT8</code> for workloads that need higher precision or more compact storage.</p>
<p>The storage difference becomes meaningful at scale. A <code>VECTOR(768, FLOAT)</code> stores 3,072 bytes of raw vector values per row. The same vector represented as <code>INT8</code> stores 768 bytes per row. Across millions or billions of rows, that difference affects table size, index size, and query cost.</p>
<p>At the Hudi table level, both the SQL DDL form and the DataFrame API form describe the same logical column: a fixed-dimensional vector that can be validated, indexed, and used for vector search. At the physical storage layer, Hudi maps the logical vector type onto storage formats in a way that balances efficiency and interoperability. Readers that do not understand the Hudi vector logical type can still access the underlying data, while Hudi-aware readers can validate dimensions and treat the column as a single vector value. The DataFrame API equivalent and on-disk encoding are covered in the <a href="https://hudi.apache.org/docs/next/unstructured-data-quick-start-guide/" target="_blank" rel="noopener noreferrer" class="">docs</a>.</p>
<p>Today, vector columns are supported as top-level columns. They cannot yet be nested inside structs, arrays, or maps. For data models that naturally contain multiple embeddings per entity, such as a product with many variants, the current pattern is to model each embedded item as its own row. Nested vector support can be added in the future as the type system and query semantics evolve.</p>
<h2 class="anchor anchorTargetStickyNavbar_Vzrq" id="querying-with-hudi-vector-search">Querying with Hudi Vector Search<a href="https://hudi.apache.org/blog/2026/07/06/bringing-vector-search-to-the-lakehouse-with-apache-hudi#querying-with-hudi-vector-search" class="hash-link" aria-label="Direct link to Querying with Hudi Vector Search" title="Direct link to Querying with Hudi Vector Search" translate="no">​</a></h2>
<p>Once embeddings are stored in a Hudi table, they can be searched directly from SQL. Hudi provides two table-valued functions:</p>
<ul>
<li class=""><code>hudi_vector_search</code> for one query vector</li>
<li class=""><code>hudi_vector_search_batch</code> for many query vectors</li>
</ul>
<p>The difference is easiest to understand through the marketplace example.</p>
<p>A single-query search answers a question like: <em>Given this shopper query, find the 10 most similar products.</em></p>
<p>A batch search answers a larger version of the same question: <em>Given this table of many shopper queries, products, or documents, find the 10 most similar products for each row.</em></p>
<p>So single-query search is one nearest-neighbor lookup. Batch search is the same lookup repeated for every row in another table, executed as one distributed job.</p>
<div style="text-align:center"><img src="https://hudi.apache.org/assets/images/blog/2026-07-06-bringing-vector-search-to-the-lakehouse-with-apache-hudi/4-querying-hudi-vector-search.png" alt="hudi_vector_search returns the top nearest products for a single shopper query, while hudi_vector_search_batch runs the same lookup across every row of a query table in one distributed job." width="1000"></div>
<h3 class="anchor anchorTargetStickyNavbar_Vzrq" id="single-query-vector-search">Single-query vector search<a href="https://hudi.apache.org/blog/2026/07/06/bringing-vector-search-to-the-lakehouse-with-apache-hudi#single-query-vector-search" class="hash-link" aria-label="Direct link to Single-query vector search" title="Direct link to Single-query vector search" translate="no">​</a></h3>
<p><code>hudi_vector_search</code> takes the table to search, the embedding column, a query vector, and <code>k</code>, plus a few optional arguments: the distance metric (<code>cosine</code>, <code>l2</code>, or <code>dot_product</code>), a filter predicate applied before distance computation, and a <code>max_distance</code> cutoff to drop weak matches. For a full list of parameters refer to the following <a href="https://hudi.apache.org/docs/next/unstructured-data-quick-start-guide/" target="_blank" rel="noopener noreferrer" class="">docs</a>.</p>
<p>For example, a shopper might search for "wireless headphones for travel." The application converts that text into an embedding and searches the product catalog:</p>
<div class="language-sql codeBlockContainer_Ckt0 theme-code-block" style="--prism-color:#F8F8F2;--prism-background-color:#282A36"><div class="codeBlockContent_QJqH"><pre tabindex="0" class="prism-code language-sql codeBlock_bY9V thin-scrollbar" style="color:#F8F8F2;background-color:#282A36"><code class="codeBlockLines_e6Vv"><span class="token-line" style="color:#F8F8F2"><span class="token keyword" style="color:rgb(189, 147, 249);font-style:italic">SELECT</span><span class="token plain"> product_id</span><span class="token punctuation" style="color:rgb(248, 248, 242)">,</span><span class="token plain"> title</span><span class="token punctuation" style="color:rgb(248, 248, 242)">,</span><span class="token plain"> brand</span><span class="token punctuation" style="color:rgb(248, 248, 242)">,</span><span class="token plain"> price</span><span class="token punctuation" style="color:rgb(248, 248, 242)">,</span><span class="token plain"> _hudi_distance</span><br></span><span class="token-line" style="color:#F8F8F2"><span class="token plain"></span><span class="token keyword" style="color:rgb(189, 147, 249);font-style:italic">FROM</span><span class="token plain"> hudi_vector_search</span><span class="token punctuation" style="color:rgb(248, 248, 242)">(</span><span class="token plain"></span><br></span><span class="token-line" style="color:#F8F8F2"><span class="token plain">  </span><span class="token string" style="color:rgb(255, 121, 198)">'product_embeddings'</span><span class="token punctuation" style="color:rgb(248, 248, 242)">,</span><span class="token plain"></span><br></span><span class="token-line" style="color:#F8F8F2"><span class="token plain">  </span><span class="token string" style="color:rgb(255, 121, 198)">'embedding'</span><span class="token punctuation" style="color:rgb(248, 248, 242)">,</span><span class="token plain"></span><br></span><span class="token-line" style="color:#F8F8F2"><span class="token plain">  ARRAY</span><span class="token punctuation" style="color:rgb(248, 248, 242)">(</span><span class="token comment" style="color:rgb(98, 114, 164)">/* embedding of the shopper query */</span><span class="token punctuation" style="color:rgb(248, 248, 242)">)</span><span class="token punctuation" style="color:rgb(248, 248, 242)">,</span><span class="token plain"></span><br></span><span class="token-line" style="color:#F8F8F2"><span class="token plain">  </span><span class="token number">10</span><span class="token punctuation" style="color:rgb(248, 248, 242)">,</span><span class="token plain"></span><br></span><span class="token-line" style="color:#F8F8F2"><span class="token plain">  </span><span class="token string" style="color:rgb(255, 121, 198)">'cosine'</span><span class="token punctuation" style="color:rgb(248, 248, 242)">,</span><span class="token plain"></span><br></span><span class="token-line" style="color:#F8F8F2"><span class="token plain">  </span><span class="token string" style="color:rgb(255, 121, 198)">'brute_force'</span><span class="token punctuation" style="color:rgb(248, 248, 242)">,</span><span class="token plain"></span><br></span><span class="token-line" style="color:#F8F8F2"><span class="token plain">  </span><span class="token string" style="color:rgb(255, 121, 198)">'category = ''electronics'' AND in_stock = true'</span><span class="token punctuation" style="color:rgb(248, 248, 242)">,</span><span class="token plain"></span><br></span><span class="token-line" style="color:#F8F8F2"><span class="token plain">  </span><span class="token number">0.4</span><span class="token plain"></span><br></span><span class="token-line" style="color:#F8F8F2"><span class="token plain"></span><span class="token punctuation" style="color:rgb(248, 248, 242)">)</span><span class="token punctuation" style="color:rgb(248, 248, 242)">;</span><br></span></code></pre></div></div>
<p>The filter is important. Since the first implementation uses brute-force search, reducing the candidate set before distance computation is one of the most effective ways to control latency. Searching 50 million product embeddings is expensive. Searching only the in-stock electronics products may reduce the search space to a much smaller subset.</p>
<p>The <code>max_distance</code> argument is another practical control. It removes results whose distance is above the threshold, so the query does not return weak or irrelevant matches simply to fill the requested <code>k</code>.</p>
<p>The output includes all corpus columns except the embedding column, plus <code>_hudi_distance</code>. Results are ordered by distance ascending, so smaller values are more similar.</p>
<h3 class="anchor anchorTargetStickyNavbar_Vzrq" id="batch-vector-search">Batch Vector Search<a href="https://hudi.apache.org/blog/2026/07/06/bringing-vector-search-to-the-lakehouse-with-apache-hudi#batch-vector-search" class="hash-link" aria-label="Direct link to Batch Vector Search" title="Direct link to Batch Vector Search" translate="no">​</a></h3>
<p>Batch search is useful when you do not have just one query. Instead, you have a whole table of things that each need their own nearest-neighbor results.</p>
<p>That is what <code>hudi_vector_search_batch</code> does: it runs top-k vector search for every row in a query table. It takes the same arguments as the single-query form, but searches with a whole <code>query_table</code> (and its <code>query_col</code>) instead of a single vector, returning the top <em>k</em> corpus rows for each query row. See the <a href="https://hudi.apache.org/docs/next/unstructured-data-quick-start-guide/" target="_blank" rel="noopener noreferrer" class="">docs</a> for the full signature.</p>
<p>For example, suppose a marketplace wants to precompute related products every night. The input is not one shopper query. The input is a table of products:</p>
<div class="language-sql codeBlockContainer_Ckt0 theme-code-block" style="--prism-color:#F8F8F2;--prism-background-color:#282A36"><div class="codeBlockContent_QJqH"><pre tabindex="0" class="prism-code language-sql codeBlock_bY9V thin-scrollbar" style="color:#F8F8F2;background-color:#282A36"><code class="codeBlockLines_e6Vv"><span class="token-line" style="color:#F8F8F2"><span class="token keyword" style="color:rgb(189, 147, 249);font-style:italic">SELECT</span><span class="token plain"></span><br></span><span class="token-line" style="color:#F8F8F2"><span class="token plain">  query_product_id</span><span class="token punctuation" style="color:rgb(248, 248, 242)">,</span><span class="token plain"></span><br></span><span class="token-line" style="color:#F8F8F2"><span class="token plain">  product_id </span><span class="token keyword" style="color:rgb(189, 147, 249);font-style:italic">AS</span><span class="token plain"> related_product_id</span><span class="token punctuation" style="color:rgb(248, 248, 242)">,</span><span class="token plain"></span><br></span><span class="token-line" style="color:#F8F8F2"><span class="token plain">  title</span><span class="token punctuation" style="color:rgb(248, 248, 242)">,</span><span class="token plain"></span><br></span><span class="token-line" style="color:#F8F8F2"><span class="token plain">  _hudi_distance</span><span class="token punctuation" style="color:rgb(248, 248, 242)">,</span><span class="token plain"></span><br></span><span class="token-line" style="color:#F8F8F2"><span class="token plain">  _hudi_query_index</span><br></span><span class="token-line" style="color:#F8F8F2"><span class="token plain"></span><span class="token keyword" style="color:rgb(189, 147, 249);font-style:italic">FROM</span><span class="token plain"> hudi_vector_search_batch</span><span class="token punctuation" style="color:rgb(248, 248, 242)">(</span><span class="token plain"></span><br></span><span class="token-line" style="color:#F8F8F2"><span class="token plain">  </span><span class="token string" style="color:rgb(255, 121, 198)">'product_embeddings'</span><span class="token punctuation" style="color:rgb(248, 248, 242)">,</span><span class="token plain"></span><br></span><span class="token-line" style="color:#F8F8F2"><span class="token plain">  </span><span class="token string" style="color:rgb(255, 121, 198)">'embedding'</span><span class="token punctuation" style="color:rgb(248, 248, 242)">,</span><span class="token plain"></span><br></span><span class="token-line" style="color:#F8F8F2"><span class="token plain">  </span><span class="token string" style="color:rgb(255, 121, 198)">'query_products'</span><span class="token punctuation" style="color:rgb(248, 248, 242)">,</span><span class="token plain"></span><br></span><span class="token-line" style="color:#F8F8F2"><span class="token plain">  </span><span class="token string" style="color:rgb(255, 121, 198)">'query_embedding'</span><span class="token punctuation" style="color:rgb(248, 248, 242)">,</span><span class="token plain"></span><br></span><span class="token-line" style="color:#F8F8F2"><span class="token plain">  </span><span class="token number">20</span><span class="token punctuation" style="color:rgb(248, 248, 242)">,</span><span class="token plain"></span><br></span><span class="token-line" style="color:#F8F8F2"><span class="token plain">  </span><span class="token string" style="color:rgb(255, 121, 198)">'cosine'</span><span class="token punctuation" style="color:rgb(248, 248, 242)">,</span><span class="token plain"></span><br></span><span class="token-line" style="color:#F8F8F2"><span class="token plain">  </span><span class="token string" style="color:rgb(255, 121, 198)">'brute_force'</span><span class="token punctuation" style="color:rgb(248, 248, 242)">,</span><span class="token plain"></span><br></span><span class="token-line" style="color:#F8F8F2"><span class="token plain">  </span><span class="token string" style="color:rgb(255, 121, 198)">'in_stock = true'</span><span class="token punctuation" style="color:rgb(248, 248, 242)">,</span><span class="token plain"></span><br></span><span class="token-line" style="color:#F8F8F2"><span class="token plain">  </span><span class="token number">0.4</span><span class="token plain"></span><br></span><span class="token-line" style="color:#F8F8F2"><span class="token plain"></span><span class="token punctuation" style="color:rgb(248, 248, 242)">)</span><span class="token punctuation" style="color:rgb(248, 248, 242)">;</span><br></span></code></pre></div></div>
<p>The result contains the matching corpus rows, the query row metadata, and the computed distance. Hudi omits the embedding columns from the output because applications usually need the matching records and scores, not the raw vectors.</p>
<p>Batch search also adds <code>_hudi_query_index</code>, which identifies which query row produced each result. This is useful because the output contains many groups of top-k results: one group per query row.</p>
<p>Put simply:</p>
<ul>
<li class=""><code>hudi_vector_search</code> returns the nearest rows for one query.</li>
<li class=""><code>hudi_vector_search_batch</code> returns the nearest rows for each query in a query table.</li>
</ul>
<p>Batch search is especially useful for offline or scheduled workloads: related-product generation, duplicate detection, content matching, recommendation refreshes, and document similarity jobs.</p>
<h2 class="anchor anchorTargetStickyNavbar_Vzrq" id="how-the-brute-force-implementation-works">How the brute-force implementation works<a href="https://hudi.apache.org/blog/2026/07/06/bringing-vector-search-to-the-lakehouse-with-apache-hudi#how-the-brute-force-implementation-works" class="hash-link" aria-label="Direct link to How the brute-force implementation works" title="Direct link to How the brute-force implementation works" translate="no">​</a></h2>
<p>The previous section introduced the SQL interface for single-query and batch vector search. The next natural question is: what happens when those queries run?</p>
<div style="text-align:center"><img src="https://hudi.apache.org/assets/images/blog/2026-07-06-bringing-vector-search-to-the-lakehouse-with-apache-hudi/5-brute-force-vector-search.png" alt="Distributed execution of a single-query vector search: the driver broadcasts the query vector to executors, each executor scans its Hudi table partitions and keeps a local top-k, and the driver merges local results into the global top-k." width="1000"></div>
<p>For a single-query search, the execution flow is:</p>
<ol>
<li class="">Spark receives the query vector and broadcasts it to executors.</li>
<li class="">Each executor scans its portion of the corpus.</li>
<li class="">For each row, the executor computes the distance between the query vector and the row's embedding.</li>
<li class="">Each executor keeps only its local top <code>k</code> matches.</li>
<li class="">The driver merges those local top-k results into the final global top-k result.</li>
</ol>
<p>This keeps executor memory bounded by <code>k</code>, rather than by the total size of the corpus. Even if an executor scans millions of rows, it only needs to keep the best candidates it has seen so far.</p>
<p>The main cost is distance computation. If the query searches 50 million embeddings, Hudi has to compare the query vector against 50 million candidate vectors. If the query first filters the corpus down to 200,000 relevant rows, the amount of distance computation drops dramatically.</p>
<p>That is why the filter argument in <code>hudi_vector_search</code> matters. It lets applications apply structured predicates before vector distance computation runs. For example, an ecommerce query may search only products where <code>category = 'electronics'</code> and <code>in_stock = true</code>, rather than searching the entire product catalog.</p>
<p>Batch search follows the same idea, but repeats it for many query vectors in one distributed job. Instead of asking, "what are the nearest products for this one shopper query?", batch search asks, "what are the nearest products for every row in this query table?"</p>
<p>For example, a nightly recommendation job might take every active product as a query row and find the top 20 related products for each one. Hudi compares each query embedding against the filtered corpus, ranks matches per query, and returns one group of top-k results for each query row.</p>
<p>Because batch search multiplies the amount of work by the number of query rows, the candidate set matters even more. A small query table searched against a well-filtered corpus can be practical. A large query table searched against an unfiltered corpus can become expensive quickly. This is why batch search is best suited for offline or scheduled workloads such as related-product generation, duplicate detection, content matching, and recommendation refreshes.</p>
<p>The <code>max_distance</code> argument is useful in both single-query and batch search. It removes weak matches above a distance threshold before returning results. In batch jobs, this can reduce output size significantly because each query row may otherwise produce up to <code>k</code> results, even when some matches are not useful.</p>
<p>This is why vector search becomes more powerful when it is integrated with table semantics. Many applications do not want nearest neighbors across all data. They want nearest neighbors within a meaningful slice of the table: active products, recent documents, records for a specific customer, listings in a region, or items that pass business rules. It is most useful when combined with the same filtering, pruning, and table layout techniques that data systems already use to reduce the amount of data read.</p>
<h2 class="anchor anchorTargetStickyNavbar_Vzrq" id="future-vector-index">Future Vector index<a href="https://hudi.apache.org/blog/2026/07/06/bringing-vector-search-to-the-lakehouse-with-apache-hudi#future-vector-index" class="hash-link" aria-label="Direct link to Future Vector index" title="Direct link to Future Vector index" translate="no">​</a></h2>
<p>The SQL interface is designed to stay stable as new algorithms are added. Today, <code>algorithm =&gt; 'brute_force'</code> uses a distributed linear scan. In the future, indexed implementations such as IVF-PQ can replace the scan with an index lookup plan.</p>
<p>In that model, Hudi would use a <a href="https://github.com/apache/hudi/issues/18676?issue=apache%7Chudi%7C18851" target="_blank" rel="noopener noreferrer" class="">vector index</a> to produce candidate record IDs and distances, then join those candidates back to the table to return the same output shape. Applications written against the current table-valued function interface should not need to change when switching from brute-force search to indexed search.</p>
<h2 class="anchor anchorTargetStickyNavbar_Vzrq" id="conclusion">Conclusion<a href="https://hudi.apache.org/blog/2026/07/06/bringing-vector-search-to-the-lakehouse-with-apache-hudi#conclusion" class="hash-link" aria-label="Direct link to Conclusion" title="Direct link to Conclusion" translate="no">​</a></h2>
<p>For the past few years, adding semantic search to a lakehouse application usually meant adding a separate vector database, copying embeddings into it, and maintaining a sync pipeline between the two systems. That architecture works, but it adds operational complexity at exactly the point where correctness matters: keeping records, embeddings, metadata, deletes, and updates aligned.</p>
<p>Hudi vector search takes a different approach. Embeddings are stored in the same table as the rest of the row, with the same schema, snapshots, updates, and table services. Vector search becomes part of the query layer instead of a separate sidecar system that has to be synchronized with the lakehouse.</p>
<p>The first implementation focuses on making this model simple and usable: a <code>VECTOR</code> type for embeddings, SQL functions for single-query and batch vector search, and integration with Hudi's existing table semantics. This is especially useful for RAG, document intelligence, recommendation refreshes, deduplication, clustering, and similarity search jobs where correctness, freshness, and SQL composability matter as much as raw serving latency.</p>
<p>The best way to understand the model is to try it directly. Start with <a href="https://hudi.apache.org/blog/2026/06/07/apache-hudi-release-1-2-announcement/" target="_blank" rel="noopener noreferrer" class="">Hudi 1.2</a>, create a table with a <code>VECTOR</code> column, load a small set of embeddings, and run your first <code>hudi_vector_search</code> query. From there, try adding structured filters, experimenting with <code>hudi_vector_search_batch</code>, or materializing similarity results back into a Hudi table.</p>
<p>If you are building AI applications on lakehouse data, this is the core idea to test: semantic search does not have to live outside the table. With Hudi, embeddings, metadata, updates, and search can move through the same data system.</p>]]></content:encoded>
            <category>vector</category>
            <category>vector search</category>
            <category>ai</category>
            <category>rag</category>
            <category>lakehouse</category>
            <category>data lakehouse</category>
            <category>apache spark</category>
            <category>architecture</category>
            <category>querying</category>
        </item>
        <item>
            <title><![CDATA[Building Indexes on a Moving Target]]></title>
            <link>https://hudi.apache.org/blog/2026/06/25/building-indexes-on-a-moving-target</link>
            <guid>https://hudi.apache.org/blog/2026/06/25/building-indexes-on-a-moving-target</guid>
            <pubDate>Thu, 25 Jun 2026 00:00:00 GMT</pubDate>
            <description><![CDATA[Async indexing in Hudi lets you add a new index to a multi-petabyte table without stopping ingestion — and the design only works because MOR put the right primitives in place years earlier.]]></description>
            <content:encoded><![CDATA[<hr>
<p><em>This is the third post in a series on Merge-On-Read as an architectural shift in Apache Hudi. The <a class="" href="https://hudi.apache.org/blog/2026/05/14/mor-isnt-a-storage-optimization-its-an-architectural-shift">first post</a> made the broad case; the <a class="" href="https://hudi.apache.org/blog/2026/06/05/why-metadata-has-to-be-mutation-friendly">second</a> showed how the metadata table inherits the same append-first model; this one follows the pattern one step further — into how new indexes can be added to a live table without stopping it.</em></p>
<hr>
<div style="font-size:1.15rem;line-height:1.75"><p>The first post in this series argued that Merge-On-Read is not a storage optimization but an <a class="" href="https://hudi.apache.org/blog/2026/05/14/mor-isnt-a-storage-optimization-its-an-architectural-shift">architectural shift</a> — a strategy for time-shifting work in systems where mutation and analytical scan run at different rhythms. The second grounded that argument in Hudi's metadata table, showing how metadata itself became another <a class="" href="https://hudi.apache.org/blog/2026/06/05/why-metadata-has-to-be-mutation-friendly">append-first workload</a>. <a class="" href="https://hudi.apache.org/docs/metadata_indexing">Async indexing</a> extends the same architectural pattern one step further: enabling new capabilities to be introduced into a live table without stopping the system.</p><p>This article assumes some familiarity with Hudi's metadata table and its indexing capabilities. If you're new to the topic, the motivation for metadata indexes, their role in query planning, and the evolution of the metadata table are covered in detail in <a href="https://github.com/apache/hudi/blob/master/rfc/rfc-45/rfc-45.md" target="_blank" rel="noopener noreferrer" class="">RFC-45</a> as well as our earlier articles on the <a class="" href="https://hudi.apache.org/docs/metadata">metadata table</a> and <a class="" href="https://hudi.apache.org/blog/2026/06/05/why-metadata-has-to-be-mutation-friendly">mutation-friendly metadata</a>. Here, we focus on a different question: <strong>once a table is already serving production writes, how can a new metadata index be introduced without interrupting ingestion or exposing inconsistent state?</strong></p><p>The title is literal. In Hudi, you can build a new index — record-level, secondary, expression, column stats, bloom filter — over an existing multi-petabyte table that doesn't stop while you build it. Ingest writers keep committing. No outage. No drained pipeline. No swap window. The bootstrap reads historical state into a freshly initialized metadata partition; concurrent writers route new mutations into the same partition's file groups; a reconciliation step at the end stitches the seam.</p><p>That capability does not exist in any other open lakehouse format at the spec level — not because nobody implemented it, but because the storage layer every other format is built on cannot express the pattern. Async indexing is not a feature added to Hudi; it is a consequence of decisions Hudi made in 2017. The only systems that can build indexes this way are the ones whose mutation model is already append-first.</p><p><a href="https://github.com/apache/hudi/blob/master/rfc/rfc-45/rfc-45.md" target="_blank" rel="noopener noreferrer" class="">RFC-45</a> is the design doc, and the <a href="https://www.onehouse.ai/blog/asynchronous-indexing-using-hudi" target="_blank" rel="noopener noreferrer" class="">Onehouse engineering blog</a> walks through the mechanics. Those explain <em>what</em> async indexing does and <em>how</em> it's wired in. This post is about why the design works at all — what architectural primitives have to exist underneath before something like it becomes a coherent thing to build.</p><hr><h2 class="anchor anchorTargetStickyNavbar_Vzrq" id="the-shape-of-the-problem">The Shape of the Problem<a href="https://hudi.apache.org/blog/2026/06/25/building-indexes-on-a-moving-target#the-shape-of-the-problem" class="hash-link" aria-label="Direct link to The Shape of the Problem" title="Direct link to The Shape of the Problem" translate="no">​</a></h2><p>Adding a new index over an existing large table has always been an operationally expensive thing for analytical storage to do. Scan every row, project the indexed columns or compute the indexed expression, materialize the index. The trouble is that "scan every row" of a multi-petabyte table takes hours, and during those hours the table either has to stop accepting writes or has to coordinate with a table that won't sit still.</p><p>Two responses dominate the lakehouse conversation, and both inherit the limitations of their foundation.</p><p>The first is to bind the index to the write path: compute the value at ingestion time and write it alongside the data — column-level statistics, min/max metadata, file summaries. This is what Iceberg and Delta do by default. It works, but it cannot retroactively add an index. If the table was written for two years without it, the write path can't help; the data is already on disk in its final form.</p><p>The second is to lock-and-rebuild: global or per-partition lock, scan, materialize, atomic swap, release. Fine at small scale. At petabyte scale on a continuously ingesting table the lock duration is hours, the ingest pipeline has to stop or buffer indefinitely, and any failure during the rebuild discards the work.</p><p>Hudi's predicament when <a href="https://cwiki.apache.org/confluence/pages/viewpage.action?pageId=147427331" target="_blank" rel="noopener noreferrer" class="">RFC-15</a> introduced the metadata table was that MDT was going to host indexes — <code>files</code>, <code>column_stats</code>, <code>bloom_filters</code>, <code>record_index</code>, <code>partition_stats</code>, <code>secondary_index</code>, <code>expression_index</code>. Every one of those would eventually need to be added over an existing table by someone who'd been running Hudi for a year before the index existed. If index addition required taking the table offline, the metadata table's growth story collapses to <em>enable everything at table creation or never</em>. That isn't a metadata layer; that's a one-shot schema decision. <a href="https://github.com/apache/hudi/blob/master/rfc/rfc-45/rfc-45.md" target="_blank" rel="noopener noreferrer" class="">RFC-45</a> is the document that fixes this.</p><hr><h2 class="anchor anchorTargetStickyNavbar_Vzrq" id="what-cow-makes-impossible--not-hard-impossible">What COW Makes Impossible — Not Hard, Impossible<a href="https://hudi.apache.org/blog/2026/06/25/building-indexes-on-a-moving-target#what-cow-makes-impossible--not-hard-impossible" class="hash-link" aria-label="Direct link to What COW Makes Impossible — Not Hard, Impossible" title="Direct link to What COW Makes Impossible — Not Hard, Impossible" translate="no">​</a></h2><p>Imagine implementing async indexing on a Copy-On-Write storage layer — a generic columnar lakehouse format where the unit of mutation is the file rewrite. Bootstrap scans history, computes, materializes. Concurrent writers continue ingesting. Index has to be queryable at the end.</p><p>The bootstrap is slow under any architecture; that's not what breaks. What breaks is that there is no place for concurrent writers to put their contribution to the new index while the bootstrap is running.</p><p>Under COW, every index update is a rewrite of a finalized index file. There is no append channel. If the indexer has materialized <code>idx_000.parquet</code> for history up to <code>t</code>, and a writer commits at <code>t+1</code>, the writer cannot contribute to <code>idx_000.parquet</code>. The writer's only option is <code>idx_001.parquet</code> — which means the bootstrap has to coordinate with the writer about which file was produced, validate it, possibly merge it. That coordination is the synchronization we were trying to avoid. The deeper problem is the absence of a stable logical container: the index isn't something the writer can point at and append into; it's a collection of physical files whose ownership has to be negotiated.</p><p>The workaround in rewrite-oriented systems is <em>bootstrap-and-swap</em>: build against a frozen snapshot at <code>t</code>; writers ingest normally but don't touch the new index; at <code>t + Δ</code>, replay all writes between <code>t</code> and <code>t + Δ</code> into the index, then atomically swap. This works, but the replay window has no upper bound (if ingest exceeds replay rate, catchup never converges), and the index is unavailable for query during the entire bootstrap-plus-replay duration.</p><p>Hudi doesn't need bootstrap-and-swap because the foundation offers something neither Iceberg nor Delta has: a stable file group with an append channel, and a timeline that expresses index construction as a first-class action with its own reconciliation semantics. Both primitives are downstream of MOR.</p><p>Before examining the architectural primitives individually, it's helpful to look at the end-to-end lifecycle of an async index build. The figure below intentionally presents the high-level execution flow rather than the implementation details. For readers interested in the complete design, state-machine transitions, and failure handling, <a href="https://github.com/apache/hudi/blob/master/rfc/rfc-45/rfc-45.md" target="_blank" rel="noopener noreferrer" class="">RFC-45</a> provides the authoritative specification. Here, we focus on the architectural primitives that make the design possible.</p><div style="text-align:center"><img src="https://hudi.apache.org/assets/images/blog/2026-06-25-building-indexes-on-a-moving-target/async-indexing-end-end-flow.png" alt="Async indexing end-to-end flow" width="1000"></div><hr><h2 class="anchor anchorTargetStickyNavbar_Vzrq" id="the-three-primitives-all-of-them-mor-consequences">The Three Primitives, All of Them MOR Consequences<a href="https://hudi.apache.org/blog/2026/06/25/building-indexes-on-a-moving-target#the-three-primitives-all-of-them-mor-consequences" class="hash-link" aria-label="Direct link to The Three Primitives, All of Them MOR Consequences" title="Direct link to The Three Primitives, All of Them MOR Consequences" translate="no">​</a></h2><p>Async indexing in Hudi rests on three substrate-level primitives. Each one is something the MOR design forced into existence for other reasons, and each one is what makes RFC-45's design coherent rather than a heroic engineering effort against the storage layout.</p><h3 class="anchor anchorTargetStickyNavbar_Vzrq" id="primitive-1-the-file-group-as-a-stable-mutation-container">Primitive 1: The File Group as a Stable Mutation Container<a href="https://hudi.apache.org/blog/2026/06/25/building-indexes-on-a-moving-target#primitive-1-the-file-group-as-a-stable-mutation-container" class="hash-link" aria-label="Direct link to Primitive 1: The File Group as a Stable Mutation Container" title="Direct link to Primitive 1: The File Group as a Stable Mutation Container" translate="no">​</a></h3><p>The <a class="" href="https://hudi.apache.org/docs/storage_layouts">file group</a> is a stable logical mutation container. Once initialized, multiple independent processes — not just ingest writers, but background services such as index builders — can deterministically contribute mutations into the same logical object while deferring physical reconciliation. Async indexing is one application of this primitive. Later in the series, we'll see the same stable mutation containers reused by other Hudi capabilities, including async compaction and eventually non-blocking concurrency control.</p><p>When the indexer initializes a new metadata partition, it creates empty file groups for that partition. From that moment, they exist on disk and are addressable. A writer that sees the partition is inflight (via table config) can immediately append log blocks into those file groups, even though base files haven't been written yet. The writer doesn't need to know whether the indexer has finished the base file or coordinate physical layout at all. It needs only to know which file group to route into — a deterministic function of the record key (via bucket index or RLI's hash routing).</p><p>Under COW this primitive doesn't exist. There's no "container the indexer prepares and writers append into" — only files, produced atomically and consumed read-only. The closest analog is a shared directory both processes write new files into; but that's a namespace, not a container, and the synchronization problem returns immediately.</p><h3 class="anchor anchorTargetStickyNavbar_Vzrq" id="primitive-2-indexing-as-a-first-class-timeline-action">Primitive 2: INDEXING as a First-Class Timeline Action<a href="https://hudi.apache.org/blog/2026/06/25/building-indexes-on-a-moving-target#primitive-2-indexing-as-a-first-class-timeline-action" class="hash-link" aria-label="Direct link to Primitive 2: INDEXING as a First-Class Timeline Action" title="Direct link to Primitive 2: INDEXING as a First-Class Timeline Action" translate="no">​</a></h3><p><a class="" href="https://hudi.apache.org/docs/timeline">Hudi's timeline</a> is an append-only event log: commits, deltacommits, cleans, compactions, restores, rollbacks. RFC-45 adds <strong>INDEXING_ACTION</strong>. An index build is not a side process; it is represented as its own sequence of timeline instants: <code>&lt;t&gt;.indexing.requested</code>, <code>&lt;t&gt;.indexing.inflight</code>, and <code>&lt;t&gt;.indexing</code> (completed), alongside data writes and other table services.</p><p>That placement matters. Because the action lives on the timeline, concurrent writers discover it through the same timeline refresh they already perform for commits and table services; no separate coordination channel is required. Because it follows the standard requested → inflight → completed lifecycle, existing mechanisms for recovery, rollback, and lock-protected state transitions apply without introducing a parallel state machine. Async indexing is built by extending the timeline rather than bypassing it.</p><p>More importantly, the timeline and table configuration together govern <strong>visibility</strong>, not just execution. Bootstrap and Catchup may execute for minutes while the table continues accepting writes, but the new index remains invisible to readers throughout that period.</p><p>Internally, the indexing action progresses through the standard <code>requested → inflight → completed</code> lifecycle on the timeline, while the table configuration independently tracks whether the metadata partition is still under construction (<code>metadata.partitions.inflight</code>) or has become queryable (<code>metadata.partitions</code>). Readers observe the new index only after both transitions have completed. This separation is subtle but fundamental. Bootstrap and Catchup describe <strong>how</strong> the index is constructed. The timeline and table configuration determine <strong>when</strong> that construction becomes visible. By separating progress from publication, Hudi allows long-running background work to proceed without exposing partially constructed metadata to readers.</p><p>Although async indexing predates the completion-time semantics introduced later for non-blocking concurrency control, it already exhibits the same architectural pattern: background work progresses independently, reconciliation completes asynchronously, and publication is deferred until the system can present a consistent view. Later in the series, we'll see how Hudi generalizes this idea for concurrent writers using completion-time ordering.</p><h3 class="anchor anchorTargetStickyNavbar_Vzrq" id="primitive-3-two-stage-partition-lifecycle-in-table-config">Primitive 3: Two-Stage Partition Lifecycle in Table Config<a href="https://hudi.apache.org/blog/2026/06/25/building-indexes-on-a-moving-target#primitive-3-two-stage-partition-lifecycle-in-table-config" class="hash-link" aria-label="Direct link to Primitive 3: Two-Stage Partition Lifecycle in Table Config" title="Direct link to Primitive 3: Two-Stage Partition Lifecycle in Table Config" translate="no">​</a></h3><p>The subtlest of the three, and the one most directly inherited from MOR. <code>hoodie.properties</code> carries two related fields: <code>hoodie.table.metadata.partitions.inflight</code> lists partitions initialized and receiving writes but not yet queryable; <code>hoodie.table.metadata.partitions</code> lists partitions fully built and safe to read.</p><p>That split is what makes async indexing safe under split-brain reader/writer views. When scheduling completes — file groups initialized, plan persisted — the partition is added to <code>*_INFLIGHT</code>. Writers see it and start routing log blocks. Readers see it too, and ignore the partition: table config is the source of truth for query readiness. Only after the bootstrap base is written, Catchup reconciles any missing metadata updates from concurrent commits, and the indexing action completes does the partition move from <code>metadata.partitions.inflight</code> to <code>metadata.partitions</code>.</p><p>The two-stage config is MOR's base-file-vs-log-file separation lifted to the partition level. Log files are writable before the next base file is materialized; readers reconcile both at query time. Inflight partitions are writable before the bootstrap completes; readers ignore them until done. Same idea, different scope. You can see this in code at <code>RunIndexActionExecutor.updateTableConfigAndTimeline</code>: the partition transitions from inflight to active as part of the indexing completion sequence, allowing the new index to become visible only after both the timeline action and table configuration reflect a completed build.</p><p>The abort path at <code>RunIndexActionExecutor.abort</code> removes the partition from both lists and deletes the physical partition; on any build failure, the partition reverts cleanly to "doesn't exist."</p><p>Because both scheduling and completion execute under Hudi's timeline lock provider, these metadata transitions remain serialized even though the bootstrap itself proceeds concurrently with foreground writers.</p><p>Interestingly, these primitives turn out to be useful well beyond index construction. Stable mutation containers, timeline-ordered visibility, and deferred reconciliation reappear repeatedly throughout Hudi's architecture. Async indexing is one application. Later in the series, we'll see the same foundations support background optimization and eventually reshape how concurrent writers coordinate against the same data.</p><div style="text-align:center"><img src="https://hudi.apache.org/assets/images/blog/2026-06-25-building-indexes-on-a-moving-target/async-indexing-architectural-consequence.png" alt="Async indexing as an architectural consequence of MOR" width="1000"></div><hr><h2 class="anchor anchorTargetStickyNavbar_Vzrq" id="indexing-catchup-as-deferred-reconciliation-generalized">Indexing Catchup as Deferred Reconciliation, Generalized<a href="https://hudi.apache.org/blog/2026/06/25/building-indexes-on-a-moving-target#indexing-catchup-as-deferred-reconciliation-generalized" class="hash-link" aria-label="Direct link to Indexing Catchup as Deferred Reconciliation, Generalized" title="Direct link to Indexing Catchup as Deferred Reconciliation, Generalized" translate="no">​</a></h2><p>The three primitives set up the most interesting piece of the design: indexing catchup. The implementation lives in <code>AbstractIndexingCatchupTask</code>, orchestrated by <code>RunIndexActionExecutor</code>. It's where the MOR-as-foundation argument becomes concrete.</p><p>The indexing plan is scheduled against a bootstrap target — the latest completed data-table instant at scheduling time. Bootstrap builds the historical portion of the index by scanning the table up to that point. Meanwhile, the data timeline continues advancing.</p><p>A common misconception is that Bootstrap alone constructs the index. In reality, every commit that arrives while Bootstrap is running is equally part of the index build — it simply reaches the metadata partition through the incremental write path rather than the historical scan. Writers that observe the indexing action after it becomes inflight begin appending metadata updates into the new file groups. By the time Bootstrap completes, the metadata partition consists of a historical base together with an unknown amount of concurrent incremental state.</p><p>The indexer cannot simply assume that every eligible commit has been reflected in the metadata timeline. Some writers may have started before the indexing action became visible. Others may still be inflight. Catchup exists to reconcile that uncertainty before the index becomes visible.</p><p>Catchup walks the relevant portion of the data timeline and compares each eligible data-table instant against the metadata timeline using the indexing action's requested-time identifier. If the corresponding metadata update already exists, the writer has already contributed its portion of the index. Otherwise, Catchup synthesizes the missing metadata update on the writer's behalf.</p><p>Inflight writers receive special treatment. Rather than immediately treating them as missing, Catchup waits for active writers to complete while monitoring their heartbeats. Writers that complete normally are incorporated through the usual reconciliation path. Writers whose heartbeats have expired are treated as abandoned and skipped, allowing index construction to converge without waiting indefinitely. The wait itself is bounded by <code>hoodie.metadata.index.check.timeout.seconds</code> (900 seconds by default), after which the indexing operation aborts and the partially constructed partition is discarded.</p><p>This is deferred reconciliation in its purest form. Bootstrap constructs historical state. Concurrent writers continue making forward progress. Catchup reconciles any remaining divergence between the data timeline and metadata timeline before publication. Readers never observe intermediate state.</p><blockquote>
<p>The pattern is exactly what Merge-On-Read readers already do for every file group: reconcile a stable base with subsequent incremental mutations to produce a consistent view. Async indexing applies the same architectural idea at the scope of an entire metadata partition.</p>
</blockquote><p>The mechanism is different, but the underlying principle is identical: foreground progress continues independently, while background reconciliation converges the system before publication.</p><hr><h2 class="anchor anchorTargetStickyNavbar_Vzrq" id="the-races-and-how-the-architecture-absorbs-them">The Races, and How the Architecture Absorbs Them<a href="https://hudi.apache.org/blog/2026/06/25/building-indexes-on-a-moving-target#the-races-and-how-the-architecture-absorbs-them" class="hash-link" aria-label="Direct link to The Races, and How the Architecture Absorbs Them" title="Direct link to The Races, and How the Architecture Absorbs Them" translate="no">​</a></h2><p>RFC-45 enumerates four error cases. Each one is a place where synchronous coordination would have required a lock, and each one is handled instead by deferred reconciliation against timeline state. They're worth walking through because they make the architectural argument concrete.</p><p><strong>Writer fails while the indexer is inflight.</strong> The writer crashed before logging its update. The indexer doesn't care. Catchup observes the failed instant, sees no metadata deltacommit, and either skips it (if data-table rollback already cleaned up) or fills it in. The metadata partition isn't corrupted because reader visibility hasn't flipped yet.</p><p><strong>Indexer fails while a writer is inflight.</strong> The writer continues logging into the new file groups. The partition stays in <code>*_INFLIGHT</code> because no commit metadata was written. Readers continue ignoring it. When the indexer is re-triggered, it sees the partition is inflight, picks up the build (idempotent by partition name in <code>ScheduleIndexActionExecutor</code>), and proceeds. The writer's log files aren't lost — they're already in the file group, waiting for catchup.</p><p><strong>Writer goes inflight just after the indexing request, before the indexer executes.</strong> A writer begins after the indexing action becomes visible and therefore participates in index construction from the outset. By bootstrap completion, log files already exist. Catchup observes them, sees the corresponding deltacommit, moves on. No coordination — writer and indexer were independently pointing at the same stable container.</p><p><strong>Inflight writer about to commit but indexing just completed.</strong> The trickiest race. The writer started knowing the partition was inflight; during its execution the partition flipped to completed. The writer commits, producing a log file in the now-completed partition, and the indexer missed it. Because the reader engine is designed from day one to merge base + logs on the fly, <strong>the reader's merge layer inherently immunizes the system against race conditions.</strong> The storage layout itself acts as the concurrency buffer. The reader's merge logic doesn't know the difference between a log file produced during catchup and one produced just after. RFC-45 discusses an alternative design based on locking metadata-timeline appends, but explicitly rejects it in favor of the eventual-consistency path adopted by the implementation. The existing design relies on deferred reconciliation rather than stronger synchronization.</p><p>In each case, the response shape is the same: stable container, timeline record, reconciliation at the next quiet moment. None of this is async-indexing-specific. It's what every MOR table service does.</p><p>Notice that none of these cases require the bootstrap itself to synchronize with foreground writers. Synchronization is reserved for timeline state transitions, while correctness is achieved through deferred reconciliation before publication.</p><hr><h2 class="anchor anchorTargetStickyNavbar_Vzrq" id="why-this-isnt-expressible-in-rewrite-oriented-formats">Why This Isn't Expressible in Rewrite-Oriented Formats<a href="https://hudi.apache.org/blog/2026/06/25/building-indexes-on-a-moving-target#why-this-isnt-expressible-in-rewrite-oriented-formats" class="hash-link" aria-label="Direct link to Why This Isn't Expressible in Rewrite-Oriented Formats" title="Direct link to Why This Isn't Expressible in Rewrite-Oriented Formats" translate="no">​</a></h2><p>Can async indexing be added to Iceberg or Delta with sufficient engineering effort? It can be approximated, but the approximation either gives up the "no synchronization with writers" property or requires inventing the three primitives above on a foundation that doesn't naturally support them.</p><p>Iceberg has manifests, manifest lists, snapshots — no stable logical container with an append channel. Each commit produces new manifest files; there's no "manifest the indexer prepares and writers contribute to." Row lineage and v3 deletion vectors give stable row identity, but that's not the same primitive as a stable append container. Adding async indexing to Iceberg would require a new manifest layout where indexers stage skeletons that subsequent writers contribute to — a spec change, not a feature.</p><p>Delta has the transaction log, deletion vectors, and (in Databricks Runtime) row-level concurrency for non-overlapping MERGE/UPDATE/DELETE. None provide an append container stable across commits. Coordinated commits in 4.0 improve multi-engine atomic commit, but the unit of commit is still a set of new files; there's no notion of writers appending into a file the indexer staged. Delta's data skipping and statistics are written during the data commit; they cannot be retroactively built without bootstrap-and-swap.</p><p>Async indexing isn't a missing feature in those formats; it's missing because the foundation cannot express the pattern. Adding it would require lifting append-first mutation into the storage model itself. That is most of what MOR is.</p><p>The "async-indexing-ish" capabilities those formats have shipped are either write-path additions (compute during writes, can't retroactively add) or batch-and-swap (block readers during a window, schedule a maintenance window). Neither is what Hudi async indexing actually does.</p><hr><h2 class="anchor anchorTargetStickyNavbar_Vzrq" id="what-this-unlocks-operationally">What This Unlocks Operationally<a href="https://hudi.apache.org/blog/2026/06/25/building-indexes-on-a-moving-target#what-this-unlocks-operationally" class="hash-link" aria-label="Direct link to What This Unlocks Operationally" title="Direct link to What This Unlocks Operationally" translate="no">​</a></h2><p>The architectural argument lands on a small set of capabilities that change how teams operate Hudi tables.</p><p>The most immediate: <em>adding indexes to existing tables without a maintenance window.</em> RLI on a 1 PB table for primary-key lookup. Secondary index on <code>customer_email</code> for a query pattern that wasn't important when the table was created. Expression index on <code>LOWER(country_code)</code> because the workload changed. Issue <code>CREATE INDEX</code> (or run <code>HoodieIndexer</code>) against the running table. Bootstrap fills history. Writers continue. Catchup closes the seam.</p><p>Second: <em>index re-initialization as a routine operation.</em> If an index gets into a bad state — schema evolution mismatch, corrupted base file, missed catchup window — recovery is "delete the partition, re-run the indexer." Not a migration event. The same <code>ScheduleIndexActionExecutor.abort</code> path handles cleanup before re-initialization.</p><p>Third: <em>staged index rollouts.</em> You don't have to enable every metadata partition at table creation. Enable <code>files</code> and <code>column_stats</code> initially, add <code>record_index</code> later when point-lookup load justifies it, <code>secondary_index</code> later still when a new query pattern emerges. The metadata table's footprint grows with the workload's actual needs. A table running for years can adopt a new index without dropping a write.</p><p>None of these are theoretical. Take any one primitive away — make the file group non-append, take INDEXING off the timeline, collapse the two-stage partition config — and the operational story collapses with it.</p><hr><h2 class="anchor anchorTargetStickyNavbar_Vzrq" id="what-mor-ultimately-made-possible">What MOR Ultimately Made Possible<a href="https://hudi.apache.org/blog/2026/06/25/building-indexes-on-a-moving-target#what-mor-ultimately-made-possible" class="hash-link" aria-label="Direct link to What MOR Ultimately Made Possible" title="Direct link to What MOR Ultimately Made Possible" translate="no">​</a></h2><p>A single architectural decision — append-first mutation with deferred reconciliation — produces a family of capabilities that look like independent features but are the same idea applied to different scopes. The metadata table is MOR because the commit cadence demands it. Async indexing is possible because the same stable mutation containers can be initialized empty and evolved by concurrent contributors.</p><p>Looked at in isolation, each capability has its own moving parts: RFC-15 for MDT, RFC-45 for async indexing, and later in the series we'll examine how Hudi extends the same architectural principles — stable mutation containers, deferred reconciliation, and timeline-mediated publication — to support non-blocking concurrency control. The descriptions are all correct. They miss the bigger point, which is that the moving parts exist because MOR established the underlying mutation model — and the same model gets reused every time a new operational requirement shows up.</p></div>]]></content:encoded>
            <category>mor</category>
            <category>merge on read</category>
            <category>indexing</category>
            <category>async indexing</category>
            <category>metadata</category>
            <category>architecture</category>
            <category>data lakehouse</category>
        </item>
        <item>
            <title><![CDATA[Accelerating Data Operations: Metica's Journey with Apache Hudi]]></title>
            <link>https://hudi.apache.org/blog/2026/06/15/apache-hudi-at-metica</link>
            <guid>https://hudi.apache.org/blog/2026/06/15/apache-hudi-at-metica</guid>
            <pubDate>Mon, 15 Jun 2026 00:00:00 GMT</pubDate>
            <description><![CDATA[Metica built its first lakehouse from scratch on Apache Hudi and Amazon EMR — scaling from ~16 GB to billions of events without re-architecting, where every growth milestone became a config change, not a migration.]]></description>
            <content:encoded><![CDATA[<hr>
<p><em>This blog post summarizes Metica's presentation led by Subash Prabanantham at the Apache Hudi Community Sync. Watch the recording on <a href="https://www.youtube.com/watch?v=6B2hMK9FuoQ" target="_blank" rel="noopener noreferrer" class="">YouTube</a>.</em></p>
<hr>
<p><img decoding="async" loading="lazy" alt="og" src="https://hudi.apache.org/assets/images/img1-f589e305a835d189ad1b1b2c150b25ab.png" width="1200" height="630" class="img_ev3q"></p>
<div class="theme-admonition theme-admonition-tip admonition_xJq3 alert alert--success"><div class="admonitionHeading_Gvgb"><span class="admonitionIcon_Rf37"><svg viewBox="0 0 12 16"><path fill-rule="evenodd" d="M6.5 0C3.48 0 1 2.19 1 5c0 .92.55 2.25 1 3 1.34 2.25 1.78 2.78 2 4v1h5v-1c.22-1.22.66-1.75 2-4 .45-.75 1-2.08 1-3 0-2.81-2.48-5-5.5-5zm3.64 7.48c-.25.44-.47.8-.67 1.11-.86 1.41-1.25 2.06-1.45 3.23-.02.05-.02.11-.02.17H5c0-.06 0-.13-.02-.17-.2-1.17-.59-1.83-1.45-3.23-.2-.31-.42-.67-.67-1.11C2.44 6.78 2 5.65 2 5c0-2.2 2.02-4 4.5-4 1.22 0 2.36.42 3.22 1.19C10.55 2.94 11 3.94 11 5c0 .66-.44 1.78-.86 2.48zM4 14h5c-.23 1.14-1.3 2-2.5 2s-2.27-.86-2.5-2z"></path></svg></span>TL;DR</div><div class="admonitionContent_BuS1"><p>Metica, a B2B SaaS startup building AI-driven personalization for the gaming industry, built its first lakehouse from scratch on Apache Hudi running on Amazon EMR. Hudi sits on the "gold" layer of a medallion architecture on AWS, with StarRocks (via CelerData) querying the data in place. The team grew their data from ~16 GB to billions of events without re-architecting — turning each scaling milestone into a configuration change rather than a migration. By enabling inline clustering with sort columns, they cut S3 object counts from ~17K to ~3K (a ~6x storage reduction) and roughly halved query latency on StarRocks (from ~200 ms to ~90 ms on a 40M-row table). This post covers their architecture, the access patterns that drove Hudi adoption, the feature timeline they followed as they scaled, the performance wins, and what's next.</p></div></div>
<p>Managing data platforms in a B2B SaaS startup requires a fine balance between long-term reliability and architectural flexibility. As data grows unpredictably alongside new client acquisitions, engineering teams often face the challenge of constantly refactoring their storage and compute tiers.</p>
<p>At a recent Apache Hudi Community Sync, Subash Prabanantham, a lead data engineer at <a href="https://metica.com/" target="_blank" rel="noopener noreferrer" class="">Metica</a>, shared how his team navigated these exact challenges. Spun out of an experienced team of ex-Apple and ex-King engineers, Metica personalizes gaming experiences via specialized ML techniques, helping studios maximize player lifetime value (LTV) and revenue.</p>
<p>Below is an in-depth breakdown of Metica's data platform architecture, their evolutionary journey using Apache Hudi, practical performance optimizations, and lessons learned along the way.</p>
<h2 class="anchor anchorTargetStickyNavbar_Vzrq" id="about-metica">About Metica<a href="https://hudi.apache.org/blog/2026/06/15/apache-hudi-at-metica#about-metica" class="hash-link" aria-label="Direct link to About Metica" title="Direct link to About Metica" translate="no">​</a></h2>
<p><a href="https://metica.com/" target="_blank" rel="noopener noreferrer" class="">Metica</a> is a VC-backed B2B SaaS company (Play Ventures, Firstminute Capital) focused on personalizing every gaming experience. The team uses machine learning to deliver tailored offers, intelligent bundles, and contextual optimization, working together as an integrated suite that helps game developers and studios personalize for individual players — lifting player lifetime value, and with it, revenue.</p>
<h2 class="anchor anchorTargetStickyNavbar_Vzrq" id="why-a-lakehouse-and-why-hudi">Why a Lakehouse, and Why Hudi?<a href="https://hudi.apache.org/blog/2026/06/15/apache-hudi-at-metica#why-a-lakehouse-and-why-hudi" class="hash-link" aria-label="Direct link to Why a Lakehouse, and Why Hudi?" title="Direct link to Why a Lakehouse, and Why Hudi?" translate="no">​</a></h2>
<p>This was Metica's first lakehouse. Before it, the team worked primarily with plain Parquet and hand-built many of the features <a href="https://hudi.apache.org/" target="_blank" rel="noopener noreferrer" class="">Hudi</a> provides out of the box. When they designed a real data platform, they set four requirements:</p>
<ul>
<li class=""><strong>Open source and community-backed:</strong> Not a clever one-off project, but a format with an active community behind it.</li>
<li class=""><strong>Scales with uneven data growth:</strong> As a startup, clients of wildly different sizes arrive on unpredictable timelines. The platform had to stay stable whether the next customer was small or enormous.</li>
<li class=""><strong>Supports different query engines:</strong> Avoiding multiple copies of data was a hard requirement; they wanted a query engine that reads directly on top of the underlying lakehouse data.</li>
<li class=""><strong>Low Maintenance:</strong> Offers easier maintenance across both the platform and the data format.</li>
</ul>
<p>Apache Hudi was selected not just as a table format, but as a robust <em>data platform</em> that offered built-in table services, administrative tools like the Hudi CLI, and seamless ingestion utilities.</p>
<h2 class="anchor anchorTargetStickyNavbar_Vzrq" id="the-architecture-aws--apache-hudi">The Architecture: AWS + Apache Hudi<a href="https://hudi.apache.org/blog/2026/06/15/apache-hudi-at-metica#the-architecture-aws--apache-hudi" class="hash-link" aria-label="Direct link to The Architecture: AWS + Apache Hudi" title="Direct link to The Architecture: AWS + Apache Hudi" translate="no">​</a></h2>
<p>Metica implements a classic Medallion (Bronze/Silver/Gold) architecture entirely hosted on AWS. It decouples storage, compute, and cataloging to let each layer scale independently.</p>
<p><img decoding="async" loading="lazy" alt="Metica Medallion Architecture" src="https://hudi.apache.org/assets/images/img2-c3f4834bc86d1584893941bc189efdfb.png" width="2636" height="1458" class="img_ev3q"></p>
<p style="text-align:center;font-style:italic"></p><p>Metica's medallion architecture on AWS. Kinesis to raw JSON, Spark on EMR to Parquet, Apache Hudi for curated Gold tables, cataloged in Glue and served via CelerData.</p><p></p>
<ul>
<li class=""><strong>Ingestion &amp; Bronze Layer:</strong> Real-time event streams are gathered via Amazon Kinesis and journaled directly into Amazon S3 as raw JSON objects.</li>
<li class=""><strong>Processing (Silver):</strong> Spark and Spark Streaming on Amazon EMR apply minimal cleaning and transformation, writing transformed events to S3 as Parquet.</li>
<li class=""><strong>Curated Layer (Gold):</strong> Data products and aggregates are written as Apache Hudi tables. This is where mutability, indexing, and table services earn their keep.</li>
<li class=""><strong>Catalog &amp; Query Engine:</strong> Metadata is maintained through the AWS Glue Data Catalog. For the consumption layer, Metica leverages CelerData (a managed solution for StarRocks) as its primary query engine to achieve sub-second query latencies without duplicating data into an isolated data warehouse and serves BI, AI/ML, and analytics consumers.</li>
</ul>
<p>The most consequential decision sits at the query layer. In prior architectures the team had seen, a data lake fed a separate data warehouse (Snowflake, Teradata) — an extra hop, an extra copy, and a freshness lag between the two. Metica wanted to query lakehouse data in place, without copying it into a warehouse. After benchmarking, StarRocks delivered the sub-second latencies their reporting workloads needed while reading straight off Hudi tables.</p>
<p>One deliberate choice: bronze and silver aren't Hudi yet. The team kept the early layers as JSON and Parquet for flexibility, proving the table format out on the gold layer first. Ingestion is currently immutable (overwriting partitions) and there's no incremental processing in the pipeline. The plan is that when incremental processing arrives, flipping silver and bronze over to Hudi should be a configuration change — not an architectural migration.</p>
<h2 class="anchor anchorTargetStickyNavbar_Vzrq" id="the-use-case-personalization-data-and-access-patterns">The Use Case: Personalization Data and Access Patterns<a href="https://hudi.apache.org/blog/2026/06/15/apache-hudi-at-metica#the-use-case-personalization-data-and-access-patterns" class="hash-link" aria-label="Direct link to The Use Case: Personalization Data and Access Patterns" title="Direct link to The Use Case: Personalization Data and Access Patterns" translate="no">​</a></h2>
<p>Before adopting Hudi, the team mapped each top-level use case to the nature of its data and the operations performed on it. This framing drove where Hudi added value:</p>
<p><img decoding="async" loading="lazy" alt="Personalization Data and Access Patterns" src="https://hudi.apache.org/assets/images/img3-c5ee717a8b0fd51c293c53c777870271.png" width="2346" height="908" class="img_ev3q"></p>
<p style="text-align:center;font-style:italic"></p><p>Each use case mapped to its data nature and operations, showing where Hudi's mutability, snapshot reads, and time travel fit.</p><p></p>
<p>Two of these illustrate why a table format was necessary. Experimentation data is fine-grained — user-level and event-level, sliced across many dimensions — producing huge, highly mutable tables with constant record-level deletes and updates. Ad-hoc analysis has a different problem: writers continuously append to a table while analysts and data scientists read from it simultaneously. With plain Parquet, a reader can hit files that change mid-read after a writer commits. The team had been working around this with bespoke partition-by-date patterns; Hudi gave them snapshot isolation out of the box, with no custom plumbing.</p>
<h2 class="anchor anchorTargetStickyNavbar_Vzrq" id="growing-up-with-hudi-a-feature-timeline">Growing Up with Hudi: A Feature Timeline<a href="https://hudi.apache.org/blog/2026/06/15/apache-hudi-at-metica#growing-up-with-hudi-a-feature-timeline" class="hash-link" aria-label="Direct link to Growing Up with Hudi: A Feature Timeline" title="Direct link to Growing Up with Hudi: A Feature Timeline" translate="no">​</a></h2>
<p>The most resonant part of the talk was a <a href="https://hudi.apache.org/docs/timeline" target="_blank" rel="noopener noreferrer" class="">timeline</a> showing how Metica adopted Hudi features incrementally as the company and the data grew, from ~16 GB in August 2023 to billions of events today. The point worth dwelling on: each step was a configuration change, not a structural rewrite.</p>
<p><img decoding="async" loading="lazy" alt="Hudi Feature Timeline" src="https://hudi.apache.org/assets/images/img4-63d6e5f54d9b4fa3252f5a55c8e11cac.png" width="2528" height="1376" class="img_ev3q"></p>
<p style="text-align:center;font-style:italic"></p><p>Metica's Hudi features adoption over time. Starting from Copy-on-Write and Merge-on-Read to table services, HoodieStreamer, and granular configs as data grew 10x.</p><p></p>
<ul>
<li class=""><strong>August 2023 (16GB Baseline):</strong> Initially started with standard <a href="https://hudi.apache.org/docs/table_types#copy-on-write-table" target="_blank" rel="noopener noreferrer" class="">Copy-on-Write</a> <strong>(CoW)</strong> tables because data volumes were small and write performance wasn't a bottleneck.</li>
<li class=""><strong>Real-time reporting:</strong> As real-time user reporting expanded, fine-grained updates began touching a high percentage of files. Re-writing entire Parquet files via CoW became expensive, leading Metica to migrate latency-sensitive pipelines to <a href="https://hudi.apache.org/docs/table_types#merge-on-read-table" target="_blank" rel="noopener noreferrer" class="">Merge-on-Read</a> <strong>(MoR)</strong> tables.</li>
<li class=""><strong>Managing 5x data growth:</strong> When mid-sized clients started sending millions of events, read latency suffered. Until this point the team had run on defaults — no <a href="https://hudi.apache.org/docs/clustering" target="_blank" rel="noopener noreferrer" class="">clustering</a>, indexing, or <a href="https://hudi.apache.org/docs/cleaning" target="_blank" rel="noopener noreferrer" class="">cleaning</a>. Rather than overhauling the architecture, Metica stabilized performance simply by modifying Hudi cluster configurations to trigger automatic clustering, cleaning, and <a href="https://hudi.apache.org/docs/metadata_indexing" target="_blank" rel="noopener noreferrer" class="">indexing</a>.</li>
<li class=""><strong>Seamless Version Upgrades:</strong> Wanting record-level indexes (introduced in 0.14), the team upgraded their EMR Hudi client from 0.13 to 0.14.1 — moving the internal table version from v5 to v6. They were nervous about existing tables, but Hudi handled the upgrade automatically on first write, with no config changes. The <a href="https://hudi.apache.org/docs/cli" target="_blank" rel="noopener noreferrer" class="">Hudi CLI</a> became a valued admin tool for inspecting and reasoning about table state, and Hudi Streamer worked as expected where they used it.</li>
<li class=""><strong>10x data growth:</strong> Bigger clients and new features pushed data into the billions of events, reviving the read-latency challenge. The fix was optimizing data layout further: clustering with sort columns on predicate columns, cleaning commits to limit retained commits (some tables keep only the latest two), and file sizing to pack small files into larger ones.</li>
</ul>
<h2 class="anchor anchorTargetStickyNavbar_Vzrq" id="practical-scenarios--performance-gains">Practical Scenarios &amp; Performance Gains<a href="https://hudi.apache.org/blog/2026/06/15/apache-hudi-at-metica#practical-scenarios--performance-gains" class="hash-link" aria-label="Direct link to Practical Scenarios &amp; Performance Gains" title="Direct link to Practical Scenarios &amp; Performance Gains" translate="no">​</a></h2>
<p>This is where the real engineering lessons live. Here are before-and-after numbers from a real workload — a CelerData/StarRocks cluster of 1 frontend and 1 backend node, on a ~40M-row table.</p>
<h3 class="anchor anchorTargetStickyNavbar_Vzrq" id="1-too-many-small-files">1. Too Many Small Files<a href="https://hudi.apache.org/blog/2026/06/15/apache-hudi-at-metica#1-too-many-small-files" class="hash-link" aria-label="Direct link to 1. Too Many Small Files" title="Direct link to 1. Too Many Small Files" translate="no">​</a></h3>
<p>Without a table format manager, standard Apache Spark pipelines writing to raw Parquet require rigid <code>repartition()</code> or <code>coalesce()</code> configuration parameters. When data volumes are low, this practice forces a "small-file problem" across S3 objects.</p>
<p><strong>Optimization applied:</strong> Enable inline clustering and let Hudi pack files adaptively to a target size.</p>
<div class="language-properties codeBlockContainer_Ckt0 theme-code-block" style="--prism-color:#F8F8F2;--prism-background-color:#282A36"><div class="codeBlockContent_QJqH"><pre tabindex="0" class="prism-code language-properties codeBlock_bY9V thin-scrollbar" style="color:#F8F8F2;background-color:#282A36"><code class="codeBlockLines_e6Vv"><span class="token-line" style="color:#F8F8F2"><span class="token plain">hoodie.clustering.inline = true</span><br></span></code></pre></div></div>
<p><strong>Result:</strong> Total objects in S3 dropped from <strong>~17K to ~3K</strong> — which is <strong>~6x storage reduction</strong>.</p>
<h3 class="anchor anchorTargetStickyNavbar_Vzrq" id="2-slow-reads-on-wide-file-scans">2. Slow Reads on Wide File Scans<a href="https://hudi.apache.org/blog/2026/06/15/apache-hudi-at-metica#2-slow-reads-on-wide-file-scans" class="hash-link" aria-label="Direct link to 2. Slow Reads on Wide File Scans" title="Direct link to 2. Slow Reads on Wide File Scans" translate="no">​</a></h3>
<p>Because StarRocks prunes storage scans using parquet file-footers and file-level metadata, unsorted clustering still forced the engine to scan wide swaths of blocks.</p>
<p><strong>Optimization applied:</strong> Add sort columns on the predicate columns so clustering co-locates the data that queries actually filter on.</p>
<div class="language-properties codeBlockContainer_Ckt0 theme-code-block" style="--prism-color:#F8F8F2;--prism-background-color:#282A36"><div class="codeBlockContent_QJqH"><pre tabindex="0" class="prism-code language-properties codeBlock_bY9V thin-scrollbar" style="color:#F8F8F2;background-color:#282A36"><code class="codeBlockLines_e6Vv"><span class="token-line" style="color:#F8F8F2"><span class="token plain">hoodie.clustering.inline = true</span><br></span><span class="token-line" style="color:#F8F8F2"><span class="token plain">hoodie.clustering.plan.strategy.sort.columns = col1,col2</span><br></span></code></pre></div></div>
<p><strong>Result:</strong> The same query with the same predicates went from <strong>~200 ms to ~90 ms</strong> — a minimum <strong>~2x improvement</strong>.</p>
<h2 class="anchor anchorTargetStickyNavbar_Vzrq" id="key-challenges">Key Challenges<a href="https://hudi.apache.org/blog/2026/06/15/apache-hudi-at-metica#key-challenges" class="hash-link" aria-label="Direct link to Key Challenges" title="Direct link to Key Challenges" translate="no">​</a></h2>
<p>Even with notable successes, Metica identified two production challenges:</p>
<ul>
<li class=""><strong>Column statistics on wide tables.</strong> Hudi enables column stats for all columns by default. On tables with very large column counts, that slowed down writes. The workaround is moving those services to async rather than inline. For now, the team is prioritizing <a href="https://hudi.apache.org/docs/indexes#record-index" target="_blank" rel="noopener noreferrer" class="">record-level indexes</a> over column stats, though they may combine the two later.</li>
<li class=""><strong>Partition evolution.</strong> A known Hudi limitation — once partition columns are defined, changing them is awkward and involves real work. The lesson: be deliberate about choosing partition columns up front.</li>
</ul>
<h2 class="anchor anchorTargetStickyNavbar_Vzrq" id="whats-next">What's Next<a href="https://hudi.apache.org/blog/2026/06/15/apache-hudi-at-metica#whats-next" class="hash-link" aria-label="Direct link to What's Next" title="Direct link to What's Next" translate="no">​</a></h2>
<p>The team is actively exploring several Hudi capabilities to push further:</p>
<ul>
<li class=""><strong>Record-level index (RLI):</strong> With more upsert-heavy workloads arriving, RLI's fast file lookup is the obvious next step; offline analysis showed it locates the right files very quickly.</li>
<li class=""><strong>Apache XTable adoption:</strong> <a href="https://hudi.apache.org/docs/syncing_xtable" target="_blank" rel="noopener noreferrer" class="">Apache XTable</a> provides interoperability between table formats. Metica's angle is standardizing on a single read format: write whatever format fits a use case (say, Iceberg on ingestion) but translate it to Hudi so the query engine always reads Hudi tables — avoiding separate catalogs per format.</li>
<li class=""><strong>StarRocks honoring RLI:</strong> Enabling an index in Hudi only helps if the query engine reads those statistics. StarRocks currently falls back to file-level indexing; Metica is working with both communities to close that gap so the benefit lands end-to-end.</li>
<li class=""><strong>Async table services:</strong> Moving from inline to async management via Hudi's indexer, <a href="https://hudi.apache.org/docs/compaction" target="_blank" rel="noopener noreferrer" class="">compactor</a>, and clustering utilities, so the write pipeline can focus on producing gold tables while table maintenance runs in the background.</li>
</ul>
<h2 class="anchor anchorTargetStickyNavbar_Vzrq" id="conclusion">Conclusion<a href="https://hudi.apache.org/blog/2026/06/15/apache-hudi-at-metica#conclusion" class="hash-link" aria-label="Direct link to Conclusion" title="Direct link to Conclusion" translate="no">​</a></h2>
<p>Adopting Apache Hudi paid off in a few concrete ways for Metica. The biggest win was being able to scale from ~16 GB to billions of events without ever re-architecting, since every jump in data volume became a configuration change instead of a painful migration. On the performance side, turning on inline clustering cut their S3 object count from ~17K to ~3K (about a 6x storage reduction), and adding sort columns on predicate columns roughly halved query latency on StarRocks, dropping it from ~200 ms to ~90 ms on a 40M-row table. <a href="https://hudi.apache.org/" target="_blank" rel="noopener noreferrer" class="">Hudi</a> removed a lot of manual work. Snapshot isolation let writers and readers hit the same tables safely without custom plumbing, and built-in table services, the Hudi CLI, and the indexer, compactor, and clustering utilities let a small team keep a growing platform healthy without building that machinery themselves.</p>]]></content:encoded>
            <category>data lakehouse</category>
            <category>metica</category>
            <category>clustering</category>
            <category>starrocks</category>
        </item>
        <item>
            <title><![CDATA[From Concept to Reality: Apache Hudi at the Foundation of Penn Entertainment's Data Platform]]></title>
            <link>https://hudi.apache.org/blog/2026/06/15/apache-hudi-at-penn-interactive</link>
            <guid>https://hudi.apache.org/blog/2026/06/15/apache-hudi-at-penn-interactive</guid>
            <pubDate>Mon, 15 Jun 2026 00:00:00 GMT</pubDate>
            <description><![CDATA[How Penn Interactive, the online sports betting arm of Penn Entertainment, modernized its data platform using Apache Hudi HudiStreamer to ingest CDC from thousands of Kafka topics, orchestrate 100+ concurrent jobs with Argo Workflows, and deliver near real-time data freshness across thousands of tables.]]></description>
            <content:encoded><![CDATA[<hr>
<p><em>This blog post summarizes Penn Entertainment's presentation led by Senior Data Engineer Sydney Horan at the Apache Hudi Community Sync. Watch the recording on <a href="https://www.youtube.com/watch?v=ZVN6t8zWeZ4" target="_blank" rel="noopener noreferrer" class="">YouTube</a>.</em></p>
<p><strong>Note:</strong> The video presentation references DeltaStreamer, which was the legacy name for the tool. It has since been renamed to <a href="https://hudi.apache.org/docs/hoodie_streaming_ingestion#hudi-streamer" target="_blank" rel="noopener noreferrer" class="">Hudi Streamer</a>.</p>
<hr>
<p><img decoding="async" loading="lazy" alt="og" src="https://hudi.apache.org/assets/images/img1-a5b248102f2eb1829fdb43e2ce73b195.png" width="2618" height="1466" class="img_ev3q"></p>
<div class="theme-admonition theme-admonition-tip admonition_xJq3 alert alert--success"><div class="admonitionHeading_Gvgb"><span class="admonitionIcon_Rf37"><svg viewBox="0 0 12 16"><path fill-rule="evenodd" d="M6.5 0C3.48 0 1 2.19 1 5c0 .92.55 2.25 1 3 1.34 2.25 1.78 2.78 2 4v1h5v-1c.22-1.22.66-1.75 2-4 .45-.75 1-2.08 1-3 0-2.81-2.48-5-5.5-5zm3.64 7.48c-.25.44-.47.8-.67 1.11-.86 1.41-1.25 2.06-1.45 3.23-.02.05-.02.11-.02.17H5c0-.06 0-.13-.02-.17-.2-1.17-.59-1.83-1.45-3.23-.2-.31-.42-.67-.67-1.11C2.44 6.78 2 5.65 2 5c0-2.2 2.02-4 4.5-4 1.22 0 2.36.42 3.22 1.19C10.55 2.94 11 3.94 11 5c0 .66-.44 1.78-.86 2.48zM4 14h5c-.23 1.14-1.3 2-2.5 2s-2.27-.86-2.5-2z"></path></svg></span>TL;DR</div><div class="admonitionContent_BuS1"><p>Penn Interactive successfully modernized its data platform to meet the high-volume, real-time demands of the online sports betting industry by adopting Apache Hudi at its core.The architecture uses Hudi Streamer to natively consume CDC messages from thousands of Kafka topics, performing efficient upserts into the S3 Parquet lakehouse. Argo Workflows and custom automation manage over a hundred concurrent jobs, ensuring high reliability via auto-resubmission logic and optimized resource utilization. The team achieved operational resilience by implementing a Disaster Recovery workflow to quickly patch data gaps using a JDBC source when Kafka logs were unavailable. A custom web-based system now monitors and visualizes the real-time data freshness of all tables, ensuring timely delivery of business intelligence and reports.</p></div></div>
<p>Penn Interactive and The Score merged in 2022 under Penn Entertainment, combining sports betting and sports media for customers across the U.S. and Canada. Known for apps like Barstool Sportsbook and The Score Bet, the company operates at the forefront of the online and retail sports betting industry.</p>
<p>After the merger, the data engineering team faced the challenge of building a new data analytics platform from scratch. At the core of this platform was <a href="https://hudi.apache.org/" target="_blank" rel="noopener noreferrer" class="">Apache Hudi</a>, chosen for its flexibility, scalability, and integration capabilities with a modern data ecosystem.</p>
<h2 class="anchor anchorTargetStickyNavbar_Vzrq" id="challenges-in-building-a-data-platform">Challenges in Building a Data Platform<a href="https://hudi.apache.org/blog/2026/06/15/apache-hudi-at-penn-interactive#challenges-in-building-a-data-platform" class="hash-link" aria-label="Direct link to Challenges in Building a Data Platform" title="Direct link to Challenges in Building a Data Platform" translate="no">​</a></h2>
<p>The company’s data ecosystem includes a wide range of operational databases, third-party services, and outputs from data science models. The data platform needed to support diverse use cases, including:</p>
<ul>
<li class="">Daily reporting for casino partners and marketing vendors.</li>
<li class="">Business intelligence dashboards for finance, marketing, and promotions teams.</li>
<li class="">Data science workflows for predictive analytics.</li>
</ul>
<p>Traditional solutions, which relied on batch or streaming replication of database tables, proved inefficient. They often required batch overrides and failed to fully leverage change data capture (CDC), creating operational bottlenecks and delays in delivering timely insights.
The new system required near real-time replication that integrated smoothly with end-user reporting tools, as well as a mechanism to proactively identify pipeline anomalies and ensure data freshness across thousands of tables.</p>
<h2 class="anchor anchorTargetStickyNavbar_Vzrq" id="the-source-and-target-ecosystems">The Source and Target Ecosystems<a href="https://hudi.apache.org/blog/2026/06/15/apache-hudi-at-penn-interactive#the-source-and-target-ecosystems" class="hash-link" aria-label="Direct link to The Source and Target Ecosystems" title="Direct link to The Source and Target Ecosystems" translate="no">​</a></h2>
<p>The platform ingests data from dozens of PostgreSQL operational databases across multiple service teams. The tables vary in structure, data types, and throughput - some tables generate a million CDC messages per hour, while others update only a few rows per year. The ingestion pipeline needed to scale effectively with this variable throughput.In addition to internal databases, the platform also integrates data from third-party file transfers, APIs, and outputs from other data teams.
The target environment is structured in layers:</p>
<ol>
<li class=""><strong>Raw Layer:</strong> A mirror of the source data, stored in S3 using the Parquet file format and written efficiently by <a href="https://hudi.apache.org/docs/hoodie_streaming_ingestion/#hudi-streamer" target="_blank" rel="noopener noreferrer" class="">Hudi Streamer</a>.</li>
<li class=""><strong>Query Layer:</strong> Redshift external tables provide a structured access point over the S3 data for easy querying.</li>
<li class=""><strong>Curated Layers:</strong> The data team further refines this raw data into models and analysis views for BI and data science.</li>
</ol>
<p><img decoding="async" loading="lazy" alt="Architecture diagram of the Hudi pipeline" src="https://hudi.apache.org/assets/images/img2-2ed1f9e7be6230b19d2ed132ec2c9f15.png" width="1360" height="1340" class="img_ev3q"></p>
<p style="text-align:center;font-style:italic"></p><p>Kafka topics (one per table) are ingested by Hudi Streamer, which handles checkpointing, de-duplication, transactions, indexing, and metadata cleaning, into Parquet on S3 and is queried through Redshift external tables.</p><p></p>
<h2 class="anchor anchorTargetStickyNavbar_Vzrq" id="the-solution-apache-hudi-and-hudi-streamer">The Solution: Apache Hudi and Hudi Streamer<a href="https://hudi.apache.org/blog/2026/06/15/apache-hudi-at-penn-interactive#the-solution-apache-hudi-and-hudi-streamer" class="hash-link" aria-label="Direct link to The Solution: Apache Hudi and Hudi Streamer" title="Direct link to The Solution: Apache Hudi and Hudi Streamer" translate="no">​</a></h2>
<p><a href="https://hudi.apache.org/" target="_blank" rel="noopener noreferrer" class="">Apache Hudi</a> was selected as the ideal foundation for the data lakehouse architecture for several reasons:</p>
<ul>
<li class=""><strong>Handling Updates and Incremental Processing:</strong> Hudi efficiently manages data updates, performing upserts instead of just appending, which is critical for <a href="https://hudi.apache.org/docs/quick-start-guide#cdc-query" target="_blank" rel="noopener noreferrer" class="">CDC</a> data. It allows for incremental processing of large data volumes.</li>
<li class=""><strong>Diverse Data Ingestion:</strong> Hudi gracefully handles both streaming CDC messages and external batch sources.</li>
<li class=""><strong>Ecosystem Compatibility:</strong> Hudi integrates smoothly with the Apache ecosystem and tools like Spark, simplifying operations.</li>
</ul>
<p><a href="https://hudi.apache.org/docs/hoodie_streaming_ingestion/#hudi-streamer" target="_blank" rel="noopener noreferrer" class="">Hudi Streamer</a> became the core database replication tool. It natively processes CDC messages streamed through Kafka topics, applying the exact changes to the Hudi tables to maintain synchronization. The team leveraged its capabilities for:</p>
<ul>
<li class=""><strong>Operational Flexibility:</strong> Choosing between continuous mode (for high-throughput, near real-time tables) and run-once mode (for smaller, periodic jobs).</li>
<li class=""><strong>Scalability:</strong> Managing over a hundred concurrent Hudi Streamer jobs, consuming from nearly a thousand topics, by optimizing resource use through table grouping.</li>
</ul>
<h2 class="anchor anchorTargetStickyNavbar_Vzrq" id="the-journey-of-implementation-and-scale">The Journey of Implementation and Scale<a href="https://hudi.apache.org/blog/2026/06/15/apache-hudi-at-penn-interactive#the-journey-of-implementation-and-scale" class="hash-link" aria-label="Direct link to The Journey of Implementation and Scale" title="Direct link to The Journey of Implementation and Scale" translate="no">​</a></h2>
<p><img decoding="async" loading="lazy" alt="Platform Stack" src="https://hudi.apache.org/assets/images/img3-e541d6087546ea1aff5b890b86794163.png" width="1360" height="640" class="img_ev3q"></p>
<p style="text-align:center;font-style:italic"></p><p>The technology stack: Confluent Cloud (Debezium, Kafka, Schema Registry) for change capture, Google Cloud (Dataproc, GKE, Storage) for compute and storage, and Python/Kubernetes/Docker for orchestration and packaging.</p><p></p>
<p>The implementation process involved several key phases, moving from initial concept to large-scale automation:</p>
<h3 class="anchor anchorTargetStickyNavbar_Vzrq" id="1-proof-of-concept">1. Proof of Concept<a href="https://hudi.apache.org/blog/2026/06/15/apache-hudi-at-penn-interactive#1-proof-of-concept" class="hash-link" aria-label="Direct link to 1. Proof of Concept" title="Direct link to 1. Proof of Concept" translate="no">​</a></h3>
<p>Early experiments with homegrown solutions and traditional streaming methods highlighted major pain points such as manual checkpointing, batch sizing, and scalability concerns. The combination of Debezium, Kafka, and Hudi Streamer was ultimately chosen, as Hudi Streamer provided built-in solutions for these complex operational challenges.</p>
<p><img decoding="async" loading="lazy" alt="Comparison of a homegrown CDC solution versus Kafka, Debezium, and PySpark/Hudi" src="https://hudi.apache.org/assets/images/img4-e01b64f235fd7a3cc2ae2b005f6a5fb9.png" width="1360" height="720" class="img_ev3q"></p>
<p style="text-align:center;font-style:italic"></p><p>A homegrown solution vs. Kafka, Debezium, and PySpark/Hudi experimentation and weighing checkpointing, batching, and scaling before committing to a Hudi-based pipeline.</p><p></p>
<p>The data platform's CDC pipeline relies on Debezium connectors to capture changes from PostgreSQL databases, streaming them into Kafka topics hosted on Confluent Cloud. Hudi Streamer consumes these messages, applies the updates to Hudi tables in S3, and integrates them into the Redshift query layer.
The system also handles non-CDC data sources, such as:</p>
<ul>
<li class="">Third-party vendor files via SFTP or cloud storage.</li>
<li class="">Data from vendor APIs.</li>
<li class="">Retail data from on-premise Casino systems.</li>
</ul>
<p>These workflows ingest and upsert data into Hudi tables, ensuring consistency and availability for downstream analytics.</p>
<h3 class="anchor anchorTargetStickyNavbar_Vzrq" id="2-infrastructure-and-knowledge">2. Infrastructure and Knowledge<a href="https://hudi.apache.org/blog/2026/06/15/apache-hudi-at-penn-interactive#2-infrastructure-and-knowledge" class="hash-link" aria-label="Direct link to 2. Infrastructure and Knowledge" title="Direct link to 2. Infrastructure and Knowledge" translate="no">​</a></h3>
<p>The platform runs on <strong>Google Cloud Platform</strong>, using Dataproc for Spark execution and Google Kubernetes Engine for container orchestration. They integrated Confluent Cloud for Kafka and the schema registry, ensuring the environment was ready to ingest data from the source Postgres databases into the S3 target.</p>
<h3 class="anchor anchorTargetStickyNavbar_Vzrq" id="3-community-collaboration-and-custom-enhancements">3. Community Collaboration and Custom Enhancements<a href="https://hudi.apache.org/blog/2026/06/15/apache-hudi-at-penn-interactive#3-community-collaboration-and-custom-enhancements" class="hash-link" aria-label="Direct link to 3. Community Collaboration and Custom Enhancements" title="Direct link to 3. Community Collaboration and Custom Enhancements" translate="no">​</a></h3>
<p>During development, the engineering team actively collaborated with Hudi developers. Through Slack channels, GitHub issues, and weekly calls, the team resolved technical challenges and incorporated improvements. They created a custom fork to address specific needs, such as:</p>
<ul>
<li class=""><strong>Timestamp Format:</strong> Incorporating <strong>epoch microseconds</strong> for Debezium timestamps.</li>
<li class=""><strong>Job Reliability:</strong> Resolving an occasional bug where multi-table jobs would hang by implementing a forceful shutdown mechanism for background processes.</li>
<li class=""><strong>Tombstone Messages:</strong> Implementing a filter to handle and remove null values caused by Kafka Tombstone messages, which previously triggered null pointer exceptions.</li>
</ul>
<h3 class="anchor anchorTargetStickyNavbar_Vzrq" id="4-automation-and-orchestration">4. Automation and Orchestration<a href="https://hudi.apache.org/blog/2026/06/15/apache-hudi-at-penn-interactive#4-automation-and-orchestration" class="hash-link" aria-label="Direct link to 4. Automation and Orchestration" title="Direct link to 4. Automation and Orchestration" translate="no">​</a></h3>
<p>To ingest data from thousands of tables without manual shell scripts, the team built a custom Python wrapper script running as a Kubernetes pod.</p>
<ul>
<li class=""><strong>Configuration Control:</strong> Job parameters are centralized in a config file that the wrapper script reads to dynamically build Spark arguments. This allows the team to easily define tables, key columns, and required Spark resources (categorized as small, medium, large) without modifying code.</li>
<li class=""><strong>Orchestration:</strong> Argo Workflows act as the central orchestrator, running the wrapper script on a scheduled basis.</li>
</ul>
<h3 class="anchor anchorTargetStickyNavbar_Vzrq" id="5-scaling-and-reliability">5. Scaling and Reliability<a href="https://hudi.apache.org/blog/2026/06/15/apache-hudi-at-penn-interactive#5-scaling-and-reliability" class="hash-link" aria-label="Direct link to 5. Scaling and Reliability" title="Direct link to 5. Scaling and Reliability" translate="no">​</a></h3>
<p>The Argo workflow includes a crucial pulse check and resubmission mechanism. It constantly monitors continuous mode jobs and quickly resubmits any that fail due to network timeouts or platform glitches, ensuring high reliability for 24/7 ingestion.</p>
<p>The team established a workflow for Disaster Recovery for scenarios where Kafka logs have expired. They developed a process to temporarily pause the streaming job and kick off a dedicated Hudi Streamer job with a JDBC source. This job directly upserts missing data from the source, quickly patching the data gap before the streaming job is resumed.</p>
<p><img decoding="async" loading="lazy" alt="Hudi Timeline" src="https://hudi.apache.org/assets/images/img5-786565c93250a44e2435d2c9c11c2d96.png" width="2000" height="720" class="img_ev3q"></p>
<p style="text-align:center;font-style:italic"></p><p>A data-corruption bug in the streaming job (around Apr '23) is repaired by a Hudi Streamer batch job that uses JdbcSource to overwrite the corrupted dates with source data.</p><p></p>
<h3 class="anchor anchorTargetStickyNavbar_Vzrq" id="6-monitoring-data-freshness">6. Monitoring Data Freshness<a href="https://hudi.apache.org/blog/2026/06/15/apache-hudi-at-penn-interactive#6-monitoring-data-freshness" class="hash-link" aria-label="Direct link to 6. Monitoring Data Freshness" title="Direct link to 6. Monitoring Data Freshness" translate="no">​</a></h3>
<p>A final piece of the puzzle was ensuring data freshness. Given the extreme variance in Kafka offsets, a custom, more insightful monitoring solution was required. Custom scripts and Datadog dashboards were used initially, but as the system scaled, a web-based monitoring interface was developed. This API continuously crawls each Hudi table, retrieving its maximum timestamp and comparing it against established freshness thresholds.</p>
<p>This solution provides a user-friendly web interface that gives the entire team a single, clear view of all tables, easily highlighting any that are lagging. This capability is being integrated into a comprehensive alerting system to minimize the need for manual intervention during data latency incidents.</p>
<p><img decoding="async" loading="lazy" alt="automated freshness crawler UI" src="https://hudi.apache.org/assets/images/img6-60f16eaa7235df3a9e82d05f3ec44ef9.png" width="2438" height="1152" class="img_ev3q"></p>
<p style="text-align:center;font-style:italic"></p><p>The automated freshness crawler UI fed by custom log parsing and Datadog monitoring</p><p></p>
<h2 class="anchor anchorTargetStickyNavbar_Vzrq" id="conclusion">Conclusion<a href="https://hudi.apache.org/blog/2026/06/15/apache-hudi-at-penn-interactive#conclusion" class="hash-link" aria-label="Direct link to Conclusion" title="Direct link to Conclusion" translate="no">​</a></h2>
<p>The implementation of Apache Hudi has been transformative for Penn Interactive and The Score's data platform. By choosing Apache Hudi, the organization successfully consolidated data from diverse sources with high flexibility and scalability. The custom automation, robust monitoring, and disaster recovery processes built around Hudi Streamer ensure the platform provides accurate and timely insights to stakeholders across business intelligence, marketing, and data science.</p>
<p>With Apache Hudi at its core, the data platform is well-positioned to scale alongside the growing demands of the online sports betting and media industry.</p>
<p>This blog is based on Penn Entertainment's presentation at the Apache Hudi Community Sync. If you are interested in watching the recorded version of the video, you can find it <a href="https://www.youtube.com/watch?v=ZVN6t8zWeZ4" target="_blank" rel="noopener noreferrer" class="">here</a>.</p>]]></content:encoded>
            <category>data lakehouse</category>
            <category>penn interactive</category>
        </item>
        <item>
            <title><![CDATA[Modernizing Data Infrastructure at Southwest with Apache Hudi]]></title>
            <link>https://hudi.apache.org/blog/2026/06/15/apache-hudi-at-southwest-airlines</link>
            <guid>https://hudi.apache.org/blog/2026/06/15/apache-hudi-at-southwest-airlines</guid>
            <pubDate>Mon, 15 Jun 2026 00:00:00 GMT</pubDate>
            <description><![CDATA[Southwest Airlines migrated its reservation data pipeline from an on-premise Ab Initio warehouse to an AWS lakehouse on Apache Hudi and EMR — implementing SCD Type 2 at scale and cutting job runtime from ~5 hours to ~1.5.]]></description>
            <content:encoded><![CDATA[<hr>
<p><em>This blog post summarizes Southwest Airlines's presentation <strong>led by Tech Lead Data Engineer Koti Darla</strong> at the Apache Hudi Community Sync. Watch the recording on <a href="https://www.youtube.com/watch?v=X3FW4IYmYE4" target="_blank" rel="noopener noreferrer" class="">YouTube</a>.</em></p>
<hr>
<p><img decoding="async" loading="lazy" alt="og" src="https://hudi.apache.org/assets/images/img1-33af224bd34ca5b9057932ae4830a0e4.png" width="1920" height="1080" class="img_ev3q"></p>
<div class="theme-admonition theme-admonition-tip admonition_xJq3 alert alert--success"><div class="admonitionHeading_Gvgb"><span class="admonitionIcon_Rf37"><svg viewBox="0 0 12 16"><path fill-rule="evenodd" d="M6.5 0C3.48 0 1 2.19 1 5c0 .92.55 2.25 1 3 1.34 2.25 1.78 2.78 2 4v1h5v-1c.22-1.22.66-1.75 2-4 .45-.75 1-2.08 1-3 0-2.81-2.48-5-5.5-5zm3.64 7.48c-.25.44-.47.8-.67 1.11-.86 1.41-1.25 2.06-1.45 3.23-.02.05-.02.11-.02.17H5c0-.06 0-.13-.02-.17-.2-1.17-.59-1.83-1.45-3.23-.2-.31-.42-.67-.67-1.11C2.44 6.78 2 5.65 2 5c0-2.2 2.02-4 4.5-4 1.22 0 2.36.42 3.22 1.19C10.55 2.94 11 3.94 11 5c0 .66-.44 1.78-.86 2.48zM4 14h5c-.23 1.14-1.3 2-2.5 2s-2.27-.86-2.5-2z"></path></svg></span>TL;DR</div><div class="admonitionContent_BuS1"><p>Southwest Airlines migrated from an on-premise data warehouse powered by Ab Initio ETL to a modern AWS-based lakehouse architecture using Apache Hudi on Amazon EMR. The team tackled the complex challenge of implementing Slowly Changing Dimensions Type 2 (SCD Type 2) on reservation data flattening deeply nested XML payloads (20–25 levels) into 30+ tables while preserving full historical tracking. Through careful optimization of Spark configurations, partitioning strategies, Hudi indexing, and EMR resource management, they achieved 40–60% faster execution times, reducing job runtime from ~5 hours to ~1.5 hours. This blog details their architecture, implementation patterns, the challenges they faced, and the optimizations that made it work.</p></div></div>
<p>As data volumes scale, legacy on-premise data infrastructure often struggles to keep up with the performance, cost efficiency, and flexibility required by modern applications. At Southwest Airlines, the data platform team underwent a major journey to modernize their infrastructure. By migrating from legacy on-premise systems to a high-performance lakehouse architecture powered by <a href="https://hudi.apache.org/" target="_blank" rel="noopener noreferrer" class="">Apache Hudi</a>, they tackled massive processing bottlenecks, reduced cloud costs, and successfully implemented complex data patterns like Slowly Changing Dimensions (SCD) Type 2 at scale.
Here is an inside look at their architectural transition, key performance challenges, and how they resolved them.</p>
<h2 class="anchor anchorTargetStickyNavbar_Vzrq" id="the-legacy-architecture">The Legacy Architecture<a href="https://hudi.apache.org/blog/2026/06/15/apache-hudi-at-southwest-airlines#the-legacy-architecture" class="hash-link" aria-label="Direct link to The Legacy Architecture" title="Direct link to The Legacy Architecture" translate="no">​</a></h2>
<p>Southwest's previous data stack was built around a traditional ETL pattern:</p>
<p><img decoding="async" loading="lazy" alt="Legacy Architecture" src="https://hudi.apache.org/assets/images/img2-06b88c14ffaf5001ddf553d178867640.png" width="1920" height="1080" class="img_ev3q"></p>
<p style="text-align:center;font-style:italic"></p><p>Southwest's old ETL stack — sources processed through Ab Initio into an on-premise database feeding Tableau and Alteryx.</p><p></p>
<ul>
<li class=""><strong>Source applications</strong>: SWA internal applications, third-party applications, and mobile applications fed data into the pipeline.</li>
<li class=""><strong>ETL layer</strong>: Ab-Initio handled transformation, standardization, validation, cleansing, business logic, change data capture, and window functions.</li>
<li class=""><strong>Storage</strong>: Transformed data landed in an on-premise data warehouse.</li>
<li class=""><strong>Consumption</strong>: Tools like Alteryx connected directly to the warehouse to extract business insights and reports.</li>
</ul>
<p>While functional, this architecture had limits in scalability, cost flexibility, and the ability to support modern data science and streaming workloads.</p>
<h2 class="anchor anchorTargetStickyNavbar_Vzrq" id="the-new-architecture-aws--apache-hudi">The New Architecture: AWS + Apache Hudi<a href="https://hudi.apache.org/blog/2026/06/15/apache-hudi-at-southwest-airlines#the-new-architecture-aws--apache-hudi" class="hash-link" aria-label="Direct link to The New Architecture: AWS + Apache Hudi" title="Direct link to The New Architecture: AWS + Apache Hudi" translate="no">​</a></h2>
<p>The modernized stack is fully cloud-native and built around a multi-zone lakehouse pattern:</p>
<p><img decoding="async" loading="lazy" alt="Datalake Architecture" src="https://hudi.apache.org/assets/images/img3-2228e88b59c5db680d4695c81f6475a4.png" width="3440" height="1734" class="img_ev3q"></p>
<p style="text-align:center;font-style:italic"></p><p>The modernized cloud-native lakehouse. Streaming/batch sources land in Hudi-backed Landing, Clean, and Curated zones, processed on EMR and served to Redshift, Alteryx, and Tableau.</p><p></p>
<ol>
<li class=""><strong>Ingestion Layer:</strong> Data from source applications is captured via SFTP (for files) or Kafka S3 Sync Connectors and placed into an Amazon S3 Landing Zone.</li>
<li class=""><strong>Processing &amp; Cleaning:</strong> Amazon EMR with PySpark and AWS Glue run the data pipelines. Pipelines validate, clean, and standardize data, then write to the Clean Zone on S3.</li>
<li class=""><strong>Curated Layer &amp; Apache Hudi:</strong> Another set of PySpark pipelines transforms and enriches the data, writing to the Curated Zone where Hudi tables live.</li>
<li class=""><strong>Orchestration &amp; Cataloging:</strong> AWS Step Functions orchestrate the workflows. AWS Glue handles the data catalog, and Amazon Athena is used for ad-hoc querying.</li>
<li class=""><strong>Serving Layer:</strong> Data is also loaded into Amazon Redshift for BI consumption via Alteryx and Tableau. Data science teams build and run models directly on the Hudi tables.</li>
</ol>
<h2 class="anchor anchorTargetStickyNavbar_Vzrq" id="the-use-case-reservation-events-and-scd-type-2">The Use Case: Reservation Events and SCD Type 2<a href="https://hudi.apache.org/blog/2026/06/15/apache-hudi-at-southwest-airlines#the-use-case-reservation-events-and-scd-type-2" class="hash-link" aria-label="Direct link to The Use Case: Reservation Events and SCD Type 2" title="Direct link to The Use Case: Reservation Events and SCD Type 2" translate="no">​</a></h2>
<p>When a customer books a flight, Southwest receives a reservation event as an XML payload with 20–25 nested levels. The data modeling team normalized this into a third-normal-form model with 30+ tables, most of which require SCD Type 2 historical tracking. Only a handful are insert-only.</p>
<h3 class="anchor anchorTargetStickyNavbar_Vzrq" id="starting-point-an-aws-blog">Starting Point: An AWS Blog<a href="https://hudi.apache.org/blog/2026/06/15/apache-hudi-at-southwest-airlines#starting-point-an-aws-blog" class="hash-link" aria-label="Direct link to Starting Point: An AWS Blog" title="Direct link to Starting Point: An AWS Blog" translate="no">​</a></h3>
<p>The team discovered an AWS blog titled <em>"<a href="https://aws.amazon.com/blogs/big-data/build-slowly-changing-dimensions-type-2-scd2-with-apache-spark-and-apache-hudi-on-amazon-emr" target="_blank" rel="noopener noreferrer" class="">Build Slowly Changing Dimensions Type 2 (SCD2) with Apache Spark and Apache Hudi on Amazon EMR</a>"</em> which became their foundation.</p>
<p>Key takeaways they adopted:</p>
<ul>
<li class="">Use Hudi <a href="https://hudi.apache.org/docs/write_operations#upsert" target="_blank" rel="noopener noreferrer" class="">upsert</a> capabilities for seamless insert/update handling.</li>
<li class="">Use EMR for autoscaling, cost efficiency, and high availability.</li>
<li class="">Leverage <a href="https://hudi.apache.org/docs/record_merger/#event_time_ordering" target="_blank" rel="noopener noreferrer" class="">pre-combine</a> logic for change detection and de-duplication.</li>
<li class="">Maintain old records with effective start/end dates; use the latest effective end date to identify current records.</li>
</ul>
<h3 class="anchor anchorTargetStickyNavbar_Vzrq" id="why-copy-on-write-cow">Why Copy-on-Write (CoW)?<a href="https://hudi.apache.org/blog/2026/06/15/apache-hudi-at-southwest-airlines#why-copy-on-write-cow" class="hash-link" aria-label="Direct link to Why Copy-on-Write (CoW)?" title="Direct link to Why Copy-on-Write (CoW)?" translate="no">​</a></h3>
<p>After evaluating both Hudi table types, the team chose <a href="https://hudi.apache.org/docs/table_types#copy-on-write-table" target="_blank" rel="noopener noreferrer" class="">Copy-on-Write</a> as the better fit for their needs. Their workload is primarily batch processing, and SCD Type 2 requires reading previous records to end-date them. CoW provides faster, consistent queries for current data without the extra log-file overhead that <a href="https://hudi.apache.org/docs/table_types#merge-on-read-table" target="_blank" rel="noopener noreferrer" class="">Merge-on-Read</a> introduces. CoW also ensures atomic updates by overwriting base files, which simplifies version tracking and aligns naturally with SCD Type 2's need to maintain full record versions and handle reprocessing of out-of-order data.</p>
<h3 class="anchor anchorTargetStickyNavbar_Vzrq" id="orchestration-step-functions--dynamodb">Orchestration: Step Functions + DynamoDB<a href="https://hudi.apache.org/blog/2026/06/15/apache-hudi-at-southwest-airlines#orchestration-step-functions--dynamodb" class="hash-link" aria-label="Direct link to Orchestration: Step Functions + DynamoDB" title="Direct link to Orchestration: Step Functions + DynamoDB" translate="no">​</a></h3>
<p>With 30+ tables loaded every two hours and strict referential integrity requirements, the team needed robust orchestration with restart-from-failure capability. At the time, Step Functions didn't yet offer "redrive from failure," so they built a custom tracking layer using DynamoDB.</p>
<p>The team built a custom tracking layer in DynamoDB. Two tables:</p>
<ol>
<li class="">The job-stream tracking table records each batch file and its status per target table. So if batch 02 is in flight and tables A, B, and C completed but D and E failed, only D and E get reprocessed on retry. The completed ones get skipped.</li>
<li class="">The Hudi-to-Redshift tracking table stores the last Hudi commit timestamp loaded into Redshift. Subsequent loads only pick up the delta. No full reloads, no double-counting.</li>
</ol>
<p>This design gave them restart safety, referential integrity, and a clean foundation for monitoring dashboards.</p>
<h3 class="anchor anchorTargetStickyNavbar_Vzrq" id="handling-out-of-order-data">Handling Out-of-Order Data<a href="https://hudi.apache.org/blog/2026/06/15/apache-hudi-at-southwest-airlines#handling-out-of-order-data" class="hash-link" aria-label="Direct link to Handling Out-of-Order Data" title="Direct link to Handling Out-of-Order Data" translate="no">​</a></h3>
<p>Reservation versions don't always arrive in the right sequence and that created a real challenge. Imagine a PNR with three versions such as v1 ($100), v2 ($200), v3 ($300). If v1 and v3 arrive first, then v2 arrives later, naive sequential processing would produce incorrect historical tracking.</p>
<p><img decoding="async" loading="lazy" alt="Handling Out-of-Order Data" src="https://hudi.apache.org/assets/images/img4-cf7a44613510a723416bae0b732442d7.png" width="1920" height="765" class="img_ev3q"></p>
<p style="text-align:center;font-style:italic"></p><p>When PNR versions arrive out of order, Hudi merges the delta against existing records via an index lookup before writing — keeping history correct.</p><p></p>
<p>To solve this, the team built a custom merge flow that begins by collecting all keys in the incoming delta batch and then going back to the target Hudi table to pull all related historical records for those keys. New versions are run through the full transformation logic, while already-transformed records are reused as-is to avoid redundant computation. With the complete picture in hand, the pipeline recalculates all effective start and end dates across the merged set, then uses <a href="https://hudi.apache.org/docs/indexes/#bloom-filters" target="_blank" rel="noopener noreferrer" class="">Bloom Index</a> with Hudi upsert to insert and update records accordingly. If the recalculation reveals orphan records — i.e., rows that should no longer exist because a later-arriving version invalidated them — the pipeline issues explicit deletes to keep the history clean. Notably, this logic is fully custom; the team did not use Hudi's built-in <a href="https://hudi.apache.org/docs/record_merger" target="_blank" rel="noopener noreferrer" class="">record mergers</a> (event-time or commit-time ordering) because their workflow required expiring previous records and applying complex business gap-filling logic across multiple related tables.</p>
<h2 class="anchor anchorTargetStickyNavbar_Vzrq" id="key-performance-challenges--solutions">Key Performance Challenges &amp; Solutions<a href="https://hudi.apache.org/blog/2026/06/15/apache-hudi-at-southwest-airlines#key-performance-challenges--solutions" class="hash-link" aria-label="Direct link to Key Performance Challenges &amp; Solutions" title="Direct link to Key Performance Challenges &amp; Solutions" translate="no">​</a></h2>
<p>The team encountered four major categories of performance challenges.</p>
<h3 class="anchor anchorTargetStickyNavbar_Vzrq" id="1-xml-flattening-bottlenecks">1. XML Flattening Bottlenecks<a href="https://hudi.apache.org/blog/2026/06/15/apache-hudi-at-southwest-airlines#1-xml-flattening-bottlenecks" class="hash-link" aria-label="Direct link to 1. XML Flattening Bottlenecks" title="Direct link to 1. XML Flattening Bottlenecks" translate="no">​</a></h3>
<p>Exploding deeply nested XML generated billions of rows, causing massive shuffle overhead (observed: ~11 billion records, 2.2 TB shuffle reads, 5.1 TB writes), severe data skew, executor failures from memory pressure, and lost executors from heavy shuffle I/O.</p>
<p><strong>Optimizations applied:</strong> They balanced partitions using explicit <code>repartition()</code> and <code>coalesce()</code> steps, tuned <code>maxPartitionBytes</code> to break data into smaller, manageable shuffle blocks, and implemented salting techniques on heavily skewed keys. They also utilized Adaptive Query Execution (AQE) to dynamically auto-adjust shuffle partitions.</p>
<p><strong>Result:</strong> 40–60% faster execution, reduced shuffle overhead, and improved memory efficiency.</p>
<h3 class="anchor anchorTargetStickyNavbar_Vzrq" id="2-complex-transformations-and-massive-dags">2. Complex Transformations and Massive DAGs<a href="https://hudi.apache.org/blog/2026/06/15/apache-hudi-at-southwest-airlines#2-complex-transformations-and-massive-dags" class="hash-link" aria-label="Direct link to 2. Complex Transformations and Massive DAGs" title="Direct link to 2. Complex Transformations and Massive DAGs" translate="no">​</a></h3>
<p>Long lineage chains from window functions, multi-table joins, and business logic created huge DAGs. Executors ran out of memory, cached stages got lost, and re-execution further slowed things down.</p>
<p><strong>Optimizations applied:</strong> They systematically broke the Spark DAGs by implementing intermediate local file reads and writes (checkpointing to disk). This completely cleared memory caching pressures. Additionally, they dropped unused columns early in the pipeline and used broadcast joins for smaller reference tables. Used repartition before explodes, joins, and aggregations. Continuous fine-tuning by reading Spark UI and Ganglia charts.</p>
<p><strong>Result:</strong> Smaller, more manageable DAGs and the same 40–60% efficiency gains.</p>
<h3 class="anchor anchorTargetStickyNavbar_Vzrq" id="3-historical-backfill-and-partitioning">3. Historical Backfill and Partitioning<a href="https://hudi.apache.org/blog/2026/06/15/apache-hudi-at-southwest-airlines#3-historical-backfill-and-partitioning" class="hash-link" aria-label="Direct link to 3. Historical Backfill and Partitioning" title="Direct link to 3. Historical Backfill and Partitioning" translate="no">​</a></h3>
<p>The team initially attempted to backfill 10 years of historical booking data directly into Hudi using Day-level partitioning. However, creating millions of fine-grained S3 prefixes and processing indexing lookups over a decade of data stalled the cluster.</p>
<p><strong>Optimizations applied:</strong> Worked with business stakeholders to redefine the historical requirement to a rolling 3-year window, which captures nearly all practical booking lifecycle updates. Tuned the Hudi recordKey by prefixing it with a datetime string to significantly accelerate Bloom Index lookups.</p>
<p><strong>Result:</strong> Significantly shorter job execution, reduced cluster cost, and faster incremental processing.</p>
<h3 class="anchor anchorTargetStickyNavbar_Vzrq" id="4-spiking-emr-cluster-costs">4. Spiking EMR Cluster Costs<a href="https://hudi.apache.org/blog/2026/06/15/apache-hudi-at-southwest-airlines#4-spiking-emr-cluster-costs" class="hash-link" aria-label="Direct link to 4. Spiking EMR Cluster Costs" title="Direct link to 4. Spiking EMR Cluster Costs" translate="no">​</a></h3>
<p>Fluctuating workloads combined with standard EC2 instance pricing and inefficient auto-scaling behaviors initially led to high operational costs on Amazon EMR.</p>
<p><strong>Optimizations applied:</strong> Switched cluster node architecture to ARM-based AWS Graviton instances, providing a direct 20% cost reduction. Disabled standard Auto Scaling in favor of EMR Managed Scaling to prevent premature instance termination and re-initialization loops. Added memory overhead settings at cluster creation to resolve resource contention when multiple Spark jobs ran on the same cluster. Partitioning by PNR date so that grouping all bookings for a given date together makes out-of-order processing and version updates dramatically more efficient.</p>
<p><strong>Result:</strong> Lower EMR cost, optimized resource utilization, improved job performance, and met SLAs.</p>
<h2 class="anchor anchorTargetStickyNavbar_Vzrq" id="whats-next">What's Next<a href="https://hudi.apache.org/blog/2026/06/15/apache-hudi-at-southwest-airlines#whats-next" class="hash-link" aria-label="Direct link to What's Next" title="Direct link to What's Next" translate="no">​</a></h2>
<p>The team is actively exploring several Hudi capabilities to push further:</p>
<ul>
<li class="">While Copy-on-Write perfectly fits batch processing needs today, they are testing MOR tables to see if they can achieve lower write latency and even faster upsert times during high-frequency incremental updates.</li>
<li class="">Revisiting <a href="https://hudi.apache.org/docs/indexes/#record-index" target="_blank" rel="noopener noreferrer" class="">Record Level Index</a> for update efficiency.</li>
<li class="">Add <a href="https://hudi.apache.org/docs/indexes/#secondary-index" target="_blank" rel="noopener noreferrer" class="">Secondary Indexes</a> — speeds up queries that filter on non-key columns, accelerating selective queries that would otherwise scan every partition.</li>
<li class="">Upgrading to Hudi 1.0+ to unlock latest improvements in <a href="https://hudi.apache.org/docs/metadata_indexing" target="_blank" rel="noopener noreferrer" class="">indexing</a>, <a href="https://hudi.apache.org/docs/compaction" target="_blank" rel="noopener noreferrer" class="">compaction</a>, and performance optimizations.</li>
</ul>
<h2 class="anchor anchorTargetStickyNavbar_Vzrq" id="conclusion">Conclusion<a href="https://hudi.apache.org/blog/2026/06/15/apache-hudi-at-southwest-airlines#conclusion" class="hash-link" aria-label="Direct link to Conclusion" title="Direct link to Conclusion" translate="no">​</a></h2>
<p>Southwest Airlines moved its reservation data pipeline from an on-premise, Ab-Initio based warehouse onto an AWS lakehouse built on Apache Hudi and Amazon EMR. Using <a href="https://hudi.apache.org/docs/table_types#copy-on-write-table" target="_blank" rel="noopener noreferrer" class="">Copy-on-Write</a> tables, a custom orchestration layer built on AWS Step Functions and DynamoDB, and custom logic for SCD Type 2 and out-of-order events, the team flattened deeply nested reservation XML into more than 30 well-structured (third-normal-form) tables without losing any history. Getting there meant solving real problems: flattening the XML, taming large Spark jobs, backfilling and partitioning historical data, and keeping EMR costs in check. The modernization yielded significant improvements: Job runtime dropped from about five hours to roughly an hour and a half (a 40–60% improvement), EMR costs came down, and the team kept hitting its SLAs. The team is continuing to evaluate further Hudi capabilities, including Merge-on-Read, record-level and secondary indexing, and an upgrade to Hudi 1.0+.</p>
<p>For the full walkthrough — including the architecture, the SCD Type 2 and out-of-order handling, and the Spark and EMR optimizations — watch the recording on <a href="https://www.youtube.com/watch?v=X3FW4IYmYE4" target="_blank" rel="noopener noreferrer" class="">YouTube</a>.</p>]]></content:encoded>
            <category>data lakehouse</category>
            <category>southwest airlines</category>
        </item>
        <item>
            <title><![CDATA[Stateless Global Upserts for Flink Streaming in Apache Hudi 1.2.0]]></title>
            <link>https://hudi.apache.org/blog/2026/06/10/stateless-global-upserts-for-flink-streaming-in-apache-hudi-1-2-0</link>
            <guid>https://hudi.apache.org/blog/2026/06/10/stateless-global-upserts-for-flink-streaming-in-apache-hudi-1-2-0</guid>
            <pubDate>Wed, 10 Jun 2026 00:00:00 GMT</pubDate>
            <description><![CDATA[How Hudi 1.2.0's Record Level Index brings stateless global upserts to Flink — replacing multi-hundred-GB keyed state with a table-backed, engine-agnostic index.]]></description>
            <content:encoded><![CDATA[<h2 class="anchor anchorTargetStickyNavbar_Vzrq" id="motivation">Motivation<a href="https://hudi.apache.org/blog/2026/06/10/stateless-global-upserts-for-flink-streaming-in-apache-hudi-1-2-0#motivation" class="hash-link" aria-label="Direct link to Motivation" title="Direct link to Motivation" translate="no">​</a></h2>
<p><a href="https://hudi.apache.org/releases/download#release-120" target="_blank" rel="noopener noreferrer" class="">Apache Hudi 1.2.0</a> introduces <a href="https://github.com/apache/hudi/pull/17610" target="_blank" rel="noopener noreferrer" class="">Record Level Index</a> (RLI) support for Flink writers, bringing scalable global upserts to streaming ingestion for the first time.</p>
<p>Until now, Flink users had to choose between two indexing options. The <a href="https://hudi.apache.org/docs/ingestion_flink/#using-bucket-index" target="_blank" rel="noopener noreferrer" class="">bucket index</a> is fast and scalable, but only within a partition. The flink_state index can perform global lookups, but its index lives entirely inside Flink state, growing with table cardinality and becoming increasingly expensive to checkpoint, recover, and migrate.</p>
<p>At large scale, the problem is not lookup correctness. It is operational sustainability. As tables grow to hundreds of millions or billions of records, index state can reach hundreds of gigabytes, turning savepoints, upgrades, and failover recovery into major operational events.</p>
<p>Spark users have long addressed this problem with Hudi's Record Level Index (RLI), which stores record locations in the <a href="https://hudi.apache.org/docs/metadata/" target="_blank" rel="noopener noreferrer" class="">Metadata Table</a> (MDT). With Hudi 1.2.0, Flink gains the same capability: a global index that is part of the table itself rather than part of a single streaming job.</p>
<h3 class="anchor anchorTargetStickyNavbar_Vzrq" id="a-concrete-example-the-orders-table">A concrete example: the orders table<a href="https://hudi.apache.org/blog/2026/06/10/stateless-global-upserts-for-flink-streaming-in-apache-hudi-1-2-0#a-concrete-example-the-orders-table" class="hash-link" aria-label="Direct link to A concrete example: the orders table" title="Direct link to A concrete example: the orders table" translate="no">​</a></h3>
<p>Take a typical e-commerce orders table:</p>
<ul>
<li class="">
<p><strong>Partition column</strong>: datestr (order creation date)</p>
</li>
<li class="">
<p><strong>Record key</strong>: order_id</p>
<p>Inserts naturally land in the latest partition. But updates like status changes, fulfillment events, returns usually arrive <em>without</em> the original datestr. The upstream system knows the
order_id changed; it doesn't always know which day's partition that order originally lived in.</p>
<p>Without a global index, the writer has two bad choices:</p>
<ol>
<li class=""><strong>Bucket index</strong>: assume the update belongs to today's partition and write a duplicate. The original row in the older partition is now stale, and the table has two rows for the same order_id.</li>
<li class=""><strong>flink_state index</strong>: works correctly, but every order_id the table has ever seen must live in Flink state. For a table with hundreds of millions of historical orders, this is a multi-hundred-GB state blob attached to the Flink job, and rebuilding it on recovery (or on migrating to a new pipeline) takes hours.</li>
</ol>
</li>
</ul>
<p>This is exactly the gap that Record Level Index (RLI) closes. Instead of storing record locations inside a single Flink job, RLI stores the global key-to-location mapping in Hudi's Metadata Table. Any writer can resolve an <code>order_id</code> to its current location regardless of partition, while keeping the index independent of Flink state.</p>
<h2 class="anchor anchorTargetStickyNavbar_Vzrq" id="flink-rli">Flink RLI<a href="https://hudi.apache.org/blog/2026/06/10/stateless-global-upserts-for-flink-streaming-in-apache-hudi-1-2-0#flink-rli" class="hash-link" aria-label="Direct link to Flink RLI" title="Direct link to Flink RLI" translate="no">​</a></h2>
<p>RLI stores the key-to-file-group mapping in the Hudi metadata table (MDT), as a dedicated record_index partition. The mapping is:</p>
<p>- Global: partition-agnostic; an update for any order_id resolves to the correct file group regardless of partition.
- Engine-agnostic: Spark writes and reads RLI today; Flink now does too. A table indexed by Spark RLI can be picked up by a Flink streaming job, and vice versa, with no rebuild.
- Decoupled from the writer's lifecycle: the index is part of the table, not part of the Flink job's keyed state. New pipelines can attach immediately; savepoints stay small; recovery
doesn't replay the index.
- Shardable: RLI is partitioned into N file groups (you choose), each holding a slice of the key space. Lookups and writes parallelize naturally.</p>
<h3 class="anchor anchorTargetStickyNavbar_Vzrq" id="high-level-design">High level design<a href="https://hudi.apache.org/blog/2026/06/10/stateless-global-upserts-for-flink-streaming-in-apache-hudi-1-2-0#high-level-design" class="hash-link" aria-label="Direct link to High level design" title="Direct link to High level design" translate="no">​</a></h3>
<p>Incoming records are grouped by the same record-key layout the metadata table uses, so each Flink task only needs the relevant RLI shards. (A “shard” here is one RLI file group in the metadata table; the number of shards equals the configured RLI file-group count.) Flink then looks up record locations in mini-batches, uses a small local cache for records still in flight, and sends each record to the right write target (Figure 1).</p>
<p>After routing, the data path and index path move together. The writer persists the data change and sends a compact record-location update to the metadata table. Hudi then commits the data and index results under the same <a href="https://hudi.apache.org/docs/timeline/" target="_blank" rel="noopener noreferrer" class="">instant</a>, so the table and its index advance as one consistent unit.</p>
<div style="text-align:center"><img src="https://hudi.apache.org/assets/images/blog/stateless-global-upserts-for-flink-streaming-in-apache-hudi-1-2-0/img1.png" alt="Figure 1. Index write flow." width="800"></div>
<h1></h1>
<h2 class="anchor anchorTargetStickyNavbar_Vzrq" id="detailed-design">Detailed Design<a href="https://hudi.apache.org/blog/2026/06/10/stateless-global-upserts-for-flink-streaming-in-apache-hudi-1-2-0#detailed-design" class="hash-link" aria-label="Direct link to Detailed Design" title="Direct link to Detailed Design" translate="no">​</a></h2>
<h3 class="anchor anchorTargetStickyNavbar_Vzrq" id="how-flink-finds-existing-records">How Flink Finds Existing Records<a href="https://hudi.apache.org/blog/2026/06/10/stateless-global-upserts-for-flink-streaming-in-apache-hudi-1-2-0#how-flink-finds-existing-records" class="hash-link" aria-label="Direct link to How Flink Finds Existing Records" title="Direct link to How Flink Finds Existing Records" translate="no">​</a></h3>
<p>When global RLI is enabled, Flink switches from a per-task state lookup to a mini-batched metadata-table lookup. Records are routed to the task responsible for the relevant record-index shard, so each task reads only the slice of the metadata table it needs. This keeps lookup work local and avoids asking every task to scan every shard (Figure 2).</p>
<p>During streaming upserts and deletes, Flink buffers a small batch of records before consulting RLI. It first checks the local in-flight cache, then asks the metadata table for any missing record locations. Once each record’s previous location is known, the normal assignment logic can decide whether to update an existing file or write the record as a new insert.</p>
<div style="text-align:center"><img src="https://hudi.apache.org/assets/images/blog/stateless-global-upserts-for-flink-streaming-in-apache-hudi-1-2-0/img2.png" alt="Figure 2. RLI access pattern." width="800"></div>
<h3 class="anchor anchorTargetStickyNavbar_Vzrq" id="why-a-small-local-cache-still-exists">Why a Small Local Cache Still Exists<a href="https://hudi.apache.org/blog/2026/06/10/stateless-global-upserts-for-flink-streaming-in-apache-hudi-1-2-0#why-a-small-local-cache-still-exists" class="hash-link" aria-label="Direct link to Why a Small Local Cache Still Exists" title="Direct link to Why a Small Local Cache Still Exists" translate="no">​</a></h3>
<p>.Flink RLI is fundamentally table-backed, but a small checkpoint-scoped cache is still required to bridge the gap between in-flight updates and committed metadata. It remembers recent record-to-location changes that have already passed through the Flink pipeline but may not yet be visible in the committed metadata table. That matters when the same record appears more than once before the corresponding Hudi instant finishes committing; the later record still needs to route to the latest in-flight location (Figure 3).</p>
<p>Each checkpoint produces a new <em>cache generation</em> — a snapshot of the record-to-location changes seen during that checkpoint. On checkpoint completion, Flink learns which commits are still in flight, keeps the cache generations that may still be needed, and refreshes its view of the metadata table. Older generations can then be cleaned up when memory pressure requires it. The result is a small correctness buffer for the commit gap plus a fresh table-backed read view after commits land.</p>
<div style="text-align:center"><img src="https://hudi.apache.org/assets/images/blog/stateless-global-upserts-for-flink-streaming-in-apache-hudi-1-2-0/img3.png" alt="Figure 3. Cache lookup and upsert." width="800"></div>
<p>Cache eviction is intentionally lazy. A completed checkpoint does not immediately delete cache data. Instead, Flink asks the operator coordinator — Flink’s write coordinator on the JobManager, which drives Hudi commits — which Hudi instants are still in flight, and uses the earliest in-flight checkpoint as the retention boundary. Cache generations older than that boundary become evictable. They are actually closed and removed only when a memory check decides the cache needs space for a new checkpoint generation or for continued updates. This keeps recovery-safe entries around while still bounding memory usage (Figure 4).</p>
<div style="text-align:center"><img src="https://hudi.apache.org/assets/images/blog/stateless-global-upserts-for-flink-streaming-in-apache-hudi-1-2-0/img4.png" alt="Figure 4. Cache eviction workflow." width="800"></div>
<h2 class="anchor anchorTargetStickyNavbar_Vzrq" id="-1"><a href="https://hudi.apache.org/blog/2026/06/10/stateless-global-upserts-for-flink-streaming-in-apache-hudi-1-2-0#-1" class="hash-link" aria-label="Direct link to -1" title="Direct link to -1" translate="no">​</a></h2>
<h3 class="anchor anchorTargetStickyNavbar_Vzrq" id="keeping-the-index-consistent-during-failures">Keeping the Index Consistent During Failures<a href="https://hudi.apache.org/blog/2026/06/10/stateless-global-upserts-for-flink-streaming-in-apache-hudi-1-2-0#keeping-the-index-consistent-during-failures" class="hash-link" aria-label="Direct link to Keeping the Index Consistent During Failures" title="Direct link to Keeping the Index Consistent During Failures" translate="no">​</a></h3>
<p>The tricky window is between a successful Flink checkpoint and a completed Hudi instant. A checkpoint can succeed while the asynchronous acknowledgment or commit step is delayed, lost, or interrupted by <a href="https://nightlies.apache.org/flink/flink-docs-stable/api/java/org/apache/flink/api/common/state/CheckpointListener.html" target="_blank" rel="noopener noreferrer" class="">task/job failover</a>. During that gap, data may be valid from Flink’s point of view, but the corresponding MDT index commit may still be in flight.</p>
<p>The design handles this through checkpoint-aware recovery. Flink persists enough write metadata to retry commits that completed in Flink but did not finish in Hudi. After task failover or job restart, Hudi can recommit those pending instants and refresh the table state before new records continue reading from RLI.</p>
<p>Flink also flushes pending mini-batches before checkpoint <a href="https://nightlies.apache.org/flink/flink-docs-master/docs/learn-flink/fault_tolerance/#how-does-state-snapshotting-work" target="_blank" rel="noopener noreferrer" class="">barriers</a>, so a data change and its matching index update stay within the same checkpoint boundary.</p>
<h3 class="anchor anchorTargetStickyNavbar_Vzrq" id="how-index-updates-are-written">How Index Updates Are Written<a href="https://hudi.apache.org/blog/2026/06/10/stateless-global-upserts-for-flink-streaming-in-apache-hudi-1-2-0#how-index-updates-are-written" class="hash-link" aria-label="Direct link to How Index Updates Are Written" title="Direct link to How Index Updates Are Written" translate="no">​</a></h3>
<p>Index updates are written in batches rather than one record at a time. Before each checkpoint, or when the buffer is full, Flink deduplicates updates by record key so only the latest location is kept. It then writes those updates to the metadata table and reports the result to the same coordinator that commits the data files. This keeps the data commit and index commit aligned.</p>
<h3 class="anchor anchorTargetStickyNavbar_Vzrq" id="routing-keeping-reads-and-writes-on-the-same-shard">Routing: Keeping Reads and Writes on the Same Shard<a href="https://hudi.apache.org/blog/2026/06/10/stateless-global-upserts-for-flink-streaming-in-apache-hudi-1-2-0#routing-keeping-reads-and-writes-on-the-same-shard" class="hash-link" aria-label="Direct link to Routing: Keeping Reads and Writes on the Same Shard" title="Direct link to Routing: Keeping Reads and Writes on the Same Shard" translate="no">​</a></h3>
<p>Global RLI uses the same record-key routing on both sides of the pipeline: first when records are looked up, and again when index updates are written. The task that owns a slice of the global record index also receives the matching updates for that slice.</p>
<p>Every record key maps deterministically to an RLI shard:</p>
<p><code>s = h(k) mod N_RLI</code></p>
<p>The same shard assignment is reused for both lookups and index updates, ensuring that reads and writes for a given key are handled by the same slice of the index.</p>
<p>In other words, the record key first chooses a stable RLI shard. That shard is then mapped to the bucket-assign task for lookup and to the index-writer task for metadata updates. This keeps the read side and write side aligned without requiring every task to touch every shard.</p>
<h2 class="anchor anchorTargetStickyNavbar_Vzrq" id="compaction">Compaction<a href="https://hudi.apache.org/blog/2026/06/10/stateless-global-upserts-for-flink-streaming-in-apache-hudi-1-2-0#compaction" class="hash-link" aria-label="Direct link to Compaction" title="Direct link to Compaction" translate="no">​</a></h2>
<p>RLI adds write traffic to the metadata table, so metadata <a href="https://hudi.apache.org/docs/compaction" target="_blank" rel="noopener noreferrer" class="">compaction</a> becomes an important operational concern. (The benchmark uses a Merge-on-Read table, so both the data table and the always-MOR metadata table run compaction.) The <a href="https://github.com/apache/hudi/pull/17610" target="_blank" rel="noopener noreferrer" class="">RFC</a> keeps <a href="https://hudi.apache.org/docs/compaction/#metadata-table-compaction-trigger-strategy" target="_blank" rel="noopener noreferrer" class="">metadata compaction</a> asynchronous so ingestion can continue while metadata files are merged in the background. As the Perf and Benchmarking section below shows, data-table compaction stays moderate while metadata-table compaction produces the largest tail latency — the main metric to watch (Figure 5).</p>
<div style="text-align:center"><img src="https://hudi.apache.org/assets/images/blog/stateless-global-upserts-for-flink-streaming-in-apache-hudi-1-2-0/img5.png" alt="Figure 5. Index compaction flow." width="800"></div>
<h2 class="anchor anchorTargetStickyNavbar_Vzrq" id="performance-benchmarking">Performance Benchmarking<a href="https://hudi.apache.org/blog/2026/06/10/stateless-global-upserts-for-flink-streaming-in-apache-hudi-1-2-0#performance-benchmarking" class="hash-link" aria-label="Direct link to Performance Benchmarking" title="Direct link to Performance Benchmarking" translate="no">​</a></h2>
<p>Because RLI maintenance sits directly on the write path, the key question is whether a metadata-table-backed global index can sustain production streaming workloads without becoming a bottleneck.</p>
<p>Our evaluation focused on three questions:</p>
<ul>
<li class="">Can Flink RLI sustain high-throughput ingestion against large existing tables?</li>
<li class="">Can checkpoint completion remain comfortably within streaming SLAs?</li>
<li class="">Does maintaining the metadata-table index materially impact write throughput or operational stability?</li>
</ul>
<p>The goal was not to compare RLI against bucket index. Bucket index is a partition-local solution and will always have lower lookup costs. Instead, the goal was to validate that global indexing on Flink is operationally viable at production scale.</p>
<h3 class="anchor anchorTargetStickyNavbar_Vzrq" id="benchmark-environment">Benchmark Environment<a href="https://hudi.apache.org/blog/2026/06/10/stateless-global-upserts-for-flink-streaming-in-apache-hudi-1-2-0#benchmark-environment" class="hash-link" aria-label="Direct link to Benchmark Environment" title="Direct link to Benchmark Environment" translate="no">​</a></h3>
<p>Both benchmark runs used the same baseline environment:</p>
<ul>
<li class="">~1 billion existing records</li>
<li class="">~1 TB table size on disk</li>
<li class="">100 date partitions</li>
<li class="">100 RLI shards</li>
<li class="">Flink standalone cluster with 4 workers<!-- -->
<ul>
<li class="">64 vCPU / 256 GB RAM per worker</li>
</ul>
</li>
</ul>
<p>We evaluated two ingestion profiles:</p>
<table><thead><tr><th style="text-align:left">Run</th><th style="text-align:left">Ingestion Rate</th><th style="text-align:left">Incremental Records</th><th style="text-align:left">Checkpoint Interval</th></tr></thead><tbody><tr><td style="text-align:left">Baseline</td><td style="text-align:left">20K records/sec</td><td style="text-align:left">50M</td><td style="text-align:left">5 minutes</td></tr><tr><td style="text-align:left">Aggressive</td><td style="text-align:left">50K records/sec</td><td style="text-align:left">100M</td><td style="text-align:left">3 minutes</td></tr></tbody></table>
<h3 class="anchor anchorTargetStickyNavbar_Vzrq" id="results">Results<a href="https://hudi.apache.org/blog/2026/06/10/stateless-global-upserts-for-flink-streaming-in-apache-hudi-1-2-0#results" class="hash-link" aria-label="Direct link to Results" title="Direct link to Results" translate="no">​</a></h3>
<p>The aggressive workload successfully sustained 50,000 records per second against a table containing approximately 1 billion existing records while maintaining a 3-minute checkpoint SLA.</p>
<p>Despite the increased ingestion pressure, checkpoints remained comfortably within budget, averaging 24.1 seconds end-to-end. The job completed the full 100 million-record workload without instability, source backpressure remained manageable, and ingestion throughput stayed near the target rate throughout the run.</p>
<table><thead><tr><th>Metric</th><th>Average</th><th>P99</th></tr></thead><tbody><tr><td>Checkpoint SLA</td><td>180s</td><td>180s</td></tr><tr><td>Checkpoint E2E</td><td>24.1s</td><td>—</td></tr><tr><td>Minibatch Lookup Latency</td><td>3.35s</td><td>8.94s</td></tr><tr><td>Data Flush Latency</td><td>0.29s</td><td>1.43s</td></tr><tr><td>Index Flush Latency</td><td>2.33s</td><td>4.84s</td></tr></tbody></table>
<p>Lookup latency increased relative to the baseline run due to higher minibatch fan-out and reduced cache locality. However, both data writes and index maintenance remained well below checkpoint deadlines, demonstrating that MDT-backed global indexing can operate within typical streaming SLAs at this scale.</p>
<p>Note: The benchmark intentionally stresses the worst-case scenario. Many real-world CDC workloads exhibit strong temporal locality, causing repeated updates to a relatively small working set of keys. In those environments, cache hit rates and effective lookup latency are expected to improve further.</p>
<h3 class="anchor anchorTargetStickyNavbar_Vzrq" id="takeaways-from-the-benchmarks">Takeaways from the benchmarks<a href="https://hudi.apache.org/blog/2026/06/10/stateless-global-upserts-for-flink-streaming-in-apache-hudi-1-2-0#takeaways-from-the-benchmarks" class="hash-link" aria-label="Direct link to Takeaways from the benchmarks" title="Direct link to Takeaways from the benchmarks" translate="no">​</a></h3>
<p>The benchmark demonstrates that MDT-backed global indexing is operationally viable for large-scale Flink ingestion. Even under the more aggressive workload, checkpoint completion remained comfortably within the configured SLA, while lookup and index maintenance costs stayed bounded.</p>
<p>More importantly, index scalability is no longer tied to Flink state size. The global record index lives in the table itself, allowing new jobs to attach immediately, minimizing savepoint overhead, and enabling Spark and Flink writers to share the same index infrastructure.</p>
<h2 class="anchor anchorTargetStickyNavbar_Vzrq" id="conclusion">Conclusion<a href="https://hudi.apache.org/blog/2026/06/10/stateless-global-upserts-for-flink-streaming-in-apache-hudi-1-2-0#conclusion" class="hash-link" aria-label="Direct link to Conclusion" title="Direct link to Conclusion" translate="no">​</a></h2>
<p>Flink RLI closes one of the longest-standing gaps between Spark and Flink in Hudi's indexing architecture. By moving the global record index into the Metadata Table, Hudi decouples index scalability from Flink state while preserving the correctness guarantees required for global upserts.</p>
<p>For teams already using Spark RLI, Flink can now operate on the same indexed tables without rebuilding state. For teams relying on flink_state, RLI provides a path toward smaller savepoints, faster recovery, and a more operationally sustainable architecture.</p>
<p>With Hudi 1.2.0, stateless global upserts are now a viable and robust capability for Flink streaming workloads.</p>]]></content:encoded>
            <category>apache flink</category>
            <category>indexing</category>
            <category>record level index</category>
            <category>rli</category>
            <category>streaming</category>
            <category>metadata</category>
            <category>performance</category>
            <category>release</category>
        </item>
        <item>
            <title><![CDATA[Apache Hudi 1.2: Expanding the Open Lakehouse for AI and Multimodal Data]]></title>
            <link>https://hudi.apache.org/blog/2026/06/07/apache-hudi-release-1-2-announcement</link>
            <guid>https://hudi.apache.org/blog/2026/06/07/apache-hudi-release-1-2-announcement</guid>
            <pubDate>Sun, 07 Jun 2026 00:00:00 GMT</pubDate>
            <description><![CDATA[Apache Hudi 1.2 makes the open lakehouse a unified foundation for analytics and AI, with first-class support for vectors, binary objects, and semi-structured data alongside the records you already manage.]]></description>
            <content:encoded><![CDATA[<p>The Apache Hudi community is excited to announce the release of <a href="https://hudi.apache.org/releases/release-1.2" target="_blank" rel="noopener noreferrer" class="">Apache Hudi 1.2</a>, a major milestone that makes the open lakehouse ready for the next generation of data and AI. By introducing first-class support for semi-structured data, vectors, and binary objects, Hudi 1.2 unifies multimodal data for Analytics and AI-native applications into a single transactional lakehouse table, eliminating the need to duplicate data across multiple storage silos.</p>
<h2 class="anchor anchorTargetStickyNavbar_Vzrq" id="unified-data-foundation-for-analytics-and-ai">Unified Data Foundation for Analytics and AI<a href="https://hudi.apache.org/blog/2026/06/07/apache-hudi-release-1-2-announcement#unified-data-foundation-for-analytics-and-ai" class="hash-link" aria-label="Direct link to Unified Data Foundation for Analytics and AI" title="Direct link to Unified Data Foundation for Analytics and AI" translate="no">​</a></h2>
<p><strong>Embracing Multimodal Data</strong>: AI-native applications increasingly combine structured records, semi-structured data, embeddings, and binary assets within a single workflow. These data representations are no longer peripheral artifacts. They are becoming core business assets that power search, recommendations, retrieval-augmented generation (RAG), observability, and intelligent applications.</p>
<p>When the <a href="https://www.cidrdb.org/cidr2021/papers/cidr2021_paper17.pdf" target="_blank" rel="noopener noreferrer" class="">lakehouse architecture</a> emerged, it promised a single, open platform capable of managing all structured, semi-structured, and unstructured data. In reality, however, the industry’s focus over the last few years has largely centered on analytical workloads. As a result, most lakehouse deployments have become highly optimized for BI, reporting, and large-scale analytics, while support for embeddings, binary objects, and other AI-centric data types has remained relatively limited.</p>
<p>History shows that restricting a data platform to a single data paradigm leads to storage silos. Continuously expanding support for multimodal data is a must: every major generation of data systems eventually broadens the range of data it can natively understand and manage. Relational databases like <a href="https://www.postgresql.org/docs/9.3/functions-json.html" target="_blank" rel="noopener noreferrer" class="">Postgres</a>, and <a href="https://docs.oracle.com/en/database/oracle/oracle-database/19/adjsn/json-in-oracle-database.html" target="_blank" rel="noopener noreferrer" class="">Oracle</a> evolved beyond rows and columns to support semi-structured and domain-specific data. Search engines such as <a href="https://www.elastic.co/docs/reference/elasticsearch/mapping-reference/keyword" target="_blank" rel="noopener noreferrer" class="">ElasticSearch</a> and <a href="https://docs.opensearch.org/latest/mappings/supported-field-types/keyword/" target="_blank" rel="noopener noreferrer" class="">OpenSearch</a> evolved from keyword retrieval to vector search. Data warehouses like <a href="https://docs.aws.amazon.com/redshift/latest/dg/r_SUPER_type.html" target="_blank" rel="noopener noreferrer" class="">Redshift</a> and <a href="https://docs.snowflake.com/en/sql-reference/data-types-semistructured" target="_blank" rel="noopener noreferrer" class="">Snowflake</a> embraced nested and semi-structured data as analytics workloads became more sophisticated.</p>
<p>Today, Lakehouses are positioned to natively embrace semi-structured and unstructured data within the storage engine, unlocking a unified data foundation for modern AI.</p>
<div style="text-align:center"><img src="https://hudi.apache.org/assets/images/blog/2026-06-07-apache-hudi-release-1-2-announcement/1-evolution-data-platforms.png" alt="The history of data platforms is a story of expansion. Rows and columns evolved into structured and semi-structured analytics, and lakehouses are now evolving to manage embeddings, binary assets, and data for AI as first-class citizens." width="800"></div>
<p><strong>The Fragmented Modern Reality and the Sync Dilemma</strong>: For the first time, AI and large language models (LLMs) can reason directly over natural, unstructured data: text, documents, web pages, voice, images, and video.</p>
<p>Consider how this shifts real-world workloads:</p>
<ul>
<li class=""><strong>Content Optimization</strong>: Instead of engineering complex pipelines to strip HTML and extract structured data for Search Engine Optimization (SEO), storing raw internet content with the document structure intact is preferable for modern Answer Engine Optimization (AEO) or Generative Engine Optimization (GEO).</li>
<li class=""><strong>Autonomous Driving</strong>: Rather than transforming a multimodal stream of sensor data, video feeds, and crash logs into structured fields, models can analyze the entire event context natively.</li>
</ul>
<p>Driven by use cases like RAG, multimodal applications, and semantic search, modern workloads now depend entirely on such data that traditional lakehouses were never designed to serve.</p>
<p>This shift is creating a new operational challenge. A single core asset may now exist simultaneously as structured metadata in a lakehouse, embeddings in a vector database, documents and images in object storage, and semi-structured data in a document store. While these architectures unlock powerful new capabilities, they also create a fragmented data landscape where multiple representations of the same entity must be continuously synchronized. As data volumes grow and update frequencies increase, maintaining consistency across these systems becomes increasingly difficult. There is now a growing sync dilemma around data management for AI applications.</p>
<div style="text-align:center"><img src="https://hudi.apache.org/assets/images/blog/2026-06-07-apache-hudi-release-1-2-announcement/2-sync-dilemma.png" alt="Modern AI architectures often distribute structured data, embeddings, binary assets, and semi-structured payloads across multiple systems, creating duplication, synchronization challenges, governance gaps, and operational complexity." width="1000"></div>
<p><strong>Managing Multimodal and AI Data directly on Apache Hudi</strong>: We believe organizations should not need separate databases, warehouses, or even another specialized "AI lakehouse": structured records, documents, images, and embeddings all drive the same business workflows. Isolating semi-structured and unstructured data into specialized databases, warehouses, or Lakehouses introduces data silo, complex synchronization, and operational overhead. AI as a technology impacts all aspects of the business and thus should be natively supported on the mainstream lakehouse or data lake directly, alongside existing data.</p>
<p>The solution is to naturally evolve the existing lakehouse to support the data types used by modern AI applications. <strong>Hudi 1.2</strong> delivers this unified foundation for both analytics and AI workloads. By introducing native VECTOR, BLOB, and VARIANT data types directly into the engine, Hudi eliminates the need for an additional specialized storage layer.  Multimodal data seamlessly inherits the same operational foundation from Hudi that already powers large-scale lakehouse deployments:</p>
<ul>
<li class=""><a href="https://hudi.apache.org/docs/timeline" target="_blank" rel="noopener noreferrer" class="">Transactional Timeline</a>: guarantees atomic updates and consistency across structured, semi-structured, and binary datasets simultaneously.</li>
<li class=""><a href="https://www.oreilly.com/content/ubers-case-for-incremental-processing-on-hadoop/" target="_blank" rel="noopener noreferrer" class="">Incremental Processing</a>: efficiently tracks and captures change streams for multimodal records to minimize pipeline latency.</li>
<li class=""><a href="https://hudi.apache.org/docs/indexes" target="_blank" rel="noopener noreferrer" class="">Advanced Indexing</a>: powers fast lookups and point updates, completely avoiding the brute-force table scans common when handling complex AI datasets.</li>
<li class="">Automated Table Services: executes background <a href="https://hudi.apache.org/docs/cleaning" target="_blank" rel="noopener noreferrer" class="">cleaning</a>, <a href="https://hudi.apache.org/docs/compaction" target="_blank" rel="noopener noreferrer" class="">compaction</a>, <a href="https://hudi.apache.org/docs/clustering" target="_blank" rel="noopener noreferrer" class="">clustering</a>, and layout optimization across text, vectors, and blobs without manual intervention.</li>
<li class=""><a href="https://hudi.apache.org/docs/concurrency_control" target="_blank" rel="noopener noreferrer" class="">Multi-Writer Concurrency Control</a>: secures transactional integrity across high-throughput streaming and concurrent AI workloads.</li>
<li class="">Open Lakehouse Ecosystem: exposes multimodal assets natively to the existing analytical stack, spanning Spark, Flink, Trino, Athena, and cloud object storage.</li>
</ul>
<div style="text-align:center"><img src="https://hudi.apache.org/assets/images/blog/2026-06-07-apache-hudi-release-1-2-announcement/3-hudi-foundation-multimodal-data.png" alt="Multimodal and AI data participate in the same transactions, timeline, incremental processing model, and operational framework that power large-scale Hudi deployments today." width="1000"></div>
<h2 class="anchor anchorTargetStickyNavbar_Vzrq" id="release-highlights">Release Highlights<a href="https://hudi.apache.org/blog/2026/06/07/apache-hudi-release-1-2-announcement#release-highlights" class="hash-link" aria-label="Direct link to Release Highlights" title="Direct link to Release Highlights" translate="no">​</a></h2>
<p>Apache Hudi 1.2 expands the lakehouse for multimodal and AI workloads while continuing to strengthen its streaming and operational foundations.</p>
<ul>
<li class=""><strong>Native VECTOR Type and Vector Search</strong>: First-class support for embeddings with built-in similarity search directly on Hudi tables.</li>
<li class=""><strong>Native BLOB and VARIANT Support</strong>: Store binary objects, documents, images, and semi-structured data alongside traditional analytical records.</li>
<li class=""><strong>Lance File Format Integration</strong>: Optimized storage for vector and multimodal data, for AI workloads within the Hudi ecosystem.</li>
<li class=""><strong>Major Flink Performance Boosts</strong>: Record Level Index support, dynamic bucket scaling, and a new FLIP-27 based source for large-scale streaming workloads.</li>
<li class=""><strong>Distributed Co-ordination at Scale</strong>: Expanded multi-writer concurrency control, automated table services, and operational improvements across the platform.</li>
</ul>
<h2 class="anchor anchorTargetStickyNavbar_Vzrq" id="technical-deep-dives">Technical Deep Dives<a href="https://hudi.apache.org/blog/2026/06/07/apache-hudi-release-1-2-announcement#technical-deep-dives" class="hash-link" aria-label="Direct link to Technical Deep Dives" title="Direct link to Technical Deep Dives" translate="no">​</a></h2>
<h3 class="anchor anchorTargetStickyNavbar_Vzrq" id="native-vector-type-and-built-in-vector-search">Native VECTOR Type and Built-in Vector Search<a href="https://hudi.apache.org/blog/2026/06/07/apache-hudi-release-1-2-announcement#native-vector-type-and-built-in-vector-search" class="hash-link" aria-label="Direct link to Native VECTOR Type and Built-in Vector Search" title="Direct link to Native VECTOR Type and Built-in Vector Search" translate="no">​</a></h3>
<p>Embeddings have become a fundamental building block of modern AI systems. Whether powering retrieval-augmented generation (RAG), recommendation systems, semantic search, or clustering, embeddings provide the representation layer that allows machines to reason about similarity and meaning. Hudi 1.2 introduces <a href="https://hudi.apache.org/docs/sql_ddl#vector" target="_blank" rel="noopener noreferrer" class="">VECTOR</a> as a first-class logical type, allowing embeddings to be represented explicitly within the table schema rather than as generic arrays. This provides a consistent abstraction that storage formats and query engines can optimize around while preserving interoperability across workloads.</p>
<p>Alongside VECTOR, Hudi introduces built-in <a href="https://hudi.apache.org/docs/sql_queries#vector-similarity-search" target="_blank" rel="noopener noreferrer" class="">vector search</a> capabilities directly on Hudi tables. Rather than exporting embeddings into a separate vector database, retrieval logic can remain within the lakehouse and participate in the same SQL workflows as the rest of the data platform. This allows organizations to manage embeddings within the same transactional and operational framework as their analytical data.</p>
<div class="language-sql codeBlockContainer_Ckt0 theme-code-block" style="--prism-color:#F8F8F2;--prism-background-color:#282A36"><div class="codeBlockContent_QJqH"><pre tabindex="0" class="prism-code language-sql codeBlock_bY9V thin-scrollbar" style="color:#F8F8F2;background-color:#282A36"><code class="codeBlockLines_e6Vv"><span class="token-line" style="color:#F8F8F2"><span class="token keyword" style="color:rgb(189, 147, 249);font-style:italic">CREATE</span><span class="token plain"> </span><span class="token keyword" style="color:rgb(189, 147, 249);font-style:italic">TABLE</span><span class="token plain"> products </span><span class="token punctuation" style="color:rgb(248, 248, 242)">(</span><span class="token plain"></span><br></span><span class="token-line" style="color:#F8F8F2"><span class="token plain">  id </span><span class="token keyword" style="color:rgb(189, 147, 249);font-style:italic">BIGINT</span><span class="token punctuation" style="color:rgb(248, 248, 242)">,</span><span class="token plain"></span><br></span><span class="token-line" style="color:#F8F8F2"><span class="token plain">  title STRING</span><span class="token punctuation" style="color:rgb(248, 248, 242)">,</span><span class="token plain"></span><br></span><span class="token-line" style="color:#F8F8F2"><span class="token plain">  embedding VECTOR</span><span class="token punctuation" style="color:rgb(248, 248, 242)">(</span><span class="token number">1024</span><span class="token punctuation" style="color:rgb(248, 248, 242)">)</span><span class="token plain"></span><br></span><span class="token-line" style="color:#F8F8F2"><span class="token plain"></span><span class="token punctuation" style="color:rgb(248, 248, 242)">)</span><span class="token plain"> </span><span class="token keyword" style="color:rgb(189, 147, 249);font-style:italic">USING</span><span class="token plain"> hudi</span><br></span><span class="token-line" style="color:#F8F8F2"><span class="token plain">TBLPROPERTIES </span><span class="token punctuation" style="color:rgb(248, 248, 242)">(</span><span class="token plain">primaryKey </span><span class="token operator">=</span><span class="token plain"> </span><span class="token string" style="color:rgb(255, 121, 198)">'id'</span><span class="token punctuation" style="color:rgb(248, 248, 242)">)</span><span class="token punctuation" style="color:rgb(248, 248, 242)">;</span><span class="token plain"></span><br></span><span class="token-line" style="color:#F8F8F2"><span class="token plain" style="display:inline-block"></span><br></span><span class="token-line" style="color:#F8F8F2"><span class="token plain"></span><span class="token keyword" style="color:rgb(189, 147, 249);font-style:italic">SELECT</span><span class="token plain"> </span><span class="token operator">*</span><span class="token plain"></span><br></span><span class="token-line" style="color:#F8F8F2"><span class="token plain"></span><span class="token keyword" style="color:rgb(189, 147, 249);font-style:italic">FROM</span><span class="token plain"> hudi_vector_search</span><span class="token punctuation" style="color:rgb(248, 248, 242)">(</span><span class="token plain"></span><br></span><span class="token-line" style="color:#F8F8F2"><span class="token plain">  </span><span class="token keyword" style="color:rgb(189, 147, 249);font-style:italic">table</span><span class="token plain">           </span><span class="token operator">=</span><span class="token operator">&gt;</span><span class="token plain"> </span><span class="token string" style="color:rgb(255, 121, 198)">'products'</span><span class="token punctuation" style="color:rgb(248, 248, 242)">,</span><span class="token plain"></span><br></span><span class="token-line" style="color:#F8F8F2"><span class="token plain">  embedding_col   </span><span class="token operator">=</span><span class="token operator">&gt;</span><span class="token plain"> </span><span class="token string" style="color:rgb(255, 121, 198)">'embedding'</span><span class="token punctuation" style="color:rgb(248, 248, 242)">,</span><span class="token plain"></span><br></span><span class="token-line" style="color:#F8F8F2"><span class="token plain">  query_vector    </span><span class="token operator">=</span><span class="token operator">&gt;</span><span class="token plain"> ARRAY</span><span class="token punctuation" style="color:rgb(248, 248, 242)">(</span><span class="token number">0.12</span><span class="token plain">F</span><span class="token punctuation" style="color:rgb(248, 248, 242)">,</span><span class="token plain"> </span><span class="token operator">-</span><span class="token number">0.03</span><span class="token plain">F</span><span class="token punctuation" style="color:rgb(248, 248, 242)">,</span><span class="token plain"> </span><span class="token number">0.81</span><span class="token plain">F</span><span class="token punctuation" style="color:rgb(248, 248, 242)">,</span><span class="token plain"> </span><span class="token punctuation" style="color:rgb(248, 248, 242)">.</span><span class="token punctuation" style="color:rgb(248, 248, 242)">.</span><span class="token punctuation" style="color:rgb(248, 248, 242)">.</span><span class="token punctuation" style="color:rgb(248, 248, 242)">)</span><span class="token punctuation" style="color:rgb(248, 248, 242)">,</span><span class="token plain"></span><br></span><span class="token-line" style="color:#F8F8F2"><span class="token plain">  k               </span><span class="token operator">=</span><span class="token operator">&gt;</span><span class="token plain"> </span><span class="token number">10</span><span class="token punctuation" style="color:rgb(248, 248, 242)">,</span><span class="token plain"></span><br></span><span class="token-line" style="color:#F8F8F2"><span class="token plain">  distance_metric </span><span class="token operator">=</span><span class="token operator">&gt;</span><span class="token plain"> </span><span class="token string" style="color:rgb(255, 121, 198)">'cosine'</span><span class="token plain"></span><br></span><span class="token-line" style="color:#F8F8F2"><span class="token plain"></span><span class="token punctuation" style="color:rgb(248, 248, 242)">)</span><span class="token plain"></span><br></span><span class="token-line" style="color:#F8F8F2"><span class="token plain"></span><span class="token keyword" style="color:rgb(189, 147, 249);font-style:italic">ORDER</span><span class="token plain"> </span><span class="token keyword" style="color:rgb(189, 147, 249);font-style:italic">BY</span><span class="token plain"> _hudi_distance</span><span class="token punctuation" style="color:rgb(248, 248, 242)">;</span><br></span></code></pre></div></div>
<p>The initial implementation performs distributed brute-force search, while ongoing community work on native vector indexing and ANN acceleration will significantly improve similarity search performance without changing the query interface. For more information please refer to <a href="https://github.com/apache/hudi/blob/master/rfc/rfc-102/rfc-102.md" target="_blank" rel="noopener noreferrer" class="">RFC 102</a>.</p>
<h3 class="anchor anchorTargetStickyNavbar_Vzrq" id="native-support-for-binary-and-semi-structured-data">Native Support for Binary and Semi-Structured Data<a href="https://hudi.apache.org/blog/2026/06/07/apache-hudi-release-1-2-announcement#native-support-for-binary-and-semi-structured-data" class="hash-link" aria-label="Direct link to Native Support for Binary and Semi-Structured Data" title="Direct link to Native Support for Binary and Semi-Structured Data" translate="no">​</a></h3>
<p>Modern AI applications increasingly manage more than structured records. Images, documents, audio, video, application logs, model outputs, agent traces, and telemetry have become first-class data assets. Hudi 1.2 introduces two new logical types to embrace such data types.</p>
<p><a href="https://hudi.apache.org/docs/sql_ddl#blob" target="_blank" rel="noopener noreferrer" class="">BLOB</a> introduces native support for binary objects within Hudi, enabling images, documents, audio, video, and other unstructured assets to be managed directly in the lakehouse. Both inline and out-of-line variants are supported. Hudi also added support for <a href="https://hudi.apache.org/docs/sql_queries#reading-blob-columns" target="_blank" rel="noopener noreferrer" class="">read_blob()</a> table-valued function to enable lazy materialization for efficient access to metadata for applications. <a href="https://hudi.apache.org/docs/sql_ddl#variant" target="_blank" rel="noopener noreferrer" class="">VARIANT</a> introduces native support for semi-structured data, preserving rich hierarchical structures without requiring rigid schemas. Together, these capabilities allow structured data, binary objects, and semi-structured data to coexist within the same transactional lakehouse table.</p>
<div class="language-sql codeBlockContainer_Ckt0 theme-code-block" style="--prism-color:#F8F8F2;--prism-background-color:#282A36"><div class="codeBlockContent_QJqH"><pre tabindex="0" class="prism-code language-sql codeBlock_bY9V thin-scrollbar" style="color:#F8F8F2;background-color:#282A36"><code class="codeBlockLines_e6Vv"><span class="token-line" style="color:#F8F8F2"><span class="token keyword" style="color:rgb(189, 147, 249);font-style:italic">CREATE</span><span class="token plain"> </span><span class="token keyword" style="color:rgb(189, 147, 249);font-style:italic">TABLE</span><span class="token plain"> media_assets </span><span class="token punctuation" style="color:rgb(248, 248, 242)">(</span><span class="token plain"></span><br></span><span class="token-line" style="color:#F8F8F2"><span class="token plain">  asset_id STRING</span><span class="token punctuation" style="color:rgb(248, 248, 242)">,</span><span class="token plain"></span><br></span><span class="token-line" style="color:#F8F8F2"><span class="token plain">  content  </span><span class="token keyword" style="color:rgb(189, 147, 249);font-style:italic">BLOB</span><span class="token punctuation" style="color:rgb(248, 248, 242)">,</span><span class="token plain"></span><br></span><span class="token-line" style="color:#F8F8F2"><span class="token plain">  metadata VARIANT</span><br></span><span class="token-line" style="color:#F8F8F2"><span class="token plain"></span><span class="token punctuation" style="color:rgb(248, 248, 242)">)</span><span class="token plain"> </span><span class="token keyword" style="color:rgb(189, 147, 249);font-style:italic">USING</span><span class="token plain"> hudi</span><br></span><span class="token-line" style="color:#F8F8F2"><span class="token plain">TBLPROPERTIES </span><span class="token punctuation" style="color:rgb(248, 248, 242)">(</span><span class="token plain">primaryKey </span><span class="token operator">=</span><span class="token plain"> </span><span class="token string" style="color:rgb(255, 121, 198)">'asset_id'</span><span class="token punctuation" style="color:rgb(248, 248, 242)">)</span><span class="token punctuation" style="color:rgb(248, 248, 242)">;</span><span class="token plain"></span><br></span><span class="token-line" style="color:#F8F8F2"><span class="token plain" style="display:inline-block"></span><br></span><span class="token-line" style="color:#F8F8F2"><span class="token plain"></span><span class="token keyword" style="color:rgb(189, 147, 249);font-style:italic">SELECT</span><span class="token plain"> read_blob</span><span class="token punctuation" style="color:rgb(248, 248, 242)">(</span><span class="token plain">content</span><span class="token punctuation" style="color:rgb(248, 248, 242)">)</span><span class="token plain"> </span><span class="token keyword" style="color:rgb(189, 147, 249);font-style:italic">FROM</span><span class="token plain"> media_assets </span><span class="token keyword" style="color:rgb(189, 147, 249);font-style:italic">WHERE</span><span class="token plain"> asset_id </span><span class="token operator">=</span><span class="token plain"> </span><span class="token string" style="color:rgb(255, 121, 198)">'001'</span><span class="token punctuation" style="color:rgb(248, 248, 242)">;</span><br></span></code></pre></div></div>
<h3 class="anchor anchorTargetStickyNavbar_Vzrq" id="lance-file-format-support">Lance File Format Support<a href="https://hudi.apache.org/blog/2026/06/07/apache-hudi-release-1-2-announcement#lance-file-format-support" class="hash-link" aria-label="Direct link to Lance File Format Support" title="Direct link to Lance File Format Support" translate="no">​</a></h3>
<p>While VECTOR, BLOB, and VARIANT establish the logical foundation for multimodal data in Hudi, different workloads often require different storage layouts. Traditional analytical workloads favor columnar scans and aggregations, while vector search and multimodal applications benefit from storage formats optimized for high-dimensional vectors and random access patterns.</p>
<p>To address these needs, Hudi 1.2 adds support for the <a href="https://hudi.apache.org/docs/storage_layouts#lance-base-file-format" target="_blank" rel="noopener noreferrer" class="">Lance file format</a> alongside Parquet, ORC, and HFile. Rather than forcing users to choose between analytical and AI-optimized storage systems, Hudi allows multiple storage formats to participate in the same table abstraction. Users can choose the format that best matches their workload while preserving the same transactional guarantees, indexing infrastructure, table services, and ecosystem integrations.</p>
<div style="text-align:center"><img src="https://hudi.apache.org/assets/images/blog/2026-06-07-apache-hudi-release-1-2-announcement/4-storage-formats.png" alt="Hudi decouples table semantics from storage format, allowing users to choose the format best suited for their workload while preserving the same transactional guarantees and operational framework." width="800"></div>
<h3 class="anchor anchorTargetStickyNavbar_Vzrq" id="scaling-real-time-ai-and-streaming-workloads">Scaling Real-Time AI and Streaming Workloads<a href="https://hudi.apache.org/blog/2026/06/07/apache-hudi-release-1-2-announcement#scaling-real-time-ai-and-streaming-workloads" class="hash-link" aria-label="Direct link to Scaling Real-Time AI and Streaming Workloads" title="Direct link to Scaling Real-Time AI and Streaming Workloads" translate="no">​</a></h3>
<p>AI systems are only as useful as the freshness of the data they operate on. Recommendation engines continuously ingest user interactions. Retrieval systems require updated embeddings as source content evolves. Observability platforms process a constant stream of traces, logs, and model events. As AI workloads become increasingly real-time, the infrastructure responsible for moving and managing data must scale accordingly.</p>
<p>Hudi 1.2 includes significant investments across Flink ingestion, indexing, and streaming reads. <a href="https://github.com/apache/hudi/discussions/17452" target="_blank" rel="noopener noreferrer" class="">Record Level Index (RLI)</a>, one of Hudi's signature indexing technologies for large-scale upsert workloads, is now available for Flink, bringing efficient record routing, global indexing, and dynamic bucket scaling to streaming deployments.</p>
<div style="text-align:center"><img src="https://hudi.apache.org/assets/images/blog/2026-06-07-apache-hudi-release-1-2-announcement/5-flink-rli.png" alt="Record Level Index (RLI) support on Flink" width="1000"></div>
<p>The release also introduces a new FLIP-27 based <a href="https://hudi.apache.org/docs/ingestion_flink#flink-source-v2" target="_blank" rel="noopener noreferrer" class="">Flink Source V2</a> with resumable split assignment, improved fault tolerance, and stronger pushdown capabilities. Together, these improvements strengthen Hudi's ability to power large-scale real-time AI and streaming workloads.</p>
<h3 class="anchor anchorTargetStickyNavbar_Vzrq" id="operational-foundations-table-services-and-multi-writer-concurrency">Operational Foundations: Table Services and Multi-Writer Concurrency<a href="https://hudi.apache.org/blog/2026/06/07/apache-hudi-release-1-2-announcement#operational-foundations-table-services-and-multi-writer-concurrency" class="hash-link" aria-label="Direct link to Operational Foundations: Table Services and Multi-Writer Concurrency" title="Direct link to Operational Foundations: Table Services and Multi-Writer Concurrency" translate="no">​</a></h3>
<p>Supporting multimodal and AI workloads requires more than new data types. Large-scale data platforms must also manage storage growth, optimize file layouts, coordinate concurrent writers, and maintain predictable performance over time. These operational concerns are often where specialized systems introduce additional complexity.</p>
<p>Hudi 1.2 continues to strengthen the lakehouse foundation through investments in table services, metadata management, and concurrency control. Cleaning and clustering gain new planning and automation capabilities, while cloud-native multi-writer support now extends across all major cloud providers through <a href="https://hudi.apache.org/docs/concurrency_control#azure-storage-based-lock" target="_blank" rel="noopener noreferrer" class="">native storage-based locking</a> for S3, GCS, Azure Blob Storage, and ADLS Gen2. Together with Hudi's ACID guarantees, incremental processing framework, and automated table services, these capabilities help organizations operate large-scale lakehouse deployments without building custom operational tooling.</p>
<h2 class="anchor anchorTargetStickyNavbar_Vzrq" id="whats-next">What's Next<a href="https://hudi.apache.org/blog/2026/06/07/apache-hudi-release-1-2-announcement#whats-next" class="hash-link" aria-label="Direct link to What's Next" title="Direct link to What's Next" translate="no">​</a></h2>
<p>Apache Hudi 1.2 introduces new capabilities for vectors, binary objects, and semi-structured data, but more importantly, it lays the foundation for bringing multimodal data into the lakehouse. We believe the future of data platforms will be defined not by new silos for every data type, but by a unified foundation that can manage structured, semi-structured, and multimodal data within the same transactional and operational framework.</p>
<p>The community is already investing in the next generation of capabilities, including:</p>
<ul>
<li class="">Native vector indexing and ANN acceleration</li>
<li class="">VARIANT shredding and nested-field pushdown</li>
<li class="">Smarter storage layouts for multimodal workloads</li>
<li class="">Continued investments in streaming scalability, indexing, and operational automation</li>
</ul>
<p>Whether you are building retrieval systems, recommendation engines, AI observability platforms, multimodal datasets, or large-scale streaming applications, we invite you to try Apache Hudi 1.2 and help shape the future of the open lakehouse.</p>
<p>Check out the <a href="https://hudi.apache.org/releases/release-1.2" target="_blank" rel="noopener noreferrer" class="">1.2 release notes</a> and <a href="https://hudi.apache.org/docs/overview#getting-started" target="_blank" rel="noopener noreferrer" class="">quick start guides</a> to learn more. Join the Apache Hudi community on <a href="https://hudi.apache.org/slack/" target="_blank" rel="noopener noreferrer" class="">Slack</a>, <a href="https://github.com/apache/hudi" target="_blank" rel="noopener noreferrer" class="">GitHub</a>, <a href="https://www.linkedin.com/company/apache-hudi" target="_blank" rel="noopener noreferrer" class="">LinkedIn</a>, <a href="http://x.com/apachehudi" target="_blank" rel="noopener noreferrer" class="">X</a>, and the <a href="mailto:dev@hudi.apache.org" target="_blank" rel="noopener noreferrer" class="">dev@hudi.apache.org mailing list</a>. We look forward to building the next generation of the open lakehouse together.</p>]]></content:encoded>
            <category>release</category>
            <category>ai</category>
            <category>multimodal</category>
            <category>vector</category>
            <category>vector search</category>
            <category>blob</category>
            <category>variant</category>
            <category>lance</category>
            <category>rag</category>
            <category>streaming</category>
            <category>concurrency control</category>
            <category>lakehouse</category>
            <category>apache flink</category>
            <category>apache spark</category>
        </item>
        <item>
            <title><![CDATA[Why Metadata Has to Be Mutation-Friendly]]></title>
            <link>https://hudi.apache.org/blog/2026/06/05/why-metadata-has-to-be-mutation-friendly</link>
            <guid>https://hudi.apache.org/blog/2026/06/05/why-metadata-has-to-be-mutation-friendly</guid>
            <pubDate>Fri, 05 Jun 2026 00:00:00 GMT</pubDate>
            <description><![CDATA[The Hudi metadata table is itself a Merge-on-Read table — here's why MOR's append-first mutation model is what makes a continuously evolving, indexing-rich metadata layer scale.]]></description>
            <content:encoded><![CDATA[<hr>
<p><em>This post is the second in a series on Merge-On-Read as an architectural shift in Apache Hudi. The <a class="" href="https://hudi.apache.org/blog/2026/05/14/mor-isnt-a-storage-optimization-its-an-architectural-shift">first post</a> made the broad case; this one focuses on the place where the argument becomes impossible to ignore — the metadata table.</em></p>
<hr>
<div style="font-size:1.15rem;line-height:1.75"><p>The <a class="" href="https://hudi.apache.org/blog/2026/05/14/mor-isnt-a-storage-optimization-its-an-architectural-shift">previous post in this series</a> argued that <a class="" href="https://hudi.apache.org/docs/table_types#merge-on-read-table">Merge-On-Read (MOR)</a> is not merely a storage optimization, but an architectural shift — a way of designing systems around the reality that mutation and analytical scans operate at fundamentally different rhythms. That argument was intentionally broad. MOR's effects are distributed across Hudi's architecture: concurrency control, indexing, streaming ingestion, asynchronous services, partial updates, and metadata management all inherit properties from that single design decision. But there is one place where the argument becomes impossible to ignore: the metadata table.</p><p>Hudi's <a class="" href="https://hudi.apache.org/docs/metadata">metadata table (MDT)</a> is itself a MOR table. That detail can easily look like an implementation choice, but it reflects something much deeper. The MDT exposes what happens when metadata itself becomes a continuously mutating system.</p><p>Traditional lakehouse metadata systems were largely designed around immutable replacement. Files changed infrequently, snapshots mostly referenced static objects, and metadata primarily described storage layout. Under those assumptions, metadata behaved more like inventory than coordination — its job was to help readers discover immutable files efficiently. MOR changes that model fundamentally because once updates become incremental, base files stop being complete truth. Mutations accumulate across generations, and state must be reconstructed continuously from evolving fragments. At that point, metadata stops being passive bookkeeping and starts becoming operational infrastructure.</p><p>The Hudi MDT makes this visible because it inherits the mutation profile of the data plane itself. Every commit mutates metadata. Every file rewrite, every index update, and every record relocation compounds continuously inside the metadata layer. At sufficient scale, the metadata system itself becomes one of the highest-write-frequency components in the architecture. Under these workloads, Copy-On-Write is not simply inefficient; it becomes structurally misaligned with the problem.</p><hr><h2 class="anchor anchorTargetStickyNavbar_Vzrq" id="metadata-before-mor">Metadata Before MOR<a href="https://hudi.apache.org/blog/2026/06/05/why-metadata-has-to-be-mutation-friendly#metadata-before-mor" class="hash-link" aria-label="Direct link to Metadata Before MOR" title="Direct link to Metadata Before MOR" translate="no">​</a></h2><p>Before MOR-style mutation became common, metadata systems solved a relatively straightforward problem: mapping logical table state onto immutable storage objects.</p><p>Metadata tracked what files existed, which partitions contained them, what statistics described them, and which snapshots referenced them. That model worked extremely well when state was already materialized inside immutable files. Readers simply needed efficient mechanisms to locate the correct files and prune unnecessary scans.</p><p>In those systems, metadata primarily described storage layout.</p><p>MOR changes the nature of the problem because state no longer exists entirely inside base files. Once updates become incremental, truth becomes distributed across base files, delta logs, delete markers, and compaction history. Metadata now has to coordinate how those pieces compose into current table state.</p><p>That is the deeper architectural transition. In Copy-On-Write systems, metadata describes files. In Merge-On-Read systems, metadata increasingly describes how to reconstruct truth.</p><div style="text-align:center"><img src="https://hudi.apache.org/assets/images/blog/2026-06-05-why-metadata-has-to-be-mutation-friendly/mdt-before-and-after-mor.png" alt="Metadata with and without MOR" width="800"></div><p>That distinction seems subtle initially, but it has enormous consequences for scalability. Under immutable snapshot-oriented systems, metadata mutation remains relatively infrequent. A new snapshot arrives, some manifests are updated, and readers transition cleanly between versions. But once workloads shift toward streaming ingestion, CDC pipelines, row-level updates, and continuous upserts, the mutation profile changes fundamentally.</p><p>Metadata no longer evolves occasionally; it evolves continuously. That transition is what ultimately forces the architectural distinction between rewrite-oriented systems and append-oriented systems.</p><hr><h2 class="anchor anchorTargetStickyNavbar_Vzrq" id="why-the-metadata-table-changes-the-cost-model">Why the Metadata Table Changes the Cost Model<a href="https://hudi.apache.org/blog/2026/06/05/why-metadata-has-to-be-mutation-friendly#why-the-metadata-table-changes-the-cost-model" class="hash-link" aria-label="Direct link to Why the Metadata Table Changes the Cost Model" title="Direct link to Why the Metadata Table Changes the Cost Model" translate="no">​</a></h2><p>The metadata table was introduced to eliminate one of the largest bottlenecks in large-scale lakehouses: filesystem listing. Instead of repeatedly asking object storage, "What files exist in this partition?", Hudi stores that information inside an indexed internal table under <code>.hoodie/metadata/</code>.</p><p>But the important architectural detail is not that the MDT stores metadata. It is <em>how</em> the metadata evolves.</p><p>The MDT is itself a Hudi table, with its own timeline, file groups, compaction lifecycle, and indexing structures. Over time, it evolved beyond file listing into a generalized metadata substrate supporting a rich set of <a class="" href="https://hudi.apache.org/docs/indexes">indexes</a> — record indexes, bloom indexes, column statistics, expression indexes, and additional indexing structures layered into the same append-first architecture.</p><p>The files partition alone does not fully explain why MOR becomes necessary. File-listing metadata is often relatively manageable in size, even for large deployments. The deeper pressure comes from the evolution of the metadata table itself.</p><p>As the MDT expanded beyond filesystem listings into record indexes, bloom filters, column statistics, and other lookup-oriented structures, the mutation profile changed fundamentally. These metadata partitions are continuously updated, highly incremental, and optimized around lookup efficiency rather than rewrite frequency.</p><p>Record-level indexes make the difference especially visible. A high-ingest workload may continuously mutate millions of record-location mappings across existing metadata structures. Under Copy-On-Write, those updates repeatedly rewrite accumulated metadata state. Under MOR, they become incremental append-only mutations that can be compacted later.</p><p>That distinction is what changes the economics. The issue is not simply metadata size, but that continuously mutating indexing structures behave fundamentally differently from static catalog metadata. Once metadata evolves into an operational indexing substrate, append-first mutation becomes far more natural than synchronous rewrite-oriented updates.</p><p>Interestingly, early <a href="https://cwiki.apache.org/confluence/pages/viewpage.action?pageId=147427331" target="_blank" rel="noopener noreferrer" class="">MDT design discussions</a> largely treated append-first mutation as a given rather than a tradeoff that needed extensive justification. The focus was not on debating MOR versus COW, but on how to structure continuously evolving metadata efficiently at scale. In hindsight, that assumption reflects how strongly the workload itself constrained the architecture once metadata stopped behaving like static catalog state.</p><p>This is the key architectural property MOR introduces:</p><blockquote>
<p><strong>MOR decouples mutation cost from accumulated state.</strong></p>
</blockquote><div style="text-align:center"><img src="https://hudi.apache.org/assets/images/blog/2026-06-05-why-metadata-has-to-be-mutation-friendly/mor-decouples-mutation-cost.png" alt="MoR decouples mutation cost" width="800"></div><p>Under COW, metadata write cost scales with the size of existing metadata. Under MOR, metadata write cost scales primarily with the amount of change. That distinction is what makes continuously mutating metadata systems feasible at large scale.</p><hr><h2 class="anchor anchorTargetStickyNavbar_Vzrq" id="compaction-and-deferred-coordination">Compaction and Deferred Coordination<a href="https://hudi.apache.org/blog/2026/06/05/why-metadata-has-to-be-mutation-friendly#compaction-and-deferred-coordination" class="hash-link" aria-label="Direct link to Compaction and Deferred Coordination" title="Direct link to Compaction and Deferred Coordination" translate="no">​</a></h2><p>MOR does not eliminate rewrite cost. It changes <em>when</em> and <em>how</em> the system pays it.</p><p>Eventually, incremental mutations still need to be merged back into optimized base files through <a class="" href="https://hudi.apache.org/docs/compaction">compaction</a>. But the important distinction is that compaction becomes schedulable coordination work rather than synchronous commit-path amplification.</p><p>Under Copy-On-Write, rewrite cost appears directly on every commit path. Every mutation pays the full rewrite penalty immediately, regardless of how small the incremental change actually is. Under MOR, rewrite cost becomes asynchronous, batchable, deferrable, and operationally controllable.</p><p>That separation changes the operational model entirely. Mutation handling and storage optimization become decoupled activities, allowing the system to absorb high-frequency updates incrementally while reorganizing storage on its own schedule.</p><p>The result is not simply lower write amplification; it is operational elasticity.</p><p>The platform can now decide when compaction runs, how aggressively it runs, how much work it batches together, and what resources it consumes. That flexibility becomes increasingly important as metadata itself evolves into a high-frequency operational subsystem.</p><hr><h2 class="anchor anchorTargetStickyNavbar_Vzrq" id="append-first-paradigm-changes-the-execution-model">Append-First Paradigm Changes the Execution Model<a href="https://hudi.apache.org/blog/2026/06/05/why-metadata-has-to-be-mutation-friendly#append-first-paradigm-changes-the-execution-model" class="hash-link" aria-label="Direct link to Append-First Paradigm Changes the Execution Model" title="Direct link to Append-First Paradigm Changes the Execution Model" translate="no">​</a></h2><p>Once mutation becomes append-oriented, entirely different execution patterns become possible.</p><p>Streaming metadata writes are a good example. Instead of running metadata updates as a separate sequential phase after a data commit completes, metadata mutations can flow directly inside the same execution DAG as the data write itself. The metadata layer evolves alongside the ingest pipeline rather than lagging behind it.</p><p>That execution model becomes difficult to express efficiently under rewrite-oriented storage systems. The underlying base files used by the MDT are optimized for point lookups and immutable reads. They are not naturally designed for continuous streaming mutation. The only practical way to evolve those structures incrementally is to append changes first and reorganize later.</p><p>The same pattern appears repeatedly throughout Hudi's architecture. Asynchronous indexing, non-blocking concurrency control, incremental services, and partial updates all inherit properties from the same append-first foundation. Once mutation becomes cheap and incremental, background coordination and asynchronous system evolution become dramatically easier to express.</p><p>This is why MOR ends up influencing much more than storage layout — because it changes the execution model of the system itself. Once append-first mutation becomes the default assumption, the system naturally starts evolving toward asynchronous coordination rather than synchronous rewrites. That shift is deeper than storage optimization because it changes how the platform organizes work.</p><hr><h2 class="anchor anchorTargetStickyNavbar_Vzrq" id="metadata-scaling-is-becoming-the-next-lakehouse-bottleneck">Metadata Scaling Is Becoming the Next Lakehouse Bottleneck<a href="https://hudi.apache.org/blog/2026/06/05/why-metadata-has-to-be-mutation-friendly#metadata-scaling-is-becoming-the-next-lakehouse-bottleneck" class="hash-link" aria-label="Direct link to Metadata Scaling Is Becoming the Next Lakehouse Bottleneck" title="Direct link to Metadata Scaling Is Becoming the Next Lakehouse Bottleneck" translate="no">​</a></h2><p>One of the clearest trends across modern lakehouse systems is the increasing focus on metadata scalability itself.</p><p>As workloads evolve toward streaming ingestion, CDC, row-level mutation, near-real-time freshness, and continuous upserts, metadata systems increasingly become high-frequency mutation systems.</p><p>The scaling pressure shows up everywhere: manifest growth, planning overhead, metadata pruning complexity, incremental reconciliation, mutation tracking costs, and metadata maintenance operations.</p><p>At sufficiently large scale, metadata itself starts becoming one of the dominant <a href="https://www.ryft.io/blog/streaming-with-apache-iceberg-the-operational-problems-at-scale" target="_blank" rel="noopener noreferrer" class="">operational concerns</a> in the system.
Large lakehouse deployments operating over tens of <a href="https://www.e6data.com/blog/apache-iceberg-million-files-metadata" target="_blank" rel="noopener noreferrer" class="">millions of files</a> have documented metadata footprints reaching terabyte scale,
where planning queries can take <a href="https://medium.com/the-data-therapy/how-to-fix-slow-apache-iceberg-queries-by-rewriting-manifest-files-3f7e62e8aa3c" target="_blank" rel="noopener noreferrer" class="">unsustainable latencies</a> or fail simply from the amount of metadata that must be processed before execution even begins.</p><p>The ecosystem's response increasingly reflects this pressure.</p><div style="text-align:center"><img src="https://hudi.apache.org/assets/images/blog/2026-06-05-why-metadata-has-to-be-mutation-friendly/mdt-stops-catalog-state.png" alt="Metadata stops being just catalog state" width="800"></div><p>Manifest rewrite operations, metadata maintenance workflows, planning optimizations, deletion tracking systems, and richer metadata hierarchies are becoming necessary parts of operating large mutation-heavy lakehouse deployments. Discussions around metadata bloat, streaming ingestion instability, and metadata compaction increasingly treat metadata itself as an actively evolving operational subsystem rather than static catalog state.</p><p>That shift is important because it changes what metadata systems are responsible for. Metadata stops being lightweight bookkeeping and increasingly becomes continuous coordination infrastructure under mutation pressure.</p><p>Hudi's architecture started from those assumptions much earlier. The metadata table was designed from the beginning as incrementally mutable, append-oriented, and optimized around continuous metadata evolution.</p><p>That same architectural foundation later enabled the MDT to evolve beyond simple file listing into a generalized indexing substrate supporting record indexes, bloom indexes, column statistics, expression indexes, and more adaptive indexing strategies.</p><p>The important point is not any individual feature. It is the architectural posture underneath them.</p><blockquote>
<p>The metadata layer was treated as an operational system from the beginning — not merely a static catalog of immutable files.</p>
</blockquote><hr><h2 class="anchor anchorTargetStickyNavbar_Vzrq" id="what-mor-ultimately-changed">What MOR Ultimately Changed<a href="https://hudi.apache.org/blog/2026/06/05/why-metadata-has-to-be-mutation-friendly#what-mor-ultimately-changed" class="hash-link" aria-label="Direct link to What MOR Ultimately Changed" title="Direct link to What MOR Ultimately Changed" translate="no">​</a></h2><p>The metadata table is one of the clearest demonstrations of MOR's architectural consequences because the workload pressure is so extreme. Every commit mutates metadata, every mutation compounds continuously, and every scaling problem becomes visible quickly.</p><p>Under those conditions, the distinction between COW and MOR becomes more than a storage-layout preference. It becomes a question of whether mutation cost scales with accumulated state or incremental change.</p><p>That distinction increasingly defines modern lakehouse architecture.</p><p>MOR did not simply optimize write amplification. It changed the role metadata plays inside the system.</p><p>Metadata stopped being a lightweight inventory of files and became the machinery responsible for coordinating continuously evolving table state. And once metadata evolves into a continuously mutating system, append-first architecture stops being an optimization. It becomes the natural model for the workload itself.</p></div>]]></content:encoded>
            <category>metadata</category>
            <category>mor</category>
            <category>merge on read</category>
            <category>indexing</category>
            <category>architecture</category>
            <category>data lakehouse</category>
            <category>streaming</category>
        </item>
    </channel>
</rss>